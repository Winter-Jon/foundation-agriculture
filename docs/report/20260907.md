# 农业智能识别项目双周进展汇报（PPT 文案草案）

**说明：** 按 5 页 PPT 编排。表中加粗数字表示经过人工审阅的数据。

---

## 第 1 页｜Motivation

### 标题

**从直接识别到分类器 + HCV：分别处理已知类与未知类问题**

### 内容

- 直接训练后的模型在 Known 类上有一定效果，但在 Unknown 类上的下降很明显；同一套模型很难同时把已知类精度和未知类泛化都做好。
- RAG 对 Unknown 类更有帮助，但仅靠检索并不能解决已知类的细粒度区分，特别是相似病虫害之间的判断。
- 这一轮将任务拆开：Known 类先由分类模型给出候选；LLM 再结合图像和类别证据判断候选是否合理；有疑问时通过 RAG 补充和验证证据。

### 本页想说明的事

> 已知类主要靠分类模型做稳，未知类和不确定样本再通过 HCV/RAG 处理。后面不再把 Direct 和 RAG 当成互相替代的两条路线。

### 建议版式

一条简单流程：图片 → 分类模型（Known 候选）→ LLM 判断证据 → RAG 验证 → 最终类别。流程下方标注“Known”和“Unknown / 不确定”的不同分支。

---

## 第 2 页｜双周进展（一）：重新整理数据与划分

### 标题

**重新划分 Known / Unknown，并把数据清洗和类别规范化放到训练前完成**

### 内容

本阶段完成以下工作：

1. 重新按类别划分 Known 和 Unknown。划分时同时考虑类别长尾程度和视觉近邻关系，不再只按类别频次切分。
2. 使用经过审阅的图像建立测试集，共 **1,019** 张，覆盖全部类别。
3. 重新规范类别名称，修复重复类别、类别表述不统一等原有标注问题。
4. 清洗数据：用 pHash 去重，导入规范化后的类别表，删除或改造不符合约束的数据；对清洗后样本不足的类别补采图像。
5. 将部分失败样本改造成纠错样本。先由教师模型判断图片与 ground truth 是否一致，再决定该样本能否进入纠错数据。

| 数据集 / 划分 | Overall | Disease | Pest |
| --- | ---: | ---: | ---: |
| 类别数 | 217（Known 109 / Unknown 108） | 145（73 / 72） | 72（36 / 36） |
| Dev | 812（Known 542 / Unknown 270） | 542（362 / 180） | 270（180 / 90） |
| Test | **1,019**（Known 534 / Unknown 485） | **547**（283 / 264） | **472**（251 / 221） |
| 数据库 | 401（Known 192 / Unknown 209） | 286（136 / 150） | 115（56 / 59） |
| 训练候选图像 | 141,482（Known） | 69,907 | 71,575 |

### 简短观察

- 测试集不再只看已知类别，Unknown 占 485/1,019；后续结果必须同时报告 Known 和 Unknown。
- Unknown 类不进入分类模型训练，分类模型只服务于 Known 类候选生成。

### 建议版式

左侧列 5 项数据工作，右侧放数据表。将 Test 行和 1,019 张测试图像用深色或绿色强调。

---

## 第 3 页｜双周进展（二）：4B 模型，200 step 对照

### 标题

**4B：RAG 显著缩小 Known / Unknown 差距；Direct 能力仍受训练配比影响**

### 内容

所有模型训练 200 step。结果如下（准确率，%）：

| 训练策略 | 推理 | 语言 | Overall | Known | Unknown | Known − Unknown |
| --- | --- | --- | ---: | ---: | ---: | ---: |
| Base | Direct | EN | 2.55 | 2.39 | 2.74 | -0.35pp |
|  | Direct | ZH | 8.34 | 8.62 | 8.02 | 0.60pp |
|  | RAG | EN | 16.98 | 16.33 | 17.72 | -1.39pp |
|  | RAG | ZH | 19.04 | 17.61 | 17.72 | -0.11pp |
| Direct-Only | Direct | EN | 30.91 | 46.24 | 13.29 | 32.95pp |
|  | Direct | ZH | 25.91 | 38.53 | 11.39 | 27.14pp |
|  | RAG | EN | 30.23 | 44.95 | 13.29 | 31.66pp |
|  | RAG | ZH | 25.32 | 37.06 | 11.81 | 25.25pp |
| Direct + RAG | Direct | EN | 22.47 | 35.96 | 6.96 | 29.00pp |
|  | Direct | ZH | 21.49 | 30.83 | 10.76 | 20.07pp |
| Direct + RAG | RAG | EN | **33.37** | 34.31 | **32.28** | **2.03pp** |
|  | RAG | ZH | **31.89** | 34.86 | **28.48** | **6.38pp** |

### 观察

- Direct-only 主要提升了 Known 类，对 Unknown 类的泛化仍然较弱。
- Direct + RAG 的 RAG 路径明显缩小了 Known 和 Unknown 之间的差距，说明 RAG 训练对泛化有帮助。
- 混合训练后 Direct 能力有所下降。当前所有模型固定训练 200 step，Direct 样本分到的训练量减少是一个可能原因。
- Option 数据不能简单去掉。它除了训练最终选项，也提供了候选类别和负例信息；后续需要把 Direct、RAG、Option 的比例拆开做对照。

### 建议版式

表格放主体。右侧或表格下方只放三条观察；突出 Direct+RAG 的两行 RAG 推理结果，以及 Known−Unknown 一列。

---

## 第 4 页｜双周进展（三）：8B 模型

### 标题

**8B：Direct×5 + RAG 保留 RAG 泛化**

### 内容

8B 结果（准确率，%）：

| 训练策略 | 推理 | 语言 | Overall | Known | Unknown |
| --- | --- | --- | ---: | ---: | ---: |
| Base | Direct | EN | 5.20 | 5.32 | 5.06 |
|  | Direct | ZH | 9.03 | 10.28 | 7.59 |
|  | RAG | EN | 21.69 | 19.82 | 23.84 |
|  | RAG | ZH | 22.67 | 20.00 | 25.74 |
| Direct×5 + RAG | Direct | EN | 25.61 | 37.98 | 11.39 |
|  | Direct | ZH | 17.96 | 26.79 | 7.81 |
| Direct×5 + RAG | RAG | EN | **33.86** | 35.96 | **31.43** |
|  | RAG | ZH | **31.31** | 29.36 | **33.54** |

### 观察

- 增加 RAG 训练后，模型在 Unknown 类上的表现没有像 Direct-only 那样明显掉下去。
- Direct 数据占比不能过低。后续需要继续比较 Direct 与 RAG 的配比，找到兼顾 Direct 能力和 RAG 泛化的训练方案。

### 建议版式

表格放主体。表格下方只放两条观察，突出 Direct×5 + RAG 的 RAG 推理结果。

---

## 第 5 页｜双周进展（四）：MAE 与分类模型进展

### 标题

**MAE 预训练完成，分类模型进入阶段性评测**

### 内容


- MAE 预训练已完成：使用 1,668,771 张农业图像训练 ViT-L/16 编码器，且预训练阶段排除了开发集和测试集图像。
- 基于 MAE 初始化闭集分类模型，当前仍在训练，尚未接入 HCV。
- 当前模型使用 107 个 Known 类训练；与新划分的 109 个 Known 类仍需完成口径对齐。

| 阶段性 Known-dev 结果 | Top-1 | Balanced Accuracy | Macro-F1 | Disease Macro-F1 | Pest Macro-F1 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 当前最佳 checkpoint | **92.14%** | **91.96%** | **92.02%** | **94.90%** | **86.10%** |

### 观察

- 当前结果用于选择 checkpoint，不是最终测试结果。
- 训练结束后先检查虫害和视觉近邻类别的混淆，再决定是否接入 HCV。

### 建议版式

上半部分用两条短文字说明 MAE 和分类模型状态；下半部分放结果表。页脚标注“Known-dev，用于 checkpoint 选择，非最终测试”。

---

## 第 6 页｜下周计划

### 标题

**下周：跑完分类模型，接入 HCV，再把训练配比拆开做对照**

### 内容

1. 完成分类模型训练和 Known-dev 评测，查看类别级混淆，重点看虫害及视觉近邻类别；模型选定后再跑一次完整测试集中的 Known 切片。
2. 将分类模型接入 HCV 推理：先取 Known 类候选，再让 LLM 核对图像和证据；候选不可信或样本属于 Unknown 时走 RAG 验证。
3. 在固定 200 step 和统一测试集下，拆开比较 Direct、RAG、Option 数据比例，确认 Direct+RAG 中 Direct 能力下降是否主要来自平均训练量减少。
4. 继续补齐清洗后不足类别，并完成纠错样本的教师一致性检查；人工审阅数据继续只用于测试和关键训练数据，不混入未审阅样本。

### 本周需要回答的问题

> 分类模型能否提高 Known 类效果；RAG 能否保持 Unknown 泛化；Direct、RAG、Option 的训练比例应该如何设置。

---

## 制作备注（不放入 PPT）

- 文案保持组会记录的语气，表格优先，观察只写从当前数据能直接看到的内容。
- 第 3、4、5 页的加粗数值为本页需要重点讲的结果，不额外引入“显著”“形成强基线”等结论性套话。
- 如果飞书原文中三处无法导出的表格可提供截图或复制文本，制作 PPT 前可将对应表格替换为完整版本。
