# Changes — 2026-W34

## 2026-08-17 01:20:00 CST - Rebuild cumulative Direct + Blind RAG controls

- Updated reconstructive control code so full-stage class floors are domain-specific: disease cells require 100 classes and pest cells require 50, while intermediate cells remain at 18. Full Option cells additionally require at least 40 each of A/B/C/D.
- Added deterministic historical RAG re-audit and selection: strict per-row protocol checks, Direct/diagnostic isolation exclusion, one global winner per query image (fewest tool turns then sample ID), class-balanced selection, and auditable row/class shortages.
- Replaced the blanket contacted-image exclusion with immutable lineage for independent trajectories. An unknown delivery or strict rejection can receive one new recorded resend; two retryable outcomes or any accepted trajectory retires the image. Historical traces are never rewritten.
- Validation: `.venv/bin/python -m pytest -q tests/test_rebuild_sft.py` passed 13 tests; `compileall`, experiment loading, YAML loading, and `git diff --check` passed. No teacher request, freeze, SFT, or evaluation was started.
- Next: run the cumulative audit against historical accepted rows and Direct/diagnostic hash manifests, then prepare only the intermediate missing-cell targets after local visual-retrieval screening.

## 2026-08-17 01:35:00 CST - Separate reusable sampling contract from collection specification

- Renamed the two control layers to `Sampling Contract` and `Collection Specification`. The contract is the stable, versioned behavioral interface; the specification contains the replaceable current-stage quotas, coverage floors, and attempt budgets.
- Added `configs/sampling/reconstructive-blind-rag-contract-v1.yaml` and `configs/sampling/reconstructive-blind-rag-collection-spec-v1.yaml`; the registered data control now references both instead of duplicating collection targets.
- The planner accepts `--collection-spec`, and the Python loader is named `load_collection_specification`; compatibility validation continues to derive its stage view from the current specification.
- Validation: `tests/test_rebuild_sft.py` passed 14 tests; module compilation, experiment loading, and `git diff --check` passed. No collection, freeze, SFT, or evaluation was started.

## 2026-08-17 02:00:00 CST - Complete configuration refactor and strictly re-audit history

- Made the `Sampling Contract` and `Collection Specification` YAML files the single source of truth for the reconstructive controls. Python now loads both versioned files, while the planner accepts an explicit `--collection-spec` for a replacement collection demand.
- The cumulative auditor now accepts a reproducible newline-delimited accepted-file list, writes its deterministic selected rows to JSONL, recognizes the legacy but semantically equivalent `tool_response` retrieval-return serialization, and applies Option/Open answer contracts during historical re-audit.
- Result: 220 historical accepted rows are Blind and current-contract valid; 191 survive same-image selection and class caps. The intermediate 280-row freeze remains blocked: row deficits are 11 open/en/pest, 6 open/zh/disease, 27 open/zh/pest, 5 option/en/disease, 14 option/en/pest, 8 option/zh/disease, and 18 option/zh/pest. Class deficits remain in five of those cells. Accepted-only history has no complete per-cell attempt ledger, so no Blind budget was inferred.
- Evidence: `outputs/experiments/reconstructive_direct_blind_rag_v1/cumulative_audit_v1/report.json` and `selected.jsonl`. Validation: `tests/test_rebuild_sft.py` passed 17 tests; compilation and `git diff --check` passed. No teacher request, freeze, SFT, or evaluation was started.

## 2026-08-17 09:34:00 CST - Complete intermediate SFT conversion, training, and release audit

- Converted the immutable 840-row freeze (`560 Direct + 280 RAG`) into validated SFT data without modifying the raw freeze. The derived dataset contains 840 unique single-image records; `agrinet data validate --kind sft` passed.
- Trained the intermediate candidate from `models/Qwen3-VL-4B-Instruct`; run `20260817T043817-e259e1da-a01` completed at checkpoint-135 (135/135 steps, five epochs).
- Completed matched 192-row Direct and Blind RAG diagnostics for raw base and candidate. Candidate RAG versus raw base is `-14.0625pp`, bootstrap lower bound `-20.8333pp`; Direct is `+2.0833pp` overall but `question_type=option` is `-5.2083pp`.
- Added `tools/rag_distill/write_intermediate_gate_report.py` and generated `outputs/experiments/reconstructive_direct_blind_rag_v1/evaluation/intermediate_release_gate_report.json`.
- Decision: intermediate release gate failed. Do not collect or train the final 4,200-row phase.

## 2026-08-17 10:10:00 CST - Align reconstructive SFT and evaluation with Swift Hermes Agent protocol

- Replaced the reconstructive route's manual-JSON derivative with Swift Agent-compatible Hermes data: `tools` is a JSON string, trajectories preserve `tool_call` and `tool` roles, and tool results remain non-loss context in the Swift template. Immutable raw freeze data remains unchanged.
- Added `src/agrinet/rag/hermes_protocol.py` for shared Hermes system-prompt rendering, XML tool-call parsing, Swift trajectory normalization, and fail-closed sequence checks. The RAG evaluator now uses this Hermes prompt and records per-row protocol health.
- Created `sft-hermes/data.jsonl` with 840 validated records and hash `1c4f0fb21e9e3185821cea5c110b64343a750723476bf65adba574f6cf5261a6`; its manifest records `swift-hermes/v1` and the immutable source hash.
- Registered a separate 1-epoch / `2e-6` Hermes smoke SFT configuration. It has not been launched; it must reach the planned protocol-health smoke gate before any complete retraining is considered.
- Validation: 30 focused protocol, evaluator, reconstruction, and template tests passed; SFT validation and experiment config parsing passed.

## 2026-08-17 10:24:00 CST - Separate teacher collection controls from student Hermes prompts

- Reduced `agrinet_rag_search` descriptions to tool and parameter semantics. Retrieval strategy, answer gating, and quality constraints remain in the teacher-only collection prompts.
- The Hermes converter now removes all teacher `system` messages before writing student SFT data. The student receives only the image question, Swift-standard tool trajectory, and the Hermes tool schema injected by the template.
- The RAG evaluator likewise uses the minimal Hermes system prompt plus the manifest question; tool continuations contain only actual public retrieval results.
- Regenerated the 840-row Hermes derivative: no system messages, `tools` is a JSON string, and the new hash is `f1ba4a5e7fae21fecabc8ba344aac1b5cd22d694720fbea493b88a066b315eab`. Validation passed.

## 2026-08-17 12:26:28 CST - Make the direct VLM training entrypoint DDP-safe

- `agrinet vlm train` now applies `local_training_env(config)`, matching `agrinet vlm submit --operation train`. Registered DeepSpeed experiments therefore receive their declared GPU visibility and `NPROC_PER_NODE` irrespective of which supported train entrypoint is used.
- Added `scripts/vlm/run_hermes_smoke_eval_after_sft.sh`, a fail-closed tmux-queue launcher that waits for the registered smoke status, requires exactly one retained checkpoint, and only then invokes the fixed 192-row Hermes RAG evaluation. It does not retry SFT or diagnostic work.
- Validation: shell syntax check, `compileall`, focused tests (`48 passed`), and `git diff --check`.

## 2026-08-17 13:10:02 CST - Make post-SFT Hermes diagnostics fail closed and comparable

- Corrected the evaluation queue's registered run terminal state (`complete`) and checkpoint lookup depth (checkpoint is retained below one version directory), then verified the 192-row candidate diagnostic completed with exit code 0.
- Added `scripts/vlm/run_hermes_smoke_rag_baselines.sh`: a serial tmux launcher for raw-base and checkpoint-165 Hermes RAG diagnostics. It uses the candidate's fixed manifest, retrieval and decoding settings, writes unique output roots, and refuses to reuse an output containing a metrics or exit-status artifact.

## 2026-08-17 13:45:52 CST - Add matched Direct retention smoke comparison

- Added `scripts/vlm/run_hermes_smoke_direct_comparison.sh`, which serially evaluates the Hermes smoke checkpoint and raw base with no tools on the fixed 192-row manifest, using unique output roots and refusing output reuse. This preserves the Direct branch's independent no-tool retention check while the RAG branch uses the full Hermes tool prompt.

## 2026-08-17 19:33:57 CST - Implement audited long-Direct / Blind-RAG 1:1 v2 controls

- Added `configs/sampling/hermes-direct-blind-rag-1to1-collection-spec-v2.yaml`: 70 Direct and 70 Blind RAG rows per Open/Option × English/Chinese × disease/pest cell (560 + 560), at least 35 classes and no more than two rows per class, Option-letter floors, exact neutral uncertainty text, and long-trajectory requirements.
- Added `tools/rag_distill/audit_legacy_long_direct.py` to retain historical Direct traces unchanged only after current-contract auditing, then deterministically select bilingual pairs under class caps. The audit records every rejection reason and never renders a short replacement trace.
- Extended `src/agrinet/data/rebuild_sft.py` and `tools/rag_distill/freeze_reconstructive_sft.py` with the v2 hard gates: long visual comparison structure, class/current-image/pair mapping, Direct/RAG and held-out image isolation, Hermes call→public-JSON-tool-response adjacency, pre-tool visual thought, evidence-based negative exclusions, and 192/618 manifest checks plus immutable lineage.
- Updated the SFT conversion so Direct has only the minimal `<think>/<answer>` system contract and RAG adds only the standard Hermes tool grammar; teacher candidate/retrieval workflow text is removed. Added a planned four-GPU, five-epoch, full-parameter Qwen3-VL-4B 1:1 experiment at `5e-6`, 1,120 rows, effective batch 32, and final-checkpoint retention.
- Validation: `.venv/bin/python -m pytest tests/test_rebuild_sft.py tests/test_hermes_protocol.py -q` passed 27 tests; `compileall`, YAML parsing, and `git diff --check` passed. No historical audit, teacher collection, freeze, SFT, or evaluation was launched.
- Next: run the legacy Direct and historical RAG audits against the actual 192/618 and train-freeze image hashes; collect only documented shortages, then create the v2 immutable freeze before the paired 192-row gate.

## 2026-08-17 20:42:40 CST - Audit historical sources and build complete Hermes 1:1 shortage plan

- Fixed isolation set: 192-row diagnostic, formal 618-row manifest, and existing 840-row freeze comprise 869 unique image hashes under `outputs/experiments/hermes_long_direct_blind_rag_1to1_v2/isolation/`.
- The 2,114 legacy Direct rows yielded zero v2-reusable rows: 1,067 contain prohibited Knowledge Verification/wiki text, 2,101 lack required neighbor exclusions, 1,191 lack the candidate structure, and 782 overlap fixed held-out/prior-freeze images. The source is Open-only (1,068 English and 1,046 Chinese), so it cannot supply Option rows unchanged.
- The 313 historical accepted RAG rows yielded zero v2-reusable rows: all lack the 3 descriptive candidates and 2 evidence-based exclusions; 280 also overlap fixed isolation. No legacy row was rewritten or converted into a compliant trajectory.
- Added source-aware audits and a deterministic bounded-image plan. It has 560 Direct targets (280 bilingual pairs) plus 560 Blind RAG targets (560 unique images): 840 unique images, no cross-route/held-out overlap, class floors/caps satisfied, and Option letters A/B/C/D distributed 72/72/68/68 per route.
- Evidence: `outputs/experiments/hermes_long_direct_blind_rag_1to1_v2/{direct_audit,rag_audit,shortage_plan}/`; Direct target hash `f2fcc6b973e5cf9fe4cd7e6f0d0a92ae2259a671182d38461b3094d7b5be4609`, RAG target hash `c9cc5b6ac2135e32dd1cd11a0e9d0bed19aa0e870d33ff03f3cbba4c7e3f6879`.
- Validation: target-plan hard check, focused 27-test data/protocol suite, compilation, and `git diff --check` passed. No teacher request, collection, freeze, SFT, or evaluation was started.
- Next: review the immutable target plan before issuing any teacher collection request.

## 2026-08-17 21:22:30 CST - Validate Hermes v2 collection pilots

- Added the isolated v2 collector and registered local hermes-collect pilots. It uses tool-free Direct and Hermes RAG role order, rejects output reuse, and validates final answers against public target contracts.
- First Direct image pair reached two known strict rejections and is retired. A fresh English/Chinese Direct pair passed with shared image hash 46be9d546d7d67e760890ff7a25c4f72fb8136ea115d8f6072c4a18061ae3ee0 and zero strict errors.
- First RAG image reached two known strict rejections and is retired. Fresh RAG target 002 passed with user, assistant, tool_call, tool, assistant role order; image hash 63f0488ab208a6f0fd6ecb08dcbe46f0b66e72b6afe3babd72453c389c442ebc; zero strict errors.
- Evidence: outputs/experiments/hermes_long_direct_blind_rag_1to1_v2/collection/. All pilots had CLI dry-runs first. Focused 27-test suite, compilation, and diff check passed.
- Next: build small per-cell shards excluding retired image hashes; validate each shard before further expansion.

## 2026-08-17 21:33:30 CST - Complete first per-cell Hermes v2 collection shard

- Built the deterministic first-per-cell shard after excluding four contacted hashes: 8 Direct rows (four bilingual image pairs) and 8 Blind RAG rows (one per Open/Option × language × domain cell). Evidence: outputs/experiments/hermes_long_direct_blind_rag_1to1_v2/shards/first-per-cell/.
- Direct shard finished 5 accepted and 3 known strict rejections. All accepted rows re-pass strict Direct validation. Rejections were two answer-target mismatches and one short think; no unknown delivery. Two Open image pairs and one Option disease pair are incomplete and each failed language/image trajectory has at most one independent resend remaining.
- RAG shard finished 6 accepted and 2 known strict rejections. All accepted rows re-pass strict Hermes validation. Both rejections were Chinese Open answer-target mismatches; no unknown delivery. Each has at most one independent resend remaining.
- No whole-shard replay was performed. No freeze, SFT, or evaluation was started. Focused 27-test suite and diff check passed after output audit.
- Next: create separate one-target resend records for only the five known-rejection trajectories, preserving every first-attempt artifact; do not scale batch size until the repair acceptance rate is reviewed.

## 2026-08-17 21:39:45 CST - Exhaust first-shard independent resend allowance

- Added exact target-ID selection to the v2 collector and registered five independent one-target resend experiments, each with a separate output directory. Every command was dry-run before execution.
- All five permitted resends were known strict rejections, not unknown delivery: Direct had two answer-target mismatches and one short Chinese Option chain; RAG had two Chinese Open answer-target mismatches. Each corresponding image trajectory is retired and must not be replayed.
- The current prompts now reliably satisfy format gates for many rows but not these target/image semantic cases. Do not add more target disclosure or retries to force labels; use fresh isolated replacement images under the unchanged public-evidence route.
- Validation: focused 27-test suite and diff check passed. No full shard replay, freeze, SFT, or evaluation was started.
- Next: rebuild the next fresh-image per-cell shard from the original fixed plan while excluding every contacted or retired hash.

## 2026-08-17 22:01:00 CST - Bound Oracle to RAG and complete the second fresh-image shard

- The user constrained Oracle to RAG. Removed the Oracle Direct pilot and made --oracle reject non-RAG collection. Direct retains its existing internal quality-control target; Oracle RAG rows are separately marked generation_route: oracle_grounded, label_visible_to_teacher: true, and retain a separate lineage reason.
- Added final-freeze hard gates to the v2 collection specification and validator: no more than 14 Oracle RAG rows and at least 56 Blind-evidence RAG rows per 70-row cell. Added focused tests for both simultaneous failure modes and for rejecting Oracle wording in persisted training messages. Compilation and the focused protocol/data suite pass: 29 tests.
- Oracle RAG pilot hermes-v2-rag-open-zh-disease-001 completed at collection/oracle-rag-open-zh-disease-001/: one accepted row, strict Hermes errors [], roles user -> assistant -> tool_call -> tool -> assistant, public JSON tool evidence, canonical answer 杨桃健康叶片, and no persisted private-Oracle wording. This is one lineaged Oracle exception, not Blind evidence.
- Built shards/second-per-cell/ with 8 Direct rows (four bilingual image pairs) and 8 RAG rows (one per cell), excluding all 16 then-contacted hashes. The target hashes are Direct 16082a45f1d3585e6a126633c5a1ab21e2c50c92a20736abb36eb381c79897c8 and RAG 9b8abc03355935b255bf61b97cabaa39aea48a02173a70744f1d4536ed90b88d.
- Second Direct shard: 3 strict accepts and 5 recorded rejections. Accepts are hermes-v2-direct-option-en-disease-002, hermes-v2-direct-option-en-pest-002, and hermes-v2-direct-option-zh-pest-002; all re-pass strict Direct validation. Rejections are four answer-target mismatches plus one short/mismatched Chinese Open trace. Second Blind RAG shard: 5 strict accepts and 3 answer-target-mismatch rejections; every accepted row is blind_evidence, has label_visible_to_teacher: false, correct Hermes role order, public JSON evidence, no Oracle wording, and zero strict errors.
- The second target shard contains 12 distinct image hashes (4 Direct source images + 8 RAG images); all are now recorded in collection lineage. Accepted Direct uses two source image hashes, accepted RAG five, with zero accepted cross-route overlap. No retry, freeze, SFT, or evaluation was started.
- Evidence: outputs/experiments/hermes_long_direct_blind_rag_1to1_v2/{shards/second-per-cell,collection/oracle-rag-open-zh-disease-001,collection/direct-second-shard,collection/rag-second-shard}/; local run records are under outputs/runs/rag/ for the three corresponding experiments.
- Next: use only fresh images for the next bounded shard; preserve Oracle RAG below the final 14-per-cell cap and continue to collect Blind evidence as the predominant route.

## 2026-08-17 22:42:00 CST - Complete third fresh-image shard and bounded Oracle RAG recovery

- Built shards/third-per-cell/ after excluding 28 contacted image hashes. It has the complete 8 Direct / 8 Blind RAG per-cell layout: four bilingual Direct source images and eight globally distinct Blind RAG images, with no cross-route overlap. Target hashes are Direct c6c56e978122e2736e4f0e7343cf2191b87ae7d4bd8865593d2cd62448477efb and RAG 2f041d07a432323908c211a4a1f1dbe354803a7b427fdfa1ab9fb7dae0491486.
- Third Direct collection: 5 strict accepts and 3 recorded rejections. The accepts re-pass long Direct validation; the rejections are one answer-target mismatch and two short chains. Third Blind RAG collection: 4 strict accepts and 4 answer-target-mismatch rejections. Each accepted RAG trace has the required Hermes role order, public JSON tool evidence, blind_evidence metadata, label_visible_to_teacher false, and no Oracle-text leak.
- Made the bounded Oracle recovery manifest shards/oracle-recovery-001/rag_targets.jsonl from the eight fresh Blind-mismatch trajectories that had not already completed Oracle. The manifest has only 2 open/en/pest, 2 open/zh/disease, 3 open/zh/pest, and 1 option/zh/pest targets; each is below the 14-row final Oracle cap and retains its original Blind rejection lineage.
- Oracle recovery completed 7 strict accepts and 1 answer-target-mismatch rejection. All seven have generation_route oracle_grounded, label_visible_to_teacher true, public tool JSON, correct Hermes role order, canonical answers, and zero persisted private-Oracle text. The remaining open/en/pest-002 Oracle mismatch is preserved and not replayed.
- Across all collection artifacts, Oracle accepts total eight: open/en/pest 1, open/zh/disease 3, open/zh/pest 3, option/zh/pest 1; no cell exceeds the cap. Focused compilation, protocol/data tests (29), and git diff check pass. No freeze, SFT, or evaluation was started.
- Evidence: outputs/experiments/hermes_long_direct_blind_rag_1to1_v2/{shards/third-per-cell,shards/oracle-recovery-001,collection/direct-third-shard,collection/rag-third-shard,collection/oracle-rag-recovery-001}/ and outputs/runs/rag/ corresponding experiment directories.
- Next: build the fourth fresh-image per-cell shard, excluding every contacted image; keep Oracle only as separately lineaged RAG recovery within the cell cap.

## 2026-08-17 22:53:00 CST - Complete fourth fresh-image shard and second bounded Oracle recovery

- Built shards/fourth-per-cell/ after excluding 40 contacted image hashes. It retained the complete 8 Direct / 8 Blind RAG per-cell structure, with four bilingual Direct source images, eight distinct Blind RAG images, and no cross-route overlap. Target hashes are Direct 9c1f20b8e84f5a35b384402c3cbff318536f98c6b869ae830a9774a6ab2c49d7 and RAG 5f13f5303f44b70b1dfd0e9fc25eb501a6fb9f8676ac4f68053077d9283f97cd.
- Fourth Direct collection yielded 5 strict accepts and 3 retained rejections (one answer mismatch, two short chains). Fourth Blind RAG collection yielded 6 strict accepts and 2 answer mismatches. Every accepted Blind trace re-passed strict Hermes validation, is blind_evidence with label_visible_to_teacher false, uses public JSON evidence, and contains no Oracle text.
- Oracle recovery-002 targeted only the two new Blind mismatch images, both in cells with three Oracle accepts at the time. It yielded one strict Oracle acceptance for open/zh/disease-004 and one retained answer mismatch for open/zh/pest-004. The accepted row has public evidence, zero strict errors, and no private-Oracle text leak.
- Oracle accepts now total 9: open/en/pest 1, open/zh/disease 4, open/zh/pest 3, option/zh/pest 1. Every cell remains below 14; no Oracle row is counted as Blind evidence. Compilation, focused tests (29), and diff check pass. No freeze, SFT, or evaluation was started.
- Evidence: outputs/experiments/hermes_long_direct_blind_rag_1to1_v2/{shards/fourth-per-cell,shards/oracle-recovery-002,collection/direct-fourth-shard,collection/rag-fourth-shard,collection/oracle-rag-recovery-002}/.
- Next: build the fifth fresh-image per-cell shard with every contacted image excluded; continue to use Oracle only as bounded, separately lineaged RAG recovery.

## 2026-08-17 23:05:00 CST - Complete fifth fresh-image shard and third bounded Oracle recovery

- Built shards/fifth-per-cell/ after excluding 52 contacted hashes. It contains eight Direct rows (four bilingual source images) and eight distinct Blind RAG rows, fully covering the eight cells with no cross-route overlap. Target hashes are Direct 817a5dad4c6573d673aacbc4234125e9048d6f3acd534c1beabf7d0caaecff8a and RAG ce1e05da555969f1df40fff9ed4e1483c561fbb5e59a9f3213ea675653e87e12.
- Fifth Direct collection yielded 6 strict accepts and 2 retained short-chain rejections. Fifth Blind RAG collection yielded 6 strict accepts and 2 answer mismatches. All accepted Direct and RAG rows re-pass their strict validators; accepted RAG rows remain blind_evidence, label_visible_to_teacher false, use public JSON tool evidence, and contain no Oracle text.
- Oracle recovery-003 targeted only the two new Chinese Open Blind mismatch images. It yielded one strict acceptance (open/zh/pest-005) and one retained answer mismatch (open/zh/disease-005). The accepted Oracle trace has zero strict errors and no private Oracle text leak.
- Oracle accepts now total 10: open/en/pest 1, open/zh/disease 4, open/zh/pest 4, option/zh/pest 1. Every cell remains below 14, and Oracle rows remain separate from the Blind count. Compilation, focused tests (29), and diff check pass. No freeze, SFT, or evaluation was started.
- Evidence: outputs/experiments/hermes_long_direct_blind_rag_1to1_v2/{shards/fifth-per-cell,shards/oracle-recovery-003,collection/direct-fifth-shard,collection/rag-fifth-shard,collection/oracle-rag-recovery-003}/.
- Next: calculate the cumulative strict per-cell shortage and build the next fresh-image bounded shard using every contacted hash as an exclusion.

## 2026-08-17 23:25:00 CST - Complete sixth fresh-image shard and fourth bounded Oracle recovery

- Built shards/sixth-per-cell/ after excluding 64 contacted hashes. It retains complete eight-cell coverage with four Direct bilingual source images and eight distinct Blind RAG images; Direct/RAG have no cross-route overlap. Target hashes are Direct cb1a7ba6291f86321d1a9c3e3b415661bf601a69af89b858612f5679e335aa47 and RAG f7dd6bffd4f69c8bf518dfa148afb03f332e9e7135f32e848fbc13dd689afcdf.
- Sixth Direct collection yielded 6 strict accepts and 2 retained short-chain rejections. Sixth Blind RAG collection yielded 5 strict accepts and 3 answer mismatches. Accepted RAG rows are all blind_evidence with label_visible_to_teacher false, valid Hermes call/tool adjacency, public JSON evidence, and no Oracle-text leakage.
- Oracle recovery-004 contained exactly the three new Blind-mismatch targets. All three were accepted and re-pass strict RAG validation with public evidence and zero private-Oracle text leakage. Oracle accepts total 13: open/en/pest 2, open/zh/disease 5, open/zh/pest 5, option/zh/pest 1; all cells remain below the 14-row cap.
- Compilation, focused protocol/data tests (29), and diff check pass. No freeze, SFT, or evaluation was started.
- Evidence: outputs/experiments/hermes_long_direct_blind_rag_1to1_v2/{shards/sixth-per-cell,shards/oracle-recovery-004,collection/direct-sixth-shard,collection/rag-sixth-shard,collection/oracle-rag-recovery-004}/.
- Next: build the seventh fresh-image per-cell shard after excluding every contacted image; retain Oracle only as bounded, separately lineaged RAG recovery.

## 2026-08-18 00:03:00 CST - Complete seventh and eighth fresh-image shards; recover Oracle EOFs once

- Seventh shard excluded 76 contacted hashes and produced a complete 8 Direct / 8 Blind RAG layout. Direct yielded 5 strict accepts and 3 short-chain rejections; Blind RAG yielded 4 strict accepts and 4 Open answer mismatches. Oracle recovery-005 accepted two of those four; open/en/pest-007 and open/zh/disease-007 remain answer mismatches and are not replayed.
- Eighth shard excluded 88 contacted hashes and retained complete cell coverage with no Direct/RAG target-image overlap. Direct yielded 4 strict accepts and 4 retained rejections; Blind RAG yielded 4 strict accepts and 4 answer mismatches. All accepted Blind RAG rows remain blind_evidence with label_visible_to_teacher false, valid Hermes public-JSON evidence, and no Oracle-text leak.
- Oracle recovery-006 initially had four recorded SSL UNEXPECTED_EOF unknown-delivery outcomes. A separately lineaged one-time resend accepted open/zh/disease-008 and open/zh/pest-008 with zero strict errors and no Oracle-text leak. open/en/pest-008 encountered the same SSL EOF again and option/en/pest-008 was an answer mismatch. Both have exhausted this Oracle retry allowance and will not be replayed.
- Oracle accepts total 17: open/en/disease 1, open/en/pest 2, open/zh/disease 6, open/zh/pest 7, option/zh/pest 1. Every cell remains below the 14-row cap. Compilation, focused protocol/data tests (29), and diff check pass. No freeze, SFT, or evaluation was started.
- Evidence: outputs/experiments/hermes_long_direct_blind_rag_1to1_v2/{shards/seventh-per-cell,shards/eighth-per-cell,shards/oracle-recovery-005,shards/oracle-recovery-006,collection/direct-seventh-shard,collection/rag-seventh-shard,collection/oracle-rag-recovery-005,collection/direct-eighth-shard,collection/rag-eighth-shard,collection/oracle-rag-recovery-006,collection/oracle-rag-recovery-006-resend}/.
- Next: build the ninth fresh-image per-cell shard, excluding every contacted image and preserving the cap/exhaustion ledger.

## 2026-08-18 00:41:00 CST - Complete ninth/tenth shards and enforce the Oracle cap before collection

- Ninth shard excluded 100 contacted hashes. Direct yielded 6 strict accepts and 2 short-chain rejections; Blind RAG yielded 3 strict accepts and 5 answer mismatches. Oracle recovery-007 accepted open/zh/disease-009, open/zh/pest-009, and option/zh/disease-009; open/en/pest-009 and option/en/pest-009 remained answer mismatches.
- Tenth shard excluded 112 contacted hashes. Direct yielded 5 strict accepts and 3 retained rejections; Blind RAG yielded 6 strict accepts and two Chinese Open answer mismatches. Oracle recovery-008 accepted both Chinese Open targets with zero strict errors and no private Oracle text leakage.
- Added an Oracle request-time capacity preflight to tools/rag_distill/collect_hermes_1to1_v2.py. It scans all accepted Oracle trajectories below the collection root and rejects any Oracle request that would make a cell exceed 14 before contacting the teacher. Added a boundary test; focused compilation, tests (30), and diff check pass.
- Oracle accepts total 22: open/en/disease 1, open/en/pest 2, open/zh/disease 8, open/zh/pest 9, option/zh/disease 1, option/zh/pest 1. No cell exceeds 14. No freeze, SFT, or evaluation was started.
- Evidence: outputs/experiments/hermes_long_direct_blind_rag_1to1_v2/{shards/ninth-per-cell,shards/tenth-per-cell,shards/oracle-recovery-007,shards/oracle-recovery-008,collection/direct-ninth-shard,collection/rag-ninth-shard,collection/oracle-rag-recovery-007,collection/direct-tenth-shard,collection/rag-tenth-shard,collection/oracle-rag-recovery-008}/.
- Next: build the eleventh fresh-image per-cell shard, keeping the bounded Oracle ledger active.

## 2026-08-18 00:42:00 CST - Prepare eleventh shard; stop cleanly at local credential preflight

- Built shards/eleventh-per-cell/ after excluding 124 contacted image hashes: 8 Direct rows (four bilingual source images) and 8 Blind RAG rows, all eight cells covered and no cross-route target-image overlap. The Direct target hash is bba2d52b847c252553970770ba0cccceca058f01ce30f4a0d7fb4cb3e66be6b9; the RAG target hash is cd4dcaed391f9b1045ed5887f813d3bdd795185b22f234948e2a0792351417a4.
- Registered the matching Direct/RAG experiments and both CLI dry-runs passed. The Direct submission then stopped before launch because the local Yunwu credential preflight could not decrypt its GPG-backed API key. A no-output credential availability check also failed.
- Verified no eleventh-shard collection output JSONL and no local run directory were created, so there is no unknown remote delivery, no partial dataset, and no target-image state requiring retirement. No retry was sent.
- Next: restore the local credential availability, run Direct eleventh-shard exactly once, audit it, and then run the matching Blind RAG shard.

## 2026-08-18 01:43:45 CST - Make Hermes v2 RAG evidence publicly readable and recollect the medium batch

- Quality review of rag-medium-001 found that all 27 strict-accepted Blind RAG records retained raw service hit identifiers such as N04030 in their tool payloads. Although the raw response also contained public English/Chinese names, a persisted trajectory could not itself substantiate the named evidence claim.
- Added public_retrieval_evidence() to tools/rag_distill/collect_hermes_1to1_v2.py. It now converts local retrieval responses to a public evidence object containing only source, retrieval type, visual query, and ranked public English/Chinese class names, aliases, and similarity. Internal IDs, codes, matched-image paths, and reference-image paths are discarded before the second teacher call and before persistence.
- Strengthened strict_rag_trajectory_errors() to reject tool evidence lacking readable class names and to reject internal identifier/path fields. Added tests for both the normalization and the new strict errors; focused tests/test_rebuild_sft.py tests/test_hermes_protocol.py -q passed 32 tests, with compilation and git diff --check also passing.
- Registered rag-hermes-blind-rag-medium-002-v2 against the same 40 isolated shards/medium-002/rag_targets.jsonl targets but a new output directory. Explicit --operation hermes-collect dry-run resolved to the intended v2 collector, and the detached recollection started as run 20260818T014326-e259e1da-a01, PID 2619423, writing to collection/rag-medium-002/.
- Volatile: the recollection is running; do not use rag-medium-001 as final-freeze source and do not launch a second recollection of these same targets. The old output remains preserved as lineage evidence.
- Next: after the run reaches a terminal state, run the strengthened strict audit, inspect a public tool payload, and only then select rows for the cumulative RAG pool and calculate bounded Oracle recovery for answer mismatches.

## 2026-08-18 02:23:30 CST - Complete readable-evidence medium RAG recollection and bounded Oracle recovery

- The independent Blind medium recollection completed at collection/rag-medium-002 with 28 accepted and 12 rejected of 40. Every accepted record re-passed strict Hermes validation, retained Blind lineage and hidden teacher label, contained three named public retrieval results, and had no internal IDs or private Oracle text. All 12 rejections were answer_target_mismatch.
- Built the lineaged 12-target Oracle recovery manifest from exactly those Blind mismatches. Capacity check predicted maximum possible counts of 3 open/en/pest, 13 open/zh/disease, 14 open/zh/pest, and 2 option/zh/pest, all within the 14-row cap. The request-time collector independently rechecked the same cap before contacting the teacher.
- Oracle recovery completed with 3 strict accepts and 9 answer_target_mismatch rejections. The accepts are one open/zh/disease and two open/zh/pest records; all have readable public class-name tool evidence, correct Hermes role order, matching answer, and no persisted Oracle wording. The nine rejected Oracle trajectories are terminal and must not be replayed.
- Actual cumulative Oracle counts are open/en/disease 1, open/en/pest 2, open/zh/disease 9, open/zh/pest 11, option/zh/disease 1, and option/zh/pest 1. No cell exceeded 14; unsuccessful requests do not consume the accepted-row cap.
- Evidence: outputs/experiments/hermes_long_direct_blind_rag_1to1_v2/{collection/rag-medium-002,collection/oracle-rag-recovery-medium-002,shards/oracle-recovery-medium-002}/ and outputs/runs/rag/{rag-hermes-blind-rag-medium-002-v2/20260818T014326-e259e1da-a01,rag-hermes-oracle-rag-medium-002-recovery-v2/20260818T021857-e259e1da-a01}/.
- Next: retain rag-medium-002 Blind accepts and the three accepted Oracle records as the only eligible outcomes of this target set; exclude all 40 contacted hashes from the next fresh-image plan and do not use the old unreadable-evidence medium output in a freeze.

## 2026-08-18 03:02:00 CST - Start immutable Hermes v2 large 1:1 collection in tmux

- Added a deterministic large-plan builder, capacity-aware Oracle-recovery truncation, a final 560:560 freeze preflight selector, and a tmux queue runner. The runner is fixed at four concurrent workers: two Direct and two Blind RAG, each writing a separate collection directory and log; it later creates a single capacity-capped Oracle manifest from only Blind answer-target mismatches.
- Initial large planning attempts exposed a real catalog issue: the historical 32-class contrast catalog cannot satisfy the 35-class/cell requirement. The builder now uses the 320-class Milvus canonical catalog and preserves the prior incomplete plans as non-contacted preflight evidence. A capacity-aware final plan selects classes with sufficient fresh-image reserve across globally isolated Direct and RAG routes.
- Immutable large-003 plan passed independent validation: 1,120 Direct rows and 1,120 Blind RAG rows; every one of the 16 route/language cells has 140 attempts drawn from 35 classes with four independent images/class. Direct has 560 bilingual source-image pairs, RAG has 1,120 unique images, and all 1,680 image hashes have zero overlap with Direct/RAG counterparts, the 192 diagnostic, the formal 618, prior freeze, or every contacted collection image. Hashes: Direct 52b4ed5a247a94580ba2174102756b25586bcd8ae358ba79bfea68dc8f14e0d5; RAG e34f8d2f8524581544ad02fe43a3f652e7a6925be0ab409a6b0c7da6d1ddd333.
- Started tmux session hermes-v2-large-20260818-large-003. Startup verification found exactly four workers: Direct open/en/disease and open/en/pest, Blind RAG open/en/disease and open/en/pest; the queue will advance without row-level monitoring. Logs are under outputs/runs/rag/hermes-large-1to1-20260818-large-003/logs/.
- Validation before launch: focused 33-test suite passed, Python compilation, zsh syntax check, credential availability check, and git diff --check passed.
- Volatile: the tmux queue is actively contacting the teacher. Do not launch a second session or reuse its target/output paths. Final-freeze preflight may report a shortfall; it must not freeze, train, or evaluate unless every 560:560 hard gate passes.

## 2026-08-18 03:39:00 CST - Recover stalled large collection with hard request timeouts and checkpoints

- The original four-worker tmux queue made no RAG progress after 18 minutes: four established teacher HTTPS connections had no accepted/rejected checkpoint and empty logs. The session was stopped before any further targets were contacted. Two Direct cells had already completed before cancellation: open/en/disease 130 strict accepts / 10 mismatches, and open/en/pest 50 strict accepts / 90 mismatches; all accepted Direct records re-pass strict audit.
- Added a 120-second hard wall-clock timeout around teacher and local retrieval HTTP requests in the v2 collector, and rewrites accepted/rejected JSONL after every target. A stalled request now creates a durable unknown_delivery_or_runtime_error record instead of losing an entire cell's progress.
- Two Chinese Direct and two English Open RAG cell starts had preview-only state when the queue was cancelled. Their first target is now separately recorded as unknown delivery with exactly one independent resend target; their remaining 139 never-contacted targets retain the immutable original manifest. No full cell was replayed.
- Fixed the conservative resume shell orchestration and started a two-window tmux recovery: one Direct continuation and one Blind RAG unknown-delivery resend, preserving the reduced 1 Direct + 1 RAG concurrency. The old tmux session remains stopped; do not revive it.
- Validation: focused 33-test suite, compilation, zsh syntax checks, and git diff --check pass. Volatile outputs are under outputs/runs/rag/hermes-large-1to1-20260818-large-003-resume/logs/.
- Next: let the checkpointing resume progress without row-level intervention; after terminal output, audit all accepted records and only then create the bounded Oracle recovery and freeze preflight.

## 2026-08-18 04:08:17 CST - Repair Chinese Direct gate and restart a fresh checkpointed large queue

- Chinese Direct traces retain the complete four-candidate / three-neighbor-exclusion audit. The language-specific floor is now 280 Chinese characters instead of the English 320; this corrects the observed encoding-density bias without relaxing structure. `re_audit_hermes_direct_rejections.py` promoted 18 qualifying prior Chinese rejections and retained all other failures.
- The collector now has a 120-second request deadline and commits accepted/rejected JSONL after every target. Historical large-003 and its resume were stopped; they are lineage-only and will not be revived.
- A fresh large-004 plan was generated after excluding every held-out and contacted hash: 1,120 Direct + 1,120 Blind-RAG attempts, 35 classes × four images per cell, Direct/RAG target-image disjointness, and zero shortages. It is running in tmux as one Direct plus one Blind-RAG worker at `outputs/runs/rag/hermes-large-1to1-20260818-large-004/logs/`.
- Repaired `select_hermes_1to1_large_freeze.py` to discover the checkpointed `large-*` layout and explicit `oracle-rag-*` recoveries using `generation_route`, include the Chinese re-audit, and exclude historical `rag-medium-001`. Added a regression test; focused protocol/data tests pass 35/35, compilation and `git diff --check` pass.
- Status: at 04:08 CST the queue had durable Direct 40 accepted / 1 rejected and Blind RAG 10 accepted / 14 rejected checkpoints. This is strictly pre-freeze; student conversion and SFT remain gated on an exact valid 560/560 selection.
- Next: let large-004 terminate, then select/audit the strict corpus; only a valid immutable freeze may be converted and trained.

## 2026-08-18 10:21:09 CST - Repair Micu credential mapping after large-004 HTTP 403 failures

- large-004 completed but is invalid as a freeze source. Its initial Open cells had known delivery, while later Option and RAG cells received persistent `HTTP Error 403: Forbidden`; all are durably recorded as unknown delivery and will not be replayed.
- Root cause: `apikey env micu_slb` exports `MICU_SLB_API_KEY` and `MICU_SLB_API_BASE_URL`, while the collector reads `YUNWU_API_KEY` and `YUNWU_API_BASE_URL`. The large runner evaluated the profile without mapping these names, permitting stale inherited values to control the request endpoint.
- Updated all large-run launch/resume scripts to map the freshly decrypted Micu variables explicitly. The checkpointed large runner now performs an authenticated `GET /models` preflight before any collection output is made, logging only status and never credentials or response content.
- Validation: syntax checks pass; the corrected mapping returns `/models` HTTP 200 with eight entries; `git diff --check` passes.
- Next: generate a fresh isolated replacement plan which excludes every large-004 contacted hash, then collect only after the new preflight succeeds.

## 2026-08-18 20:05:31 CST - Complete Hermes conversion and four-GPU SFT smoke from large-005 rows

- large-005 reached terminal accounting for all 16 collection cells. It is not a formal 560:560 freeze: Direct has 572 accepts but has severe Chinese shortfalls, while Blind RAG has 675 accepts but both Chinese Open cells have zero accepts. Manual review of accepted RAG traces also identified a semantic gap not expressed by the current structural validator: candidate lists can be bare retrieved class names rather than visual descriptions.
- Built an immutable, explicitly `trial_only_not_release` 96-row smoke freeze at `outputs/artifacts/datasets/agrinet-hermes-large005-trial-smoke-v1/`: 48 strict audited Direct rows and 48 strict audited Hermes RAG rows, with cross-route image isolation. The source/derived hashes are `0ceb0b5c66ec3fe04ebfa4071f252c42a13832fa660363f8e362052c6cd9f31e` and `52a0bd90376d83d68c17f75f9200730232281026f22112d98fbba912ac4237f1`.
- Hermes conversion succeeded with 48 Direct `system,user,assistant` records and 48 RAG `system,user,assistant,tool_call,tool,assistant` records; all image paths exist.
- Registered detached four-GPU full-parameter one-epoch SFT completed successfully: run `20260818T200239-e259e1da-a01`, exit 0, three optimizer steps, final checkpoint `outputs/vlm_sft/qwen3_vl_4b_hermes_large005_trial_smoke_v1/v0-20260818-200253/checkpoint-3`. This is a pipeline smoke artifact only, not a release candidate or diagnostic input.
- Next: tighten RAG semantic audit and collect fresh isolated rows for shortfall cells; retain the formal 560 Direct + 560 RAG freeze gate before the configured 5-epoch training.

## 2026-08-18 20:20:00 CST - Diagnose Chinese Open RAG mismatch and strengthen semantic evidence gates

- Chinese Open Blind RAG's 0/140 accepted result was primarily an answer-equivalence failure, not an absence of valid visual/tool trajectories: targets store Chinese canonical names while the teacher often emits the correct public English name. `answer_matches_target()` now accepts either public language for the target canonical class.
- Strengthened `strict_rag_trajectory_errors()` so the tool-preceding candidates must include concrete visual attributes rather than bare class names, and an exclusion based only on rank/similarity is rejected. The final RAG collection prompt now demands candidate-name-plus-visible-trait lines and morphology/symptom conflicts for exclusions.
- Added regression tests for bilingual Open answer matching and semantic RAG failures. Focused tests pass 37/37, compilation and diff checks pass.
- Applying the stronger audit to large-005 accepted RAG rows leaves: open/en/disease 38/85, open/en/pest 16/65, option/en/disease 62/134, option/en/pest 42/130, option/zh/disease 56/127, and option/zh/pest 50/134. The old accepted pool is consequently not eligible for a freeze without fresh targeted supplementation.
- Next: create fresh isolated targets for shortages using the repaired bilingual matcher and upgraded RAG teacher prompt; do not reinterpret rejected large-005 images as replayable.

## 2026-08-18 20:25:00 CST - Update the Hermes v2 candidate pool after bilingual and semantic-audit repair

- Created the immutable derived candidate view `outputs/experiments/hermes_long_direct_blind_rag_1to1_v2/candidate_pool/20260818-semantic-rag-bilingual-answer-v1/`. It preserves source collection JSONL unchanged and records the exact source files, selected hashes, and exclusions.
- The updated pool has 278 Direct candidates (hash `6513766771db8effa4e9f3d780e0ce1ab7cc074c37fdaa80fe17f4283380fc94`) and 244 RAG candidates under the strengthened semantic contract (hash `7856f857578d3c95712179aa81c8e1fb33db3b69ff66994f401f0d94e5ced52b`). It remains explicitly unauthorized as a formal freeze.
- The bilingual repair identifies 228 historical Chinese Open answer mismatches that are now canonical-name equivalent: 163 disease and 65 pest. They are diagnostic recovery evidence only: rejected records retain first/final teacher text but not the actual public tool return, so promoting them would violate the persisted evidence contract.
- Validation: focused protocol/data tests pass 37/37 and diff checks pass.
- Next: plan fresh isolated supplemental collection for the measured per-cell shortfalls, using the repaired answer matcher and semantic prompt/validator.

## 2026-08-18 20:30:00 CST - Persist completed known RAG rejections and materialize cell shortfalls

- Updated the Hermes collector so a completed Direct or RAG trajectory that fails a quality/answer gate is saved in `rejected.jsonl` with `target`, `errors`, `status: known_strict_rejection`, and the complete sanitized `trajectory`. This preserves public tool evidence for future contract re-audits. Unknown delivery remains target/error-only because no complete trajectory exists.
- Generated `outputs/experiments/hermes_long_direct_blind_rag_1to1_v2/shortage_reports/20260818-semantic-rag-bilingual-answer-v1.json` from the non-mutating candidate pool. It reports rows, class coverage, per-class cap state, and Option-letter deficits for every formal 70-row cell.
- Largest deficits include Direct option/{en,zh}/pest (62 rows and 27 classes each) and Blind RAG open/{zh}/disease plus open/{zh}/pest (69 rows and 34 classes each).
- Validation: focused tests pass 38/38; compilation and diff checks pass.
- Next: build a fresh-image, cell-targeted preflight manifest from this report, then collect only after the repaired Chinese/RAG prompt passes its small new-image acceptance check.

## 2026-08-18 21:50:00 CST - Start fresh isolated high-risk repair preflight

- Built immutable large-006 plan after excluding 4,763 held-out and contacted hashes. The plan has full 1,120 Direct + 1,120 Blind-RAG capacity with 35 classes per cell and zero Direct/RAG target-image overlap.
- Extracted a targeted 48-row preflight: 12 English/Chinese Direct Option-pest image pairs plus 12 Blind RAG Chinese Open-disease and 12 Blind RAG Chinese Open-pest targets. It preserves Direct bilingual pairing and has zero cross-route overlap.
- Validation: plan and preflight manifest hashes are recorded under `large-20260818-large-006/{plan,preflight}/`; focused tests pass 38/38; Micu authenticated `/models` preflight is HTTP 200.
- Started tmux `hermes-v2-targeted-preflight-large-006` with one Direct and one Blind-RAG worker. Known rejections will retain complete sanitized trajectories.
- Next: audit the four small cells after terminal completion; scale only paths that clear the repaired answer and semantic-quality contracts.

## 2026-08-18 23:03:40 CST - Audit large-006 supplementation and isolate a second zh Open pest preflight

- The safe large-006 targeted supplement is terminal with no unknown delivery or Micu authentication failure: Direct option/en/pest accepted 125/128; Direct option/zh/pest accepted 68/128; Blind RAG open/zh/disease accepted 63/128. Completed known rejections remain persisted with sanitized trajectories.
- Re-auditing the new Direct zh rejects (0/10 promoted) and RAG zh-disease rejects (0/65 promoted) found no parser-only false rejections; they remain excluded under the current long-trajectory contracts.
- The immutable candidate view `outputs/experiments/hermes_long_direct_blind_rag_1to1_v2/candidate_pool/20260818-large006-supplement-reaudit-v3/` now contains 388 Direct (hash `3124244d398e27e430280c0af2e72e127f4ef8252c9cbf128740b5b8433a11e7`) and 306 RAG (hash `766e1110633f36a7bda44e089bff98265be2aec30debae71ce242c04e95dc29f`) rows. Its `shortage_reports/20260818-large006-supplement-reaudit-v3.json` remains not freeze-authorized.
- Direct option/{en,zh}/pest is near readiness at 63 rows and 36 classes each. Blind RAG remains the main deficit; open/zh/pest is high-risk at 4 rows and 4 classes, so it was not scaled.
- Added a `--no-direct` extraction mode to the targeted-preflight builder. A 12-image open/zh/pest offset-12 manifest uses untouched large-006 targets and is running as the sole new collection in tmux `hermes-v2-preflight-rag-open-zh-pest-v2`; it must be strictly audited before scale-up.
- Validation: generator compilation, runner `zsh -n`, and targeted `git diff --check` pass.
- Next: after this preflight is terminal, audit it and rebuild the candidate/shortage view; derive only fresh hash-isolated, class-targeted supplements from the remaining gaps.

## 2026-08-19 00:06:18 CST - Recover unambiguous Direct Open-pest public common names

- large-007 remains active as one Direct and one Blind-RAG worker. Its Direct Open-pest English traces exposed an answer-contract issue: the image comparison and Chinese same-image answer were valid, but English answers used public common names such as `Black cutworm` where the canonical catalog stores `agrotis ypsilon`.
- Added a deliberately small, one-to-one public common-name equivalence table to Open-answer matching. It only accepts an alias when it uniquely identifies one canonical class; ambiguous names such as `Cotton bollworm` and `Fall armyworm` remain rejected. Direct prompts and their existing internal quality-control information were not changed.
- Re-auditing the persisted Direct open/en/pest rejects promoted 42/49 complete trajectories; 7 remain rejected. The new-English plus Chinese result has 59 same-image valid pairs from this supplement (63 English / 66 Chinese), subject to final global selection caps.
- Validation: 41 focused tests pass, compilation and `git diff --check` pass.
- Next: allow large-007 to finish; only then rebuild the derived candidate/shortage view and plan fresh images for any residual pair/class deficits.

## 2026-08-19 05:00:29 CST - Monitor large-008 and attach an accounting-gated freeze preflight

- The fresh isolated `large-008` targeted supplementation is active in tmux session `hermes-v2-large008-targeted-supplement`. At the checkpoint, three Direct targets had terminal accounting (16/16 Open English disease, 62 accepted plus 8 rejected Open English pest, and 16/16 Open Chinese disease); Open Chinese pest and Blind Open English pest were continuing. No 403, authentication, timeout, or unknown-delivery pattern was found in current run logs.
- One selected accepted trajectory from each active route was inspected. Direct retained four concrete visible observations and three specific neighbor exclusions without tools; Blind RAG used a public-name tool return with visual candidates before the call. The focused contract suite passed 41 tests.
- Added `scripts/rag/watch_hermes_v2_large008_then_preflight_tmux.sh` and started tmux `hermes-v2-large008-postflight`. It waits for actual collector processes (not the persistent interactive collector tmux shell), verifies closed `accepted + rejected = manifest` accounting for every large-008 target, then runs one fresh strict selector preflight at `outputs/experiments/hermes_long_direct_blind_rag_1to1_v2/large-20260819-freeze-preflight-v2/`. It never launches or replays sampling.
- Next: inspect the postflight result only after all collectors are terminal. Freeze, student conversion, and the formal 5-epoch SFT remain gated on a valid exact 560 Direct + 560 RAG selection.

## 2026-08-19 12:53:37 CST - Rebuild formal evaluation around native SGLang DP8 and durable async samples

- Added `configs/experiments/vlm/vlm-formal618-native-sglang-dp8-v1.yaml` and `scripts/vlm/run_formal618_native_dp8.sh`. The new entrypoint always creates a fresh `outputs/runs/vlm/.../<run-id>/` root, runs candidate/base Direct then RAG sequentially with native `sglang.launch_server`, and does not read historical `formal618`, `formal618-dp8`, or `formal618-native-dp8` outputs.
- Added a native service manager with GPU-product validation, dedicated service stdout/stderr/PID/config/health/exit records, `/model_info` then `/v1/models` health checks, minimal chat fallback, and process-group cleanup. Default evaluation topology is GPU 0--7 with TP=1, DP=8.
- Reworked Direct and RAG endpoint evaluators to durable async sample execution. Defaults are Direct 64 and RAG 24 concurrent samples; RAG keeps each sample's model/tool/model state machine serialized. Every completed sample is appended to a fingerprinted snapshot, resume reuses only matching manifest/model/protocol/generation state, and final predictions are re-emitted in manifest order.
- Scoring now fail-closes on missing, duplicate, unknown, reordered, or explicit-error prediction rows when given a manifest. Formal paired reviews run only after all four routes score cleanly; the final summary captures manifest and route hashes, DP topology, concurrency, retries, and bootstrap seed.
- Validation: `bash -n`, Python compilation, `git diff --check`, and 22 focused tests (`test_formal_eval_runtime.py`, Direct/RAG boundary tests, and Hermes protocol tests) passed. No model service or formal evaluation was launched.
- Next: invoke `bash scripts/vlm/run_formal618_native_dp8.sh` only when the formal candidate is authorized; inspect the new run-root `summary.json` and route/service logs rather than legacy formal directories.

## 2026-08-19 22:25:00 CST - Make Hermes RAG formal evaluation protocol v3 strict and machine-gated

- Scope: evaluation-only refactor. No model weights, training data, collection ratio, or retrieval index changed.
- `vlm/eval/tools/run_qwen3_vl_rag_sglang_eval.py` now classifies conservative syntactic tool attempts beyond literal XML tags: malformed bare JSON, multiple JSON objects, malformed XML JSON/envelopes, wrong names, invalid arguments, and multiple calls. Ordinary final prose is not treated as a tool attempt. Per-row protocol records now separate malformed reasons, noncanonical recoveries, post-budget attempts, terminal closures/failures, fallback turns, and answer-format corrections.
- Added `--invalid-tool-call-policy {strict,recovery}`. The default and the formal route are `strict`: malformed calls never trigger a synthetic retrieval; they receive one public terminal-answer closure, and any further tool attempt is an explicit error. `recovery` retains the fixed, label-blind visual fallback exclusively as an explicit diagnostic mode. The policy and terminal-policy revision participate in the durable request fingerprint.
- `scripts/vlm/run_rag_formal618_native_dp8.sh` now requires strict policy, runs a machine-checkable post-score gate (zero errors, final tool-call rows, unparseable rate, and terminal-closure failures), then permits paired bootstrap. Its formal summary schema is `agrinet.rag-formal-evaluation-native-sglang-dp8/v3` and includes detailed protocol counters. The registered RAG config declares the same strict terminal policy.
- Validation: focused RAG/formal-runtime/language/Hermes/manual-template suite passed (`44 passed`); Python compilation, shell syntax, and `git diff --check` passed. No service, smoke, or formal 618 evaluation was launched.
- Next: run a new-root, non-formal DP8 strict-policy smoke and inspect zero terminal-closure failures before authorizing a new v3-strict formal 618 comparison.

## 2026-08-20 20:03:00 CST - Establish comparison-centric paper motivation

Tags: HCV, open-vocabulary, agricultural recognition, RAG, motivation

### Note
Added `docs/motivation.md` as a paper-facing motivation document. It formulates
open-vocabulary agricultural disease and pest recognition as comparative
hypothesis verification and proposes Hypothesize--Contrast--Verify (HCV):
Direct visual hypotheses anchor a contrast state, retrieval seeks
similar-class differential evidence, and a visual--knowledge loop verifies the
final decision. It positions the agricultural visual--semantic knowledge base
as a proposed comparative evidence space.

### Status
The document explicitly separates verified observations (including M1 and M3)
from proposed method, database, training, and evaluation work. Teacher
distillation and evidence-delta filtering are placed as implementation and
data-quality mechanisms, not headline novelty claims.

### Next
Discuss and refine the HCV method claim before changing the tool contract,
collecting new data, or launching SFT/evaluation.

## 2026-08-20 20:41:16 CST - Return curated similar-class evidence in public RAG results

Tags: RAG, Milvus, similar-classes, HCV, evaluation-contract

### Note
The wiki already contains `similar_english_classes` and
`similar_chinese_classes`, but the online retrieval field allowlist and
evaluation compacting path omitted them. The RAG backend now requests these
fields from new indexes and safely falls back to the canonical public wiki for
the currently running pre-extension Lite index. Compact public hits and the
model-visible evaluator `tool` turn preserve up to five bilingual similar
classes; no entry IDs, local paths, or evaluation labels are exposed.

### Status
Verified on the restarted managed service at
`outputs/runs/rag/rag-serve-siglip2-milvus-local-v1/20260820T204116-e259e1da-a01/`:
a real visual request returned `Pomegranate Cercospora` plus five public
similar classes, and the evaluator serialization retained the same list. The
old index was not rebuilt or mutated. RAG evaluator protocol fingerprint is now
`agrinet.hermes-rag-sglang-async/v4-native-json-strict-similar-classes`, which
prevents resume from mixing old and expanded evidence snapshots. Focused tests
passed: 68 tests across HTTP, retrieval service, strict RAG evaluation, and
SFT recovery checks.

### Next
Use the public similar-class evidence in the planned image-disjoint HCV
preflight; do not reinterpret historical formal results under the new evidence
contract.

## 2026-08-22 17:50:13 CST - Complete MQ-0 text-only and evidence-lineage contract

Tags: HCV, RAG, MQ-0, image-none, evaluator-contract

### Note
Completed the first HCV implementation gate. `semantic` and `name` retrieval
now require `image=none` and no HTTP image path; `visual`, `balanced`, and
`rrf` require `image=query_image` and an image path. The evaluator preserves
request/response ledgers, rejects name lookups before public evidence or names
not copied from public result/alias/similar-class fields, and rejects exact
duplicate requests before dispatch.

### Status
Focused contract, HTTP, service, evaluator, and SFT-recovery tests pass
(75 tests). The service was restarted at
`outputs/runs/rag/rag-serve-siglip2-milvus-local-v1/20260822T175013-865e28b9-a01/`.
A real image-free semantic request returned `Pomegranate Anthracnose` with its
public bilingual similar-class list. This also fixed a Lite-only schema-layout
bug that had returned semantic distance rows without readable class metadata.

### Next
Build an image-disjoint preflight to measure first-to-second-turn evidence
delta before constructing or training any HCV SFT derivative.

## 2026-08-22 17:56:58 CST - Commit MQ-0 baseline and start HCV Phase-1

Tags: HCV, RAG, MQ-0, reproducibility, git

### Note
Committed the validated MQ-0 implementation as `380ce126a`
(`feat(rag): enforce multi-query evidence contracts`). The commit contains the
strict image-mode contract, public similar-class return path, evaluator
lineage/duplicate checks, the image-isolated HCV preflight tool, focused tests,
and the associated roadmap/entrypoint updates.

### Status
The repository is clean immediately after the baseline commit; focused test
suite passed 75 tests and `py_compile` plus `git diff --check` passed. The
branch remains local-only and is 66 commits ahead of `origin/main`.

### Next
Execute the Phase-1 preflight according to the dated HCV plan. It is a
hard gate: no new SFT freeze or DP8 training may begin until image isolation,
public evidence lineage, and evidence-delta checks pass.

## 2026-08-22 18:30:00 CST - Validate isolated HCV visual-expansion preflight

Tags: HCV, RAG, retrieval, isolation, preflight

### Note
Hardened `tools/rag_distill/hcv_retrieval_preflight.py` for direct script
execution, fixed-source hash audits, public-only reusable manifests, per-row
error records, per-cell summaries, and a fail-closed quality gate. The gate
requires a real truth-hit repair, not merely a changed candidate list.

### Status
The accepted real-service preflight is
`outputs/experiments/hcv_multi_query_rag/preflight/20260822T183000-phase1-visual-expand/`.
It selected 32 unique images (all 8 cells full), excluded 2,361 known hashes
including 618 formal and M3 SFT sources, and had zero retrieval/protocol
errors. A top-3 visual call followed by an explicit top-10 visual candidate
expansion changed evidence for 32/32 rows and repaired 6 initially absent
truth hits. Fixed `balanced`, `semantic`, and `name` follow-ups repaired 0
rows, so they remain diagnostic-only pending per-trajectory evidence value.
Focused tests: 36 passed across preflight, contract, HTTP, service, and strict
evaluator coverage.

### Next
Freeze an image-isolated candidate pool containing Direct replay, one-call
stop, and only auditable visual-expansion trajectories; token-balance it before
registering an 8-GPU SFT run.

## 2026-08-22 20:45:00 CST - Complete blind HCV teacher plan and freeze gates

Tags: HCV, RAG, teacher-collection, image-isolation, freeze-gate

### Note
Expanded the retrieval-only image-isolated audit to a 320-image union with
prior manifests explicitly excluded at selection time. Added a visual top-3 to
top-10 strategy to the collector, bilingual public Option presentation with
private-only answer letters, and a fail-closed post-collection audit.

### Status
The complete public plan is
outputs/experiments/hcv_multi_query_rag/teacher_plan/20260822T204000-expand-320-complete-bilingual/.
It has 32 rows, four per Open/Option × en/zh × disease/pest cell, unique image
hashes, blind-evidence route only, and no truth fields. The post-collection
audit requires blind provenance, exactly two visual calls, second-turn public
evidence delta, a correct private audit match, unique images, and four valid
rows per cell. Focused tests passed: 39.

No teacher request was sent: the local GPG secret cannot be decrypted in this
noninteractive environment. This is a credential-unlock prerequisite, not an
unknown provider delivery.

### Next
After interactive GPG unlock, run a one-image teacher preflight against the
complete plan, then collect and post-audit the 32 rows before HCV SFT freeze
or 8-GPU training.

## 2026-08-22 21:05:00 CST - Register fail-closed HCV native-DP8 SFT/evaluation queue

Tags: HCV, DP8, SFT, freeze-gate, reproducibility

### Note
Registered `vlm-sft-qwen3vl4b-hcv-manual-json-8gpu-v1` and its native
manual-JSON configuration. The queue trains on all eight local GPUs
(`TP=1`, `DP=8` for evaluation), then serially runs strict 64-row smoke and
618-row paired formal routes for checkpoints 36, 72, and 108. It forwards an
HCV freeze-audit path to the queue script.

### Status
Before creating queue artifacts or invoking Swift, the script now requires the
audit JSON to exist and contain `training_authorized: true`; otherwise it exits
without training. The expected 18 optimizer steps/epoch follows the established
8-GPU, per-device batch 2, accumulation 8 schedule, so the checkpoint mapping
is 2/4/6 epochs to 36/72/108 steps. A focused 66-test suite, shell syntax
check, config discovery, and dry-run passed.

The historical duplicate experiment definition with a non-canonical filename
was moved to `configs/archive/rag_sft/experiment_definitions/`; its canonical
active counterpart remains unchanged. This fixes CLI discovery without deleting
historical provenance.

### Next
The queue must remain idle until the blind teacher collection passes its audit
and `freeze_hcv_sft.py` produces the immutable authorized artifact.

## 2026-08-23 00:00:34 CST - HCV teacher preflight stopped by outbound network

Tags: HCV, teacher-collection, preflight, network, fail-closed

### Note
The Yunwu credential helper succeeded after interactive GPG unlock. The planned
one-image HCV preflight was then invoked against the public, blind teacher plan
and the live local retrieval service.

### Status
The preflight failed before receiving a provider response: the socket connect to
`https://yunwu.ai/v1` raised `Errno 101: Network is unreachable`. The durable
report records `status: failed`, `reachable: false`, and
`failure_classification: transport_or_runtime_error` at
`outputs/experiments/hcv_multi_query_rag/teacher_plan/20260822T204000-expand-320-complete-bilingual/teacher_preflight.json`.
This is a known connection failure, not an unknown-delivery request. No teacher
trajectory, freeze, training, or formal evaluation was started.

### Next
Restore outbound connectivity to the configured Yunwu endpoint, rerun exactly
the one-image preflight, and only then start the serialized 32-row collection.

## 2026-08-23 00:10:00 CST - Switch HCV collection to Micu and stop on local expansion bug

Tags: HCV, Micu-SLB, teacher-collection, fail-closed, recovery

The user selected the existing `micu_slb` profile. Its one-image HCV preflight
passed against `https://api-slb.micuapi.ai/v1` with `gpt-5.6-terra`, including
a valid real-image manual JSON tool call. The first 32-row collection then
revealed a local `UnboundLocalError`: HCV top-3-to-top-10 expansion referenced
`visible_call` before initialization on the valid-tool branch. Twelve rows
failed locally with zero retrieval calls; the thirteenth was interrupted with
unknown delivery. None of these 13 images may be reused.

The collector initializes the public envelope before branching, and a regression
test now verifies valid visual calls at top-k 3 then 10. The CLI forwards the
declared two-turn parameters and a registered Micu HCV collection entry holds
the serialized contract. Focused HCV/CLI tests passed (17). The stopped run is
lineage exclusion evidence only. A replacement retrieval audit excluding the
original plan images is replenishing true visual-expansion repairs.

Next: finish enough image-isolated replacements to restore four unused repair
trajectories in every affected cell, rebuild the blind plan, then preflight and
collect through Micu.

## 2026-08-23 00:45:00 CST - HCV Micu collection passes protocol but fails answer-quality freeze gate

Tags: HCV, Micu-SLB, quality-gate, unknown-delivery, freeze

The repaired collection produced 26 accepted two-turn visual trajectories. Six
later Micu TLS EOF failures were marked unknown delivery and never replayed. A
fresh six-row, image-isolated supplement completed with six accepts and zero
unknown outcomes. The merge has exactly 32 unique IDs and exact private-audit
coverage.

The strict post-collection audit rejected freeze: all rows meet blind evidence,
visual top-3 to top-10, public evidence-delta, and image-uniqueness checks,
but only 13 final answers are correct and no cell reaches four valid rows. The
merged artifact at
`outputs/experiments/hcv_multi_query_rag/teacher_collection/20260823T004500-micu-complete-merged/`
is diagnostic evidence only. A capacity audit found only 25 uncontacted genuine
visual-expansion repairs remaining, with no Chinese Option repair candidates.

The plan rebuild now excludes both retired sample IDs and their image hashes,
so unknown-delivery images cannot re-enter through a later local audit. Next:
diagnose why teacher final selection ignores or misranks truthful expanded
evidence, improve the decision contract, and verify it on a fresh, per-cell
quality-gated pilot before any new SFT freeze.

## 2026-08-23 01:09:56 CST - Validate HCV expanded-candidate decision pilot and reject promotion

Tags: HCV, Micu-SLB, candidate-comparison, quality-gate, image-isolation

### Context
The prior complete Micu collection met the two-turn evidence contract but often
selected retrieval rank 1 despite the private truth appearing elsewhere in the
expanded top-10 evidence.

### Changes
`run_pilot.py` now builds a public-only expanded candidate ledger after visual
top-3→top-10, asks the teacher to compare at least three candidates (including
one newly expanded candidate), and requires two trait-based rejections. The
same condition is checked before accepting a trajectory and by
`audit_hcv_teacher_collection.py`. The plan builder can exclude image hashes
from prior teacher plans; diagnostic-pilot plans are permanently marked
`freeze_authorized: false`. A registered serialized Micu 8-row pilot uses
fresh image hashes across every Open/Option × en/zh × disease/pest cell.

### Validation
Focused HCV/CLI tests passed (23). The managed run
`outputs/runs/rag/rag-hcv-visual-expand-micu-decision-pilot-v1/20260823T010403-5bacde03-a01/`
exited 0 with 8 accepted, 0 rejected, and 0 unknown deliveries. All rows used
exactly visual `top_k=3` then visual `top_k=10`.

### Impact
- Verified: the candidate-comparison protocol is executable and auditable, but
  its private audit is only 5/8 correct; `pilot_authorized=false` and
  `freeze_authorized=false` at
  `outputs/experiments/hcv_multi_query_rag/teacher_collection/20260823T010600-micu-decision-contract-pilot/reports/hcv_collection_pilot_audit.json`.
- Verified: each erroneous truth is still present in top-10 (ranks 4, 8, and
  5), so the remaining failure is discriminative selection rather than recall
  or manual-JSON/tool sequencing.
- Compatibility / scope: no unknown-delivery request was replayed; no new HCV
  output is eligible for SFT freeze, training, or DP8 evaluation.

### Next
Strengthen the public evidence representation or candidate-discrimination method
before any further Micu request, then run another new-image quality pilot.
