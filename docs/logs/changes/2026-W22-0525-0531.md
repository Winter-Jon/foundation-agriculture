# Change Log - 2026-W22 - 0525-0531

## 2026-05-26 15:51:03 CST - Initialize structured research logs

### Context

The repository needed a durable research-recording structure that supports experiment logs, change logs, plans, decisions, observations, and handoff notes while allowing new conversations to recover context quickly.

### Changes

- Created `docs/logs/` as the repository-local research log root.
- Added type directories for experiments, changes, plans, decisions, observations, and handoffs.
- Added `START_HERE.md` as the fast startup entrypoint for future conversations.
- Added `INDEX.md` as a compact link index.
- Established weekly file naming as `YYYY-WW-MMDD-MMDD.md` and per-record timestamps precise to seconds.

### Validation

Not recorded yet.

### Impact

Future research records should be appended under `docs/logs/` and should link large artifacts from their original locations instead of copying them into Markdown logs.

### Next

Use the `research-record` skill for future structured research records.

## 2026-05-26 15:51:03 CST - Organize existing docs by research log type

### Context

The repository already had top-level Markdown documents under `docs/`. These documents needed to be placed under the new type-based log structure without rewriting their historical content.

### Changes

- Moved experiment-oriented documents to `docs/logs/experiments/`.
- Moved planning and proposal documents to `docs/logs/plans/`.
- Moved handoff-oriented documents to `docs/logs/handoffs/`.
- Added historical topic links to `docs/logs/INDEX.md`.

### Validation

Searched for stale references to the old top-level `docs/*.md` paths and found no external references beyond the new index entries.

### Impact

Existing documents remain intact as historical topic documents while new routine records should use weekly type logs.

### Next

When a historical topic document becomes relevant, link to it from a concise weekly record rather than copying its full content.

## 2026-05-26 18:33:49 CST - Add AgriNet-1K open-domain WDS packing workflow

### Context

AgriNet-1K needed an open-domain split derived from `datasets/AgriNet-1K/all`, with rare classes withheld from training and exposed as unknown classes in validation/test. The WDS output must contain real image bytes, not symlink entries.

### Changes

- Added `tools/create_agrinet1k_open_domain_split.py` to build the open-domain split plan, summary, rollback script, and WDS shards.
- Added `scripts/vision/preprocess/pack_agrinet1k_open_domain.slurm` to run the WDS packing step through Slurm with 32 CPU workers.
- The packing path reads `open_domain/manifests/split_info_20260526_180357.json`, opens each `all/<class>/<image>` source image, resizes short side to 256, and writes `<key>.jpg` plus `<key>.cls` into tar shards.

### Validation

- `python3 tools/create_agrinet1k_open_domain_split.py --pack-only --dry-run --split-info datasets/AgriNet-1K/open_domain/manifests/split_info_20260526_180357.json`
- Dry-run counts: train 1,164,286; val 167,455; test 338,616.
- Submitted Slurm job `17453` with scheduler logs `slurm/pack-agrinet1k-open_17453.out` and `slurm/pack-agrinet1k-open_17453.err`.

### Impact

Program outputs are under `datasets/AgriNet-1K/open_domain/`, especially `manifests/`, `logs/`, `scripts/rollback_20260526_180357.sh`, and `wds_split/`.

### Next

Monitor job `17453`; after completion, verify shard counts and inspect a sample tar to confirm `.jpg` and `.cls` members are present.

## 2026-05-27 17:40:59 CST - Rebuild AgriNet-1K strict open-domain split and WDS shards

### Context

AgriNet-1K open-domain needed a stricter split based on the original `datasets/AgriNet-1K/split_info.json` and `datasets/AgriNet-1K/cross_entropy.csv`. The target was to double the original validation/test sizes to `val=100000` and `test=200000`, keep unknown classes out of train, put all unknown classes in test, and keep validation entropy close to train while making test high entropy.

### Changes

- Extended `tools/create_agrinet1k_open_domain_split.py` with `--split-policy original-double-cross-entropy`.
- Rebuilt `datasets/AgriNet-1K/open_domain/` from `datasets/AgriNet-1K/all/` with `--replace-open-domain --skip-pack` and timestamp `20260527_145700_strict`.
- Generated strict artifacts:
  - `datasets/AgriNet-1K/open_domain/manifests/migration_plan_20260527_145700_strict.csv`
  - `datasets/AgriNet-1K/open_domain/manifests/migration_summary_20260527_145700_strict.csv`
  - `datasets/AgriNet-1K/open_domain/manifests/split_info_20260527_145700_strict.json`
  - `datasets/AgriNet-1K/open_domain/logs/migration_20260527_145700_strict.log`
  - `datasets/AgriNet-1K/open_domain/scripts/rollback_20260527_145700_strict.sh`
- Updated `scripts/vision/preprocess/pack_agrinet1k_open_domain.slurm` to pack from `split_info_20260527_145700_strict.json` with 32 workers, shard size 2000, and target image short side 256.
- Updated the Slurm script to use `/home/jiangwentao/dev/miniconda/envs/torch/bin/python` because the batch `python3` and `/usr/bin/python3` environments lacked `PIL` on compute nodes.

### Validation

- Dry-run strict split produced `train=1370357`, `val=100000`, `test=200000`, `unknown_classes=154`, `val_unknown_classes=46`, and `test_only_unknown_classes=108`.
- Formal `migration_plan_20260527_145700_strict.csv` has `1670358` lines, equal to `1670357` images plus header.
- Symlink view counts match split info:
  - train: `1370357`
  - val: `100000`
  - test: `200000`
- Slurm WDS pack job `17479` completed successfully:
  - Scheduler state: `COMPLETED`
  - Exit code: `0:0`
  - Elapsed: `02:23:23`
  - Scheduler logs: `slurm/pack-agrinet1k-open_17479.out`, `slurm/pack-agrinet1k-open_17479.err`
- WDS shard counts:
  - train: `686` tar files
  - val: `50` tar files
  - test: `100` tar files
  - `.tar.tmp` residuals: `0`
- Sample tar inspection confirmed paired `.jpg` and `.cls` members. Sample `.jpg` files start with JPEG magic bytes `ffd8ff`, confirming real image bytes were written instead of symlink entries.
- Entropy distribution from `split_info_20260527_145700_strict.json`:
  - train: mean `0.192238`, p50 `0.150757`, p90 `0.308838`, p99 `0.960938`
  - val: mean `0.206007`, p50 `0.150635`, p90 `0.352783`, p99 `1.033203`
  - test: mean `2.397627`, p50 `1.938477`, p90 `5.488281`, p99 `8.507812`
- Unknown-class constraints:
  - train unknown classes: `0`
  - val unknown classes: `46`
  - test unknown classes: `154`
  - test contains all unknown classes: true

### Impact

The current canonical open-domain dataset is the strict split under `datasets/AgriNet-1K/open_domain/`. Validation now has a train-like entropy distribution for model selection, while test is intentionally high entropy for difficult open-domain evaluation. The WDS shards are ready for downstream training/evaluation workflows.

Previous failed pack jobs `17477` and `17478` were environment failures caused by missing `PIL`; their logs are superseded by successful job `17479`.

### Next

Use `datasets/AgriNet-1K/open_domain/manifests/split_info_20260527_145700_strict.json` and `datasets/AgriNet-1K/open_domain/wds_split/` for subsequent experiments. If downstream loaders assume the earlier non-strict split counts, update their config or documentation before running training.
