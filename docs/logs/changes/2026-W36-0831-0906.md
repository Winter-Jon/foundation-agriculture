# Changes — 2026-W36 (0831–0906)

## 2026-09-01 21:14:00 CST - Local artifact cleanup audit

Tags: cleanup, outputs, checkpoints, reproducibility

### Context
Audit local ignored artifacts after consolidating the current Formal-618
evaluation entrypoint.

### Changes
- Removed the failed temporary environment
  `.venv_test_tf590_failed_20260901`, `.pytest_cache`, and generated
  `__pycache__` directories.
- Restored two historical tracked `.pyc` files from `HEAD` after discovering
  they are versioned; the Git worktree is clean.
- Updated `docs/plan/direct-rag-dual-milestone-roadmap.md` to link to the
  current Formal-618 entrypoint rather than the archived milestone document.

### Validation
- Main `.venv`, `.venv_test`, `models/`, `datasets/`, and `outputs/` were not
  deleted.
- `git status --short --branch` is clean after restoration.
- `outputs/vlm_sft/` contains 60 checkpoints / 497GB.

### Impact
- Verified: the low-risk cleanup removed a failed 5.7GB environment and
  regenerable caches only.
- Verified: 3 current Formal-618 checkpoints occupy 24.8GB; 7 configured or
  historical reference checkpoints occupy 57.9GB; 50 checkpoints without an
  active text reference occupy 413.8GB.
- Compatibility / scope: lack of an active text reference is only a deletion
  candidate signal, not deletion authorization.

### Next
Review an explicit checkpoint-by-checkpoint deletion manifest before removing
any high-value model artifact.

## 2026-09-01 21:30:00 CST - Approved Batch A checkpoint cleanup

Tags: cleanup, checkpoints, batch-a, outputs

### Context
User approved Batch A from `docs/results/CHECKPOINT_CLEANUP_REVIEW.md`.

### Changes
- Removed exactly 23 approved, non-selected checkpoint directories: v12 RAG
  checkpoints 464/696; v12 Direct-only 70/140; rejected interim checkpoints
  51/102/153; non-selected M1-like and user-guidance-v3 checkpoints; Direct
  reproduction intermediates; and v4 checkpoints 64/128/192/256.

### Validation
- Pre-delete check: all 23 targets existed, had no active exact-path reference
  in docs/configs/scripts/src/tools, and totaled 190.37GiB.
- Post-delete: `outputs/vlm_sft/` fell from 497GB to 307GB and checkpoint
  count fell from 60 to 37.
- Current Formal-618 M1 checkpoint-165, RAG checkpoint-232, and v4
  checkpoint-320 were verified present. Git worktree remains clean.

### Impact
- Verified: approximately 190GB was released.
- Recovery: deleted local checkpoint data is not recoverable from Git; it must
  be recreated from training or restored from an external backup if needed.

### Next
Await explicit approval for Batch B and/or Batch C; do not remove additional
checkpoint branches automatically.

## 2026-09-01 21:45:00 CST - Approved Batch B/C conservative checkpoint cleanup

Tags: cleanup, checkpoints, batch-b, batch-c, outputs

### Context
User approved the conservative B/C cleanup proposal from
`docs/results/CHECKPOINT_CLEANUP_REVIEW.md`.

### Changes
- Removed all 12 Batch B ablation checkpoints.
- Removed 13 conservative Batch C candidates: all five open-only checkpoints,
  four latest-reproduction intermediates, and four latest-v3 intermediates.
- Retained the latest-reproduction `checkpoint-165` and latest-v3
  `checkpoint-320` endpoints for possible response replay.

### Validation
- Pre-delete: all 25 explicit targets existed, had no active exact-path
  reference, and totaled 206.92GiB.
- Post-delete: `outputs/vlm_sft/` fell from 307GB to 100GB; checkpoint count
  fell from 37 to 12.
- Current Formal-618 M1 checkpoint-165, RAG checkpoint-232, v4
  checkpoint-320, and both retained endpoints were verified present.

### Impact
- Verified: approximately 207GB was released in this batch; combined Batch A
  and conservative B/C cleanup reduced `outputs/vlm_sft/` from 497GB to 100GB.
- Recovery: removed local checkpoint data is recoverable only by retraining or
  external backup restoration.

### Next
Retain the remaining 12 checkpoints unless a new, explicit review authorizes
their removal.

## 2026-09-01 22:00:00 CST - Approved preflight-result cleanup

Tags: cleanup, preflight, smoke, outputs

### Context
User approved removal of the two high-priority completed preflight result
directories identified in the local-artifact audit.

### Changes
- Removed `outputs/runs/vlm/m1-ms-swift-latest-preflight-v2` (about 17GB).
- Removed `outputs/runs/vlm/m1-latest-ms-swift-v4-8gpu-preflight-v1` (about
  8.3GB).
- Related smoke YAML configurations and historical experiment records remain
  versioned, but their deleted local models can no longer be replayed without
  regeneration.

### Validation
- Both explicit directories existed before deletion and no associated process
  was running.
- Both paths were absent afterward. `outputs/runs/vlm` fell to 2.5GB and total
  `outputs/` fell from 272GB to 244GB.
- Git worktree remains clean.

### Next
The remaining large cleanup candidates are the historical RAG smoke/full model
artifacts and versioned recovery datasets; review their configuration and
documentation dependencies before deletion.

## 2026-09-01 22:15:00 CST - Historical vision output archive

Tags: outputs, archive, vision, compatibility

### Context
Organize the output root while preserving current Formal-618 evidence and
legacy path compatibility.

### Changes
- Moved 93GB of superseded ViT/Swin/Transformer output families to
  `outputs/archive/historical-vision/`.
- Created relative compatibility symlinks at each former top-level output path.
- Added `docs/results/OUTPUTS_ARCHIVE_INDEX.md` as the versioned navigation
  entrypoint for current evidence, reproducibility artifacts, and archives.

### Validation
- All eight symlinks resolve to their archived directories.
- Current `outputs/vlm_sft/`, `outputs/runs/`, `outputs/artifacts/`, and
  `outputs/experiments/` were not moved.

### Next
Review `outputs/artifacts/models/` and recovery-pilot dataset versions as
separate archival decisions; do not move active HCV/RAG experiment roots.

## 2026-09-01 22:30:00 CST - Complete historical-vision migration

Tags: outputs, archive, vision, migration

### Context
User requested a complete migration rather than retaining compatibility
symlinks at former top-level output paths.

### Changes
- Removed the eight top-level compatibility symlinks for the archived
  ViT/Swin/Transformer output families.
- Updated the versioned historical validation launcher to use
  `outputs/archive/historical-vision/vit-large/` directly.
- Updated `docs/results/OUTPUTS_ARCHIVE_INDEX.md` so the archive is the sole
  discovery path and the retention policy forbids replacement symlinks.

### Validation
- All eight historical output families were present under
  `outputs/archive/historical-vision/` before link removal.
- The revised launcher checkpoint and metrics paths resolve in the archive.

### Next
Classify the remaining small, unreferenced VLM run roots for archive migration;
move only after checking each retained reference.

Sync scope: mainline docs and retained historical launcher.

## 2026-09-01 22:45:00 CST - Archive completed VLM intermediate runs

Tags: outputs, archive, vlm, intermediate-results

### Context
Continue output-root organization after the user required full migrations with
no compatibility symlinks, focusing on small completed VLM run directories
that otherwise made `outputs/runs/vlm/` difficult to navigate.

### Changes
- Moved 87 zero-active-reference run roots (about 151MB) to
  `outputs/archive/vlm-runs/`, with no links left at their previous paths.
- Categorized the archive into 38 early RAG iterations, 21 legacy rounds,
  15 smoke/preflight runs, and 13 stage-validation runs.
- Retained all current Formal-618 evidence roots and every run root with an
  active exact-path reference under `outputs/runs/vlm/`.
- Added the VLM-run archive discovery path to
  `docs/results/OUTPUTS_ARCHIVE_INDEX.md`.

### Validation
- Checked candidate paths against versioned code, configs, current result
  documentation, and local custom launchers before migration; none had an
  active exact-path reference.
- No process was writing a candidate directory at migration time.
- Sampled former paths are absent, their archive paths exist, and no stale
  active reference to those sampled paths remains.
- `outputs/runs/vlm/` now has 100 top-level run roots, down from 187.

### Next
Keep the remaining referenced or current VLM run roots in place. Review any
additional current-data or evaluation-result symlinks independently before a
separate full migration, because they are outside this historical/intermediate
run cleanup scope.

Sync scope: mainline docs; local ignored output artifacts.

## 2026-09-02 00:41:38 CST - Add documentation, output, and test governance entrypoints

Tags: repository-organization, documentation, outputs, tests, reproducibility

### Context
The project has a large local evidence tree and several research routes. A review
found that the root README did not expose the current-result and output-governance
entrypoints, and that tests/ and the ignored test/ utility directory were easy to
confuse.

### Changes
- Added docs/README.md as the documentation map and review order.
- Added docs/results/README.md as the current-result and evidence-chain index.
- Added docs/outputs/README.md with target output layout, lifecycle classes,
  managed-run metadata requirements, retention workflow, and legacy-layout
  boundary.
- Added tests/README.md to distinguish the pytest suite from ignored local data
  utilities in test/, and to define a safe gradual test-suite migration.
- Linked the new entrypoints from both root READMEs and the output archive index.
- Added the narrow .gitignore exception required to version docs/outputs/README.md
  without unignoring any real output tree.
- Updated the recovery-strategy test to derive its expected rotation set from
  STRATEGY_ORDER. This captures the existing HCV strategy policies while keeping
  hcv_visual_expand excluded from generic rotation.

### Validation
- New and modified documentation links: 45 checked, 0 broken.
- Targeted recovery test module passed after the strategy-contract update.
- Full suite is pending final execution after the documentation change record.
- A broad documentation scan found seven pre-existing broken links in
  docs/archive/results/MILESTONE_EXPERIMENTS.md. They are historical evidence;
  no path was guessed, removed, or rewritten. The two log links have incorrect
  relative depth, and five output targets require an archive-location audit.

### Impact
- Verified: new researchers now have explicit entrypoints for current state,
  formal evidence, outputs, and tests.
- Verified: no outputs, datasets, checkpoints, active runs, or ignored test/
  utilities were moved, deleted, or executed.
- Compatibility / scope: existing output paths and flat pytest module paths
  remain unchanged; physical migrations require a later reference audit.

### Next
Create read-only inventory reports for managed runs, legacy run layouts, output
retention classes, and legacy test utilities; repair archive links only after
their replacement evidence paths are verified.

Sync scope: mainline docs and test contract; do not propagate local output data.

## 2026-09-02 00:47:00 CST - Implement read-only repository organization audit

Tags: repository-organization, outputs, tests, documentation, audit

### Context
The approved organization plan requires reproducible evidence before any
physical migration of local outputs or legacy utilities. Manual directory
inspection could not reliably distinguish managed runs, legacy layouts, and
historical link drift.

### Changes
- Added tools/repository_audit.py, a read-only inventory tool with schema
  agrinet.repository-audit/v1. It reports documentation links, top-level output
  sizes, managed status counts, legacy run-layout candidates, pytest modules,
  fixtures, and ignored test/ utilities.
- Restricted optional audit-file writes to outputs/migration/; all other output
  destinations are rejected by the tool.
- Added tests/test_repository_audit.py with synthetic link, output, run-layout,
  test-boundary, and destination-safety coverage.
- Repaired seven verified relative-path errors in
  docs/archive/results/MILESTONE_EXPERIMENTS.md. The five referenced output
  artifacts and two experiment logs still exist; no historical conclusion or
  output location changed.
- Added the audit command to docs/outputs/README.md and tests/README.md.

### Validation
- Latest inventory: outputs/migration/repository-organization/20260902T005200-inventory-v3.json.
- It checked 73 relative Markdown links with zero broken links.
- It recorded 271 complete, 167 failed, 1 running, and 1 completed managed run
  statuses; 96 legacy direct-script run layouts; 48 pytest modules; and 6
  ignored legacy utilities.
- Full .venv/bin/python -m pytest -q passed. git diff --check passed.

### Impact
- Verified: future organization decisions can be reviewed from a compact,
  repeatable local inventory rather than a destructive filesystem operation.
- Verified: documentation evidence links now resolve across both current and
  archived reports.
- Compatibility / scope: audit JSON files stay ignored under outputs/migration/;
  no output, checkpoint, dataset, active run, or test/ utility was moved,
  deleted, or executed.

### Next
Use the audit inventory to prepare a human-reviewable migration manifest for
the 96 legacy run layouts and separately inventory each test/ utility's inputs,
outputs, dependencies, and mutating behavior. Do not migrate either category
before the corresponding manifest is reviewed.

Sync scope: mainline tools, tests, and documentation; audit payload remains local output evidence.

## 2026-09-02 01:03:38 CST - Generate conservative migration manifests and test groups

Tags: repository-organization, outputs, tests, migration-manifest

### Context
The first repository inventory identified old run layouts and ignored local
utilities, but it did not provide a per-entry review record or a way to execute
the existing flat pytest suite by responsibility.

### Changes
- Upgraded tools/repository_audit.py to schema v2. Its output now includes a
  per-legacy-run manifest (size, markers, versioned references, and conservative
  proposed action) and a static per-utility manifest (imports, local path
  literals, mutation signals, and review action).
- Added synthetic tests for reference-safe archive candidacy, mutating utility
  detection, and output-destination restrictions.
- Added unit, integration, and regression pytest markers in pyproject.toml and
  a flat-path classification hook in tests/conftest.py. The canonical category
  names live in agrinet.common.test_taxonomy to avoid collision with an installed
  third-party tests package.
- Added tests/test_test_taxonomy.py to assert that every test module has exactly
  one classification while file paths remain unchanged.
- Updated output and test governance docs with the v2 inventory and marker use.

### Validation
- Latest manifest: outputs/migration/repository-organization/20260902T010700-inventory-v2.json.
- Legacy runs: 96 total; 56 retain_review because versioned files reference
  them, 40 archive_candidate_review with no versioned reference. All require
  human approval before any physical action.
- Legacy utilities: 3 mutation_review_required and 3
  parameterize_review_required; none was executed.
- Unit, integration, regression, and full pytest executions all completed
  without FAILED or ERROR records. git diff --check passed.
- The v2 audit reports zero broken relative documentation links.

### Impact
- Verified: migration candidates now carry enough compact evidence for human
  review, including a fail-safe retain classification when versioned references
  exist.
- Verified: test grouping is executable today without a risky bulk file move.
- Compatibility / scope: no run, output, checkpoint, dataset, or ignored test/
  utility was moved, deleted, or executed. The active RAG service remains
  outside all migration candidates.

### Next
Review the 40 archive-candidate run entries in batches grouped by research route
and confirm no local custom/ignored references before proposing a move manifest.
For test/ utilities, obtain user direction on whether they should be preserved as
supported parameterized data tools or archived as historical scripts.

Sync scope: mainline tools, tests, docs, and pytest configuration; v2 audit JSON remains local output evidence.

## 2026-09-02 01:23:00 CST - Move legacy test utilities and ignore local tools/tests

Tags: repository-organization, gitignore, tools, tests, audit

### Context
User directed that the former local test/ utilities move into tools/, and that
both tools/ and tests/ become ignored local areas rather than versioned source
trees.

### Changes
- Moved the six local legacy utilities from test/ to
  tools/legacy/data-preparation/: create_index.py, merge_csv.py, out_image.py,
  pack_missing_file.py, split_info.py, and split_stats.py. The old test/
  directory no longer exists.
- Replaced the narrow test/* ignore rule with tools/ and tests/ in .gitignore.
- Removed all 204 tools/ files and 50 tests/ files from the Git index using
  git rm --cached; the local files remain present and ignored.
- Updated repository-audit v2, documentation, and audit tests to inspect the
  new tools/legacy/data-preparation/ location.
- Kept the small source-level optional local-test taxonomy module so a present
  local tests/ tree can continue to run without importing a third-party tests
  package by mistake.

### Validation
- Git index: 0 tracked tools/ files and 0 tracked tests/ files; local workspace
  retains 210 Python tool files and 49 pytest modules.
- Latest post-ignore audit:
  outputs/migration/repository-organization/20260902T012300-post-ignore-inventory-v2.json.
- Audit checked 70 relative documentation links with zero failures. It reports
  96 legacy run layouts: 56 retain_review because versioned material references
  them, and 40 archive_candidate_review entries requiring human review.
- Legacy utility static review: 3 mutation_review_required and 3
  parameterize_review_required. No legacy utility was executed.
- Full local pytest suite passed after the move and ignore change. git diff
  --check passed.

### Impact
- Verified: tools/ and tests/ are now local-only by Git policy; a clean clone
  does not contain them, while this workstation preserves all existing files.
- Verified: legacy data-preparation scripts have one discoverable local home
  under tools/legacy/data-preparation/.
- Compatibility / scope: removing tracked files from the index is intentional
  and produces 254 staged deletions. No output, model, dataset, or running RAG
  service was moved, deleted, or stopped.

### Next
Audit the 40 review-only legacy run candidates in route batches: 37 are
rag-sft-round entries and 3 are Hermes collection/preflight entries. Because
tools/ is now unversioned, check local-tool references and active process state
in addition to versioned references before proposing any archive move.

Sync scope: Git ignore policy, docs, and source-level local-test support. Local tools/tests and audit JSON are intentionally not propagated by Git.

## 2026-09-02 01:30:11 CST - Archive all approved legacy RAG run candidates

Tags: outputs, archive, rag, migration

### Context
The user explicitly approved archival of all 40 v2
archive_candidate_review legacy RAG run roots. The candidates had no versioned
references, no status.json, no active-process match, and no destination-path
conflict. The only currently running managed RAG service was outside the set.

### Changes
- Moved all 40 candidates from outputs/runs/rag/ to outputs/archive/rag-runs/.
- Archived 37 historical Option/shard/Luna/Terra pilot and preflight roots under
  outputs/archive/rag-runs/round-pilots-and-preflights/.
- Archived three Hermes collection/preflight roots under
  outputs/archive/rag-runs/hermes-collection-preflight/.
- Created the exact ignored mapping manifest:
  outputs/migration/repository-organization/20260902T013000-archive-legacy-rag-runs-v1.tsv.
- Updated the output archive index and lifecycle document. No compatibility
  symlinks were left at the old paths.

### Validation
- Preflight: 40/40 source directories existed, 0 candidate status.json files,
  0 destination conflicts, and 0 active-process references.
- Post-move: all 40 archive destinations exist; 0 former paths remain.
- Archive contents: 37 round pilots/preflights and 3 Hermes entries; total local
  archive family size is about 656KB.
- Post-archive inventory:
  outputs/migration/repository-organization/20260902T013300-post-archive-inventory-v2.json.
  It reports zero broken relative links, 56 remaining legacy layouts, zero
  archive candidates, and 56 retain-review entries.
- Full local pytest suite and git diff --check passed.

### Impact
- Verified: the entire approved 40-entry candidate set is now in a stable RAG
  archive hierarchy with a durable old-to-new mapping manifest.
- Verified: current formal evidence, all referenced legacy runs, and the active
  RAG service remain in their original locations.
- Compatibility / scope: archive paths are local ignored output evidence. No
  source, config, script, or documentation reference used any moved old path;
  no symlink compatibility layer was created.

### Next
The remaining 56 legacy run layouts all have versioned references. Retain them
until a later review explicitly updates those references and approves a new
migration batch. Review large checkpoint and artifact retention separately.

Sync scope: local ignored output evidence plus versioned archive discovery docs.

## 2026-09-02 01:50:00 CST - Reconcile checkpoint and dataset retention state

Tags: outputs, checkpoints, datasets, audit, reproducibility

### Context
After legacy RAG run archival, the next large evidence families were
outputs/vlm_sft/ and outputs/artifacts/datasets/. The prior checkpoint cleanup
review still described a pre-archive disk state and could not safely drive any
current deletion decision.

### Changes
- Extended repository_audit.py to schema v3 with per-checkpoint and per-dataset
  inventory entries: size, file count, versioned references, metadata markers,
  declared dataset status/identity, and conservative retention state.
- Added synthetic audit coverage for checkpoint retention and artifact identity
  mismatch detection.
- Updated docs/results/CHECKPOINT_CLEANUP_REVIEW.md to label its old candidate
  table as historical and to publish the current disk reconciliation.

### Validation
- Current inventory:
  outputs/migration/repository-organization/20260902T015000-model-dataset-audit-v3.json.
- Checkpoints: 12 actual directories, about 106.6GB total. Each has at least
  four versioned references; all are retain_review and none is an archive or
  deletion candidate.
- Dataset artifacts: 77 directories, about 11.0GB total. Five recovery-pilot
  artifacts are planned yet still configuration-referenced. Four have an
  identity mismatch: v2--v5 declare artifact_id
  agrinet-rag-recovery-pilot-v1 despite their directory/config IDs.
- Full local pytest and git diff --check passed; audit reports zero broken
  relative documentation links.

### Impact
- Verified: the former 60-checkpoint / 497GB cleanup proposal is stale and is
  no longer presented as an executable deletion list.
- Verified: no checkpoint or dataset artifact was moved, modified, or deleted.
- Compatibility / scope: recovery-pilot artifact metadata inconsistency is a
  reproducibility risk, but all affected artifacts remain retained because
  active configs/scripts still reference them.

### Next
Decide whether the recovery-pilot v1--v5 experiment definitions are historical
or still runnable. If historical, archive their configs and correct each
artifact.yaml to its actual immutable artifact ID before considering an artifact
migration. If still runnable, correct metadata and add validation that config ID
matches artifact ID without moving data.

Sync scope: mainline audit tooling and retention documentation; inventory JSON remains local output evidence.

## 2026-09-02 02:03:31 CST - Repair recovery-pilot lineage metadata and overwrite guard

Tags: outputs, datasets, recovery-pilot, reproducibility

### Context
The current retention audit found that the registered active recovery-pilot v2--v5
artifact directories declared the v1 artifact ID. The v5 config also declared a
different seed from its existing immutable plan and metadata. These artifacts are
still configuration-referenced, so archival would have been incorrect.

### Changes
- Corrected the local `artifact.yaml` IDs for recovery-pilot v2--v5 to match
  their existing directory and registered configuration.
- Reconciled the v5 configuration seed to `20260805`, the seed recorded by its
  existing immutable plan, review summary, and artifact metadata.
- Updated recovery-pilot preparation to derive the artifact ID from its versioned
  output directory, require the registered experiment-ID mapping and a positive
  config seed, and reject an existing output directory instead of overwriting it.
- Extended the local repository audit with config-ID/output-path/seed lineage
  checks and synthetic coverage.

### Validation
- Lineage evidence:
  `outputs/migration/repository-organization/20260902T022000-recovery-pilot-lineage-audit-v3.json`.
  It reports five recovery-pilot artifacts and zero artifact-ID, config-identity,
  or seed mismatches.
- Focused recovery/audit tests and the full local pytest suite passed;
  `git diff --check` passed.
- The managed public-evidence RAG service remains `running` at
  `outputs/runs/rag/rag-serve-siglip2-milvus-public-evidence-v2/20260823T200451-2ef993b1-a01/`
  (PID 1252676); it was not restarted or modified.

### Impact
- Verified: v1--v5 are active registered experiment definitions and retained in
  place as immutable local research evidence.
- Verified: future preparation fails before any write if the output directory is
  not a new versioned recovery-pilot artifact destination.
- Compatibility / scope: no pilot data, accepted/rejected records, checkpoints,
  or running service was moved, regenerated, or deleted.

### Next
Keep the five recovery-pilot artifacts retained. Any future revision must use a
new versioned artifact directory and a separately reviewed configuration; do not
reuse or overwrite v1--v5.

Sync scope: source-level lineage guard and versioned discovery records; corrected
artifact metadata and audit JSON remain local ignored evidence.

## 2026-09-02 02:12:00 CST - Identify remaining repository-organization work

Tags: repository-organization, reproducibility, tools, outputs, audit

### Context
After recovery-pilot lineage repair, a read-only audit was requested to identify
remaining organization work without moving or deleting research evidence.

### Findings
- `tools/` and `tests/` are intentionally ignored, but `src/agrinet/cli/data.py`
  and `src/agrinet/cli/rag.py` still invoke local `tools/` implementations; 56
  non-archived executable scripts also do so. A clean clone therefore cannot
  reproduce all registered/current workflows.
- Managed runs contain 271 `complete`, 1 `completed`, 167 `failed`, and 1
  `running` status records. The failed history and the `complete`/`completed`
  spelling divergence require a reviewed retention/normalization policy, not a
  bulk move.
- `outputs/evaluations/` remains the active VLM CLI output root, while
  `outputs/focusnet_tiny_224/` is unreferenced and `outputs/rag_distill/` is a
  historical legacy payload with 17 versioned references. These require route-by-
  route disposition.
- `docs/logs/START_HERE.md` is 303 lines and contains current state plus a large
  historical narrative. It should be reduced to a compact current-state handoff
  while preserving the historical text in an archive document.
- `.venv_test/` is a local 4.3GB duplicate environment ignored by its nested
  `.gitignore`; it is not a Git problem, but its ownership/retention should be
  confirmed before removal.

### Validation
- Baseline inventory:
  `outputs/migration/repository-organization/20260902T022500-final-post-repair-inventory-v3.json`.
- No file, output, run, checkpoint, environment, or service was moved, deleted,
  regenerated, stopped, or restarted by this audit.

### Next
First decide the intended contract for `tools/`: either promote active workflow
implementations into versioned `src/agrinet/` modules, or explicitly define the
repository as requiring a separately provisioned local-tool bundle. Then review
failed runs, legacy output roots, and START_HERE compaction in separate batches.

Sync scope: current-state documentation only; no output retention decision is
implied by this audit.

## 2026-09-02 02:50:20 CST - Complete reproducible workflow and output-root organization

Tags: repository-organization, reproducibility, outputs, environments, audit

### Context
The approved organization scope required current workflows to stop depending on
ignored `tools/` and `tests/`, retain `.venv_test` for SFT/ms-swift, normalize
managed-run status reporting, and retire three unclassified top-level output
families without affecting active research evidence.

### Changes
- Promoted active RAG distillation, Milvus, and VLOOM implementations into
  versioned `src/agrinet/` packages; updated CLI, evaluators, scripts, and
  local tests to import those packages rather than ignored `tools/`.
- Kept `tools/` and `tests/` as ignored local areas, with their tracked index
  entries removed; test execution remains local through `.venv/bin/python -m
  pytest`.
- Declared `.venv` for CLI/Data/RAG/validation and `.venv_test` for explicit
  SFT/ms-swift use. VLM training commands now select `.venv_test/bin/python`.
- Added audit schema v4 canonical status reporting (`completed` -> `complete`)
  while retaining raw counts, malformed-status reporting, and failures grouped
  by experiment.
- Moved the retired top-level roots to `outputs/archive/rag-distill/`,
  `outputs/archive/vlm-evaluations/`, and
  `outputs/archive/historical-vision/focusnet_tiny_224/`. Current RAG
  distillation and VLM evaluation defaults use `outputs/experiments/`.

### Validation
- Full local suite: `357 passed` via `.venv/bin/python -m pytest -q`.
- Source compilation, package imports, RAG/Data CLI previews, and VLM SFT
  dry-runs passed; VLM preview selected `.venv_test/bin/python`.
- Final inventory:
  `outputs/migration/repository-organization/20260902T024504-final-repository-organization-audit-v4.json`.
  It reports 75 checked relative links with zero broken links; canonical managed
  statuses `complete=272`, `failed=167`, `running=1`; raw status accounting
  preserves the one historical `completed` record; no malformed status files.
- Archive migration mapping:
  `outputs/migration/repository-organization/20260902T024504-archive-top-level-output-roots-v1.tsv`.
  The three archived roots retain 18 files / 4,791,300 bytes; no compatibility
  symlinks remain.
- The managed RAG service stayed running at
  `outputs/runs/rag/rag-serve-siglip2-milvus-public-evidence-v2/20260823T200451-2ef993b1-a01/`
  (PID 1252676); it was neither restarted nor moved.

### Impact
- Verified: a clean clone now contains all current source workflow
  implementations; ignored local tools/tests are no longer runtime dependencies.
- Verified: checkpoint and dataset evidence were not moved or deleted.
- Compatibility / scope: historical Slurm scripts and historical log narratives
  retain their original contextual paths where appropriate; the archive index
  and mapping manifest provide their retained destinations.

### Next
No further output-root migration is authorized by this change. The remaining 56
referenced legacy run layouts and the 86 experiments with failed historical run
records require separate review before any disposition.

Sync scope: repository organization, reproducibility policy, and current
workflow source paths; local output manifests remain ignored evidence.

## 2026-09-02 11:28:50 CST - 修复 M1/M3.5 全量复现队列的续接与运行时一致性

Tags: m1, m35, sft, formal-618, tmux, runtime

### Context
在三步 SFT 冒烟复现成功后，对尚未启动的完整 M1→M3.5 tmux 串行队列进行非执行性审计，确保后续评测只消费父 SFT 的完成 checkpoint，而不会意外复训。

### Changes
- 修复 `scripts/vlm/run_m1_fixed_m35_e9_queue.sh` 的状态轮询：使用 `json.load(handle)` 读取 `status.json`，避免 M1 训练完成后队列因将文件对象传入 `json.loads` 而停止。
- M1 Direct-618 队列已支持 `PARENT_EXPERIMENT_ID` 解析完成的父 SFT 目录；其备用训练分支与项目约定一致，使用 `.venv_test/bin/python -m swift.cli.main` 且默认 4 workers。
- M3.5 全量 SFT 现使用 `.venv_test/bin/python -m swift.cli.main`，并将版本化 `vlm/sft/ms-swift` checkout 显式注入 `PYTHONPATH`；这样保留 `.venv_test` 的 SFT 环境契约，同时提供已验证的 `manual_json` 模板与 loss scale。

### Validation
- 两份授权工件均报告 `training_authorized=true`。
- M1/M3.5 训练和两个显式 checkpoint-evaluation operation 的 `agrinet vlm submit --dry-run` 均成功；M1 评测预览含 `PARENT_EXPERIMENT_ID`，M3.5 评测预览含 `SKIP_TRAIN=1`。
- `bash -n` 覆盖四个队列/启动脚本，`git diff --check` 通过；`resolve_completed_parent_sft_dir.py` 已从两个三步 smoke run 正确解析完成训练目录。

### Impact
- Verified：完整 tmux 队列会按 M1 SFT → M1 Direct-618 → M3.5 SFT → M3.5 Direct/RAG-618 串行运行；评测阶段不复训。
- Scope：本次仅更新脚本、定义与文档；没有启动 tmux、完整 SFT、RAG 服务或 Formal-618 评测。

### Next
等待明确启动授权后才运行 `scripts/vlm/launch_m1_fixed_m35_e9_tmux_queue.sh`。

Sync scope: current reproducibility workflow; no experiment artifact disposition is implied.

## 2026-09-02 18:46:47 CST - 冻结 open_agri_v2 HCV 图像与 VLM 数据基础版本

Tags: open-agri-v2, hcv, dataset-split, vlm-data, deduplication

### Context
为后续 HCV / Direct / RAG 实验建立独立于历史 open_agri_v1 的图像、类别角色、开发验证和 VLM 数据治理版本。

### Changes
- 新增 scripts/data/build_open_agri_v2.py，生成 datasets/AgriNet-1K/open_agri_v2/ 的 images/、vlm_data/ 和 manifests/ 三层结构；训练候选为指向原始 all/ 的软链接，dev/test/Milvus 为实体副本。
- 新增已验收训练监督导入器 scripts/data/ingest_open_agri_v2_accepted.py：仅接纳 known-class train-candidate 单图监督，按规范化任务指纹去重并审计拒绝项。
- 冻结类别角色：病害 73 known / 72 unknown，虫害 36 / 36；未知类均保留至少一个已知 Milvus/SigLIP2 Top-3 近邻。N04074（4 候选）与 N05053（0 候选）因无法满足 known 类五图 dev 约束而强制未知。
- 生成开发评测集：163 类、815 图（每类 5 图），覆盖全部 known 类和 54 个 unknown_dev 类；最终 test 保留 1,019 图全类覆盖，真值仅位于 private manifests。
- 分区内稳定去重、评测优先精确去重与跨训练—评测 256-bit pHash 近重复审计均已执行；pHash 缓存位于 outputs/artifacts/datasets/open-agri-v2-phash-cache-v1.jsonl。

### Validation
- open_agri_v2 目录已生成：193,281 train candidates、815 dev、1,019 test、401 Milvus 参考图及 933,368 个待审核 VLM 任务候选。
- 精确重合训练候选排除 1 条；pHash 跨边界候选排除 45 条，审计清单位于 vlm_data/audits/。
- 验证 217 类互斥完备、病害/虫害配额正确、所有未知类均有 known bridge、dev/test 公私 ID 一一对应、dev 每类恰 5 图、跨 split SHA-256 为零、训练链接都指向共享 all/。
- 新增局部测试通过：3 passed，命令为 .venv/bin/python -m pytest tests/test_open_agri_v2_builder.py tests/test_open_agri_v2_accepted.py。

### Impact
- Verified：后续 HCV 数据采集和 SFT 可引用 v2 的候选、评测与审计 manifest，而不再从未版本化的图像视图临时划分。
- Known limitation：当前 raw-base Direct 逐类结果仅覆盖 152/217 类；v2 以显式 partial-coverage 模式冻结，缺失的 65 类标为 raw_base_missing_long_tail。补齐全类 raw-base 后必须新建后续数据版本，不得原地改写 v2。

### Next
后续新教师/人工验收语料须通过 ingest_open_agri_v2_accepted.py 写入 v2；在训练配置中显式使用固定 accepted/train.jsonl 和 accepted/dev.jsonl，不得运行时随机切分。

Sync scope: dataset-reproducibility workflow and local ignored dataset artifacts; no historical model result is reinterpreted.

## 2026-09-02 21:14:16 CST - 迁移已审计历史 SFT 到 open_agri_v2 的 ms-swift 标准消息格式

Tags: open-agri-v2, historical-sft, ms-swift, direct, rag

### Context
用户明确要求历史 SFT 按 direct/rag 分类写入 v2，并采用 ms-swift Agent-support 的统一 messages 存储格式，在训练时才根据 agent_template 转换。

### Changes
- 新增 scripts/data/migrate_open_agri_v2_historical_sft.py，显式登记六个历史工件，保留输入 SHA-256、源行 SHA-256、源样本 ID、图像映射和迁移审计。
- 历史 system wire prompt 被去除；tool 统一为 tool_response；tool_call/tool_response content 统一为 JSON 字符串；旧路径重写为 v2 train_candidate 逻辑路径；无 image 占位符的单图记录补齐一个 image 占位符。
- 输出拆分为 datasets/AgriNet-1K/open_agri_v2/vlm_data/historical/canonical/direct.jsonl 与 rag.jsonl；输入工件 registry 和逐行 audit 同目录保存。
- v11 与 v12 direct-only 仅作为 v12 anchor 的 lineage-only 输入，不产生重复训练行；native-json v4 与 Hermes v3 统一规范化后按监督指纹去重；reconstructive 保留 supplementary lineage。
- accepted 导入器增加 ms-swift 标准 messages/tools 合同校验。

### Validation
- 迁移命令：.venv/bin/python scripts/data/migrate_open_agri_v2_historical_sft.py --replace。
- 结果：1,902 条 canonical 记录，其中 direct 624、rag 1,278；15,994 条输入被排除，另有 10,336 条来自 lineage-only 工件未导入。
- 五项局部测试通过：.venv/bin/python -m pytest -q tests/test_open_agri_v2_builder.py tests/test_open_agri_v2_accepted.py tests/test_open_agri_v2_historical_sft.py。
- 输出文件中 sample_id 与 supervision fingerprint 均唯一；消息角色、tool JSON 内容、v2 image 路径和源工件哈希均可审计。

### Impact
- Verified：v2 现在有 framework-neutral 的历史 Direct/RAG canonical 数据层；Hermes/manual_json/native-json 不再混入持久化监督格式。
- Assumed：训练时使用的 agent_template 和 loss_scale 仍需由具体模型/训练 profile 选择并做 encode smoke；当前迁移没有启动训练。

### Next
为 canonical/direct.jsonl 和 rag.jsonl 编写训练视图渲染 smoke，固定 agent_template/loss_scale 后再决定是否合并到 accepted/train.jsonl。

Sync scope: dataset-reproducibility workflow and local ignored dataset artifacts; no model or evaluation result is reinterpreted.

## 2026-09-02 23:11:19 CST - 以当前 strict RAG 协议重划 open_agri_v3 known/unknown

Tags: open-agri-v3, rag, dataset-split, test-informed, native-json

### Context

需要以当前 v5 native-JSON strict RAG 的 raw-base 类别难度，而非历史 Direct 分数，重新选择 HCV known/unknown，同时保留 v2 的人工审计 test 与 Milvus reference。

### Changes

- 扩展 `run_qwen3_vl_rag_sglang_eval.py`：显式传递 `top_p`/`seed`，并记录模型、图像 SHA-256、协议、prompt/tool-schema hash 和完整工具轨迹；request fingerprint 也锁定这些采样参数。`SnapshotStore` 支持将显式终态失败作为可恢复 evidence 保存。
- 新增 `prepare_open_agri_v3_rag_evidence.py`、`aggregate_open_agri_v3_rag_evidence.py` 与 `build_open_agri_v3.py`；以及 `scripts/data/README_open_agri_v3.md` 和 focused test。
- 使用八卡 DP SGLang (`TP=1, DP=8`) 与 24 路并发完成三个固定 seed；上下文长度固定为 16,384，以容纳最大公开图像请求。先前 8,192 上下文的两条启动期失败原始记录隔离保存在 evidence 根目录的 `failed-startup-attempt/`，未混入正式证据。
- 建立 `datasets/AgriNet-1K/open_agri_v3/`：test/private truth/Milvus reference 与 v2 内容逐项复用；重选角色、dev、SFT eligibility，并过滤 v2 historical canonical 到 v3 known/train-candidate 边界。

### Validation

- `outputs/artifacts/datasets/open-agri-v3-rag-role-evidence-v1/aggregation_summary.json` 验证 3,057/3,057 条预测、三个 seed 各 1,019 条、217 类状态齐全，且协议、模型、temperature、top_p、prompt/tool-schema hashes 都是单值。
- v3 为 109 known / 108 unknown（病害 73/72、虫害 36/36），815 dev 图、117,210 train candidates、1,019 test 和 401 Milvus reference；所有 unknown 均有 known Top-3 bridge。
- v2→v3 不可变复用审计验证 1,019 test 与 401 reference 图像 SHA-256 一致；四个输出 split SHA-256 零重叠，pHash 跨训练—评测审计排除 4 条训练候选。
- 重新过滤历史 canonical 后保留 Direct 368、RAG 788；`pytest` 45 项 focused 测试与 `git diff --check` 通过。

### Impact

- Verified：v3 是可复现的 `test-informed protocol` 数据版本。固定 test 仅被授权用于类别角色设计，不可作为完全独立最终测试集，也不得用于 checkpoint、训练超参数、提示词或检索策略选择。
- Compatibility / scope：`open_agri_v2` 未被改写；本次专用 8 卡 SGLang 服务在验证后已停止，既有 8078 RAG 服务未变更。

### Next

后续训练仅应从 v3 known `train_candidate` 与过滤后的 canonical 视图取数；任何新 checkpoint 或策略选择必须保持与 test-informed 角色设计隔离。

Sync scope: dataset reproducibility workflow and local output evidence; do not rewrite v2 or reinterpret it as v3.

## 2026-09-03 00:35:42 CST - 构建 test-blind 的 open_agri_v4 长尾 × 视觉混淆划分

Tags: open-agri-v4, dataset-split, test-blind, long-tail, siglip2, milvus

### Context

v3 的类别角色直接使用固定 test 的 strict-RAG 表现，因此只能作为 test-informed
困难诊断。论文主 benchmark 需要保留长尾与难类覆盖，同时使 final test 完全不参与角色选择。

### Changes

- 新增 scripts/data/build_open_agri_v4.py、scripts/data/README_open_agri_v4.md 及 focused test。v4 的角色选择仅使用 open_agri_v1/manifests/train.jsonl 的训练候选容量、冻结类别 catalog 和 SigLIP2/Milvus Top-3 邻居图；不读取 test 图像、ID、公开输入、标签或任何 RAG 分数。
- 在病害/虫害领域内，候选容量最低 40% 标为 long-tail；冻结邻居图中互惠/入邻居高的前 40% 标为 visual-confusable。Unknown 以 40/25/25/10 的 tail-confusable、tail-distinct、head-confusable、head-distinct 桶目标选择，并用固定 hash、容量强制 Unknown（少于 5 图）和 Known Top-3 bridge 约束完成确定性回填。
- 建立 datasets/AgriNet-1K/open_agri_v4/：复用 v2 的 test/private truth/Milvus reference 内容；重建 dev、Known-only SFT 资格、pHash 审计与 historical canonical 边界过滤。

### Validation

- v4 为 109 Known / 108 Unknown：病害 73/72、虫害 36/36；所有 Unknown 有最终 Known Top-3 bridge。实际 Unknown 桶为 disease 19/28/18/7、pest 9/14/9/4（依次为 tail-confusable、tail-distinct、head-confusable、head-distinct）；15 类因桶容量或 bridge 限制采用记录在 manifest 中的固定 fallback。
- 输出包括 135,541 Known train candidates、815 dev、1,019 test、401 Milvus reference 和 1,084,328 个待审核任务候选；pHash 跨训练—评测审计排除 7 条训练候选；历史 canonical 保留 Direct 400、RAG 875。
- 逐项 v2 不可变复用审计通过（1,019 test、401 reference 均 SHA-256 一致），train/dev/test/reference SHA-256 零重叠。summary 明确记录 test_informed_protocol: false 和全部角色选择输入 SHA-256。
- focused pytest 集合共 47 passed；git diff --check 通过。

### Impact

- Verified：v4 是 test-blind role-selection protocol，适合作为训练未见长尾与视觉混淆农业类别的 retrieval-supported recognition 主 benchmark。final test 只可在角色、训练资格和评测协议冻结后报告。
- Compatibility：open_agri_v2 与 v3 都未改写；v3 仍保留为明确标注的 test-informed RAG difficulty split。

### Next

后续 SFT 应从 v4 Known train-candidate 及过滤后的 historical canonical 视图取数；对 v4 的最终 test 结果必须按四个长尾/视觉混淆子集和 Known/Unknown 分别报告。

Sync scope: dataset reproducibility workflow and local ignored dataset artifacts; do not rewrite v2 or v3.

## 2026-09-03 01:00:00 CST - 收紧 open_agri_v4 Known 训练容量并保持严格 Top-3 bridge

Tags: open-agri-v4, known-capacity, dev-allocation, top3-bridge

### Context

初始 v4 中有 9 个 Known 在固定 5 图 dev 后少于 25 张最终 SFT 图，其中一个类别为 0。
将它们直接转为 Unknown 会破坏严格 Top-3 Known bridge；扩大到 Top-12 或设置 bridge
例外均不符合当前路线。

### Changes

- v4 builder 现在将最终 Known 的训练覆盖设为硬约束：每类至少 3 张、最多 5 张 dev，且
  dev/pHash 后至少 25 张 SFT train-candidate 图。
- 9 个不满足该规则的初始 Known 被强制设为 Unknown：N04020、N04067、N04084、N04089、
  N04115、N04138、N04139、N05006、N05031。
- 构建器在冻结 Top-3 图上搜索最少数量的容量合格原 Unknown bridge anchor；回补 9 类
  anchor，并在 pHash 后再次硬验证最终训练容量。每类的 forced/promotion 状态和选择原因
  记录在 class_split.jsonl。

### Validation

- 最终角色为病害 74 Known / 71 Unknown、虫害 35 Known / 37 Unknown；整体仍为 109/108。
- Known 的最终 SFT 训练图最小值为 25，Known dev 范围为 3--5；全部 Unknown 仍有严格
  Top-3 Known bridge。最终回补 anchor 为 N04012、N04044、N04049、N04077、N04094、
  N04117、N04122、N04145 和 N05014，其中 N04094 取 3 张 dev、N04117 取 4 张 dev。
- 完整重建后有 141,352 train-candidate、807 dev、1,019 test、401 Milvus reference；pHash
  排除 9 条训练候选，v2 test/reference 不可变复用契约通过。

### Next

后续训练和测试报告应使用该修订后的 v4 manifests；不得重新引入不足 25 张最终训练图的 Known。

Sync scope: dataset reproducibility workflow and local ignored dataset artifacts; do not rewrite v2 or v3.

## 2026-09-03 02:14:45 CST - Document formal OpenAgri v2 inventory and lineage

Tags: open-agri-v2, dataset-card, documentation, test-blind, reproducibility

### Context

The approved paper-facing dataset is now `datasets/AgriNet-1K/open_agri_v2/`.
The formal data-card information and the distinction from prior construction
iterations needed to be discoverable in `docs/`, with counts derived from the
actual formal manifests rather than older intermediate-build records.

### Changes

- Added and completed `docs/datasets/open_agri_v2.md` as the human-readable
  formal benchmark card. It records the v1 → v1.1 → v1.2 → v2 lineage, makes
  v2 the sole paper-facing training/final-evaluation entrypoint, and labels the
  RAG-difficulty iteration as test-informed intermediate evidence.
- Recorded the test-blind role-selection boundary, source inputs, strict
  Top-3 bridge and final Known-capacity constraints, image/SFT-layer contract,
  per-domain Known/Unknown inventory, per-class image ranges, final class
  composition, integrity evidence, and mandatory reporting slices.
- Updated `START_HERE.md` so future work begins from formal v2 rather than a
  superseded construction label; corrected its v2 boundary-filtered historical
  canonical counts to Direct 416 and RAG 908.

### Validation

- Independently recomputed the card's aggregate inventory from
  `manifests/class_split.jsonl` and `manifests/images.jsonl`: disease is 73/72
  and pest 36/36 Known/Unknown; image totals are 141,482 train-candidate, 812
  dev, 1,019 test, and 401 Milvus reference.
- Manifest audit passed: `test_informed_protocol=false`; all 109 Known classes
  have at least 25 final train candidates; 108/108 Unknown Top-3 bridges are
  valid; SHA-256 sets are disjoint across train/dev/test/reference; and 1,019
  test plus 401 reference items match v1.1 byte-for-byte.
- `git diff --check` passed. No dataset image, manifest, private truth, or raw
  evidence artifact was rewritten.

### Impact

- Verified: `docs/datasets/open_agri_v2.md` is the current human-readable
  dataset-card entrypoint; `manifests/summary.json` remains the authoritative
  machine-readable record.
- Compatibility / scope: prior v1.1/v1.2 and informal v2/v3/v4 construction
  evidence remains retained as intermediate provenance, not a new formal entrypoint.

### Next

Use formal v2 manifests and this card for new paper-facing SFT/evaluation
configuration; retain per-domain, Known/Unknown, and long-tail/confusability
reporting in every final result.

Sync scope: mainline dataset documentation and reproducibility records.

## 2026-09-03 02:39:16 CST - Import formal OpenAgri v2 VLM training supervision

Tags: open-agri-v2, vlm-data, accepted-supervision, ms-swift, provenance

### Context

The formal v2 image/candidate/evaluation layers were complete, but
vlm_data/accepted/train.jsonl was empty. The requested v1-style usable VLM
data view needs a fixed JSONL training entrypoint that preserves the formal-v2
Known-only and image-isolation contract.

### Changes

- Added scripts/data/ingest_open_agri_v2_accepted.py and
  scripts/data/migrate_open_agri_v2_historical_sft.py as stable formal-v2
  compatibility entrypoints. They re-export their existing lineage-preserving
  implementations for both CLI execution and import-based tests.
- Extended the accepted-data gate to accept repeated --source arguments in
  declared priority order; it validates the ms-swift Agent storage contract,
  requires one formal-v2 Known train-candidate query image, deduplicates by
  normalized supervision fingerprint, and writes train/audit files atomically.
- Imported canonical Direct then RAG sources into
  datasets/AgriNet-1K/open_agri_v2/vlm_data/accepted/train.jsonl`. The import
  stores source SHA-256, source line and source priority in each accepted row
  and writes accepted_train_import_summary.json plus the rejection ledger.
- Updated the formal v2 README and data card with the command, artifacts,
  coverage limitation, and the required explicit re-import after a builder
  --replace operation.

### Validation

- Imported 1,324/1,324 candidate canonical rows with zero rejections: 416
  Direct and 908 RAG. Output SHA-256 is
  2d0c60c64a3190f8f609f3d6ac31933ab4b08c75739f2188c715772d8c18f104.
- Every accepted row validates as standard ms-swift Agent data, maps to a
  final Known train-candidate image, records accepted-import provenance, and
  has a unique supervision fingerprint. It covers 537 unique images and
  61/109 Known classes; the other 48 Known classes have no accepted historical
  supervision in this freeze.
- Focused tests pass: 5 tests across accepted-import and historical-migration
  contracts, including a new repeated-source priority/dedup/audit test. Python
  compilation and git diff --check pass.

### Impact

- Verified: v2 now exposes a stable, directly trainable accepted JSONL view
  with traceable source provenance and no train/dev/test/reference leakage.
- Compatibility / scope: no image split, test, private truth, Milvus reference
  or historical canonical source was altered. The imported historical view is
  not a claim of complete Known-class supervision.

### Next

Use accepted/train.jsonl only with an explicitly selected agent-template
rendering and an encode smoke. If expanding coverage, collect/audit new rows
for the 48 uncovered Known classes and re-run the same importer with declared
source order; do not train directly from the image candidate pool.

Sync scope: mainline dataset data-contract documentation and local formal-v2
dataset artifacts.

## 2026-09-03 02:58:02 CST - Correct OpenAgri v2 historical supervision boundary

Tags: open-agri-v2, vlm-data, data-lineage, boundary-filter, correction

### Context

The 02:39 accepted-data import correctly imported every row of the then-current
v2 canonical files, but those files had been obtained by filtering v1.1's
Known/Unknown-filtered canonical view. Since 46 classes changed from v1.1
Unknown to v2 Known, that upstream filter could silently discard v2-eligible
historical supervision.

### Changes

- Recanonicalized the six registered audited historical sources directly
  against the final v2 image pool and its Known train-candidate boundary; the
  procedure retains source-priority fingerprint deduplication and all existing
  message/tool validation. It does not use v1.1 class roles as an input.
- Updated build_open_agri_v2.py so future v2 builds write the final image pool
  first, then invoke the direct raw-source canonicalization. The formal manifest
  now records historical_supervision.selection_protocol and synchronized
  historical_canonical counts. Regenerated accepted/train from the new canonical
  Direct then RAG sources through the normal accepted-data gate.
- Updated the formal data card and README to state the corrected source path
  and coverage; the earlier 1,324-row import record remains as historical
  evidence and is explicitly superseded in START_HERE.

### Validation

- Direct raw-source canonicalization accepts 2,164 rows: 708 Direct and 1,456
  RAG. Compared with the prior v1.1-filtered v2 canonical view (1,324), it
  restores 840 rows: 836 from v1.1 Unknown → v2 Known classes and four from
  classes that remained Known. No prior v2 row is lost.
- The corrected accepted/train import has 2,164/2,164 accepted rows, zero
  rejections, 889 unique query images, and 102/109 final Known classes. Output
  SHA-256: cfb0a48e461692ceb1f64e277f94dffa3595e681117b4047a83ffbbd58b9bb03.
- Every accepted row maps to a final v2 Known train-candidate image and has a
  unique supervision fingerprint. The formal manifest now agrees with the
  historical migration summary: 708 Direct / 1,456 RAG.
- A full temporary builder reconstruction also produced 708/1,456 canonical
  records while retaining test_informed_protocol=false, 1,019 test and 401
  reference items. Focused accepted-import/historical-migration tests pass;
  the unrelated legacy v1.1 builder test still calls removed choose_unknowns and
  hamming APIs and is not a regression of this correction. git diff --check
  and Python compilation pass.

### Impact

- Verified: formal v2 training supervision now uses the original audited source
  corpus under v2's own final boundary, preserving all eligible records despite
  v1.1/v2 role changes. v1.1 contributes method/protocol lineage and frozen
  evaluation reuse only; it is no longer an upstream class-role filter.
- Compatibility / scope: test, private truth, dev, Milvus reference, image
  split roles, and the 217-class v2 role selection were not changed.

### Next

Use the corrected 2,164-row accepted/train view for future SFT render/encode
smokes. The seven final Known classes still lacking accepted historical rows
are the only remaining coverage gap; fill them only with separately audited
v2-boundary supervision.

Sync scope: mainline formal-v2 data contract and local dataset artifacts.

## 2026-09-03 03:23:15 CST - Switch SFT attention configurations to FlashAttention

Tags: flash-attn, ms-swift, liger-kernel, vlm-sft

### Context

The dedicated SFT environment now has a locally compiled FlashAttention build
compatible with the PyTorch CUDA 13 runtime and local A800 GPUs. Training
configs should select this validated attention backend consistently.

### Changes

- Replaced every explicit `attn_impl: sdpa` with `attn_impl: flash_attn` in
  118 YAML configurations under `configs/`, covering 71 current VLM configs
  and 47 archived RAG-SFT reproducibility configs.
- Registered experiment templates do not define a separate attention field;
  their referenced VLM configuration now supplies `flash_attn`.

### Validation

- `.venv_test` imports `flash-attn==2.8.3.post1`; a BF16 CUDA forward/backward
  test on A800 passed with finite gradients. `ms-swift` still detects
  `liger-kernel`, and `uv pip check -p .venv_test/bin/python` passes.
- All 450 YAML files under `configs/` parse successfully. The 118 files that
  define `attn_impl` now all have the value `flash_attn`; no `attn_impl: sdpa`
  remains.
- `agrinet vlm submit vlm-sft-qwen3vl4b-hcv-v12-direct-anchor-mix-base-m35-e9-8gpu-3step-smoke-v1 --dry-run` resolves the `.venv_test` Swift entrypoint.

### Impact

- Verified: future registered SFT runs use FlashAttention when they consume
  these configuration files.
- Compatibility / scope: no training run was started; model, data, batch,
  optimizer and Liger settings were not changed.

### Next

Run the desired SFT smoke configuration and confirm the ms-swift startup log
reports the FlashAttention backend.

Sync scope: current and archived SFT configuration metadata.

## 2026-09-03 03:21:33 CST - Record formal OpenAgri v2 data state and entrypoints

Tags: open-agri-v2, dataset-contract, vlm-data, documentation

### Context

The formal dataset now has a stable test-blind role split and a completed,
four-view accepted SFT import. Future work needs one compact, unambiguous map
from purpose to the corresponding v2 manifest or data file.

### Changes

- Added an operational-entrypoint table to `docs/datasets/open_agri_v2.md` and
  a Chinese counterpart to `scripts/data/README_open_agri_v2.md`. They identify
  the formal root, machine summary, role/image manifests, four accepted SFT
  views, evaluation inputs, and private-truth boundary.
- Recorded that `manifests/summary.json` is the machine-readable contract;
  `vlm_data/candidates/` is only an image/task candidate layer; and SFT may
  read only the four explicit files under `vlm_data/accepted/train/`.

### Validation

- Read the current formal summary: 217 classes, 109 Known / 108 Unknown,
  141,482 Known train-candidate images, 812 dev images, 1,019 test images and
  401 Milvus reference images. It records `test_informed_protocol: false` and
  `final_test_accessed_for_selection: false`.
- Verified the accepted import summary: 2,164 accepted / 0 rejected rows from
  the direct-v2-boundary canonical sources, with 708 Direct and 1,456 RAG rows.
  The pairwise-disjoint train files are disease_direct 358, disease_rag 739,
  pest_direct 350 and pest_rag 717; their summary SHA-256 is
  `53a9267dece34c1f0cb461b7b7cb2583eee12285e780c7601cc377bc769b8588`.
- Rechecked the actual file layout: no aggregate `all.jsonl` and no legacy
  `accepted/train.jsonl` exist.

### Impact

- Verified: `datasets/AgriNet-1K/open_agri_v2/` is the only paper-facing data
  root. All earlier v1.1/v1.2 and later construction directories remain
  intermediate provenance rather than alternative training or final-test entrypoints.
- Compatibility / scope: this is a documentation and reproducibility record;
  no image, class role, supervision row, test input, private truth, or Milvus
  reference content changed.

### Next

New SFT configurations must name their selected one or more train views
explicitly and record the train-summary digest. Re-run the formal import command
after any intentional `build_open_agri_v2.py --replace`, because that builder
initializes the accepted train directory empty.

Sync scope: mainline formal-v2 data contract and local dataset artifacts.

## 2026-09-03 03:16:40 CST - Flatten OpenAgri v2 train views and remove aggregate copy

Tags: open-agri-v2, vlm-data, train-views, dataset-layout

### Context

The route/domain train partition should be immediately legible as four sibling
files (for example disease_direct.jsonl), and the duplicated aggregate all.jsonl
is unnecessary because a mixed run can declare its four source files explicitly.

### Changes

- Renamed the train subviews to four flat files under accepted/train/:
  disease_direct.jsonl, disease_rag.jsonl, pest_direct.jsonl and
  pest_rag.jsonl. Removed all.jsonl from both publication and builder-empty
  initialization paths.
- The train summary now records total_rows plus the four file paths, counts
  and SHA-256 values. The accepted import summary records the train-view-summary
  SHA-256 rather than a duplicate aggregate-file digest.
- Updated formal README, data card, current-state record and focused test to
  require an explicit one-file or four-file training selection.

### Validation

- Published files are exactly disease_direct (358), disease_rag (739),
  pest_direct (350), pest_rag (717), and summary.json. all.jsonl and the old
  flat train.jsonl are absent.
- The four views are pairwise disjoint and total 2,164 unique supervision
  fingerprints. Every count and file SHA-256 matches train/summary.json; its
  current SHA-256 is
  53a9267dece34c1f0cb461b7b7cb2583eee12285e780c7601cc377bc769b8588.
- Focused accepted-import/historical-migration tests pass (5 passed); Python
  compilation and git diff --check pass.

### Impact

- Verified: the sole formal SFT data entrypoint is now an unambiguous directory
  of four named subsets, with no duplicated all-data payload to drift from them.
- Compatibility / scope: sample content, source provenance, canonical data,
  test/dev/reference and all image-split constraints are unchanged.

### Next

For a mixed SFT run, declare the exact ordered list of four train JSONL files
in its versioned config and record the summary SHA-256; do not concatenate or
runtime-filter an undocumented substitute.

Sync scope: mainline formal-v2 data contract and local dataset artifacts.

## 2026-09-03 03:11:00 CST - Partition formal OpenAgri v2 training data by route and domain

Tags: open-agri-v2, vlm-data, train-views, direct, rag, disease, pest

### Context

The corrected accepted training set was a single JSONL. Formal training needs
stable, inspectable subset files for Direct/RAG and disease/pest experiments,
rather than runtime filtering from one mixed file.

### Changes

- Replaced the flat accepted/train.jsonl entrypoint with the directory
  accepted/train/. The import gate atomically publishes all.jsonl and four
  mutually exclusive route × task_domain files, then writes summary.json with
  their row counts and SHA-256 values.
- Stable subviews are direct/disease.jsonl, direct/pest.jsonl,
  rag/disease.jsonl and rag/pest.jsonl. The builder initializes the same empty
  directory schema on a fresh reconstruction; import is still explicit.
- Updated the v2 README, dataset card, accepted-data test and START_HERE to
  make train/ the formal SFT entrypoint and require explicit file selection.

### Validation

- Current counts: Direct disease 358, Direct pest 350, RAG disease 739, RAG
  pest 717. Their four-way union is exactly all.jsonl's 2,164 unique
  supervision fingerprints; no subset overlaps another.
- all.jsonl SHA-256 remains
  cfb0a48e461692ceb1f64e277f94dffa3595e681117b4047a83ffbbd58b9bb03.
  Every subset count and SHA-256 matches accepted/train/summary.json.
- Focused accepted-import/historical-migration tests pass (5 passed); Python
  compilation and git diff --check pass.

### Impact

- Verified: Direct/RAG and disease/pest training can now select a fixed,
  auditable JSONL without unversioned filtering. all.jsonl remains the only
  full-mixture view and is exactly reconstructible from the four subviews.
- Compatibility / scope: dev/test/private truth/Milvus/reference and image
  split roles are unchanged. The legacy flat train.jsonl is removed to avoid
  two competing formal training entrypoints.

### Next

Point future SFT experiment configs at a declared train subview or all.jsonl,
then run the selected agent-template encode smoke before launch.

Sync scope: mainline formal-v2 data contract and local dataset artifacts.
