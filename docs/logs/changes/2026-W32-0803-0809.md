# Changes — 2026-W32 (0803–0809)

## 2026-08-03 09:26:58 CST - Establish phase 1 AgriNet CLI and contracts

Tags: agrinet, refactor, cli, contracts, uv

### Context

The approved RAG/VLM refactor needed a stable project entrypoint and typed boundary contracts before domain migrations begin.

### Changes

- Approved `docs/refactor-20260803.md`.
- Added the Python 3.12 uv project, locked dependencies, and installed the editable package in the existing root `.venv`.
- Added versioned experiment, run, artifact, RAG search, student SFT, and evaluation contracts.
- Added experiment discovery/config override helpers and `agrinet data|rag|vlm list/show`.
- Registered one initial experiment for each domain and added focused tests.

### Validation

`.venv/bin/python -m pytest` passed 9 tests. Installed CLI smoke checks listed all three domains and bilingual experiment names.

### Impact

- Verified: phase 1 discovery interfaces and boundary schemas are executable from the project `.venv`.
- Assumed: registered specs will be refined while each domain implementation migrates.
- Compatibility / scope: no legacy entrypoint or artifact was moved or deleted.

### Next

Begin phase 2 by inventorying active Data/VLOOM entrypoints and migrating reusable behavior behind `agrinet data`.

## 2026-08-03 09:33:00 CST - Establish phase 2 Data boundaries and generic launcher

Tags: agrinet, data, vloom, slurm

### Context

Legacy Data/VLOOM behavior was spread across project-local tools, shell scripts, and Slurm files, with direct VLOOM registry imports and stale `.venv-py312` or Conda interpreter paths.

### Changes

- Added versioned prepared-sample, teacher-record, and converted-SFT boundaries.
- Added reusable JSONL prepare, validate, and convert services with atomic outputs.
- Added the `TeacherDataProvider` protocol and isolated the legacy VLOOM invocation in one adapter.
- Added `agrinet data prepare|generate|convert|validate|submit`, a generic Data Slurm launcher, bilingual Data documentation, and an integration fixture.

### Validation

`.venv/bin/python -m pytest` passed 14 tests. The fixture completed prepare → validate → convert, the provider protocol/isolation checks passed, and submit dry-run generated deterministic `sbatch --parsable` arguments.

### Impact

- Verified: new Data boundary operations and scheduler orchestration are locally executable.
- Assumed: the adapter preserves legacy VLOOM runner behavior; real provider output has not yet been generated in this refactor.
- Compatibility / scope: legacy VLOOM entrypoints remain until a production teacher run and conversion are validated.

### Next

Run one credentialed VLOOM pilot through `agrinet data generate`, validate its output boundary, then migrate the remaining production algorithms and remove superseded entrypoints.

## 2026-08-03 09:41:00 CST - Migrate current execution from Slurm to local processes

Tags: agrinet, local-runtime, gpg, yunwu

### Context

The current repository is a local workstation checkout rather than a Slurm cluster, and Yunwu credentials are already stored in the user-level GPG-encrypted `apikey` store.

### Changes

- Replaced the repository execution policy with local foreground/detached process guidance.
- Changed `agrinet data submit` from `sbatch` generation to local CLI execution with optional `--detach` run directories.
- Removed the newly introduced generic Data Slurm file and updated the approved refactor baseline and bilingual Data docs to local-runtime semantics.
- Added strict, non-shell evaluation of `~/.apikeys/bin/apikey env yunwu`; only Yunwu key/base URL variables are accepted and passed in child-process memory.

### Validation

`.venv/bin/python -m pytest` passed 16 tests. Data submit dry-run contains no scheduler command or credential. The real GPG helper successfully returned a non-empty Yunwu key and base URL during a boolean-only readiness check; secret values were not printed or persisted.

### Impact

- Verified: current Data orchestration no longer requires Slurm and encrypted Yunwu credential loading works.
- Assumed: detached status completion still needs a wrapper that records final exit state rather than only launch state.
- Compatibility / scope: existing historical `.slurm` scripts and logs remain untouched; they are no longer current entrypoints.

### Next

Vendor the currently external, unpackaged VLOOM source into this repository, declare and lock its runtime dependencies, then execute the credentialed pilot locally.

## 2026-08-03 09:56:00 CST - Vendor VLOOM and validate one-sample local dry run

Tags: agrinet, vloom, vendor, gpg, migration-pilot

### Context

The only available VLOOM implementation was an unpackaged sibling-repository directory, which made the current project depend on an ambient absolute `PYTHONPATH`.

### Changes

- Imported the clean, Git-tracked VLOOM subtree into `vendor/VLooM` and recorded source/subtree commits and unresolved license status.
- Packaged the vendored `vloom` module with AgriNet and locked its import-derived runtime dependencies in the Data extra.
- Added a one-sample pilot using real AgriNet images and a separate migration output root, plus dry-run and credentialed experiment configs.

### Validation

Vendored `vloom`, `draccus`, the dataset registry, and provider adapter import from `.venv`. The offline pilot loaded one manifest sample, rendered the complete prompt, read five real images, and wrote dry-run results. The mock response produced the expected parse-failure record. The credentialed attempt stopped before any API request because the GPG private key was no longer unlocked for non-interactive decryption; no credentialed output was created.

### Impact

- Verified: VLOOM no longer requires a sibling-repository `PYTHONPATH`; the local data/template/image path works end to end.
- Assumed: a real Yunwu response will satisfy the existing structured parser; this remains unverified.
- Compatibility / scope: no historical output was overwritten; pilot files live under `outputs/migration/vloom-local-pilot/`.

### Next

Unlock the configured GPG private key interactively with `~/.apikeys/bin/apikey env yunwu >/dev/null`, rerun `agrinet data generate data-generate-agrinet-vloom-local-pilot-v1`, then validate and convert the resulting teacher record.

## 2026-08-03 10:04:00 CST - Finalize local run manifests and exit status

Tags: agrinet, local-runtime, manifest

### Context

The initial detached launcher recorded only launch state, so PID disappearance could not distinguish successful completion from failure. Foreground runs also lacked the standard run layout.

### Changes

- Added unique local run allocation with automatic `aNN` collision handling.
- Added resolved config, versioned run manifest, dependency-lock checksum, Git commit/dirty state, status, PID, end time, and exit-code persistence.
- Added a detached worker that finalizes `complete` or `failed`, and routed foreground submit through the same lifecycle contract.

### Validation

`.venv/bin/python -m pytest` passed 21 tests. Success and exit-code-7 detached fixtures finalized correctly, the foreground fixture used the same layout, `RunManifest` re-validation passed, and an injected test credential was absent from every run file.

### Impact

- Verified: local Data runs are now traceable after process exit without relying on Slurm accounting.
- Assumed: domain-specific output artifact registration remains to be added after the credentialed VLOOM result exists.
- Compatibility / scope: no production run was launched while the GPG private key remained locked.

### Next

Unlock GPG interactively and run the registered one-sample VLOOM pilot; then register and convert its output artifact.

## 2026-08-03 10:29:00 CST - Diagnose local Yunwu connectivity and harden run preflight

Tags: agrinet, yunwu, proxy, gpg, local-runtime

### Context

After GPG was briefly unlocked, the credentialed one-sample pilot reached the OpenAI client but repeated direct connections to `yunwu.ai:443` timed out because non-interactive project commands did not inherit the user's interactive `proxy_on` setup.

### Changes

- Added strict loading of the existing interactive-shell proxy definition when no proxy environment is present; the proxy URL is passed only to the child process and is not persisted.
- Added explicit SIGINT/SIGTERM handling and removed the detached-launch status race so interrupted runs finalize as failed.
- Moved generate credential/proxy preflight before run allocation and passed the decrypted environment through the worker, preventing duplicate decryption and meaningless runs when GPG is locked.
- Corrected the interrupted first pilot manifest to `failed` with exit code 130.

### Validation

Direct Yunwu TLS timed out, while the existing local proxy reached the endpoint and received the expected unauthenticated HTTP 401. `.venv/bin/python -m pytest` passed 26 tests, including signal finalization, proxy parsing/rejection, credential non-persistence, and preflight-before-allocation.

### Impact

- Verified: the network path is now known and safely integrated; no teacher response or credentialed result was produced.
- Assumed: after the GPG key is unlocked again, the proxied authenticated request should reach the model endpoint.
- Compatibility / scope: run `20260803T101007-b0b031b2-a01` records the interrupted direct-connect attempt; run `20260803T102658-b0b031b2-a01` records a later pre-preflight decryption failure.

### Next

Unlock GPG interactively once more and immediately run the registered pilot. New preflight will abort before run allocation if the key is not available.

## 2026-08-03 10:42:00 CST - Complete credentialed Yunwu pilot and register SFT artifact

Tags: agrinet, yunwu, vloom, teacher-data, sft, artifact

### Context

After GPG was unlocked again, credential and network preflight confirmed both the encrypted provider metadata and experiment config resolve to `yunwu.ai`, and the local proxy reached the endpoint.

### Changes

- Ran the registered one-sample VLOOM pilot through the current local CLI and received HTTP 200 from `https://yunwu.ai/v1/chat/completions`.
- Normalized the vendored BasicAgent metadata envelope at the project adapter boundary so stable top-level `result`, `raw_response`, `template_vars`, and `think` fields remain compatible with validators/converters.
- Recovered and parsed the already-saved response offline instead of issuing a second API request.
- Converted the accepted trace to English/Chinese legacy SFT, wrapped English SFT in `agrinet.sft.student/v1`, and registered a checksummed dataset artifact.
- Linked the teacher result and student artifact from the successful run manifest and removed the now-invalid parse-failure diagnostic.

### Validation

Run `20260803T103736-b0b031b2-a01` is `complete` with exit code 0. Teacher validation reports one parsed record, zero missing fields, zero out-of-candidate labels, and zero errors. Both manifest artifact checksums match; the versioned SFT validates with one record. `.venv/bin/python -m pytest` passed 28 tests.

### Impact

- Verified: the vendored VLOOM + GPG Yunwu + local proxy + Data CLI path works end to end on a real multimodal request.
- Verified: the registered student artifact is `outputs/artifacts/datasets/agrinet-vloom-local-pilot-student-en-v1/`.
- Assumed: larger production batches may still expose rate-limit or response-format variability and should begin with bounded local batches.
- Compatibility / scope: historical VLOOM entrypoints remain until the production preparation algorithms and configs finish migration.

### Next

Migrate the proven AgriNet preparation/conversion algorithms into `src/agrinet/data`, register a bounded production experiment, then audit and remove only superseded legacy Data/VLOOM entrypoints.

## 2026-08-03 16:09:00 CST - Register deterministic bounded AgriNet preparation artifact

Tags: agrinet, data-preparation, artifact, migration

### Context

The legacy preparation entrypoint depends on feature-pair and representative artifacts plus `wiki.json`; those inputs are absent from the current filesystem. The current dataset provides `wiki/base.json` and the complete shared `all/` image tree.

### Changes

- Added current-wiki parsing and deterministic bounded contrast preparation under `src/agrinet/data`.
- Added domain-stratified class selection, seeded image selection, same-domain candidate construction, and a dedicated bounded artifact validator.
- Registered `data-prepare-agrinet-bounded-contrast-v1` and produced `agrinet-bounded-contrast-v1` with per-file checksums and a producer run reference.

### Validation

Run `20260803T160629-b0b031b2-a01` completed with exit code 0. The artifact contains 4 disease and 4 pest classes, 8 pair rows, 8 samples, 24/24 same-domain negatives, and no missing image paths. `agrinet data validate ... --kind bounded` passed; artifact and run contracts re-validated; 29 tests passed.

### Impact

- Verified: current raw AgriNet data can be prepared reproducibly through the new Data CLI without legacy feature artifacts.
- Assumed: deterministic same-domain candidates are an appropriate bounded migration baseline; they are not claimed to reproduce feature-ranked hard negatives.
- Compatibility / scope: legacy hard-negative entrypoints remain because their algorithm has not been equivalently replaced with currently available inputs.

### Next

Add a bounded teacher experiment consuming `agrinet-bounded-contrast-v1`, then migrate the accepted conversion policy behind the new versioned teacher/SFT contracts.

## 2026-08-03 16:36:37 CST - Complete bounded Data run, local RAG smoke, and VLM baseline migration

Tags: agrinet, yunwu, milvus, siglip2, qwen3-vl, migration

### Context

Resume the approved refactor after a transport interruption without repeating the completed Yunwu request, then establish verified local Data, RAG, and VLM paths.

### Changes

- Verified and registered the existing eight-sample bounded VLOOM teacher output; converted it to a versioned English student SFT artifact and linked both producer manifests.
- Added a real local Milvus/SigLIP2 backend, typed in-process retrieval service, CLI search, and thin HTTP transport with `/health`, `/search`, and `/search/visual`.
- Added public-entrypoint-only ms-swift and VLMEvalKit adapters plus explicit VLM inspect/doctor/preview commands.
- Copied the step-224 baseline through staging to `outputs/artifacts/models/qwen3vl4b-rag-sft-full-v1`, retained the source, and wrote the migration map.
- Added root and domain bilingual documentation for the current non-Slurm workflow.

### Validation

Teacher validation: 8/8 parsed, zero missing fields and candidate violations. Student validation: 8 records; SHA-256 `dbab8e5daa0e6535da5bb5309c64bc41238283a20ec25c66ff000db13b922989`. A real A800 query returned correct top-1 `N04001` at 0.951895. Baseline migration matched all 13 files and 8,887,255,685 bytes; both safetensors checksums matched and `AutoConfig`/`Qwen3VLProcessor` loaded from the new path. Full suite: 35 passed; `git diff --check` passed.

### Impact

- Verified: no Yunwu request was duplicated during transport recovery; credentials and proxy values were not persisted.
- Verified: current Data, RAG search, and VLM baseline inspection use `.venv/bin/agrinet` locally.
- Compatibility / scope: legacy Slurm and business-logic entrypoints remain as pre-migration history because distillation and full train/eval replacement acceptance is not complete; vendor relocation is also pending.

### Next

Finish extracting the monolithic RAG distillation policy/validator and complete real ms-swift/VLMEvalKit smoke before deleting legacy entrypoints or relocating their vendor subtrees.

## 2026-08-03 17:27:48 CST - Verify standalone local Data, RAG, and VLM launch workflows

Tags: agrinet, local-runtime, milvus, ms-swift, a800, non-slurm

### Context

Verify all practical workflows on the current workstation without calling Slurm, while retaining every historical Slurm entrypoint for later validation on a separate cluster machine.

### Changes

- Made RAG `submit` run `serve` or `distill` locally in foreground or detached mode, with standard run manifests/logs, and added a graceful `rag stop` command.
- Serialized lazy RAG backend initialization to prevent concurrent health probes from loading SigLIP2 repeatedly.
- Made VLM `submit` run train/export/evaluate locally, corrected the public ms-swift CLI invocation, and added a dedicated two-step smoke experiment.
- Added atomic Transformers checkpoint export and a lightweight exact-name evaluator emitting `agrinet.evaluation/v1`; retained the VLMEvalKit backend for full cluster-environment verification.
- Installed the existing ms-swift and VLMEvalKit sources editable into the single root `.venv`; locked the local core evaluation dependencies.

### Validation

RAG run `20260803T171912-b0b031b2-a01` served HTTP locally; `/health` and `/search/visual` returned 200 and top-1 `N04001` at 0.951895. Run `20260803T172518-b0b031b2-a01` verified `submit --detach -> stop -> complete/exit 0` with no orphan process. VLM run `20260803T171441-b0b031b2-a01` completed two full-parameter BF16/SDPA/Liger steps on A800 GPU 0 and wrote checkpoints 1 and 2. Checkpoint 2 was exported as `qwen3vl4b-rag-sft-smoke-v1` (13 files, 8,887,243,020 bytes) and loaded with `AutoConfig`/`Qwen3VLProcessor`. Local evaluation emitted `agrinet.evaluation/v1` with fixture accuracy 1.0. Full tests: 38 passed; current-code scheduler/Conda/PYTHONPATH scan clean; no worker/service/training process remains.

### Impact

- Verified: Data artifacts, RAG retrieval/service lifecycle, VLM training/export/evaluation, foreground/detached manifests, logs, and status work without Slurm.
- Blocked: the new one-sample RAG Yunwu distillation was stopped by credential preflight because the GPG agent had relocked; no run was allocated and no request was sent. Earlier real Yunwu Data teacher runs remain verified evidence for the credential/network path.
- Compatibility / scope: all old Slurm scripts and logs are deliberately retained and were not validated or modified for execution on this workstation.

### Next

After unlocking GPG, rerun only `agrinet rag submit rag-distill-candidate-followup-v6 --operation distill --detach` with the local RAG service running. Validate Slurm paths separately on the cluster machine.

## 2026-08-03 18:10:33 CST - Complete local Yunwu RAG distillation workflow validation

Tags: agrinet, rag-distill, yunwu, evidence-gate, local-runtime

### Context

Complete the final credentialed non-Slurm workflow after GPG was unlocked, without rerunning already verified Data or VLM jobs.

### Changes

- Injected the validated local proxy under `ALL_PROXY`, `HTTPS_PROXY`, and `HTTP_PROXY` for client compatibility while keeping its value only in child memory.
- Made all-sample runtime failures return nonzero instead of incorrectly finalizing complete.
- Restored legacy public `/search/visual|balanced|semantic|name|rrf` envelopes over the new typed retrieval service while retaining typed `/search`.
- Added HTTP contract tests for all preserved preset routes.

### Validation

Service run `20260803T180842-b0b031b2-a01` initialized once, served the complete contract, and stopped complete/exit 0. Distillation run `20260803T180913-b0b031b2-a01` reached Yunwu `gemini-3.1-pro-preview`, received three teacher responses, and completed three successful local tool calls (`visual`, `balanced`, `rrf`) with 3/3/5 hits. The evidence gate rejected the sample because the target `N04007` was absent from retrieved evidence; all three searches ranked Apple Black Rot first. No runtime error occurred and no unsupported student row was emitted.

### Impact

- Verified: the complete local service → Yunwu teacher → RAG tool → evidence gate → artifact path runs without Slurm.
- Verified: rejection is a correct research-quality outcome, not an infrastructure failure; the diagnostic artifact is `outputs/runs/rag-distill/candidate-followup-v6-local-smoke-retry2/`.
- Compatibility / scope: retained Slurm paths remain untouched and await separate cluster validation.

### Next

Use a retrieval fixture whose target is known to be indexed when an accepted-row smoke is specifically required; do not weaken the evidence gate to force acceptance.

## 2026-08-07 00:50:56 CST - Build and launch review-gated RAG SFT recovery Pilot

Tags: agrinet, sft, rag, milvus, rejection-sampling, yunwu

### Context

Recover the two existing SFT corpora into obvious immutable artifacts, construct a small balanced dataset before any full generation, and require user review before training.

### Changes

- Published frozen Direct and RAG SFT artifacts with checksums, statistics, lineage, and overwrite guards.
- Added deterministic seed-`20260805` planning for 48 RAG targets, three candidate attempts per target, 32 reused/derived Direct rows, and 16 strictly non-trainable unknown audit probes.
- Added English/Chinese Open and Option support, English-only Milvus queries, A/B/C/D balancing, deterministic accepted-candidate selection, rejected-trace retention, and a review-only merged corpus.
- Added the registered Data experiment, tests, review summary, and tmux launcher. Full generation beyond this Pilot and all training remain unauthorized.

### Validation

Four targeted Data/artifact tests pass and `git diff --check` is clean. Offline construction is idempotent and validates 48 RAG targets, 144 attempts, Direct Open/Option 16 each, unknown audit 16, and A/B/C/D 4 each. A real English Open smoke was accepted after two successful Milvus calls (`visual`, `balanced`). A Chinese Option smoke was accepted with answer `C`, Chinese visible analysis, and English-only Milvus queries.

### Impact

- Verified: frozen artifacts and the offline Pilot are reviewable under `outputs/artifacts/datasets/`.
- Volatile: tmux socket `agrinet-rag-debug`, session `agrinet-rag-recovery-pilot-v1`, is generating 144 candidate attempts with four workers; startup reached accepted candidates.
- Compatibility / scope: old dataset paths remain unchanged; no VLM training or full dataset generation was started.

### Next

After the tmux session exits, inspect `outputs/artifacts/datasets/agrinet-rag-recovery-pilot-v1/review/generation_summary.json` and rejected reasons. Present the review artifacts to the user and wait for explicit approval before any full generation or training.

## 2026-08-07 01:48:00 CST - Correct recovery protocol and launch candidate-first Pilot v2

Tags: agrinet, sft, rag, correction, option, milvus

### Context

Pilot v1 produced only 40/48 RAG targets and revealed that stop/correction was metadata-only, Option validation did not map letters to choices, and candidate selection always preferred candidate 1.

### Changes

- Quantized student-visible retrieval scores to two decimals.
- Unified Direct and RAG stages around visual observation and candidate analysis; RAG now exposes candidates before its first tool call.
- Added real stop/correction prompting and hard rejection when the final reasoning lacks explicit correction/confirmation.
- Added quality-based candidate selection using target rank/score, retrieval diversity, concision, and deterministic candidate-index tie-breaking.
- Updated validation for Option letter mapping, two-decimal scores, and stop/correction behavior.
- Preserved v1 and created `agrinet-rag-recovery-pilot-v2`.

### Validation

Six targeted tests pass. Real v2 Open, Chinese Option, and correction smokes each accepted one row and passed the artifact validator with accuracy 1.0 and zero errors. All showed candidate analysis before RAG, two-decimal scores, and English retrieval queries; the correction smoke included explicit correction behavior.

### Impact

- Verified: the revised protocol resolves the identified structural defects in bounded smoke tests.
- Volatile: tmux socket `agrinet-rag-pilot-v2`, session `agrinet-rag-recovery-pilot-v2`, is running 144 candidates with four workers.
- Scope: training remains unauthorized; v1 must not be used as the current training candidate.

### Next

After v2 completes, run the updated validator on the batch and final selected rows, inspect target coverage and balance, and present the review result before authorizing training.

## 2026-08-07 03:00:00 CST - Isolate languages and launch reserve-backed Pilot v3

Tags: agrinet, sft, rag, bilingual, reserve-sampling, correction

### Context

Pilot v2 reached 41/48 RAG targets but retained one failed tool response, generic pre-RAG text, mixed-language Chinese Direct templates, confirmation-heavy correction rows, and one Direct/RAG image overlap.

### Changes

- Enforced English user prompts to English visible reasoning/answers and Chinese prompts to Chinese visible reasoning/answers; internal Milvus queries remain English.
- Localized Chinese final headings and parser support for half/full-width colons.
- Replaced development-oriented rationale text with natural visual comparison and evidence language.
- Rejected any trajectory containing a failed tool call or generic first retrieval query.
- Required correction rows to explicitly change a broad initial hypothesis, not merely confirm it.
- Added two reserve targets per quota cell, quality selection of four rows per cell, Option-letter coverage, and Direct/RAG image exclusion.
- Added SSL EOF retry handling without weakening quality gates.

### Validation

Offline v3 contains 72 RAG targets, 216 attempts, 24 reserves, 32 Direct rows, 16 unknown audit probes, 12 balanced cells, and zero Direct/RAG image overlap. Six tests pass. English Open passed the artifact validator with zero errors and accuracy 1.0; preserved Chinese Option and changed-hypothesis correction smokes pass the corrected acceptance protocol with no rejection reasons.

### Impact

- Verified: v3 addresses all identified v2 structural blockers in bounded validation.
- Volatile: tmux socket `agrinet-rag-pilot-v3`, session `agrinet-rag-recovery-pilot-v3`, is generating 216 candidates with four workers.
- Scope: no SFT training is authorized until final v3 review.

### Next

After v3 completes, require 48 selected RAG rows, four per quota cell, Option A/B/C/D coverage, zero validator errors, zero failed tool calls, zero image overlap, and strict language checks before training approval.

## 2026-08-07 04:00:00 CST - Review completed Pilot v3 and retain training gate

Tags: agrinet, sft, rag, data-review, distillation-quality

### Context

Evaluate whether completed Pilot v3 is sufficiently balanced and semantically clean for SFT, and characterize the abilities its supervision can realistically teach.

### Validation

The batch contains 133 accepted and 83 rejected candidates. All accepted candidates pass the strict artifact validator: final-label accuracy 1.0, retrieval success 1.0, and zero validator errors. The selector produced 42 RAG plus 32 Direct rows, with zero image overlap and no unknown leakage. Three quota cells remain short, yielding 74 rather than 80 rows.

Semantic review found that all 23 selected Chinese RAG rows share the same pre-retrieval template. Direct rows preserve oversized legacy reasoning, including repeated stage headings and irrelevant knowledge passages; the longest final response is 7,052 characters. Target evidence ranks first in 31/42 selected RAG rows and ranks 2-4 in 11/42, indicating some label-guided evidence selection risk.

### Conclusion

The selected RAG subset is protocol-clean but the combined Pilot is not approved for production SFT. It may be used only for bounded smoke/ablation work. It can supervise tool-call syntax, candidate-before-retrieval flow, evidence citation, bilingual answer formatting, Option output, and broad-to-specific correction. It is not sufficient evidence for broad disease/pest recognition, robust retrieval policy, unknown rejection, or calibrated autonomous tool use.

### Next

Replace templated Chinese pre-retrieval reasoning with image-specific observations, rebuild Direct rows from compact structured fields instead of wrapping legacy chains, fill the three quota shortfalls, and re-review semantic quality before changing `training_authorized`.

## 2026-08-07 05:00:00 CST - Implement P0 refactor and launch preflighted Pilot v4

Tags: agrinet, rag-sft, p0, semantic-validation, milvus-preflight

### Changes

- Added the canonical roadmap at `docs/plan/rag_sft_refactor.md`.
- Rebuilt Direct reasoning by truncating legacy knowledge/wiki/taxonomy blocks and rendering compact bilingual stages.
- Added image-specific Chinese visible summaries derived from retrieval-query traits.
- Added semantic validation for length, forbidden knowledge, duplicate headings, repeated pre-RAG text, trait count, and duplicate images.
- Added Milvus target preflight with rank <= 5, score >= 0.70, reference-image requirements, deterministic per-cell selection, and Option A/B/C/D coverage.

### Validation

Direct v4 has 32 rows, lengths 411-872 (median 628), zero forbidden knowledge blocks, zero repeated headings, and zero language mismatches; both Direct semantic reports pass. Milvus screened 240 targets, found 184 eligible, and selected 48 balanced targets / 144 teacher attempts with no quota shortfall. Four Option cells each contain A/B/C/D. Six tests pass. Open, Chinese Option, and Chinese correction smokes pass protocol and initial semantic validation.

### State

Volatile: tmux socket `agrinet-rag-pilot-v4`, session `agrinet-rag-recovery-pilot-v4`, is generating the 144 preflighted candidates with four workers. No training is authorized.

### Next

After v4 completes, inspect selection coverage plus protocol and semantic reports. Approve training only if all 48 RAG and 32 Direct rows pass both gates.

## 2026-08-07 06:00:00 CST - Move semantic gate before selection and launch targeted supplement

Tags: agrinet, rag-sft, semantic-gate, supplement

### Changes

- Exposed reusable row-level semantic checks and filtered candidates before target/cell selection.
- Enforced globally unique pre-RAG text during selection, including within Option letters and non-Option cells.
- Added a supplement planner that selects only unused Milvus-eligible targets for current quota gaps, with exact missing Option-letter handling.
- Added supplement batch merging and a dedicated tmux runner that preserves the original batch.

### Validation

Strict re-selection of the existing v4 batch yields 30 RAG + 32 Direct rows with zero semantic errors. The targeted supplement contains 36 unused eligible targets and 108 attempts across nine shortfall cells; every target passed Milvus preflight and has candidate indices 1-3. Six tests pass.

### State

Volatile: tmux socket `agrinet-rag-pilot-v4-s1`, session `agrinet-rag-recovery-pilot-v4-s1`, is generating supplement-1 with four workers. Training remains unauthorized.

### Next

After supplement-1 completes, merge and re-select. If any cell remains short, create the next supplement only for the remaining eligible targets.

## 2026-08-07 20:05:21 CST - Launch final targeted Pilot v4 supplement

Tags: agrinet, rag-sft, supplement, tmux

### Changes

- Resolved supplement inputs to absolute paths before the runner changes directory, preventing silent early exit from relative-path resolution.
- Added outer tmux launch and exit-code artifacts so startup failures remain diagnosable after a session exits.
- Launched supplement-2 for the four remaining quota cells: 21 teacher attempts over seven unused Milvus-eligible targets.

### Validation

The run printed `Processing 21 samples with max_concurrent=4` and the tmux session remained active after startup. No candidate files existed from the failed first launch, so the retry did not duplicate completed requests.

### State

Volatile: tmux socket `agrinet-rag-pilot-v4-s2`, session `agrinet-rag-recovery-pilot-v4-s2`, is running supplement-2. Logs are under `outputs/artifacts/datasets/agrinet-rag-recovery-pilot-v4/review/`. Training remains unauthorized.

### Next

Do not monitor automatically. On the next requested status check, inspect the exit marker, generation summary, semantic report, and final 80-row quota acceptance criteria before requesting training approval.

## 2026-08-07 20:41:25 CST - Add RAG SFT progress report for project review

Tags: agrinet, rag-sft, report

### Note

Added `docs/rag_sft_progress_report.md`, a Chinese project report covering verified data scale, the RAG SFT method, unified evaluation results, current difficulties, the next experiment plan, and one selected sampling example. The report uses reader-facing names instead of internal experiment identifiers and distinguishes completed results from planned validation.

### Status

The report is 142 lines. `git diff --check -- docs/rag_sft_progress_report.md` passes, and the cited counts and metrics were checked against local dataset summaries and evaluation artifacts.

### Next

Update the report after the 80-row review gate and controlled training comparison produce new verified results.

## 2026-08-08 00:26:55 CST - Refocus RAG SFT report on failure and data iteration

Tags: agrinet, rag-sft, report

### Note

Reworked `docs/rag_sft_progress_report.md` into a shorter six-part report centered on why the early RAG SFT routes failed, how the SFT data is being rebuilt, current 79/80-row progress, one selected sample, and the controlled validation plan. Removed repeated background and engineering narrative while retaining verified metrics and caveats.

### Status

The report is now 110 lines and `git diff --check -- docs/rag_sft_progress_report.md` passes.

### Next

Refresh the current-progress table and evaluation section after the 80-row gate and controlled comparison complete.

## 2026-08-07 20:30:23 CST - Launch single-target Pilot v4 supplement-3

Tags: agrinet, rag-sft, supplement, rejection-sampling

### Changes

- Generated `plan/supplement-3.jsonl` from the remaining quota shortfall.
- Restricted the plan to target `rag_option-zh-pest_N05045_N05045_P00005`, the only unused eligible Chinese pest Option-C target.
- Launched three rejection-sampling candidates under tmux socket `agrinet-rag-pilot-v4-s3`, session `agrinet-rag-recovery-pilot-v4-s3`.

### Validation

The plan contains exactly three attempts, candidate indices 1-3, one target, one quota cell, Option C only, and all rows are Milvus-preflight eligible. The run printed `Processing 3 samples with max_concurrent=4` and remained active after startup.

### State

Volatile: supplement-3 is running. Pilot selection remains 47 RAG + 32 Direct until generation, semantic-first selection, and validation complete. Training remains unauthorized.

### Next

Do not monitor automatically. On the next requested check, require 48 RAG + 32 Direct, twelve full quota cells, complete Option letters, and zero protocol, semantic, failed-tool, language, leakage, and image-overlap errors before presenting the Pilot for approval.

## 2026-08-07 20:39:11 CST - Reject supplement-3 and identify retrieval-depth mismatch

Tags: agrinet, rag-sft, pilot-review, retrieval-contract

### Validation

Supplement-3 completed all three teacher attempts, but accepted none. Each trajectory made three successful Milvus calls and then produced another retrieval request instead of a final answer, so the protocol gate rejected all three. The merged Pilot remains 47 RAG + 32 Direct; semantic validation still reports zero errors and duplicate query images remain zero.

### Conclusion

The remaining target passed preflight at rank 5, while student-visible retrieval uses `top_k=3`. The target evidence is therefore not guaranteed to be visible under the actual generation contract. This is a preflight/generation depth mismatch, not a reason to weaken the evidence or final-answer gates. Training remains unauthorized.

### Next

Align preflight eligibility with the actual retrieval depth (`rank <= top_k`) and rebuild the affected Option-C target pool. If no unused rank-3 target exists, expand the audited target pool or explicitly change both preflight and generation to the same larger `top_k`, then rerun only the missing cell.

## 2026-08-07 22:49:27 CST - Implement strategy-diverse P0 retrieval contract

Tags: agrinet, rag-sft, p0, retrieval-strategy, milvus

### Changes

- Added a shared strategy catalog covering visual-to-balanced, balanced-to-name, semantic-to-visual, RRF-to-name, and balanced-stop trajectories.
- Added per-strategy Milvus preflight with the same `top_k=5` used by generation, cached by discovery preset and depth.
- Added stable target-aware strategy rotation across three rejection samples and fallback only to preflight-eligible routes.
- Propagated `strategy_id`, `preferred_sequence`, and `retrieval_top_k` through prompts, tool clamping, metadata, supplements, validation, and selection.
- Added protocol checks for route order, fixed top-k, non-initial name confirmation, and name as the final retrieval call.
- Replaced raw retrieval-type-count scoring with route conformance, legal name confirmation, repeat-call penalties, and per-cell strategy coverage.

### Validation

All 45 repository tests pass with `.venv/bin/python -m pytest -q`; syntax compilation and `git diff --check` pass. The 47 legacy v4 RAG rows remain compatible with zero validator errors and final-label accuracy 1.0. No v5 Milvus preflight, teacher sampling, or training was started.

### Next

Create a v5 bounded configuration and run strategy-aware Milvus preflight. Review eligible counts and per-cell/per-strategy coverage before authorizing a small teacher Pilot.

## 2026-08-07 23:58:07 CST - Prepare Pilot v5 and launch strategy preflight

Tags: agrinet, rag-sft, pilot-v5, milvus-preflight

### Changes

- Added the isolated `data-sft-rag-recovery-pilot-v5` experiment and artifact path.
- Prepared 240 candidate targets, 720 rejection-sampling attempt placeholders, 32 Direct rows, and 16 non-trainable unknown probes.
- Added deterministic concurrent preflight with per-strategy error capture, preserved output order, progress logging, and cached duplicate discovery calls.
- Added a dedicated local tmux runner for v5 preflight.

### Validation

The prepared plan has 12 quota cells, 16 Direct Open, 16 Direct Option with A/B/C/D each appearing four times, and 16 unknown audit rows. Eight focused tests pass; runner syntax, Python compilation, and `git diff --check` pass. Milvus health reports 320 classes and 578 images.

### State

Volatile: tmux socket `agrinet-rag-pilot-v5-preflight`, session `agrinet-rag-recovery-pilot-v5-preflight`, is preflighting 240 targets with four workers. Startup and the first four target results were confirmed. No teacher generation or training is authorized.

### Next

Do not monitor automatically. On the next requested status check, inspect the preflight exit marker, quota shortfalls, strategy eligibility distribution, and planned attempt distribution before deciding whether to launch a small teacher Pilot.

## 2026-08-08 00:26:21 CST - Review v5 preflight and reject false strategy diversity

Tags: agrinet, rag-sft, pilot-v5, rag-service, review

### Results

Preflight completed with exit 0: 184/240 targets were marked eligible, 48 targets and 144 attempts were selected, all 12 cells have four targets, Option A/B/C/D each has 12 attempts, assigned top-k is uniformly 5, and the five planned strategy labels range from 23 to 33 attempts. There are no assigned-ineligible, sequence, top-k, duplicate-key, or transport errors.

### Finding

The apparent strategy coverage is not real. All five strategies have the exact same 184 eligible targets and identical rank distribution; every target is either eligible for all five or none. Direct endpoint checks confirm visual, balanced, semantic, and RRF return identical results. `HttpSearchRequest` ignores text, preset, and weight fields, every legacy preset route calls the same handler, and `MilvusSiglipBackend` supports only image/image-to-class retrieval.

### Conclusion

The plan is balanced at the metadata level but all calls currently execute image retrieval. Teacher sampling is not authorized because it would teach fictional strategy distinctions.

### Next

Implement real text and hybrid retrieval contracts in the HTTP service/backend, add endpoint differentiation tests, restart the service, and rerun v5 preflight before teacher generation.

## 2026-08-08 00:41:39 CST - Implement real RAG presets and relaunch v5 preflight

Tags: agrinet, rag-sft, milvus, semantic-search, hybrid-search

### Changes

- Extended `agrinet.rag.search/v1` with query text and strict HTTP fields for preset, ranker, and weights.
- Bound each HTTP preset route to its real retrieval type instead of a shared image-only default.
- Implemented SigLIP text-vector semantic search, weighted text-image balanced fusion, RRF fusion, visual retrieval, and exact canonical/alias name lookup in the production backend.
- Normalized Milvus repeated scalar fields for JSON transport and deduplicated exact-name results by canonical name.
- Reframed preflight correctly: visual retrieval decides target eligibility; fixed generic text calls are only service smokes, while real teacher queries are accepted or rejected on actual retrieved evidence.
- Archived the invalid first v5 preflight under `review/false-strategy-preflight-20260808T002621/`.

### Validation

All 47 tests pass, including five-preset HTTP forwarding and rejection of ignored fields. Real endpoint smoke on one query produced four distinct rankings: visual top-1 `Peach Leaf healthy`, semantic top-1 `Cherry healthy`, balanced top-1 `Peach Leaf healthy`, and RRF top-1 `Cherry healthy`; exact name lookup returned one deduplicated `Peach Leaf healthy` row.

### State

The managed RAG service is running as `20260808T004049-6d04b380-a01`. Volatile: corrected v5 preflight is running under tmux socket `agrinet-rag-pilot-v5-preflight-r2`, session `agrinet-rag-recovery-pilot-v5-preflight-r2`, with four workers. No teacher generation or training is authorized.

### Next

Do not monitor automatically. On request, inspect corrected preflight completion, visual target eligibility, service-smoke errors, quota coverage, and strategy plan distribution before teacher sampling.

## 2026-08-08 00:57:39 CST - Approve v5 for bounded teacher Pilot

Tags: agrinet, rag-sft, pilot-v5, preflight-review

### Results

Corrected preflight r2 completed with exit 0. Visual target eligibility is 184/240; selection contains 48 targets and 144 attempts with zero quota gaps. All 12 cells contain four unique targets, Option A/B/C/D each has 12 attempts, all assigned top-k values are 5, and no service-smoke or contract errors occurred.

Selected evidence quality is strong: 47/48 targets are visual rank 1 and one is rank 2; scores range from 0.84 to 1.00. All 48 query paths and image hashes are unique. Every target has three distinct strategy sequences.

Generic-query service smokes now differentiate as expected: visual target matches 184/240, balanced 14/240, semantic 2/240, and RRF 0/240, all with zero runtime errors. These match counts are not target-strategy eligibility claims; real teacher-generated queries remain subject to evidence rejection.

### Conclusion

The corrected preflight is suitable for a bounded teacher Pilot. It is not yet approval for 144 attempts or SFT training.

### Next

Select one target from each of the 12 quota cells and retain its three strategy-diverse rejection samples, for 12 targets / 36 teacher attempts. Review strategy-specific acceptance, query specificity, legal name confirmation, language isolation, and evidence grounding before expanding.

## 2026-08-08 01:14:07 CST - Revise plan to dual Oracle and Blind routes

Tags: agrinet, rag-sft, planning, oracle-grounded, blind-evidence

### Changes

- Replaced the single Blind-only P1 route with independent `oracle_grounded` and `blind_evidence` routes.
- Defined Oracle label permissions and label-influence audit fields while retaining strict visible-evidence and anti-leakage gates.
- Defined Blind generation with private labels available only to post-generation acceptance.
- Added route-isolated accepted, rejected, review, mixture, and evaluation artifacts.
- Set the initial RAG mixture target to 50-70% Blind and 30-50% Oracle, with per-cell provenance reporting.
- Replaced the next 36-attempt single-route Pilot with a 12-target x 2-route = 24-attempt comparison smoke.
- Expanded controlled training ablations to Direct-only, Oracle RAG, Blind RAG, combined RAG, and combined typed-correction variants.
- Corrected preflight documentation to distinguish visual target eligibility from generic-query service smokes.

### Acceptance

The 24-attempt smoke requires zero selected protocol, semantic, leakage, language, failed-tool, illegal-name, strategy-order, and duplicate-image errors; accepted-row accuracy and tool success must be 1.0; every quota cell must yield at least one accepted route; and both routes must produce enough valid rows for qualitative comparison.

### Next

Implement generation-route isolation, Oracle label-influence audit metadata, Blind prompt sanitization, and typed correction fields before constructing `plan/dual-route-12x2.jsonl`. No teacher calls or training are authorized yet.

## 2026-08-08 01:23:29 CST - Implement dual routes and build 24-attempt plan

Tags: agrinet, rag-sft, oracle-grounded, blind-evidence, bounded-plan

### Changes

- Added explicit `generation_route` handling with Oracle hidden target context and Blind target-free teacher context.
- Removed private target context from Blind initial, tool-response, and retry prompts; Blind evidence retry follows its own predicted class rather than the hidden target.
- Disabled final-answer label rewriting for Blind while retaining private post-generation acceptance.
- Added label-influence audit metadata, typed correction audit metadata, safe plan sample IDs, and route-isolated accepted/rejected/report outputs.
- Added validator checks for route/label visibility consistency, label-influence audit, and typed correction completeness.
- Added `build_dual_route_plan.py` and generated `plan/dual-route-12x2.jsonl`.

### Validation

All 49 tests pass; Python compilation and `git diff --check` pass. The plan has 24 attempts over 12 unique rank-1 targets, 12 Oracle and 12 Blind rows, two different strategies per cell, Option A/B/C/D each represented twice, minimum visual score 0.86, and safe IDs without target/class codes. Strategy counts are 6 RRF-name, 6 balanced-stop, 5 visual-balanced, 4 balanced-name, and 3 semantic-visual.

### State

The dual-route plan is ready for static/human review. No teacher API calls or training have started.

### Next

Review `plan/dual-route-12x2.jsonl` and the Blind prompt boundary. If approved, add a dedicated tmux runner and launch exactly 24 attempts.

## 2026-08-08 01:27:01 CST - Validate dual-route boundary and hit GPG lock

Tags: agrinet, rag-sft, dual-route, gpg, blocked-launch

### Validation

The actual Oracle teacher payload contains its hidden target context. The Blind payload contains zero matches for target code, English/Chinese names, aliases, target id, and source id. The dedicated 24-attempt runner passes shell syntax; all 49 tests, RAG health, and `git diff --check` pass.

### State

The tmux launch exited before `Processing 24 samples` because the background process could not decrypt the GPG-backed Yunwu API key. The candidate directory contains no generated files, so no teacher requests were delivered and retry is safe after GPG unlock. Training remains unauthorized.

### Next

Unlock the existing GPG key in the user session, verify the credential helper without printing values, then relaunch the same tmux socket/session plan.

## 2026-08-08 01:30:08 CST - Launch 24-attempt dual-route teacher smoke

Tags: agrinet, rag-sft, oracle-grounded, blind-evidence, tmux

### Validation

The GPG-backed helper returned both required Yunwu keys as non-empty without exposing values. The candidate directory was still empty, so retry could not duplicate delivered work.

### State

Volatile: tmux socket `agrinet-rag-pilot-v5-dual`, session `agrinet-rag-recovery-pilot-v5-dual`, is running the 24-attempt dual-route smoke with four workers. Startup was confirmed by `Processing 24 samples with max_concurrent=4`. The run will write route-isolated accepted/rejected files and automatically execute protocol and semantic validation. Training remains unauthorized.

### Next

Do not monitor automatically. On the next requested status check, inspect exit status, Oracle/Blind acceptance and rejection reasons, strategy-specific behavior, label-influence audits, typed correction audits, protocol validation, and semantic validation.

## 2026-08-08 02:15:07 CST - Review dual-route Pilot and retain expansion gate

Tags: agrinet, rag-sft, pilot-review, rejection-sampling

### Changes

- Monitored the 24-attempt Oracle/Blind Pilot through completion and reviewed its protocol output.
- Corrected option-answer validation to accept the current canonical-class-name protocol while retaining legacy A/B/C/D compatibility.
- Added online strategy-sequence rejection, exact retrieved-name substitution for required name confirmation, and online pre-RAG-candidate rejection.
- Updated semantic duplicate detection so an intentional Oracle/Blind pair may share one query image while duplicates within a route remain invalid.
- Ran bounded recovery batches for five affected samples and the only quota cell with neither route accepted; no full-attempt expansion or SFT training was started.

### Validation

- Original artifact: 15 accepted / 9 rejected; Oracle 8/12, Blind 7/12; retrieval 40/40; corrected final-label accuracy 1.0.
- Three original accepted trajectories still violate `rrf_then_name`; they remain excluded by the repaired online gate.
- Five-sample recovery: 2 accepted / 3 rejected, protocol errors 0, retrieval success 1.0, final-label accuracy 1.0.
- Two-sample quota recovery: 0 accepted / 2 rejected. Both lacked explicit pre-RAG candidates; Blind also lacked target evidence.
- Focused tests: 10 passed; `git diff --check` passed.

### State

Expansion gate failed. The union currently has 17 accepted sample IDs across 11/12 targets (Oracle 9, Blind 8); one target still has no accepted route. Existing artifacts remain under `outputs/artifacts/datasets/agrinet-rag-recovery-pilot-v5/candidates/`.

### Next

Improve first-turn candidate construction and evidence-seeking for the failing Chinese disease cell, then rerun only `dual-05-oracle_grounded` and `dual-06-blind_evidence`. Expand remaining attempts only after all 12 target cells are covered and the merged protocol/semantic audits pass.

## 2026-08-08 10:07:20 CST - Pass Pilot gates and launch remaining dual routes

Tags: agrinet, rag-sft, pilot-review, expansion, tmux

### Changes

- Strengthened first-turn candidate construction for every retrieval type, safe evidence-derived host/organ follow-ups, Chinese visible-trait diversity, and online semantic rejection.
- Repaired only failed or semantically duplicated Pilot rows and assembled `candidates/dual-route-merged-reviewed`.
- Built `plan/dual-route-remaining-72.jsonl`, excluding all 12 Pilot targets, and launched it in tmux.

### Validation

- Final Pilot: 17 accepted rows covering 12/12 targets; Oracle 9, Blind 8; 41/41 retrieval calls successful; final-label accuracy 1.0; protocol errors 0; semantic errors 0.
- Label-influence and typed-correction audits pass for all applicable rows. Five strategies and both languages are represented.
- Remaining plan: 72 attempts over 36 new targets, Oracle/Blind 36/36, every quota cell has 6 attempts, A/B/C/D each has 6 attempts, strategy counts range 14-15.
- Focused tests: 10 passed; shell syntax and `git diff --check` pass.

### State

Volatile: tmux socket `agrinet-rag-v5-remaining`, session `agrinet-rag-recovery-v5-remaining`, is processing 72 attempts. Log: `outputs/artifacts/datasets/agrinet-rag-recovery-pilot-v5/review/dual-route-remaining-72.log`. No SFT training has started.

### Next

On completion, inspect the exit marker and route/strategy/cell rejection distribution, then merge only rows passing protocol and semantic gates.

## 2026-08-08 11:21:08 CST - Complete reviewed RAG SFT candidate generation

Tags: agrinet, rag-sft, oracle-grounded, blind-evidence, rejection-sampling

### Changes

- Monitored all 72 remaining Oracle/Blind attempts through completion without duplicate submission.
- Added online rejection for tool calls that mix final-answer text and for retrieval after name confirmation.
- Corrected semantic validation to recognize natural English multi-trait connectors while retaining exact repeated-reasoning rejection.
- Isolated three protocol-invalid accepted rows and repaired one repeated Chinese trajectory.
- Ran bounded target-coverage supplements and merged only rows passing protocol, semantic, leakage, language, evidence, and correction gates.

### Validation

- Main expansion: 38/72 initially accepted; 128/128 retrieval calls succeeded; final-label accuracy 1.0. Three rows were later excluded for protocol errors.
- Reviewed expansion plus supplements: 44 rows covering 35/36 new targets, protocol errors 0, semantic errors 0.
- Final Pilot + expansion corpus: 61 rows covering 47/48 targets; Oracle 40, Blind 21; Chinese 34, English 27.
- Strategy counts: visual-balanced 22, semantic-visual 15, balanced-stop 10, balanced-name 9, RRF-name 5.
- All visible retrievals succeeded; final-label accuracy 1.0; protocol, semantic, leakage, language, label-influence, and typed-correction audits pass. Twenty-two correction trajectories have complete typed audits.
- Focused tests: 10 passed; `git diff --check` passed.

### State

Final candidate artifact: `outputs/artifacts/datasets/agrinet-rag-recovery-pilot-v5/candidates/dual-route-sft-candidates-final/`. One target (`rag_open-en-disease_N04069_N04069_P00004`) remains uncovered after all three preflight strategies failed strict acceptance; it is intentionally omitted. No SFT training has started.

### Next

Review the 61-row final candidate corpus and decide whether to convert/freeze it as the RAG SFT training artifact. Training still requires separate explicit authorization.

## 2026-08-08 12:02:24 CST - Freeze dual-route RAG SFT v2 and validate training preview

Tags: agrinet, rag-sft, frozen-artifact, ms-swift, dry-run

### Changes

- Added an immutable dual-route freezing tool that preserves messages and Oracle/Blind, language, strategy, target, and correction provenance.
- Froze the reviewed 61-row corpus as `outputs/artifacts/datasets/agrinet-rag-dual-route-sft-v2/`.
- Added a Qwen3-VL full-parameter configuration and registered VLM experiment initialized from the retained non-RAG checkpoint-165.
- Did not use the legacy student converter because it rewrites user language and drops the new route/correction metadata.

### Validation

- Source and frozen `data.jsonl` SHA-256 both equal `38cd27beae8caefc01c9a72cf0931dfafe515a2dffe962f98a6f1ae4aa9e62b8`.
- Artifact validation: 61 unique sample IDs, 47 targets, 0 missing images, 0 duplicate sample IDs, 61 tool rows.
- ms-swift native message validation passed for all 61 rows; image placeholders and tools JSON passed.
- `agrinet vlm show` and `agrinet vlm submit ... --dry-run` pass and preview the new config only.
- Python compilation, 10 focused tests, and `git diff --check` pass.

### State

The dataset and experiment are ready for a separately authorized training launch. No training process was started.

### Next

Obtain explicit approval before launching `vlm-sft-qwen3vl4b-nonrag-init-dual-route-rag-v2`; after training, evaluate with Milvus RAG on known/unknown disease and pest splits using the established protocol.

## 2026-08-08 15:41:24 CST - Launch dual-route RAG SFT from checkpoint-165

Tags: agrinet, rag-sft, qwen3-vl, training, single-image

### Changes

- Launched the authorized registered SFT experiment from the retained non-RAG checkpoint-165.
- The first launch failed at argument initialization because local single-process execution was incompatible with the retained 8-GPU ZeRO-3 setting.
- The second launch reached DataLoader but failed before step 1 because 7-23 image paths were paired with one `<image>` placeholder.
- Preserved both failed runs, removed DeepSpeed for explicit single-A800 execution, and froze a new immutable single-image artifact without rewriting tool evidence or route metadata.
- Relaunched on physical GPU 1 through the managed detached workflow.

### Validation

- Single-image artifact: 61 rows, exactly one image and one placeholder per row, SHA-256 `99e9b073cafb00ce3283c8bd52a06a9acf30d000c84258a1d0a85d0b929832e2`.
- Active run: `20260808T153936-6d04b380-a01`, PID `4145027`.
- Training reached step 5/64 with finite losses 2.2391 at step 1 and 2.3488 at step 5. GPU 1 used about 43.7 GB at 100% utilization.
- Model reports 4.438B trainable parameters (100%).

### State

Volatile: the SFT run is actively training under `outputs/runs/vlm/vlm-sft-qwen3vl4b-nonrag-init-dual-route-rag-v2/20260808T153936-6d04b380-a01/`. Expected output root: `outputs/vlm_sft/qwen3_vl_4b_nonrag_init_dual_route_rag_sft_v2/`.

### Next

After completion, verify final status/checkpoint and evaluate with callable Milvus RAG on known/unknown disease and pest splits before comparing with the retained non-RAG SFT.

## 2026-08-08 20:50:25 CST - Complete dual-route SFT and launch Milvus RAG evaluation

Tags: agrinet, rag-sft, qwen3-vl, milvus, evaluation

### Changes

- Verified managed training run `20260808T153936-6d04b380-a01` completed with exit code 0 at 64/64 steps.
- Validated checkpoint-64 config and processor locally without loading full model weights.
- Added a non-Slurm local evaluation runner with per-run Milvus Lite DB, CPU RAG service, two-GPU SGLang service, bounded logs, and cleanup.
- Added evaluation offsets for resumable bounded sampling.
- Aligned evaluation with the training trajectory by forcing a first visual retrieval only when the model skips the mandatory first tool call; forced and model-emitted calls are separately auditable.
- Launched the established 618-row known/unknown disease/pest evaluation in four tmux shards.

### Validation

- Final checkpoint: `outputs/vlm_sft/qwen3_vl_4b_nonrag_init_dual_route_rag_sft_v2/v1-20260808-153951/checkpoint-64`; Qwen3-VL config, processor, and two weight shards are present.
- Training: runtime 546.2941 seconds, train loss 1.10155092.
- Initial 8-row probe showed 0/8 spontaneous tool calls, identifying a train/eval state-machine mismatch.
- Corrected one-row RAG smoke called `/search/visual`, returned three Milvus candidates, and changed the sample from an incorrect generic `leafhopper` answer to correct `Nephotettix cincticeps`; score 1/1. The retrieval was explicitly marked `forced_tool_turns=1`.
- Full shard sizes are 155, 155, 154, and 154 on GPU pairs 0-1, 2-3, 4-5, and 6-7.

### State

Volatile: tmux sessions `rag_ckpt64_full_0` through `rag_ckpt64_full_3` are running. Output root: `outputs/vlm_eval/qwen3_vl_4b_rag_sft/dual_route_v2_checkpoint64_rag_full_20260808/`.

### Next

After all four shard exit markers are present, verify each exit code, finalize and normalize the merged 618 predictions, report forced versus spontaneous tool-call rates, and compare known/unknown disease/pest metrics with checkpoint-224 RAG and checkpoint-165 Direct.

## 2026-08-09 01:51:51 CST - Correct RAG evaluation protocol and finalize 618 results

Tags: agrinet, rag-sft, milvus, known-unknown, disease-pest

### Changes

- Completed all four original RAG shards at exit code 0, then identified that the evaluator had replaced every manifest question with a canonical-name request. This made Open and Option outputs identical and invalidated the first merged report for protocol comparison.
- Preserved that run as a diagnostic artifact; updated evaluation messages to retain original English/Chinese questions and require A-D letters for Option samples.
- Validated an Option smoke, reran all 309 Option samples in four local tmux shards, and merged them with the 309 valid Open results in original manifest order.
- Added reusable local merge and Option-rerun launch helpers.

### Validation

- Corrected report: 394/618 = 0.637540. Exactly 309 Open and 309 Option records, 618 unique manifest IDs, and all Option answers were parseable A-D letters.
- Milvus: all 618 rows called RAG at least once; 620/620 calls succeeded with zero request errors. First-call fallback was used for 589 rows; 30 rows made at least one spontaneous call (4.85%).
- Versus checkpoint-224 RAG: overall +20.06 pp. Versus checkpoint-165 Direct: overall -1.46 pp.
- Open accuracy improved over Direct for known disease (+18.39 pp), known pest (+15.29 pp), and unknown disease (+12.70 pp); Direct retained a large Option advantage.
- Formal evidence: `outputs/vlm_eval/qwen3_vl_4b_rag_sft/dual_route_v2_checkpoint64_rag_protocol_corrected_20260809/final_report.json`.

### State

Evaluation is complete. The earlier `dual_route_v2_checkpoint64_rag_full_20260808` report is diagnostic canonical-name output and must not be used as the formal Open/Option result.

### Next

Prioritize autonomous tool-routing SFT examples and Option-format retention, then evaluate whether combining Direct choice behavior with RAG-grounded Open behavior can close the remaining 1.46-point overall gap.

## 2026-08-09 02:15:00 CST - Set up bounded RAG SFT iteration control

Tags: rag-sft, iteration-control, codex-execution, pilot, review-gate

### Changes

- Clarified that “Loop” describes the Codex execution loop; the project-side artifact is named `RAG SFT iteration control`.
- Added transport-error recovery rules to `AGENTS.md`, including segmented writes, destination verification, resumable state, and bounded validation after each segment.
- Added the iteration-control experiment configuration and a baseline initializer.
- Materialized Round 0 from the verified 618-row protocol-corrected evaluation and created a 16-attempt Round 1 plan focused on autonomous first tool calls and Option retention.

### Validation

- Baseline initializer completed with 618 records and verified report/prediction hashes.
- State is `awaiting_round_1_pilot`; `training_authorized=false` and `full_eval_authorized=false`.
- No new teacher sampling, SFT, or evaluation process was started.
- Configuration YAML parses and the initializer runs with `.venv/bin/python`.

### Evidence

- Config: `configs/experiments/data/data-rag-sft-iteration-control-v1.yaml`.
- State: `outputs/experiments/rag_sft_iteration/state.json`.
- Baseline review: `outputs/experiments/rag_sft_iteration/baseline/review.md`.
- Round 1 plan: `outputs/experiments/rag_sft_iteration/rounds/round_0001/plan/round_plan.json`.

### Next

Materialize the 16-attempt Pilot only after the control-plane review is accepted; hard-gate failures stop promotion and cannot be compensated by accuracy gains.

## 2026-08-09 02:24:00 CST - Block Round 1 Pilot on unavailable GPG credential

### Note

The Codex execution loop materialized and preflighted a 14-target unique Round 1 Pilot (the visual eligibility gate reduced the planned 16 to 14; two quota gaps remain explicit). Milvus health was valid, but the teacher controller exited before generation because `/data/home/jiangwentao/.apikeys/bin/apikey env yunwu` could not decrypt the existing GPG secret (`No secret key`).

### Status

No accepted/rejected trajectories were produced and no SFT started. The same plan is resumable after GPG unlock; no duplicate run should be launched. Review package: `outputs/experiments/rag_sft_iteration/rounds/round_0001/review/REVIEW.md`.

### Next

Unlock the existing GPG private key, verify the credential helper succeeds without printing secrets, then resume the exact 14-target Pilot.
## 2026-08-09 11:50:09 CST - Manual JSON agent template added

- Trigger: transport-error recovery and routing root-cause follow-up.
- Added vlm/sft/ms-swift/swift/agent_template/manual_json.py and registered manual_json in the vendored ms-swift agent map.
- Added unit coverage in tests/test_manual_json_agent_template.py; both tests pass.
- Enabled agent_template: manual_json in the strict39 SFT config so the next bounded run serializes role=tool_call as bare JSON.
- Evidence: .venv/bin/python -m pytest -q tests/test_manual_json_agent_template.py; agrinet vlm submit dry-run resolves successfully.
- Next action: run a bounded manual-JSON SFT from the strict16 checkpoint, then disable forced retrieval for a 16-row routing smoke; do not authorize 96/618 until the autonomous-call gate passes.
