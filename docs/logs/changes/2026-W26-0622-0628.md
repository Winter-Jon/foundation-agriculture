# Changes - 2026-W26 (0622-0628)

## 2026-06-23 10:52:11 CST - Correct Milvus split-collection startup context

### Context
The startup research context still described the AgriNet Milvus database as a legacy single collection with one image vector per class. A direct database check showed the current Lite DB uses split class/image collections.

### Changes
- Updated `docs/logs/START_HERE.md` to describe `agrinet_wiki_siglip2_classes` and `agrinet_wiki_siglip2_images` instead of the legacy single `agrinet_wiki_siglip2` collection.
- Corrected the image-vector description: image search now uses one row per local reference image in the image collection.
- Added the 2026-06-23 database check to current state and moved targeted retrieval diagnostics ahead of another teacher rerun in next actions.

### Validation
- Connected to `outputs/milvus/agrinet_wiki_lite.db` with `pymilvus.MilvusClient`.
- Confirmed collections: `agrinet_wiki_siglip2_classes` with `row_count=320` and `agrinet_wiki_siglip2_images` with `row_count=578`.

### Impact
Future analysis should not assume the old single-collection, first-reference-image retrieval structure. Remaining retrieval failures should be diagnosed against the split image collection and current dense/sparse routing.

### Next
Run targeted retrieval diagnostics for `Cherry Normal leaf` and neutral healthy-leaf queries before launching another RAG teacher pilot.
