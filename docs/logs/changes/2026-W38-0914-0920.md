# Changes — 2026-W38 (0914–0920)

## 2026-09-14 18:20:41 CST - Repair E3.22 as an authorization-gated paired v2 protocol

Tags: e322, classifier, presample, protocol-v2, no-sft

### Context

The frozen E3.22 v1 run failed its gate, but review showed its 12/32 result mixed
true simulated-Unknown difficulty with Open/Option prompt contamination, weak
local validation, and conservative budget/accounting defects. The v1 artifacts
remain immutable and terminal.

### Changes

- Split Classifier prompts by question type. Open now requires only a canonical
  public class name and deterministically rejects `— A/B/C/D`; Option retains
  the exact E3.15 `class name — LETTER` contract. Q1 is also type-specific.
- Settle token reservations for delivered generation parse/contract failures;
  only unresolved provider delivery retains exposure. Classify malformed but
  delivered private audits as `audit_contract_error`, with a distinct audit
  request ID, rather than as unknown delivery.
- Persist immutable Q1/R1/R2 continuation manifests and bind child work items to
  predecessor request, outcome, and manifest hashes. Separate attempt usage from
  final 32-sample dispositions in the v2 report.
- Add frozen-card public resolver receipts, remove persisted image data URLs from
  new public trajectories, and make independent audit derive resolver/network
  and lineage evidence rather than hard-code it.
- Bind the actual canonical registry whose SHA is `4fa642...`, replacing the
  erroneous fold-0 label-map-as-registry path.
- Register v2 prepare/run configs. The v2 source intentionally binds and reuses
  the exact v1 32-image cohort for paired diagnosis. Live execution requires an
  explicit CLI authorization flag and the registered config keeps it false.

### Validation

- Offline v2 prepare passed: 32 rows/classes, exact cells and folds, four-layer
  identity uniqueness, both scarce Option-pest rows, and all six checkpoint SHAs.
  Source SHA: `001fe3870a6feba67f52a475debeed3196f3a5a9e018c4a262beef5e29e185ba`;
  manifest SHA: `47b3ebc3e89ed019acf2444588f7a11eeaf58e004949c49d6be1350f9de5b2ed`.
- Prompt audit: all 27 Open rows contain the no-letter rule and no Option example;
  all five Option rows contain the exact format example.
- Corrected v1 `artifact-audit-v3.json` reports 36 historical Open contract
  errors across attempts, zero private binding leaks, zero tool-order errors,
  and zero classifier-network intents. It does not overwrite v1 reports.
- E3.22 + E3.15/E3.20/E3.21 focused tests passed (29); all currently collectable
  tests passed with the known historical orphan
  `tests/test_adjacent_class_folds.py` excluded. `py_compile`, CLI show/dry-run,
  and `git diff --check` passed.

### Impact

- Verified: v1 remains `presample_gate_passed=false`; it is evidence that the v1
  protocol failed, not a clean estimate of Classifier capability.
- Verified: v2 is prepared only. No provider request, RAG/Reject, remaining-493
  execution, conversion, SFT, or training occurred. All training flags are false.
- Compatibility / scope: v1 is read-only; repaired execution uses v2 and cannot
  silently reinterpret or append to the old campaign.

### Next

Interview the user before enabling the paired v2 live collection. If authorized,
review its 300k budget and then change only the explicit live-authorization field;
do not execute the remaining 493 rows or any downstream route.
