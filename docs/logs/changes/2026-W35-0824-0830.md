# Changes — 2026-W35 (0824–0830)

## 2026-08-24 00:28:21 CST - Reject v7 blind collection and add public-evidence-grounded final adjudication

Tags: hcv, micu, v7, private-audit, public-evidence, image-isolation

### Context
The 64-image Micu v7 five-turn Blind collection completed under
`outputs/runs/rag/rag-hcv-contrast-verify-micu-five-turn-v7-64-v1/20260823T233117-2ef993b1-a01/`. It could not be frozen because private auditing found final-answer mistakes despite adequate five-turn public retrieval evidence.

### Changes
- Recorded the verified collection outcome: 62 protocol-accepted trajectories, two retired trajectories, and nine private final-answer mismatches. The audit report is `outputs/experiments/hcv_multi_query_rag/teacher_collection/20260823Tv7-five-turn-8-per-cell/reports/private_audit_report.json`; `freeze_authorized=false`.
- Added an explicit `--private-final-adjudication-file` path to `src/agrinet/rag/distill/run_pilot.py` and registered it in `agrinet rag submit`. The sidecar must cover the public plan exactly and is held separately from plan/sample records.
- Added fail-closed guards requiring the private truth to already occur in actual public retrieval evidence and in the public HCV candidate ledger. Option truth must additionally appear in a public A–D choice.
- The private truth is never passed to Micu. For an eligible row, the student-visible final response is locally rendered solely from previously returned public candidate names/cards, with trace metadata stating that it was public-evidence-grounded private final adjudication. Invalid or non-public truth is rejected.
- Extended collection audit checks and focused unit coverage for the new boundary.

### Validation
`python -m py_compile` passed for the changed modules. Focused tests passed: `35 passed` for `tests/test_rag_distill_pilot_contract.py`, `tests/test_hcv_teacher_collection_audit.py`, and `tests/test_cli.py`; `git diff --check` passed.

### Impact
- Verified: v7 blind data remains rejected and is not trainable. The cause is final public-candidate adjudication, not retrieval recall, native DP8, or Micu routing.
- Verified: the new mode keeps private truth out of model-visible prompts and student-visible messages, while refusing rows without existing public evidence.
- Scope: this is a controlled collection mechanism only; it does not alter the strict evaluation, SFT freeze, or promotion gates.

### Next
Create a fresh image-isolated eight-cell Micu pilot, excluding all 64 already-contacted v7 images; then audit every row for five retrievals, public-evidence truth coverage, no private leak, and correct final rendering before scaling.

## 2026-08-24 00:31:00 CST - Micu private-final pilot preflight is ready; local credential preflight is blocked

Tags: hcv, micu, credentials, diagnostic-pilot, image-isolation

### Note
Public retrieval preflight created `outputs/experiments/hcv_multi_query_rag/preflight/20260824Tprivate-final-pilot-candidates-16/`: 16 newly isolated candidates, no retrieval errors, and all preflight quality gates passed. The public plan builder selected 14 `expanded_truth_hit` samples under `outputs/experiments/hcv_multi_query_rag/teacher_plan/20260824Tprivate-final-pilot-2-per-cell/`; two cells have one shortfall, so this is explicitly diagnostic-only and not a freeze candidate. Registered config: `configs/experiments/rag/rag-hcv-contrast-verify-micu-private-final-pilot-v1.yaml`.

### Status
`agrinet rag show` and `--dry-run` passed; retrieval service `http://127.0.0.1:8078/health` is healthy. The real detached submit performed no collection because its preflight could not decrypt `micu_slb`; an availability-only check also found `micu_main` unavailable. No teacher process was created and no new image was sent to Micu.

The credential diagnostic confirms that both encrypted profile files and the matching local private key exist. The failure is specifically non-interactive GPG unlock (`Inappropriate ioctl for device`): this execution environment cannot supply the private-key passphrase. It is not an API credential absence and must be resolved in an interactive user terminal before resubmission.

### Next
Restore a valid local Micu credential profile, then submit the registered pilot without altering its plan or output directory.

## 2026-08-27 02:02:10 CST - Add M1 Direct collection controls and run real capacity preflight

Tags: direct, m1, hcv, image-isolation, preflight

Implemented the fail-closed M1-style Direct plan/audit/freeze pipeline in
src/agrinet/data/m1_direct_collection.py and registered
data-m1-direct-current-hcv-v1. The public teacher manifest uses opaque
sample/image references; class codes, truth, correct letters, and real image paths
remain in private alignment. Independent auditor payloads contain no truth fields.

The real local preflight scanned 217 classes (145 disease, 72 pest) and 195,227
unique training images. Evidence is
outputs/artifacts/datasets/m1-direct-current-hcv-v1/preflight_report.json.
Collection is not authorized: all five N05053 images overlap isolation sources,
and N05003, N05017, N05018, N05043, and N05058 have fewer than three same-domain
neighbours in the current joint SigLIP2 top-5 report. No teacher request was sent.

Validation: 18 focused tests passed for the new Direct controls, real catalog,
preflight, and CLI; the registered preflight dry-run passed. Next action is to
resolve N05053 with genuinely new isolated imagery and recompute a wider joint
text-image neighbour report for the five pest shortages before rerunning preflight.
