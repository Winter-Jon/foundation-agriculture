# Changes 2026-W33

## 2026-08-15 00:14:24 CST - Switch active distillation teacher to gpt-5.6-luna

### Context

The user requested that the active RAG distillation teacher model move from gpt-5.4-mini to the exact model ID gpt-5.6-luna.

### Changes

- Changed the run_pilot default through DEFAULT_TEACHER_MODEL.
- Changed active local Data recovery launchers and the resumable targeted Pilot launcher to gpt-5.6-luna while preserving explicit CLI and environment overrides.
- Kept historical experiment records, generated artifacts, old Round launch scripts, and retained pre-migration Slurm scripts unchanged because they describe actual historical runs.
- Added a regression assertion for the active teacher default.

### Validation

- src/agrinet/rag/distill/run_pilot.py and tests/test_sft_recovery.py compile.
- Full tests/test_sft_recovery.py passes 22/22.
- Four changed shell launchers pass bash syntax validation.
- Targeted git diff check passes, and the five active distillation entrypoints contain no gpt-5.4-mini default.
- One credentialed Yunwu API3 real-image preflight passed with model gpt-5.6-luna: reachable=true, visual_reachable=true, manual_tool_call=true. Bounded evidence is at outputs/runs/rag/model-migration-gpt-5.6-luna/preflight/report.json.

### Impact

- Verified: the Yunwu relay accepts the requested model ID and returns a strict single manual agrinet_rag_search call for the current visual preflight.
- Unknown: official OpenAI documentation pages returned HTTP 403 during lookup, so this record does not claim that gpt-5.6-luna is an official OpenAI model identifier; it is recorded as the user-specified Yunwu relay model ID.
- Compatibility / scope: applies to new local distillation/preflight/Pilot runs only. Existing artifacts and historical metrics retain their recorded teacher models.

### Next

Resume the paused Option protocol optimization goal with gpt-5.6-luna, beginning with unknown-delivery checks and a new fresh-image strict preflight before any Pilot.

## 2026-08-15 14:25:00 CST - Prevent automatic replay after ambiguous teacher POST delivery

### Context

Round116 showed that a teacher request can remain unresolved after the local timeout. Replaying such a POST can duplicate provider work or create an untraceable second delivery.

### Changes

- Added `UnknownTeacherDelivery` to the local Pilot runner. Empty/invalid POST bodies, `URLError`, socket failures, and direct timeouts are classified as ambiguous delivery.
- HTTP 408, 429, and 5xx responses from the teacher POST are also fail-closed as ambiguous delivery; deterministic 4xx request errors remain ordinary failures.
- `post_teacher_json` now raises immediately for that class and never consumes the configured transient retry budget.
- A sample records `unknown_delivery` with `delivery_status=unknown`; a batch manifest records `counts.unknown_delivery` and `delivery_status=unknown`, and exits with code 2 when such a row occurs.
- Existing strict validators and acceptance gates were not relaxed.

### Validation

- `tests/test_sft_recovery.py`: 34/34 passed, including no-retry tests for ambiguous delivery, socket timeout, and HTTP 503.
- `py_compile` and targeted `git diff --check` passed.
- No teacher request, Pilot, SFT, or evaluation was started during this change.

### Next

Round116 remains unresolved and must not be retried until provider/request status is independently confirmed. Future bounded Pilots will fail closed on ambiguous POST delivery and preserve the evidence needed for review.

## 2026-08-15 15:05:00 CST - Persist interrupted Pilot lifecycle state

- The local runner now atomically writes a run_status.json marker with running, preflight_passed, preflight_failed, completed, or terminated_unknown_delivery state.
- SIGTERM/SIGINT handlers write delivery_status=unknown, the signal, exit code, and a no-retry decision before exiting. Preflight and normal completion also write explicit terminal states.
- No protocol validator, evidence gate, or acceptance rule was relaxed. tests/test_sft_recovery.py passes 36/36; compile and diff checks pass.
- No teacher request, Pilot, SFT, or evaluation was started. Round116 remains unknown delivery and must be independently resolved before any repeat.

## 2026-08-15 16:05:00 CST - Add non-trainable per-sample Pilot checkpoints

- The Pilot runner now writes progress/accepted_candidates.jsonl, raw_trajectories.jsonl, retrieval_calls.jsonl, and rejected_trajectories.jsonl after each completed sample, plus an atomic status update with training_eligible=false.
- This preserves complete evidence for samples finished before an interruption while preventing partial checkpoints from being mistaken for a frozen training artifact.
- Validation: tests/test_sft_recovery.py passes 38/38; compile and diff checks pass. No teacher request was made during this change.
- Existing Round117 artifacts remain unchanged and non-promotable; future bounded shards will be auditable if interrupted.

## 2026-08-15 16:20:00 CST - Preserve progress metadata on Pilot interruption

- SIGTERM/SIGINT handling now carries forward the last atomic progress fields (`completed_samples`, observed accepted/rejected counts, progress path, model, and `training_eligible=false`) when writing `terminated_unknown_delivery`.
- This prevents interruption status from erasing the audit count while keeping partial evidence non-trainable.
- Validation: `tests/test_sft_recovery.py` passes 39/39; compile and diff checks pass. No teacher request was made.

## 2026-08-15 15:22:00 CST - Add teacher provenance to expansion approval plan

- The teacher-free 31-target expansion report now records `teacher_model=gpt-5.6-luna` explicitly. It remains `teacher_free_plan_ready_for_approval`; `sft_authorized=false` and `formal_eval_authorized=false`.
- Regenerated package: 31 selected targets, 64 requested maximum teacher attempts, no cell shortfall, with the existing Blind-first and no post-hoc Option rewrite policies.
- Validation: builder completed; `tests/test_sft_recovery.py` passes 36/36; compile and diff checks pass. No teacher request was made.
- Next: obtain explicit sampling approval, then perform local preflight and only one reviewed shard.

## 2026-08-15 18:10:00 CST - Archive completed RAG SFT iterations and define one active entrypoint

- Moved completed Round 003-114 builders, paired historical training configurations, bounded-Pilot launchers, and superseded narrative reports into explicit archive directories. Their source text and experiment identifiers are retained for reproducibility; no historical artifact or result was deleted.
- Extracted active catalog, Option-contract, and image-isolation helpers into `src/agrinet/rag/distill/catalog_and_isolation.py`. Active Stage-A/Stage-B builders no longer import a historical round-specific module.
- Reduced `docs/logs/START_HERE.md` to the current two-stage gate, verified state, evidence paths, authorization state, and the next safe action. `docs/plan/rag_sft_refactor.md` remains the canonical method specification.
- Sync scope: mainline docs and local workflow organization only. No teacher call, SFT, freeze, or evaluation was started.

## 2026-08-15 18:35:00 CST - Fix the active Stage-A sampling contract

- Made the current operating scope Stage A only: its immutable milestone is exactly 32 balanced `standard` RAG rows plus 32 revalidated Direct replay rows.
- Fixed collection policy to stratify by remaining Open/Option × English/Chinese × disease/pest cell deficit, accept exactly four strict RAG rows per cell, randomize only within a fresh eligible cell pool, and stop each cell as soon as its quota is complete.
- Defined the 15 reviewed standard targets as the first reserve, not an authorization or a guarantee of 15 accepts. Any replacement target requires the same fresh-image review and explicit sampling authorization.
- Removed deferred full-milestone configuration from the active control file to avoid competing current instructions. No sampling, SFT, freeze, or evaluation was started.

## 2026-08-15 19:30:00 CST - Harden third-retrieval finalization prompt

- Round127 showed that a Chinese Open teacher response can request a fourth tool call after three successful retrievals and the existing finalization instruction.
- Strengthened the public Blind-safe finalization prompt: tools are permanently closed; the model must not request, suggest, plan, or emit retrieval/tool JSON; residual uncertainty belongs in the required uncertainty field.
- Added a state-machine regression that simulates three retrievals, one forbidden fourth tool call, and a legal Chinese final response. It proves no fourth retrieval executes and the legal final response can be accepted.
- Validation: recovery tests pass; no additional teacher request was issued by this repair.

## 2026-08-15 20:05:00 CST - Separate closed final answer from exhausted tool state

- Round128 proved that a stronger finalization prompt inside the existing manual-tool conversation was ineffective. The runner now treats the first post-budget tool request as a state transition, never as another retrieval.
- It starts one new final-only session with the original public image, real returned public evidence, and public Option choices when needed. That session receives no tool schema or preceding assistant tool calls and is forbidden from emitting JSON or requesting retrieval. A second tool request remains `tool_call_after_budget_finalization`.
- This preserves the existing strict validators and Blind boundary: no private class label, correct option, or oracle context is added to the terminal session.
- Validation: `tests/test_sft_recovery.py` passes 35/35; `py_compile` and `git diff --check` pass. No teacher request, Pilot, SFT, freeze, or evaluation was started.
- Next: require a new-ID Luna/API3 real-image preflight and one bounded Chinese Open Pilot before considering any broader release.

## 2026-08-15 20:25:00 CST - Send closed final answer without JSON response mode

- Fixed the Round129 provider-format defect: `run_sample` now passes `final_only=True` only for the closed final-answer request, preventing `chat_completion` from attaching first-turn JSON response mode. Ordinary retrieval and option-contract calls retain their original invocation shape.
- Added a regression assertion that the simulated closed final turn receives the explicit `final_only=True` contract, in addition to existing checks for public-only evidence and no private option/code exposure.
- Moved the iteration-control YAML's research-control fields beneath the registered experiment schema's `parameters` field. `agrinet data show data-rag-sft-iteration-control-v1` now validates and displays the same nested controls.
- Validation: 46 relevant tests pass; `py_compile` and `git diff --check` pass. No teacher request was issued by this repair.

## 2026-08-16 00:36:56 CST - Repair Terra standard final-contract rendering without relaxing validation

- Round148 completed known delivery with valid retrievals but omitted the strict field colons. Standard trajectories now receive at most one public-only final-contract retry when required fields are absent or evidence anchoring cannot be parsed. The retry preserves the model's preceding public final in context, forbids tools/JSON/private labels, and requires exact English or Chinese field labels plus a quoted public retrieval class.
- A tool request during that retry is rejected explicitly, so the repair cannot consume extra retrieval budget or initiate a new evidence path. The strict artifact validator itself is unchanged.
- Validation: `tests/test_sft_recovery.py`, `tests/test_rag_service.py`, `tests/test_eval_language_contract.py`, and `tests/test_manual_json_agent_template.py` pass (48 tests); `git diff --check` passes. No teacher request was made by this code change.
- Next: rebuild the active isolation plans and, under the existing Option-calibration authorization, use one fresh target for a real-image Terra/Micu-SLB preflight followed by one bounded Pilot only if preflight succeeds.

## 2026-08-16 02:10:00 CST - Make future local DeepSpeed launches explicitly distributed and freeze-single-use

- The Stage-A validation SFT failure was diagnosed as a launch-contract issue: a single ms-swift process saw eight GPUs, selected a model-parallel device map, and then rejected `deepspeed: zero3`.
- `agrinet vlm submit` now accepts an experiment-local `parameters.local_launch` declaration only when it exposes exactly one unique GPU per `NPROC_PER_NODE` worker. The Stage-A configuration declares four visible GPUs and four workers, which makes ms-swift use torchrun rather than the incompatible single-process device-map path. Dry-run now displays these two launch variables.
- Immutable-freeze experiments now enforce `allowed_sft_runs_for_freeze_hash: 1` before both dry-run and execution. A previous `pending`, `running`, `failed`, `completed`, or unreadable launch record refuses replay. The failed Stage-A freeze is therefore mechanically blocked, not only documented as blocked.
- Validation: `tests/test_vlm_adapters.py` and `tests/test_sft_recovery.py` pass; the Stage-A dry-run correctly refuses its recorded failed launch. No SFT, sampling, or evaluation was launched by this change.

## 2026-08-16 08:35:00 CST - Enforce Qwen3-VL image-placeholder parity for future validation freezes

- The authorized v2 SFT reached the first batch with correct DDP but failed in Qwen3-VL RoPE position-index construction. Inspection found the exact serialization mismatch: each RAG row retained 8--22 image paths while its messages contain one query-image placeholder; Direct rows were already one-to-one.
- The validation-freeze builder now serializes RAG training rows to their query image only and records that serialization policy in metadata. Retrieved reference evidence remains in the audited tool-response text, so this change neither deletes evidence from the trajectory nor changes strict acceptance semantics.
- The builder now enforces image/placeholder parity for real message-bearing rows. A temporary rebuild of the current candidate view produces 64 rows with zero parity mismatches; focused freeze/launcher tests pass.
- Scope: preparation for a future explicitly authorized new freeze only. It does not alter or replay the failed v2 freeze.

## 2026-08-16 16:12:18 CST - Register reconstructive Direct + Blind RAG controls

- Added isolated, versioned controls for the new route: 840-row intermediate (560 Direct + 280 RAG) and 4,200-row full target (2,800 + 1,400), both balanced over Open/Option × English/Chinese × disease/pest and initialized only from models/Qwen3-VL-4B-Instruct.
- src/agrinet/data/rebuild_sft.py and src/agrinet/rag/distill/freeze_reconstructive_sft.py enforce Direct tool-free rendering, strict RAG tool/evidence and answer contracts, Blind label invisibility, per-cell class/Oracle caps, image SHA-256 isolation from Direct/RAG/diagnostic data, immutable hashes, and fail-closed unknown delivery attempt accounting. Intermediate and full SFT recipes are full parameter tuning at length 8192 for five epochs and 1e-5; their registry definitions intentionally cannot run until actual immutable hashes replace their placeholders.
- Validation: pytest -q tests/test_rebuild_sft.py tests/test_sft_recovery.py passed (45 tests); both new experiment definitions load through agrinet show; py_compile and git diff --check passed. No teacher request, freeze, SFT, or evaluation was started.
- Sync scope: mainline code, controls, and current handoff documentation.

## 2026-08-16 17:10:00 CST - Build isolated intermediate candidates and pass first Blind calibration

- The historical Direct audit built 560 contract-rerendered rows from 280 bilingual image pairs: every Open/Option × English/Chinese × disease/pest cell has 70 rows. It excluded 824 source rows by SHA-256 isolation and has no Direct validation errors.
- The fresh-image RAG planner uses the 320-class Milvus report as its public canonical catalog and selected 280 globally unique Blind targets after excluding Direct, diagnostic, and prior exposed images. Each of eight cells has 35 targets and 18 classes; the plan retains approval-only, label-invisible, bounded-attempt metadata.
- Added a self-contained-plan runner path, reconstructive Blind calibration scope, Micu-SLB credential profile mapping, registered one-sample preflight/Pilot definitions, and a compact calibration audit. Credentials remain in the encrypted helper and are only mapped in child-process memory.
- A Yunwu-default preflight failed before visual delivery (TLS EOF) and was recorded. The Micu-SLB/Terra retry then passed endpoint and real-image protocol preflight. The first open/en/disease 12-attempt calibration completed with 6 strict accepts, 6 strict rejects, zero unknown delivery, and 50% acceptance. Accepted trajectories are Blind-only; strict validation reports 1.0 retrieval success, 1.0 final-label accuracy, zero errors. This releases only that cell beyond calibration; no freeze, SFT, Oracle fallback, or formal evaluation was started.
- Validation: 48 targeted tests pass; py_compile, registered-experiment dry-runs, strict artifact validation, and git diff --check pass.
