# Changes - 2026-W37

## 2026-09-07 16:55:23 CST - Separate migratable mainline from volatile local state

Tags: repository-hygiene, reproducibility, experiment-configs, tests

### Context

The working tree mixed reusable source and experiment definitions with
per-attempt HCV retry snapshots, tmux/queue launchers, and historical tests
whose referenced builders no longer exist.

### Changes

- Versioned reusable source, stable experiment definitions, test coverage, and
  compact reports in layered domain commits.
- Moved 156 HCV retry/restart configuration snapshots to
  \`outputs/volatile/configs/rag-hcv-v13/\` and 15 local queue/launcher scripts
  to \`outputs/volatile/scripts/vlm/\`.
- Added ignore rules and \`configs/experiments/README.md\` defining the boundary.
- Moved three orphaned tests to \`outputs/volatile/tests/legacy-orphans/\`;
  they reference absent historical v3/v4 builder scripts and are not current
  contracts.

### Validation

- \`git diff --check\` passed before each commit.
- Focused data: 49 passed; HCV/RAG: 180 passed; Vision: 7 passed; VLM: 15 passed.
- The remaining active test collection has 599 tests. Two additional test groups
  (156 tests total) passed after the migration; all 599 tests collect cleanly.

### Impact

- Verified: Git contains only portable project state; volatile state remains
  preserved locally under ignored \`outputs/volatile/\` paths.
- Compatibility / scope: retry snapshots and queue scripts are no longer
  discoverable as registered mainline workflows. Existing raw run outputs were
  not changed.

### Next

Use versioned definitions from \`configs/experiments/\`; place any new
per-attempt operational snapshot beneath \`outputs/volatile/\` alongside its run.

Sync scope: mainline docs branches only

## 2026-09-07 17:18:45 CST - Establish portable-mainline retention rule

Tags: repository-hygiene, reproducibility, retention-policy

### Note

The project rule is now explicit: the main repository retains only assets that
can be committed, migrated, and reused across checkouts—source, stable reusable
configuration, tests, compact documentation, and curated small evidence.

Keep machine-specific, credential-dependent, per-attempt, mutable, or bulky
state out of Git: runtime outputs and logs, checkpoints, downloaded datasets,
models, caches, retry/restart snapshots, and one-off queue or launcher scripts.
Place transient operational state under ignored local roots; use
`outputs/volatile/` when it is not a normal managed run output.

### Status

Recorded as the current repository retention convention. Existing ignored local
state is preserved; this rule does not authorize deleting any artifact.

### Next

Apply this classification before adding new files to Git.

## 2026-09-08 16:54:39 CST - Implement classifier HCV offline preparation

Note: implemented the missing `agrinet.rag.classifier_distill` module referenced
by the existing uncommitted RAG CLI/config draft. Preserved unrelated dirty
changes on main (ahead 13). Wrote in bounded patches with syntax checks.
The module validates frozen contract fields, allowlists independent teacher
views, enforces explicit Top-5 expansion, checks source and classifier-training
identity isolation, rejects formal Unknown/dev/test/in-sample/intervention input,
and reports independent-image shortages and paired-view request ceilings.
Each preflight keeps a separate report; the CLI defaults this experiment to the
offline operation without credential acquisition.

Status: 46 tests passed via `.venv/bin/python -m pytest
tests/test_classifier_distill.py tests/test_cli.py tests/test_hcv_v13_plan.py
tests/test_hcv_v13_contract.py -q`, process exit 0. Registered show and dry-run
passed. Real foreground submit returned the expected not-ready exit 2: frozen
registry/107 Known/104 Unknown/1,831 evaluation SHA exclusions verified, but
configured source.jsonl and exclusions.json are absent. Report:
`outputs/artifacts/hcv-classifier-distill/pilot-v1/preflight/20260908T085127Z-315e2b9e/report.json`.
Run: `outputs/runs/rag/rag-hcv-classifier-distill-preflight-v1/20260908T165126-fe65aa26-a01/`.
Smoke budget: 32 images, 64 trajectories, at most 320 RAG calls, 448 generations
and 64 private audits (512 Micu requests). No teacher service or training used.

Scope: this implements offline preparation, not the entire live research
pipeline. Input documentation at `docs/rag/classifier_distill.md` explicitly
states remaining trust boundaries: checkpoint/prediction authenticity, complete
historical exclusion provenance, source/near-duplicate grouping, semantic prompt
leakage, and actual event/mask checks require further evidence. The live adapter,
tool ledger/recovery, private auditing, conversion and student evaluations are
not implemented for this route. Four routes and all ten patterns remain in the
contract; pattern counts are sampling targets, not observed teacher behavior.

Next: obtain real fresh/OOF candidate records and complete historical exclusions;
finish live collection and isolated protocol validation before any paid smoke.
Pilot remains training-ineligible and cannot bypass two stable rounds plus human
confirmation. No additional INDEX link needed: this weekly file was already indexed.

## 2026-09-08 17:04:55 CST - Add durable classifier HCV request ledger

Note: `src/agrinet/rag/classifier_ledger.py` adds provider-independent per-image/view
request accounting. Intent is fsynced before transport, raw JSON/usage/latency
is retained afterward, and a lock spans the call. Stable request keys return
confirmed responses without calling again. Public generation/RAG and private
audit use separate directories and budgets. Unknown delivery, truncated/error
responses, partial events, duplicate intents and payload digest mismatch block
automatic continuation. Exception strings are omitted to avoid credential leakage.

Status: 57 focused tests passed, exit 0, with the previous command plus
`tests/test_classifier_ledger.py`. Tests include simultaneous requests (one actual
call), restart after confirmed delivery, timeout, interrupted intent, truncation,
provider error retention, budget exhaustion and private-channel separation.
`git diff --check` passes. All transport invocations in these tests were local
fixtures; no Micu requests or collection batch was launched.

Next: wire a real adapter and orchestrator to this ledger, with global contacted
tracking, actual response-schema checks and isolated auditing. Existing v13
`_call_micu` returns normalized text only, so it cannot alone provide the raw
response/usage contract. Source predictions and historical exclusions remain
absent at the configured paths. This is a reusable foundation, not completed
live collection or training eligibility. Updated `docs/rag/classifier_distill.md`
and START_HERE; the weekly change file is already indexed.

## 2026-09-09 00:49:40 CST - Micu classifier HCV v2 execution gate added

- Goal: continue the active v2 goal with a machine-executable contract and a safe boundary before any real teacher or student calls.
- Added configs/sampling/micu-classifier-hcv-v2/contract-v1.yaml, the registered experiment rag-micu-classifier-hcv-v2-smoke-v1, and src/agrinet/rag/micu_classifier_hcv_v2.py. The gate freezes the Micu/student/classifier choices, eight four-image cells, one primary pattern per query, cumulative retrieval budget 0/1/3/5, and training_eligible=false.
- Added CLI routing and regression coverage. The scoped suite passed: 52 tests. git diff --check passed.
- Live readiness report: outputs/artifacts/micu-classifier-hcv-v2/smoke-v1/20260908T164755Z-138b0dc5/readiness.json. The model directory, ViT-L checkpoint, and classifier training manifest are present.
- Blockers: the v2 fresh source pool and historical exclusion ledger are absent (outputs/artifacts/hcv-classifier-distill/pilot-v1/source.jsonl and exclusions.json). Local RAG ports 8077/8078 were not healthy for the required API check. No Micu call, classifier batch, student diagnostic, training, or fabricated source row was started.
- Next action: resolve source/near-duplicate provenance and exclusions through the authorized data workflow, start/verify the local RAG API, rerun the readiness gate, then perform capability checks before any 32-image collection.

## 2026-09-09 01:05:00 CST - v3 RAG service verified; classifier-independent smoke capacity is zero

- Continued the active Micu/classifier/HCV v2 goal. Rechecked the current worktree and shared dataset instead of relying on the earlier missing-path report.
- The shared `open_agri_v3` release exists and passed the frozen registry/provenance checks. The reversible Milvus name migration created and verified `open_agri_v3_classes` (320 rows) and `open_agri_v3_images` (578 rows), preserving vectors, IDs, descriptions, aliases, reference images, and similarity order.
- Added the registered service `rag-serve-open-agri-v3-micu-v2`, extended the RAG CLI with explicit collection parameters, and started it on 127.0.0.1:8077. Its `/health` is `ok`; one visual and one semantic request each returned exactly 3 public hits. The service run is intentionally still live: `outputs/runs/rag/rag-serve-open-agri-v3-micu-v2/20260909T010155-fe65aa26-a01/`.
- The v3 image manifest has 142,726 Known train_candidate images, 547 Known dev, 545 Known test, and 197 Known milvus_reference. The ViT-L classifier training manifest covers all 142,726 train_candidate images. After excluding training, dev/test, and reference splits, classifier-independent Known fresh capacity is 0.
- The prior v13 retry37 source has 32 rows but 12 overlap the ViT-L training manifest and the other 20 are not represented in the frozen v3 image manifest; it is therefore rejected for v2 and was not copied. No source or exclusions file was fabricated, and no Micu or student request was started.
- Latest readiness evidence: `outputs/artifacts/micu-classifier-hcv-v2/smoke-v1/20260908T170451Z-826b397a/readiness.json`. It remains not ready due to missing source/exclusions and insufficient classifier-independent Known capacity. Scoped tests pass and `git diff --check` passes.
- Next action requires an external data-state change: provide a frozen fresh Known image pool outside the ViT-L training/evaluation/reference boundary (or authorize a new classifier/OOF protocol). Until then, the 32-image teacher collection cannot be truthfully executed.

## 2026-09-09 01:12:40 CST - v2 goal paused on verified fresh-Known data capacity blocker

- Rechecked the active v2 inputs after the raw 4B diagnostic. No `source.jsonl` or `exclusions.json` appeared, and the frozen v3 manifest remains at zero Known images outside the ViT-L train/dev/test/reference boundaries. The v3 candidate pool contains the same frozen inventory and cannot create new independent images.
- Stopped the v3 RAG service launched for the protocol smoke via `agrinet rag stop`. Its managed status is `complete` at `outputs/runs/rag/rag-serve-open-agri-v3-micu-v2/20260909T010155-fe65aa26-a01/status.json`; port 8077 no longer responds.
- The active goal cannot safely continue until a provenance-complete 32-image Known fresh pool is supplied or the classifier/OOF constraint is explicitly changed. No teacher collection, G1/G2 derivation, or training was started.

## 2026-09-11 15:18:04 CST - Add E3.5 dual-arm RAG distillation offline contract

Tags: e35, rag-distill, classifier-cascade

### Context
Implement the future-only E3.5 data-line contract without mutating E2/E3 locks
or launching collection/training.

### Changes
Added `configs/sampling/e35-dual-arm-rag-distill-contract-v1.yaml`, append-only
patch `0020-e35-dual-arm-rag-distill`, a registered audit preflight, and
`src/agrinet/rag/e35_classifier_cascade.py`. The module validates 107 x 5
coverage per arm, 3 Open + 2 Option assignment, three-key isolation, OOF versus
class-fold label-map boundaries, deterministic 32-row audit selection,
private-reject-only cascade escalation, R0/R1/R2 recovery, and evidence-backed
RAG refusal.

### Validation
`51` focused tests passed: E3.5, E2/E3 loop, classifier ledger, and classifier
distill suites. Patch validation returned `valid=true`; registered dry-run passed
and did not acquire credentials. The historical Hermes suite remains blocked by
an unrelated missing importable `archive` package in this environment.

### Impact
- Verified: no source, teacher request, SFT, or training was launched.
- Compatibility / scope: E2/E3 locks and historical artifacts remain unchanged.

### Next
Freeze actual OOF/class-fold candidate rows with bound checkpoint, manifest,
label-map, and registry hashes; run the registered 32-image audit preflight.

## 2026-09-11 15:59:00 CST - Freeze E3.5 real six-classifier source and balanced audit R0

Tags: e35, rag-distill, audit, reproducibility

### Context
Advance E3.5 from offline contract-only state to a provenance-bound source and
an exact, balanced audit manifest without sending provider requests.

### Verified artifacts
- Frozen scoring scopes: `outputs/artifacts/e35-dual-arm-rag-distill/scopes/known-oof.jsonl` and `simulated-unknown-e3.jsonl`, each 535 rows with zero cross-arm overlap on all three identities.
- Three frozen E3 class-fold checkpoints were run in score-only mode only; their outputs were bound with OOF cards into `card-pools/known.jsonl` and `card-pools/simulated-unknown.jsonl`. Simulated-Unknown truth is checked absent from its fold label map.
- `final-candidates.jsonl` has 1,070 source rows and `audit-preflight.json` is ready with no errors; all gates are false.
- The original audit sidecar was retained immutable but is superseded for collection by `audit-source-balanced-v2.jsonl`. Its adjacent report verifies 16 rows/arm and exactly four rows in every Open/Option x disease/pest cell.
- `manifests/e35-dual-arm-audit-v2-r0.json` binds that exact 32-row source with four workers, an atomic 8,000-intent ceiling, R0 lineage, and no automatic replay.

### Implementation and validation
Added `e35_cascade_collect.py`, an explicit-adapter cascade controller. Its unit
tests show private-reject-only escalation, unresolved delivery stopping without
same-round replay, and route-tool contract rejection being kept out of delivery
recovery. `tests/test_e35_classifier_cascade.py` and
`tests/test_e3_vision_cli.py` passed (16 tests); `git diff --check` passed.

### Scope and next action
No teacher, private audit, rewrite, SFT, or model-training request was sent.
Next: bind the controller to a registered, credential-safe collection adapter,
run only this frozen 32-image R0 audit, close its private audits/rewrite audits,
then evaluate the protocol gate before creating the full-campaign manifest.

## 2026-09-11 16:18:00 CST - E3.5 balanced audit exhausts R0/R1/R2 delivery recovery

Tags: e35, audit, delivery-shortfall, recovery

### Execution
The registered `rag-e35-audit-collect-v2` R0 and its immutable R1/R2 recovery
manifests ran against the same 32 frozen images. The local typed RAG HTTP
surface returned HTTP 200 before collection. Each round's process completed
normally, but every provider intent closed as `unknown_delivery`
(`UnknownTeacherDelivery`); no image was replayed inside a round, replaced, or
sent to an R3 attempt.

### Evidence
`outputs/artifacts/e35-dual-arm-rag-distill/reports/e35-dual-arm-audit-v2-final.json`
records 16 `delivery_shortfall` rows for each arm, no protocol observation, no
RAG evidence closure, `protocol_gate_passed=false`, and
`full_campaign_expansion=not_authorized`. The R0/R1/R2 summaries and manifests
are retained under the same artifact root. A post-run ledger review found that
this exhausted lineage's recovery manifests contain the prior adapter's
synthetic unresolved identifier rather than its durable ledger UUID; it remains
a valid delivery-shortfall record but is not evidence of the required real-ID
lineage invariant. The controller was corrected and regression-tested so a
future separately authorized campaign carries the durable UUID. The live
experiment definitions also explicitly bind the validated Micu SLB base URL;
neither repair alters or permits replay of the exhausted lineage.

### Scope and next action
No SFT or training started, and all artifact gates remain false. The audit did
not produce a teacher trajectory or rewrite candidate, so public `data.jsonl`
export and full-campaign collection are correctly absent. Future progress needs
a verified external teacher-delivery repair and a separately authorized fresh
audit campaign; this exhausted lineage must never be replayed.

## 2026-09-12 10:20:00 CST - E3.5 v3 audit exposes missing private truth-name quality binding

Tags: e35, audit, private-audit, blocker

### Observation and repair
The new v3 audit used capability-visible `gpt-5.6-sol` and closed 25 parent
deliveries, but its retained trajectories were all Direct-only (zero tool/RAG
calls). Review found the isolated parent audit had received `truth_code` but
not the frozen canonical English truth name, so public English answer correctness
could not be reliably validated. Its final report remains `not_authorized` and
is not used as protocol-gate evidence.

`e35_private.py` now requires `private.truth_name`; `e35_live.py` resolves it
only in memory from the frozen 211-row registry and records its SHA only in the
private audit sidecar. Teacher projections, public ledgers, and public exports
do not receive it. E3.5 tests cover that boundary. v4 is a separately frozen
32-image balanced audit excluding every v2/v3 sample/image/source/near-duplicate
identity.

### External blocker
The v4 registered submit stopped before process creation because the user-level
credential helper failed `gpg --decrypt` for `micu_slb`. Verified v4 outcome and
campaign paths are absent: no provider intent, image dispatch, SFT, or training
occurred. Once the credential helper is restored, the immutable v4 R0 manifest
may be run as its first attempt; it is not a replay of v2/v3.

## 2026-09-12 10:35:00 CST - E3.5 private rewrite controller is ready but v3 parents are not exportable

Tags: e35, rewrite, hermes, quality-gate

`e35_rewrite.py` now provides a durable, private rewrite plus independent
rewrite-audit transaction for a delivered parent winner. Each request uses the
E3.5 atomic ledger and the same global intent ceiling; only a rewrite-audit
accept can be passed to the public candidate exporter. Its mock closed-loop test
passes.

The 25 v3 Direct parent deliveries are deliberately not sent through this
controller: their historical parent audits lacked private canonical truth-name
verification, so they do not satisfy E3.5 quality evidence. They remain
operational observations only. The v4 lineage is the first eligible target for
rewrite after its truth-name-bound parent audits close.

## 2026-09-12 10:45:00 CST - Full E3.5 expansion is code-gated on real RAG audit evidence

Tags: e35, audit-gate, full-campaign, no-sft

Added `plan-full-after-audit`, which refuses to create the 1,070-image manifest
unless the immutable audit report has both `protocol_gate_passed=true` and
`rag_evidence_closed=true`. A real v3 report was used to verify rejection; it
created no output. This keeps any full collection, rewrite campaign, Hermes
export, SFT, and training unavailable until a truth-audited v4 (or later
separately frozen) audit contains an actual public RAG call/response closure.

## 2026-09-12 10:55:00 CST - E3.5 final reports retain every class target and shortfall

Tags: e35, reporting, handoff

The public campaign report now accepts a full frozen source projection and emits
all 107 classes per arm, including `target=5`, `closed`, and `shortfall` for
classes with no winner. It still validates small audit/export subsets without
claiming absent classes are covered. The exporter passes its source projection
through this report, while all training flags remain false. Focused tests cover
both full zero-closure coverage and a single accepted export fixture.

## 2026-09-12 11:21:14 CST - E3.5 v5 private RAG-witness audit closes without evidence closure

Tags: e35, audit, rag-witness, recovery, delivery-shortfall, no-sft

### Context
v4's truth-name-bound audit completed but had no RAG terminal. To test the
required RAG evidence path without changing the shared teacher prompt or giving
the teacher private facts, v5 used a separately frozen 32-image audit source.

### Changes
`select-audit` can designate one private-only `rag_witness` per arm. The private
auditor must reject Direct and Classifier terminals for that audit coverage
control; RAG still uses normal truth/evidence review and requires an actual
retained `agrinet_rag_search` call/response. The designation is excluded from
teacher input, classifier cards, public ledger payloads, trajectories, lineage,
and Hermes exports. Controller-side validation now runs after durable private
audit delivery, so a missing witness RAG call/response is a route-contract
failure rather than an erroneous delivery retry. The final audit gate also
reads the public winner trajectory before treating a RAG route as evidence.

### Execution and evidence
`audit-source-balanced-v5.jsonl` has 32 rows, 16 per arm, four per public cell,
one private witness per arm, and zero overlap with v2/v3/v4 on sample ID, image
SHA, source group, and near-duplicate group. v5 R0 processed all 32 rows: 7
Direct winners and 25 unknown deliveries. Its R1 and R2 immutable manifests
each processed only those 25 delivery failures; both rounds remained unknown
delivery. `reports/e35-dual-arm-audit-v5-final.json` records Known 3 closed /
13 delivery shortfalls and simulated Unknown 4 closed / 12 delivery shortfalls,
with `rag_evidence_closed=false`, `protocol_gate_passed=false`, and
`full_campaign_expansion=not_authorized`. The full-manifest function was
explicitly checked against that report and rejected it without creating output.

### Validation and scope
The E3.5 plus E2/E3 regression selection passed 90 tests, and `git diff --check`
passed. No SFT, model training, full 1,070-image collection, rewrite, or export
was launched; all v5 artifacts keep `training_eligible=false`,
`training_authorized=false`, and `sft_may_start=false`. v2/v3/v4/v5 are
exhausted immutable lineages and must not be replayed.

### Next
Do not create a full manifest. Any further E3.5 collection requires a new,
separately authorized and identity-isolated audit lineage after diagnosing the
provider delivery shortfall.

## 2026-09-12 11:28:00 CST - E3.5 recovery sidecars bind immutable attempts

Tags: e35, recovery, ledger, provenance, no-replay

### Context
Read-only review of the exhausted v5 ledgers found a mixed shortfall cause:
some R1/R2 private-audit callbacks reached an existing `sample_id/route` receipt
path and raised `FileExistsError`. The old callback combined provider dispatch
and receipt writing, so this local collision was conservatively recorded as
`unknown_delivery`. Historical artifacts must remain immutable.

### Changes
Future controller attempts now pass immutable `work_id`, attempt ordinal, and
route context to the private auditor. Public trajectories and private audit
receipts are stored below the work ID, not merely the source sample ID. The
private provider response is committed to the E3.5 atomic ledger before the
receipt is written; a receipt write failure is therefore classified as a local
route/controller failure rather than a provider-delivery retry. Identical receipt
recovery is idempotent, while mismatched existing receipts fail closed.

### Validation and scope
Added a two-attempt fixture proving that the same source sample produces distinct
R0/R1 public parent paths and distinct private audit contexts. The E3.5 plus
E2/E3 regression selection passed 91 tests; compile checks and `git diff --check`
passed. This is future-only: v5 remains exhausted with its original immutable
outcomes and full campaign remains unauthorized.

## 2026-09-12 12:40:55 CST - E3.5 v6 delivery reauthorization closes R0/R1/R2

Tags: e35, delivery-reauthorization, audit, recovery, no-sft

The user explicitly authorized a fresh attempt for every v5 final
`unknown_delivery` after provider quota recovery. `reauthorize-delivery` froze
a new v6 source and R0 manifest for exactly those 25 gaps, with new request IDs
and ledgers; the seven v5 Direct winners were not resent. v6 closed 8 Direct
winners in R0 and 3 in R1. R2 processed only the remaining 14 unknown
deliveries and closed the lineage without R3.

`reports/e35-dual-arm-delivery-reauthorization-v6-final.json` combines v5's
seven initial closed rows and v6's reauthorized latest outcomes: Known 9 closed
/ 7 delivery shortfalls and simulated Unknown 9 closed / 7 delivery shortfalls.
All 18 closes are Direct; there is no retained RAG call/response winner, so
`rag_evidence_closed=false`, `protocol_gate_passed=false`, and full expansion
is `not_authorized`. The full-manifest guard was run against this report and
refused the 1,070-image manifest. Relevant E3.5/E2/E3 regressions passed 93
tests and `git diff --check` passed. No full collection, rewrite, export, SFT,
or training was started; all v6 gates remain false. v6 is exhausted and must
not be replayed.

## 2026-09-12 13:23:23 CST - E3.5 v7 quota recovery preserves native tool continuations

Tags: e35, audit, delivery-reauthorization, native-tools, recovery, no-sft

### Context

The user explicitly requested that quota-exhausted `unknown_delivery` items be
re-attempted. Read-only v6 ledger inspection had isolated a distinct
future-controller defect: after a native classifier tool call, E3.5 rewrote it
as ordinary assistant JSON and omitted the matching `tool_call_id` from the
tool response. This made the provider reject the next native continuation and
prevented RAG witnesses from reaching RAG. v6 remains immutable and exhausted.

### Changes

`e35_cascade_collect` now retains the original provider assistant
`tool_calls` message and appends a `role=tool` result with the exact native
`tool_call_id`. The public ledger projection preserves these safe protocol
fields while continuing to redact image bytes and local paths. Missing or
mismatched native identity is a delivered route-contract rejection, never a
provider retry. The final-report helper now composes prior R0/R1/R2
reauthorization groups, so a fresh v7 can carry v6 closes without resending
them. Regression tests cover continuation shape, tool-ID mismatch, ledger
projection, and layered no-replay report composition.

### Execution and evidence

v7 froze a new source and R0 manifest for exactly v6's 14 final R2 gaps, then
completed R0/R1/R2 with new work IDs, request IDs, ledgers, and campaign root.
It closed two classifier rows in R0 and one Direct row in R1. R2 handled only
the remaining 11 rows and created no R3: ten are terminal delivery shortfalls
and one delivered RAG terminal is a `route_contract_reject`. In that RAG-stage
trajectory the teacher called the classifier then finalized without invoking
`agrinet_rag_search`; the private witness correctly rejected this missing RAG
tool evidence. The repaired native continuation was nevertheless exercised at
the classifier continuation turn, without claiming RAG evidence closure.

`outputs/artifacts/e35-dual-arm-rag-distill/reports/e35-dual-arm-delivery-reauthorization-v7-final.json` is the layered v5+v6+v7
audit report: Known has 11 closed, four delivery shortfalls, and one route
contract rejection; simulated Unknown has 10 closed and six delivery
shortfalls. `rag_evidence_closed=false` and `protocol_gate_passed=false`; the
full 1,070-image campaign remains unauthorized.

### Validation and scope

The selected E3.5/E2/E3 regression suite passed 96 tests, and `git diff
--check` passed. No full collection, rewrite/export, SFT, or training was
started. All training flags remain false. v5, v6, and v7 are immutable
exhausted lineages; a later attempt needs another explicit authorization and a
fresh source/manifest, never a replay.

## 2026-09-12 13:31:00 CST - E3.5 records safe route-contract diagnostics

Tags: e35, auditability, route-contract, no-sft

Read-only examination of v7's delivered RAG rejection established that the
teacher omitted `agrinet_rag_search`; previous outcomes exposed only the Python
exception type. Future E3.5 controller outcomes now include a fixed, public-safe
`contract_error` code and preserve it in the round summary. The codes describe
controller protocol conditions only (for example
`missing_rag_call_response`); they never carry private-audit prose, truth,
fold, or witness metadata. Historical v7 outputs remain immutable and were not
rewritten. Focused E3.5 tests passed (32 tests) and `git diff --check` passed.

## 2026-09-12 14:20:00 CST - E3.5 v8 private-audit-only replay confirms provider instability

Tags: e35, private-audit, delivery-recovery, no-teacher-replay, no-sft

The user authorized retrying unstable private audits. v8 R2 ledgers identified
exactly four eligible records: the Direct parent generation was delivered, but
`direct:private-audit` was `UnknownTeacherDelivery` in the ordinary cascade.
The immutable plan
`manifests/e35-private-audit-replay-v8-r0.json` binds those four parent request
IDs and excludes all RAG-stage delivery gaps and all non-Direct parents.

The parent-only replay adapter wrote independent private-audit ledgers and made
no teacher generation request (`teacher_generation_replayed=false`). All four
new private-audit calls were delivered and returned `reject`; evidence is
`outcomes/e35-private-audit-replay-v8-r0.json`. This proves the shortfall was
private-auditor/provider delivery instability, not the local RAG endpoint. The
rejection decisions legally permit a future Classifier escalation, but that
must be a separate frozen collection scope and was not silently appended to
the completed replay ledger. Focused E3.5 tests passed and training/SFT flags
remain false.

## 2026-09-12 14:34:00 CST - E3.5 v9 collection uses three teacher prompts; Hermes export remains single-prompt

Tags: e35, hermes, rag, tool-schema, future-only, no-sft

The user directed a three-prompt collection policy after RAG-stage traces showed
the teacher could finalize after classifier evidence without invoking RAG. The
future controller now supports route-specific *teacher* prompts: Direct has no
tools; Classifier has the full `predict` and `expand` JSON Schemas; RAG has all
three schemas including `agrinet_rag_search`. On the first RAG request the
native API `tool_choice` is pinned to `agrinet_rag_search`; only after its real
tool response does it return to `auto`. A multiple-tool-call response is now a
stable route-contract error (`multiple_tool_calls`), not an unknown delivery.

This does not alter the final SFT contract: conversion discards the
route-specific collection prompt and rewrites every Hermes candidate's first
system message to the one frozen English `system_prompt` from the source row,
for Direct, Classifier, and RAG alike.

This is future-only and does not change v5--v8 artifacts. Focused tests verify
the three prompts, exact tool visibility/schema requirements, and RAG forced
first tool choice; 33 E3.5 tests passed. No SFT/training or full campaign was
started.

## 2026-09-12 19:26:35 CST - Freeze E3.5 v9 budget-controlled continuation without provider collection

Tags: e35, token-budget, continuation, private-audit, rag, no-sft

The service-provider token investigation showed that 356k/495k-scale
un-cached input aggregates came from repeated legacy E2 rewrite/prescreen
generation plus private-audit batches, not a single RAG request. E3.5 v9 now
hard-disables `rag-micu-classifier-hcv-e2-rewrite-*` execution at `rag submit`,
including dry-runs, before credentials or a run directory are created; the
historical configurations and artifacts remain read-only.

`configs/sampling/e35-v9-continuation-sidecar.json` freezes ten pre-existing
E3.5 audit samples: four `direct_confirmed_private_reject` records resume at
Classifier using the four v8 private-audit-only replay parent IDs; six resume
at RAG using five v8 RAG unknown-delivery lineages and one v7 RAG
route-contract-reject lineage. The generated immutable manifest is
`outputs/artifacts/e35-dual-arm-rag-distill/manifests/e35-dual-arm-audit-v9-r0.json`.
It binds a 200,000 uncached-input-token authorization cap, 1024 px deterministic
transport JPEGs, two public turns per route, and conservative Classifier/RAG/
private-audit reservations. Unknown delivery reservations are retained rather
than released; budget shortfalls are terminal and distinct from delivery or
quality shortfalls. R1/R2 inherit the same resume route and controls.

The local v9 adapter dry-run verified local RAG health and deterministic
in-memory encodes for all ten selected images and reported `teacher_requests=0`;
no provider completion, private audit, rewrite, SFT, full campaign, or training
was started. The focused E3.5/E2/E3 regression suite passed 100 tests and
`git diff --check` passed. All training flags remain false.

## 2026-09-12 21:24:08 CST - E3.5 v10 RAG failure is local CUDA; v11 recovery is frozen

Tags: e35, rag, cuda, tool-shortfall, lineage, no-sft

The six v10 RAG-only records were initially labelled `unknown_delivery` even
though each first teacher response was durably ledgered and contained a valid
native `agrinet_rag_search` call. The next teacher continuation was never
dispatched. A direct replay of the first stored call against
`http://127.0.0.1:8077/search` returned HTTP 500; service logs identify
`torch.AcceleratorError: CUDA error: unspecified launch failure` during visual
embedding. Workstation diagnostics also report NVIDIA driver/library mismatch
and PyTorch CUDA error 802. This is a local RAG-tool failure, not provider
delivery ambiguity.

`e35_cascade_collect.py` now classifies a local RAG runner exception after a
delivered native teacher call as `tool_shortfall` with
`quality_status=local_rag_failure`. `cascade_outcome` treats it as a delivered
quality terminal, preventing R1/R2 recovery from replaying it as unknown
provider delivery. The focused E3.5 suite passed 40 tests.

The CPU recovery service in
`configs/experiments/rag/rag-serve-open-agri-v3-micu-v2-cpu-recovery-v1.yaml`
is running at `127.0.0.1:8077` against the same OpenAgri v3 collections. The
failed sample's exact visual query returned typed `agrinet.rag.search/v1`
evidence (three entries) in 2.02 s.

`configs/sampling/e35-v11-local-rag-repair-sidecar.json` and
`manifests/e35-dual-arm-audit-v11-r0.json` freeze a fresh six-sample RAG-only
R0 lineage. Each retains its delivered v10 request ID as predecessor
provenance and gets a new request ID only on v11 dispatch. The v11 dry-run
passed with zero teacher requests. Actual collection stopped at encrypted
`micu_slb` GPG credential preflight before any provider intent was created.

Next: after the existing GPG agent is unlocked for non-interactive `micu_slb`
decryption, run the already-frozen
`rag-e35-v11-local-rag-repair-collect-v1` experiment. Do not replay v9/v10
ledgers or start full collection, export, SFT, or training.

## 2026-09-12 22:12:00 CST - v11 dispatch remains pre-provider blocked by local proxy

Tags: e35, v11, provider-preflight, proxy, no-delivery

After the GPG credential was unlocked, v11 was started only after confirming
that neither its immutable outcome destination nor campaign root existed. The
first two detached runs stopped at local RAG health preflight because the
provider proxy environment also affected the loopback RAG URL. The runtime now
sets `NO_PROXY` and `no_proxy` for `127.0.0.1`, `localhost`, and `::1`; the
same proxy-bearing environment then passed local RAG health, and 45 focused
credential/E3.5 tests passed.

The subsequent v11 run reached provider capability preflight but stopped at
`GET /models`: its configured HTTP proxy is a loopback endpoint whose port is
not listening (`ConnectionRefusedError`). It did not reach teacher generation.
Verified state: no v11 campaign directory, ledger, token-budget reservation,
outcome, provider request ID, or provider intent exists. This is a local proxy
availability blocker, not `unknown_delivery`, RAG failure, or quota use.

Next: restore the existing local provider proxy service (or configure the
approved working provider network route), verify a non-mutating `/models`
capability preflight, then launch the unchanged v11 collector once.

## 2026-09-13 01:11:00 CST - E3.5 v11/v12 repair closes four real RAG trajectories

Tags: e35, rag, v11, v12, budget, hermes, no-sft

The stale loopback provider proxy was unavailable, while authenticated direct
`/models` access was verified and included `gpt-5.6-sol`. Micu runtime setup
now preserves loopback `NO_PROXY` and bypasses only an unreachable loopback
proxy helper; remote proxy endpoints are unchanged. This allows local RAG and
provider preflight in the same child process without proxying `127.0.0.1`.

v11 completed six RAG-only items with exit code 0. Its CPU recovery RAG service
closed the previously broken native tool transition: every item obtained a real
RAG call and typed response before teacher continuation. Two records were
accepted: Known `N04001` and simulated Unknown `N05049`. Four others were
incorrectly stopped after their delivered second teacher response because the
old budget implementation treated a per-call estimate (2,775 tokens) as a
hard cap despite campaign usage being only 31,267 / 200,000.

Budget settlement now treats reservations as concurrent-dispatch protection and
enforces the actual campaign total after settlement. A new immutable v12
four-item lineage bound the four delivered v11 second-turn request IDs as
predecessors; v11 winners were excluded. v12 completed with exit code 0,
27,975 settled uncached input tokens, no unknown exposure, and 172,025 tokens
remaining. Its outcomes are: simulated Unknown `N04027` accepted; Known
`N05054` delivered but private quality-rejected; Known `N05004` delivered
route-contract reject (`multiple_tool_calls`); simulated Unknown `N05064`
delivered route-contract reject (`invalid_terminal_response`).

Across v11/v12, three accepted RAG trajectories are present: Known `N04001`;
simulated Unknown `N05049` and `N04027`. Each retains an actual
`agrinet_rag_search` native call and typed `agrinet.rag.search/v1` response.
Focused credential/E3.5 regression tests passed (49), and `git diff --check`
passed. No export, SFT, training, or full 1,070-image collection was started;
all training gates remain false.

Next: produce the immutable R0 summary and assess the 32-image audit protocol
gate using all completed E3.5 lineages. Do not replay v11/v12; any unresolved
or quality-terminal record requires separately authorized fresh scope.
