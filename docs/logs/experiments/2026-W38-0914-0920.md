## 2026-09-14 10:30:42 CST - E3.21 provider hard-deadline recovery

Trigger: E3.21 Direct-only shard-05 R0 became non-convergent after provider
TLS/socket polls remained blocked beyond the configured 180-second timeout.

Verified cause and change:

- `e320_collect` uses a four-worker `ThreadPoolExecutor`; an unreturned
  provider call prevented atomic shard outcome materialization.
- `src/agrinet/rag/e320_live.py` now invokes the established HCV v13
  process-isolated provider request helper. Each POST has a killable child and
  a 180-second hard deadline. A non-delivery becomes the existing ledgered
  `DeliveryUnresolved` / `unknown_delivery` path; it is never retried in the
  same attempt.
- Focused validation passed: `.venv/bin/python -m pytest -q
  tests/test_e320_four_stage_cascade.py tests/test_e321_direct_first.py`
  (`12 passed`).

Recovery evidence:

- The stalled pre-fix collector was terminated only after its active provider
  requests exceeded the deadline. No ledger, public trajectory, manifest, or
  prior outcome was deleted.
- Restarted run: `outputs/runs/rag/rag-e321-direct-first-hcv-campaign-v1/
  20260914T102218-09713238-a01`. Shard-05 R0 then wrote 133 outcomes: 129
  delivered and four ledger-backed `unknown_delivery` private audits retaining
  their original request IDs.
- Shard-05 Q1 closed 16 quality-repair rows, including three properly
  classified `unknown_delivery` rows. The controller generated and began only
  the Direct R1 manifest for the four R0 unknown-delivery parents. No
  Classifier, RAG, Reject, conversion, SFT, or training route was executed.

Volatile: the restored campaign is still running. Next action is to let the
single controller close shard-05 R1/R2 as needed, then continue shard-06/07;
audit each atomic outcome and keep all training flags false.

## 2026-09-14 12:54:14 CST - E3.21 Direct-only full campaign closed

Tags: e321, direct-only, hcv, delivery-recovery

### Objective

Close all eight frozen Direct R0 shards and their permitted Direct Q1/R1/R2
descendants, then freeze terminal accounting and the future Classifier queue
without running Classifier, RAG, Reject, conversion, SFT, or training.

### Results

- Managed run `outputs/runs/rag/rag-e321-direct-first-hcv-campaign-v1/20260914T102218-09713238-a01/` completed with exit code 0. It executed only the `direct` provider route.
- New Direct source terminal accounting (1,039 rows): 505 Direct winners, 519 semantic-wrong future-Classifier parents, 12 quality-repair-exhausted rows, and three R2 terminal `delivery_shortfall` rows.
- R2 shortfall reports were written from immutable R2 manifests/outcomes: `e321-direct-r0-shard-05-q1-r1-r2-delivery-shortfall.json` (1), `e321-direct-r0-shard-05-r1-r2-delivery-shortfall.json` (2), and `e321-direct-r0-shard-06-r1-r2-delivery-shortfall.json` (0). Each unresolved entry records `no_r3_created=true`.
- Final evidence is `outputs/artifacts/e321-direct-first-hcv-cascade/campaigns/e321-direct-first-hcv-full-v1/e321-final-direct-campaign-report-v3.json`. It verifies the full 1,070-image pool: Known/Unknown 535 each; 107 classes per arm; five images/class; 3 Open and 2 Option per class.
- Frozen queue `e321-future-classifier-queue-v3.json` has 525 unique entries: 519 new E3.21 semantic-wrong Direct terminals and six frozen E3.20 semantic-wrong parents. It is explicitly `executed=false`.
- Token ledger: 4,101,500 reserved, 2,857,862 settled, and 178,000 unknown-delivery exposure tokens under the 16,000,000 cap. All campaign, report, and queue training flags are false.

### Validation

- `.venv/bin/python -m pytest -q tests/test_e320_four_stage_cascade.py tests/test_e321_direct_first.py` passed (`13 passed`).
- `py_compile` of `e321_report.py` and `git diff --check` passed. Final audit found only `direct` outcome routes, no R3 manifest, 525 unique queue samples, and all training flags false.

### Next

The next possible work is a separately authorized Classifier campaign using the
frozen queue. E3.21 Direct-only is complete; do not convert or train from these
artifacts.

## 2026-09-14 17:06:51 CST - E3.22 Classifier presample closed below gate

Tags: e322, classifier, presample, hcv, no-sft

### Objective

Run an immutable 32-image Classifier-only presample derived from E3.21's frozen
525-row queue, using only frozen Top-3/Top-5 cards and isolated private audit.

### Setup

- Code: `src/agrinet/rag/e322_presample.py`, `e322_campaign.py`, and
  `e322_artifact_audit.py`; CLI experiments are
  `rag-e322-classifier-presample-{prepare-,}v1`.
- Data: `outputs/artifacts/e322-classifier-presample/source.jsonl`; exact 16/16
  arms, 32 classes, all six checkpoint SHAs, and four-layer identity isolation.
- Model: `gpt-5.6-sol`, 512px transport, 180-second isolated hard timeout, four
  workers, 300,000 uncached-input-token cap.

### Results

- Prepare run `20260914T163511-350c670c-a01` completed. Source SHA is
  `9e8a0f529164a6921fdf27e3ff668d0db6b9def63f3f185e397159d08bec7de5`;
  manifest SHA is
  `1efca3a3d4c06885ae2cc129ede2dbaf7e2a0eba9b53d8ad38504d304b8550a4`.
- First live run `20260914T163810-350c670c-a01` exposed a controller defect: the
  private audit serialized the image-bearing public message inside JSON and
  attached the image again, producing 19k--40k uncached input. It was stopped
  through `agrinet rag stop` before any outcome was written. No ledger was
  deleted or replayed.
- The fix uses `private_trajectory_projection`. The resumed managed run
  `20260914T164515-350c670c-a01` preserved three complete ledgers, froze four
  interrupted intents as unknown delivery, and used new predecessor-bound R1
  request IDs where budget permitted. It terminated with exit code 2 after
  writing all R0/Q1/R1/R2-family outcomes and the final report.
- Final disposition: 12 semantic-correct (Known 11, simulated-Unknown 1), nine
  future-RAG, seven quality-exhausted, four budget-shortfall. The remaining
  Classifier queue is 493. Budget is 246,544 settled plus 42,500 unknown
  exposure = 289,044 committed; 10,956 remains.
- Independent `artifact-audit-v2.json`: 39 delivered trajectories, every one
  exactly one frozen predict and zero expand; zero tool-order or trajectory
  contract errors, zero private binding leaks, zero online classifier calls,
  and all six checkpoint resolvers used. Persisted trajectory files contain the
  original input image bytes; this is recorded separately from private-binding
  leakage.
- Validation: all 689 currently collectable tests pass with the already-known
  legacy orphan `tests/test_adjacent_class_folds.py` excluded. Unfiltered
  collection still fails only because that test imports the absent historical
  `scripts.vision.build_adjacent_class_folds`; this pre-existing repository
  condition is also described in START_HERE. E3.22 plus E3.15/E3.20/E3.21 and
  classifier regression suites pass. Repeated prepare exits 1 at the immutable
  destination, and source/manifest plus all three parent-input SHA bindings are
  unchanged.

### Conclusion

- Verified: `presample_gate_passed=false`. Semantic thresholds, zero
  quality-exhausted, and zero budget-shortfall were not met.
- Verified: no RAG/Reject, remaining-493 execution, conversion, SFT, or training
  occurred; all training flags remain false.
- Relation to E3.21: the v3 queue and historical artifacts remain unchanged.

### Next

Interview the user before any successor. Do not enlarge this frozen budget or
replay delivered E3.22 rows. A successor would require a new protocol and
explicit scope.

## 2026-09-14 19:10:14 CST - E3.22 v3 paired Classifier preflight passed

Tags: e322, classifier, paired-preflight, protocol-v3, no-sft

### Objective

Repair the v1/v2 controller defects and rerun the same frozen 32-image cohort as
a paired operational preflight, without enabling RAG/Reject, the remaining 493
Classifier rows, conversion, SFT, or training.

### Setup

- Protocol/config: `agrinet.e322-classifier-presample/v3`;
  `rag-e322-classifier-presample-{prepare-,}v3`.
- Model/runtime: `gpt-5.6-sol`, 512px, 180-second hard timeout, four workers.
- Budget: independent 300,000 uncached-input-token cap; 8,000 generation and
  5,000 private-audit reservations; every provider call consumes one global
  intent.
- Recovery: one type-specific Q1; generation and private-audit unknown delivery
  recover only their own operation via new predecessor-bound R1/R2 requests.

### Results

- Managed run
  `outputs/runs/rag/rag-e322-classifier-presample-v3/20260914T184949-350c670c-a01/`
  completed with exit code 0.
- Final disposition: 16 semantic-correct and 16 frozen future-RAG. Known is
  16/16 semantic-correct; simulated-Unknown is 0/16 exact semantic-correct and
  16/16 oracle-routed stage-appropriate. Autonomous `INSUFFICIENT_EVIDENCE` is
  6/16 simulated-Unknown (three Open, three Option); the other 10/16 are unsafe
  closed-set accepts detected only by private audit.
- R0 was 14 semantic-correct, 15 future-RAG, one quality-reject, and two
  generation unknown. The single Q1 became future-RAG; R1 closed one semantic
  correct and one quality-reject; Q1-R1 repaired the latter to semantic-correct.
  R2 and Q1-R2 are exact empty manifests/outcomes.
- Budget settled 132,085 tokens with 16,000 unresolved exposure from the two R0
  generation unknowns: 148,085 committed and 151,915 remaining. All 33 delivered
  private audits used 1,613–2,208 tokens, below the 5,000 reservation.
- Global intent accounting exactly matches ledger provider intents: 139/139,
  with 139 unique request IDs. Six checkpoint SHAs cover all 32 terminal rows.
- Final gate: `campaign_gate_candidate=true`, `artifact_gate_passed=true`, and
  `presample_gate_passed=true`. Evidence files are `final-report.json`,
  `artifact-audit-v4.json`, and `final-gate-decision.json` under the campaign root.

### Validation

- Independent audit: zero private leakage, terminal trajectory contract errors,
  tool-order errors, resolver receipt errors, online classifier requests, lineage
  binding failures, and request-ID reuse failures. Public trajectories contain
  zero image data URLs. One repaired nonterminal trajectory error is retained.
- 35 durable resolver receipts comprise 34 completed trajectories plus one R0
  predict whose following final generation became unknown; this preserves rather
  than loses failure-path evidence. No expand, RAG, or Reject tool call occurred.
- E3.22 + E3.15/E3.20/E3.21 focused tests passed (32), and all currently
  collectable tests passed with only the known historical orphan
  `tests/test_adjacent_class_folds.py` excluded. `py_compile` and
  `git diff --check` passed. Frozen v1/v2 source and manifest hashes are unchanged.

### Conclusion

- Verified: the repaired Classifier-only orchestration and oracle-assisted routing
  satisfy the v3 preflight gate on this paired cohort.
- Verified limitation: this is not deployment-grade Unknown recognition;
  autonomous deferral is 6/16 and private audit is still required to catch 10/16
  simulated-Unknown closed-set errors.
- Compatibility/scope: all training flags remain false. Passing this gate does
  not authorize downstream routes or expansion.

### Next

Interview the user. The safest next research step is a new, identity-disjoint
open-set abstention pilot that does not depend on private audit for runtime
routing; do not run the remaining 493 rows, RAG/Reject, SFT, or training.

## 2026-09-14 20:24:02 CST - E3.23 v4 RAG preflight completed below gate

Tags: e323, rag, preflight, protocol-v4, no-reject, no-sft

### Objective

Run RAG only for the 16 E3.22 v3 future-RAG rows, preserving the private 6/10
diagnostic split and forbidding Reject, queue expansion, conversion, and training.

### Setup

- Protocol/config: `agrinet.e323-rag-preflight/v4`; 512px, `gpt-5.6-sol`,
  four workers, 180-second hard timeout, 300,000 uncached-token cap.
- Managed run: `outputs/runs/rag/rag-e323-rag-preflight-v4/20260914T200528-350c670c-a01/`.
- Earlier v1-v3 attempts are retained as fail-closed diagnostics; v4 added
  explicit planning phases and public-safe normalization of provider-native
  tool-call messages while retaining original wire responses in ledgers.

### Results

- Run completed with exit code 0. Final disposition: 12 quality-exhausted,
  three budget-shortfall, one frozen future-Reject, and zero semantic-correct.
- Budget: 284,649 settled plus 12,000 unknown exposure = 296,649 committed;
  3,351 remained. `presample_gate_passed=false`.
- Artifact audit passed: 29 trajectories, 29 classifier predicts, 29 actual
  RAG searches, zero Reject calls, and zero public leakage. All 156 global
  intents match 156 ledger intents with unique request IDs.
- Full collectable regression passed with only the known historical
  `tests/test_adjacent_class_folds.py` orphan excluded.

### Conclusion

- Verified: RAG execution and audit plumbing are operational and private-safe.
- Verified: RAG answer quality is not acceptable on this cohort; neither Reject
  nor any larger campaign is authorized.

### Next

Interview the user. Diagnose/redesign the evidence-to-answer contract in a new
protocol; do not enlarge v4's budget or replay delivered rows.

## 2026-09-14 21:19:12 CST - E3.24 v2 structured RAG preflight passed

Tags: e324, structured-rag, protocol-v2, no-reject, no-sft

### Objective

Repair E3.23's evidence-to-answer failures with an 8-image immutable preflight
that separates provider planning and closure from deterministic HCV/Hermes
rendering, while retaining exactly one actual local RAG search per image.

### Setup

- Protocol: `agrinet.e324-structured-rag/v2`; model `gpt-5.6-sol`, 512px, four
  workers, 180-second timeout, 180,000 uncached-input-token cap.
- Stable IDs bind classifier candidates as `C1`–`C3`, Option choices as
  `OA`–`OD`, and new retrieval classes as `R1+`.
- Frozen cohort: eight unique images and truth classes, split 4/4 between
  autonomous-insufficient-evidence and oracle-rescued prior outcomes.
- Managed run: `outputs/runs/rag/rag-e324-structured-rag-v2/20260914T211144-b1af5ee4-a01/`.

### Results

- Exit code 0; final disposition is seven semantic-correct and one frozen
  future-Reject. Group results are 4/4 autonomous and 3/4 oracle-rescued.
- Zero quality-exhausted, delivery-shortfall, and budget-shortfall. No Q1, R1,
  or R2 work was needed. Budget settled and committed 45,562/180,000 tokens.
- The corrected independent audit reports 8/8 valid structured trajectories,
  24 global intents = 24 ledger intents, unique request IDs, zero leakage, and
  zero Reject. Every terminal has exactly one classifier predict and one RAG.
- Authoritative evidence: `outputs/artifacts/e324-structured-rag-v2/campaign/final-report.json`,
  `artifact-audit-r1.json`, and `final-gate-decision-r1.json`; the latter is
  SHA-bound to the first two and has `presample_gate_passed=true`.
- Focused E3.19/E3.22/E3.23/E3.24 tests and two complete collectable regression
  runs passed; only the known orphan `tests/test_adjacent_class_folds.py` was
  excluded.

### Issues

- The stopped v1 attempt had 8/8 quality rejects: five candidate-name alias or
  capitalization mismatches, two non-string `rag_evidence` values, and one
  contradictory nearest-alternative requirement after retrieval changed winner.
- The initial v2 artifact audit produced a false negative because persisted JSON
  sorts object keys while the runtime renderer preserved provider key order. It
  and its false gate are retained. The r1 audit reconstructs the original closure
  from its request-ID-bound delivered ledger response and verifies semantic
  equality with frozen state before rendering. No trajectory or model result was
  rewritten.
- The sole semantic miss is Open sample
  `e35-simulated_unknown-N04107-0fc50b48ca89f540d089`, answered
  `sunflower leaf scars`; it remains frozen for future Reject.

### Conclusion

- Verified: E3.24 v2 passes the 6/8 overall and 3/4-per-group gate with complete
  artifact and intent accounting.
- Verified scope: `training_eligible=false`, `training_authorized=false`, and
  `sft_may_start=false` throughout. Reject, remaining Classifier rows,
  conversion, SFT, and training were not executed or authorized.

### Next

Interview the user. Decide whether to design a separate Reject preflight for the
single frozen miss; do not execute it or expand the cohort automatically.

## 2026-09-14 22:01:51 CST - E3.25 independent 28-image RAG preflight completed below gate

Tags: e325, structured-rag, identity-disjoint, no-reject, no-sft

### Objective

Test E3.24's structured evidence-to-answer protocol on a larger cohort that is
disjoint from E3.23/E3.24 at sample, image, source-group, and near-duplicate
levels, without running the remaining Classifier queue.

### Setup

- Protocol `agrinet.e325-structured-rag/v1`; 28 simulated-Unknown truth classes
  selected from the frozen, unlaunched E3.19 prospective RAG-witness source.
- Open/Option × disease/pest cells are exactly 7/7/7/7; class folds are 9/10/9.
  Three checkpoint SHAs are covered, and every truth class remains outside its
  bound class-fold label map.
- Model/runtime: `gpt-5.6-sol`, 512px, one local RAG per image, four workers,
  180-second timeout, and a 700,000-token fail-closed cap.
- Gate: at least 22/28 semantic-correct, at least 5/7 in every cell, and zero
  quality-exhausted, delivery-shortfall, or budget-shortfall.
- Managed run: `outputs/runs/rag/rag-e325-structured-rag-v1/20260914T214948-b1af5ee4-a01/`.

### Results

- Exit code 0. Final disposition is 22 semantic-correct, five future-Reject, and
  one quality-exhausted; there are no delivery or budget shortfalls.
- Cell correctness: Open-disease 6/7, Open-pest 3/7, Option-disease 6/7, and
  Option-pest 7/7. Aggregate Open is 9/14; Option is 13/14.
- One Open-pest closure returned a decision inconsistent with its selected
  candidate ID in both R0 and its unique Q1. No Q2 was attempted.
- Budget settled and committed 163,018/700,000 tokens. Campaign re-entry was
  read-only and left the intent count unchanged.
- The corrected independent audit passes: 28/28 RAG evidence receipts, 27 valid
  final trajectories plus the retained quality-exhausted row, 84 global intents
  equal to 84 ledger intents with unique request IDs, zero private leakage, and
  zero Reject calls. Source coverage and zero overlap with excluded cohorts are
  recorded in `artifact-audit-r1.json`.
- `final-gate-decision-r1.json` is SHA-bound to `final-report.json` and the r1
  audit. `presample_gate_passed=false` because Open-pest is below 5/7 and one
  quality-exhausted row remains.
- All focused E3.19–E3.25 tests and both pre-run and post-run collectable full
  regressions passed; only the known orphan `tests/test_adjacent_class_folds.py`
  was excluded.

### Conclusion

- Verified: the structured RAG pipeline is operational, private-safe,
  deterministic, and budget-efficient on an independent 28-image cohort.
- Verified: performance is not uniformly acceptable. Option quality is strong,
  while Open-pest candidate discrimination is the dominant weakness.
- Scope: all no-training flags remain false; Reject, remaining Classifier rows,
  conversion, SFT, and training were not executed or authorized.

### Next

Interview the user. Diagnose the four Open-pest non-winners and the repeated
decision/selected-ID contradiction before designing a successor protocol; do
not replay E3.25 or relax its preregistered gate.

## 2026-09-14 23:40:47 CST - E3.26 morphology-first RAG preflight closed below semantic gate

Tags: e326, morphology-first, balanced-top8, rrf, no-reject, no-sft

### Objective

Retest structured RAG on 28 RAG-witness rows with ID-only closure, Open queries
that cannot contain classifier candidate names, balanced Top-8 RRF retrieval,
and explicit Open-pest morphology comparison.

### Setup

- Source: outputs/artifacts/e326-structured-rag-v1/source.jsonl, derived from
  frozen E3.18 and disjoint from E3.23/E3.24/E3.25 on all four identity keys.
- Cohort: 28 truth classes; four cells 7/7/7/7; class folds 9/10/9; three
  checkpoint SHAs; all 28 rows retain RAG-witness eligibility.
- Runtime: gpt-5.6-sol, 512px, four workers, 180 seconds, exactly one local
  balanced Top-8 RRF search per image, and a 900,000-token fail-closed cap.
- Gate: at least 23/28 overall, at least 5/7 per cell, Open-pest at least 5/7,
  and zero quality, delivery, or budget shortfalls.

### Results

- Authoritative run:
  outputs/runs/rag/rag-e326-structured-rag-v1/20260914T233009-b1af5ee4-a01/;
  managed status complete with exit code 0.
- Authoritative artifacts: outputs/artifacts/e326-structured-rag-v1/campaign-r2/.
- Final disposition: 16 semantic-correct and 12 frozen future-Reject. Cell
  correctness is Open-disease 4/7, Open-pest 1/7, Option-disease 4/7, and
  Option-pest 7/7. The preregistered gate therefore fails.
- Independent artifact audit passes: 28 valid trajectories, 28 RAG receipts,
  84 global intents equal to 84 ledger intents, zero private leakage, zero
  Reject calls, and complete identity/class/fold/checkpoint coverage.
- Budget: 141,456 settled and committed of 900,000; unknown exposure 0.
- Failure attribution: truth was absent from RAG Top-8 for eight of 12 semantic
  failures. Four failures had truth retrieved but closure selected another
  candidate; one of those was Open-pest with truth at rank 1.
- Two earlier managed runs are retained under campaign/ and campaign-r1/. They
  exposed deterministic contract-adapter defects: closure map-vs-array shape,
  non-null not-applicable pest morphology, and oversized private-audit
  projection. The final run fixed these without relaxing semantic checks.
- Full pre-run regression passed with the project-prescribed adjacent-fold test
  exclusion; final focused regression passes 38 tests.

### Conclusion

- Verified: E3.26 engineering and privacy contracts are clean; name casing,
  candidate-ID rendering, tool order, delivery, and budget are not blockers.
- Verified: the semantic gate fails materially. Open-pest retrieval recall is
  the dominant bottleneck, so another prompt-only closure replay is not
  justified.
- Scope: no remaining Classifier, Reject, conversion, SFT, or training was run
  or authorized.

### Next

Interview the user. If a successor is authorized, first perform a provider-free
retrieval ablation on the frozen failures (visual-only versus weighted balanced
and query-template variants), then preregister an E3.27 cohort/protocol. Do not
replay E3.26 or relax its gate.

## 2026-09-15 00:39:44 CST - Reconfirm E3.26 result and authorize semantic retrieval ablation

Tags: e326, retrieval-ablation, provider-free

### Note

The user accepted recording the closed E3.26 result and authorized the next
provider-free semantic/retrieval ablation. The authoritative result remains
outputs/artifacts/e326-structured-rag-v1/campaign-r2/: 16/28 semantic-correct,
Open-pest 1/7, independent artifact audit passed, and presample gate failed.

### Status

No teacher, remaining Classifier, Reject, conversion, SFT, or training is
authorized. The ablation may read frozen public plans/images and private truth
only for offline scoring; private truth must never enter a retrieval query.

### Next

Implement and run an immutable local-RAG ablation comparing the original query,
deterministic morphology templates, visual-only retrieval, weighted balanced
fusion, and RRF. Report Top-8 truth recall on all 28 rows, the 12 E3.26 semantic
failures, and Open-pest separately.

## 2026-09-15 00:53:00 CST - E3.26 provider-free semantic retrieval ablation favors visual-only

Tags: e326, retrieval-ablation, visual-only, provider-free

### Objective

Determine whether E3.26's low Open-pest accuracy comes primarily from the
semantic query/fusion policy or from the image index itself.

### Setup

- Eight fixed Top-8 strategies over all 28 frozen E3.26 rows: visual-only;
  original-query RRF and weighted fusion; full-profile RRF and weighted fusion;
  morphology-only RRF, weighted fusion, and semantic-only retrieval.
- Output: outputs/artifacts/e326-semantic-ablation-v1/.
- Managed run:
  outputs/runs/rag/rag-e326-semantic-ablation-v1/20260915T004746-b1af5ee4-a01/.
- Private truth was used only after each local retrieval for rank scoring. No
  teacher or external provider request was available to the runner.

### Results

- Managed run completed with exit code 0; 224/224 strategy-sample pairs were
  cached and uniquely audited. Provider requests are 0.
- Visual-only is the clear winner. Top-8 recall is 23/28 overall, 10/12 on the
  E3.26 semantic failures, and 5/7 on Open-pest. Its Top-3 recalls are 21/28,
  9/12, and 5/7 respectively.
- Original-query RRF reaches only 14/28 overall, 3/12 failures, and 1/7
  Open-pest at Top-8. Visual-only newly retrieves seven prior failures with no
  regression among the three failures already retrieved by original RRF.
- The strongest non-visual strategy is original weighted fusion at 17/28
  overall, 5/12 failures, and 2/7 Open-pest. Morphology weighted reaches 14/28,
  4/12, and 3/7. Longer morphology/profile text therefore does not close the
  gap.
- Two Open-pest failures remain absent under every tested strategy:
  brevicoryne brassicae and mythimna separata.
- Independent audit passes at
  outputs/artifacts/e326-semantic-ablation-v1/artifact-audit.json: 224 unique
  pairs, zero generated-query truth-name occurrences, zero errors, and all
  no-training flags false. Eight truth-name occurrences exist only in the
  frozen original E3.26 planner queries and are marked as frozen provenance;
  they were not generated from private truth by this ablation.

### Conclusion

- Verified: E3.26's balanced RRF fusion suppresses useful image-neighbor
  retrieval, especially for Open-pest. Visual-only is materially better than
  every semantic or fused strategy tested.
- Verified: this is a retrieval-stage result, not evidence that a closure model
  will convert all 10 retrieved failures into correct final answers.
- Scope: no E3.27 live run, remaining Classifier, Reject, conversion, SFT, or
  training was executed or authorized.

### Next

Design a separate E3.27 preflight around one image-only Top-8 retrieval and a
shorter closure payload. Keep brevicoryne brassicae and mythimna separata as
explicit unresolved retrieval witnesses; do not claim that query tuning alone
solves them.

## 2026-09-15 02:23:20 CST - E3.27 visual Top-3 repaired-cohort preflight passes gate

Tags: e327, visual-top3, rag, visibility-grounded, repaired-cohort

### Objective

Test the user-selected Top-3 route on the frozen E3.26 cohort with one local
image-only retrieval, no planner, a short ID-only closure, deterministic
HCV/Hermes rendering, and an isolated private semantic audit.

### Setup

- Frozen source and manifest: `outputs/artifacts/e327-visual-top3-rag-v1/`.
- Prepare run: `outputs/runs/rag/rag-e327-visual-top3-prepare-v1/20260915T015543-b1af5ee4-a01/`; 28 rows and zero provider requests.
- Fixed retrieval: `retrieval_type=visual`, `top_k=3`, no ranker, exactly one
  local RAG call per image. Normal provider path is closure plus private audit.
- Gate: at least 21/28 overall, at least 5/7 in every cell, Open-pest at least
  5/7, and zero terminal quality, delivery, or budget shortfall.

### Results

- Initial immutable `campaign/` exposed an engineering defect: RAG class names
  overlapping classifier names were removed before assigning R IDs. Twenty-one
  of 28 rows therefore exposed fewer than three R candidates. It closed at
  18/28 with eight quality-exhausted rows; this run remains a failed diagnostic.
- `campaign-r2/` assigned every retrieved slot losslessly to R1/R2/R3 and
  strengthened the sole Q1 schema repair. It produced 28/28 valid trajectories,
  zero Q1, and 22/28 semantic-correct. Cells were 4/7, 5/7, 6/7, and 7/7, so
  the Open-disease cell missed its gate by one.
- Error analysis found four of six r2 semantic failures had truth absent from
  Top-3. Two had truth retrieved but closure selected another candidate. The
  two Open-pest misses were the preregistered `brevicoryne brassicae` and
  `mythimna separata`; visual Top-3/5/8 all have the same 5/7 Open-pest recall,
  so increasing K would not recover them.
- A general, truth-free visibility rule was added: assess each candidate against
  visible matches and conflicts, do not treat an ambiguous feature as decisive,
  and connect retrieved synonyms to public options. The managed `campaign-r3`
  run is `outputs/runs/rag/rag-e327-visual-top3-v3/20260915T021554-b1af5ee4-a01/`.
- r3 reached 24/28: Open-disease 5/7, Open-pest 5/7, Option-disease 7/7, and
  Option-pest 7/7. One private-audit delivery became unknown and was confirmed
  by R1; retrieval and closure were not repeated. There were 57/57 accounted
  provider intents, 76,062 committed tokens including 3,000 unknown-delivery
  exposure, zero quality-exhausted, and zero budget shortfall.
- `campaign-r3/artifact-audit.json` passes: 28/28 valid trajectories and RAG
  receipts, fixed visual Top-3/no-ranker arguments, correct tool order, zero
  private leakage, and zero Reject calls. `final-gate-decision.json` records
  `presample_gate_passed=true`. All no-training flags remain false.
- Full regression after the run passed 734 tests with
  `pytest -q --ignore=tests/test_adjacent_class_folds.py`.

### Conclusion

- Verified: Top-3 is sufficient for the preregistered repaired-cohort gate and
  materially cleaner than E3.26 Top-8 balanced RRF. Larger K is not justified
  by the persistent Open-pest misses.
- Verified: the lossless R1/R2/R3 mapping is required; classifier-name overlap
  must not suppress image-only retrieval slots.
- Limitation: the visibility-grounded rule was designed after inspecting r2
  errors on this same cohort. Thus 24/28 is a repaired-cohort validation, not an
  unbiased estimate on a fresh independent cohort.
- Scope: no remaining Classifier, Reject, conversion, SFT, or training was run
  or authorized.

### Next

Interview the user. If independent confirmation is desired, preregister a fresh
disjoint Top-3 cohort with the r3 closure contract frozen before sampling. Do
not automatically expand collection or relax the two unresolved retrieval
witnesses.

## 2026-09-15 10:59:42 CST - Start E3.28 full Classifier campaign

Tags: e328, classifier, full-campaign, recovery, no-reject

### Objective

Execute the frozen 525-row Classifier queue with 32 read-only E3.22 v3
outcomes and 493 new provider lineages, before deriving any dynamic E3.29 RAG
source.

### Setup

- Provider-free source/manifest/audit: `outputs/artifacts/e328-classifier-full-v1/`.
- Prepare audit verified 525 unique sample/image/source/near-duplicate IDs,
 105 truth classes, Known/Unknown 274/251, Open/Option 470/55, disease/pest
 365/160, six checkpoints, and shards 7×64 + 45.
- Shared campaign limits are 4,000,000 uncached input tokens and 8,000 intents
 across all shards; workers are capped at four inside each serial shard.
- Managed run: `outputs/runs/rag/rag-e328-classifier-full-v1/20260915T104729-b1af5ee4-a01/`, PID 1086853.

### Results

- Focused E3.27/E3.28/E3.29 tests passed (8). CLI show and dry-run checks
  passed for all four new E3.28/E3.29 experiment entries.
- At record time the managed E3.28 campaign is volatile and running. Its shared
  ledger has persisted provider intents and frozen public trajectories; no
  E3.29 source or campaign exists. Two unknown-delivery exposures are recorded
  with their original request IDs for the bounded R1/R2 recovery path.
- No Reject, conversion, SFT, or training was invoked; all source, manifest,
  audit, and configuration training flags are false.

### Next

Monitor PID 1086853 and the bounded run logs/ledgers to terminal state. Run
E3.29 prepare only if the E3.28 report plus independent audit hard gate passes.

## 2026-09-15 11:17:00 CST - Freeze E3.28 after terminal Classifier contract failure

Tags: e328, classifier, hard-gate, frozen, e329-blocked

### Objective

Close the E3.28 campaign safely once its zero-terminal-quality-error hard gate
became impossible, without spending the remaining shared budget on shards that
could no longer authorize E3.29.

### Results

- Immutable shard-00 R0 completed all 64 rows. Its bounded Q1/R1/R2 lineage
  also completed, preserving eight original unknown-delivery request IDs and
  operation-specific private-audit recovery.
- `e35-known-N04012-0e527a228e8bd3d9c099` remained `quality_reject` after its
  sole permitted Q1. The persisted error is `E3.22 classifier predict missing`
  on request `b8a63529107246a59d77c28cc5cea9fb`.
- The run was stopped via its managed worker after that terminal condition was
  verified. It closed with exit code 143 and no remaining campaign PID. The
  shared ledger retains 346 created provider intents; no artifacts were
  deleted or overwritten.
- `outputs/artifacts/e328-classifier-full-v1/campaign/frozen-gate-failure.json`
  records the frozen condition.
- E3.29 source/manifest/campaign were not created.
  `outputs/artifacts/e329-visual-top3-rag-full-v1/frozen-by-e328-gate.json`
  documents the prohibition.

### Conclusion

- Verified: E3.28 cannot meet its hard gate because the final Q1 terminal
  contract/quality error is nonzero.
- Verified: Reject, conversion, SFT, and training remain unexecuted; all
  training authorization flags remain false.
- Next: do not resume or reroute remaining Classifier rows under this protocol.
  A new protocol/new lineage would be required before any future collection.

## 2026-09-15 13:11:24 CST - Start E3.30 v2 fresh trace-bound-Q1 Classifier lineage

Tags: e330, classifier, trace-bound-q1, recovery, no-reject

### Objective

Resume the full Classifier route without modifying frozen E3.28/E3.29 artifacts,
by correcting the Q1 contract defect in a fresh audited lineage.

### Results

- The new `trace_bound_quality_repair` preserves the SHA-bound R0 public tool
  trace, sends Q1 with no `tools` field, validates the repaired Hermes output
  against that preserved trace, and creates no new classifier-predict call.
- E3.30 v2 provider-free preparation created
  `outputs/artifacts/e330-classifier-full-v2/`: 525 unique source rows,
  32 E3.22-v3 read-only reuse bindings, 493 fresh rows, 105 classes, all six
  checkpoints, fixed strata, and shards 7×64+45. Its prepare audit records
  zero provider intents and all no-training flags false.
- Focused E3.27/E3.28/E3.29 regression passed 9 tests. CLI show and campaign
  dry-run passed. No E3.28/E3.29/E3.30 collection process was active and the
  E3.30 v2 campaign output target was absent immediately before launch.
- Managed E3.30 v2 collection started with PID 1127942 and run root
  `outputs/runs/rag/rag-e330-classifier-full-v2/20260915T131006-b1af5ee4-a01/`.
  At the last bounded check it was `running`, with 10 shared intents, six
  confirmed request results, five request ledgers, and no completed shard yet.

### Correction

Before any shard terminal outcome, E3.30 v2 was stopped at exit code 143 and
frozen after 35 created intents and 10 request ledgers. Review found that its
dispatcher could select trace-bound Q1 for a private-audit recovery; that would
violate the operation-specific recovery rule. The frozen marker is
`outputs/artifacts/e330-classifier-full-v2/campaign/frozen-pre-audit-recovery-dispatch.json`.
E3.30 v3 corrects dispatch so trace-bound repair is selected only for
`resume_operation=generation`; private-audit recovery uses the frozen repaired
trajectory and reruns only the audit. It passed the same nine focused tests,
prepared a fresh 525-row / 7×64+45 source with zero intents, and started as
PID 1130323 at
`outputs/runs/rag/rag-e330-classifier-full-v3/20260915T131541-b1af5ee4-a01/`.

### Correction 2

E3.30 v3 was also stopped before any shard outcome (exit 143; 26 created
intents and eight request ledgers) after identifying that process-level Q1
interruption could drop the SHA-bound R0 parent from its unknown-delivery
outcome. Its frozen marker is
`outputs/artifacts/e330-classifier-full-v3/campaign/frozen-q1-interrupt-lineage.json`.
The fix preserves and revalidates that parent in `_unresolved_started`; a new
regression constructs an unresolved `quality_repair` intent and verifies the
parent path/SHA remain attached. E3.30 v4 was provider-free prepared with the
same 525-row invariants and zero provider intents, passed ten focused tests and
dry-run, then started as PID 1132636 at
`outputs/runs/rag/rag-e330-classifier-full-v4/20260915T132059-b1af5ee4-a01/`.
At the most recent bounded check it was running with 30 shared intents, 26
confirmed deliveries, and zero unknown-delivery results.

### Running-state update

E3.30 v4 remained live at PID 1132636. At 13:28 CST its shared ledger recorded
136 intents and 130 confirmed deliveries. Two R0 provider responses are
unknown-delivery exposures, not automatic replays: `generation:2` for
`e35-known-N04017-0092a94364d27a0267a1` (request
`33b04a41b73145ac8f460976c17a450b`) and `generation:3` for
`e35-known-N04015-04c72dffcb235d39b534` (request
`4db6b0c1291640e7a0b867a003253cc4`). Both retain their original per-request
ledgers and must use new-ID, predecessor-bound R1/R2 recovery only after R0
shard closure. No shard outcome had been written at this observation; RAG and
Reject remain unstarted.

### Conclusion

- Verified: E3.28 remains immutable and E3.29 remains blocked; no prior output
  was overwritten or resumed.
- Verified: Reject, conversion, SFT, and training remain unexecuted and
  unauthorized.
- Volatile: E3.30 v4 is currently running; inspect PID, status, bounded logs,
  shared ledgers, and shard outcomes before relying on its terminal state.

### Next

Monitor the E3.30 v4 managed run to terminal outcomes. Derive a fresh dynamic
visual Top-3 RAG source only after the E3.30 final report and independent audit
both satisfy the Classifier hard gate.

## 2026-09-15 13:53:30 CST - Freeze E3.30 v4; launch forced-planning E3.30 v5

Tags: e330, classifier, tool-order, forced-planning, no-reject

### Result

E3.30 v4 closed its first R0 shard and then failed closed before Q1. Seven
first-turn provider responses returned a complete answer instead of the required
standalone planning turn followed by `agrinet_classifier_predict`; they have no
legal frozen trace that Q1 could repair. The frozen v4 root contains 64 R0
outcomes, eight quality rejects, three unknown-delivery records, 229 created
intents, and no resumed requests:
`outputs/artifacts/e330-classifier-full-v4/campaign/frozen-r0-planning-contract.json`.

E3.30 v5 adds a wire-level first phase that allows only one planning `<think>`
and then exposes/forces the predict tool. Its provider-free 525=32+493 prepare
passed with 7×64+45 shards, zero provider intents, and false training flags.
26 focused tests, dry-run, syntax, and diff checks passed. The managed v5 run
started at PID 1141710 in
`outputs/runs/rag/rag-e330-classifier-full-v5/20260915T135030-b1af5ee4-a01/`.
At this record, 20 complete public trajectories each had exactly one predict and
no bad tool-order trace; the ledger had 89 intents, 85 confirmed deliveries,
and no unknown-delivery record.

### Next

Monitor E3.30 v5 to terminal audit. Do not create dynamic RAG, invoke Reject,
convert data, start SFT, or train unless the full Classifier hard gate passes.

## 2026-09-15 14:20:00 CST - Freeze E3.30 v5; launch complete-HCV-Q1 E3.30 v6

Tags: e330, classifier, q1, hcv-contract, no-reject

### Result

E3.30 v5 completed R0 shard-00 with 64 outcomes (59 semantic-correct, three
future-RAG, one unknown delivery, one quality reject). Its sole trace-bound Q1
correctly had no callable tools and retained an R0 parent SHA, but the repair
prompt did not explicitly require all six HCV fields; it therefore failed closed
with `E3.22 HCV fields are missing or unordered`. v5 was stopped at exit 143
and frozen at
`outputs/artifacts/e330-classifier-full-v5/campaign/frozen-q1-hcv-contract.json`;
its outcomes and provider requests are not resumed.

E3.30 v6 restates the complete mechanical HCV contract inside trace-bound Q1:
six ordered headings, three observations, two visible-trait rejection clauses,
and confidence/limitation, while retaining the no-tools and frozen-parent rules.
Provider-free v6 prepare retained the 525=32+493 partition, 7×64+45 shards,
zero provider intents and false training flags. 26 focused tests, syntax,
diff checks and dry-run passed. v6 started at PID 1150346 in
`outputs/runs/rag/rag-e330-classifier-full-v6/20260915T141837-b1af5ee4-a01/`;
at record time five completed trajectories each had exactly one predict trace,
with 26 intents, 22 deliveries, and zero unknown delivery.

### Next

Monitor E3.30 v6 to terminal audit; RAG remains forbidden until the Classifier
hard gate passes. Reject, conversion, SFT, and training remain unauthorized.

## 2026-09-15 15:25:51 CST - Freeze E3.30 v6 on terminal delivery shortfall; launch E3.31 fresh lineage

Tags: e330, e331, classifier, delivery-recovery, shard-gate, no-rag, no-reject

### Result

E3.30 v6 completed shard-00 R0/R1/R2 recovery. Its 64 R0 outcomes were 47
semantic-correct, three future-RAG, and 14 unknown-delivery. R1 resolved five
of the 14; R2 resolved four more. Five samples exhausted the only legal R1/R2
recovery path with terminal unknown delivery:

- `e35-known-N04030-01ea078117e1f252084a`
- `e35-known-N04040-00013810172b90c96d84`
- `e35-known-N04044-040d42031964fb9922d8`
- `e35-known-N04046-00541056380e0d4c47c4`
- `e35-known-N04051-0c3e8a9175a0ae9a1766`

All R1/R2 records have new predecessor-bound request IDs; no R3, Reject,
RAG, conversion, SFT, or training was created. A diagnostic `SIGUSR1` caused
the first local wrapper to exit after all R2 outcomes were already durable
(`exit_code=-10`); recovery was read from those immutable outcomes only. The
campaign runner now fail-closes at a completed shard with terminal delivery or
disposition shortfall, preventing later-shard spend. Its frozen evidence is
`outputs/artifacts/e330-classifier-full-v6/campaign/frozen-shard-gate-failure.json`.

Focused E3.22/E3.27/E3.28/E3.29 regression passed: 27 tests. Provider-free
E3.31 prepare then passed with 525 rows = 32 read-only E3.22-v3 reuse + 493
fresh rows, 7x64+45 shards, 105 truth classes, six checkpoints, zero initial
provider intents, and all three training flags false. Its source SHA is
`9deeab58190381fcbfe7a7bff59b889832731968b19dd4c64ceb9ed9606e6277`.

E3.31 began as the independent fresh lineage at wrapper PID `1170548`, child
PID `1170549`, and
`outputs/runs/rag/rag-e331-classifier-full-v1/20260915T152448-b1af5ee4-a01/`.
Volatile: the collection is running; verify PID, status, bounded ledgers and
shard outcomes before relying on it.

### Next

Monitor E3.31 without replaying any request. Create its dynamic Top-3 RAG
successor only if its full Classifier report, artifact audit, and hard gate all
pass. E3.30 v6 remains frozen, and all data conversion, Reject, SFT, and
training remain unauthorized.

## 2026-09-15 17:39:44 CST - Freeze E3.31 after all legal delivery recovery is exhausted

Tags: e331, classifier, delivery-recovery, shard-gate, no-rag, no-reject

### Result

E3.31 completed every legal R0/R1/R2 action for shard-00 and then stopped
fail-closed at exit code 2. The immutable freeze is
`outputs/artifacts/e331-classifier-full-v1/campaign/frozen-shard-gate-failure.json`.
Only shard-00 exists; no shard-01, E3.32/RAG artifact, Reject call, conversion,
SFT, or training was created. All training authorization flags remain false.

The durable terminal chain is: R0 21 semantic-correct, one quality-reject, and
42 unknown-delivery; Q1 one unknown-delivery; R1 14 semantic-correct, one
quality-reject, and 28 unknown-delivery; R2 seven semantic-correct, one
quality-reject, and 20 unknown-delivery; Q1-R2 repaired its separate quality
row to semantic-correct. The shard's final merge has 21 shortfalls: 20 R2
delivery shortfalls plus `e35-known-N04030-05f737b16f7d264a0d20`, whose
trace-bound Q1 was itself recovered by R1 but remained a quality-reject after
its only permitted quality repair.

Independent lineage checks verified all 43 R1 work items bind their direct
R0/Q1 predecessor, and all 28 R2 work items bind their direct R1 predecessor.
The four private-audit recoveries preserve frozen public trajectories and
generation IDs. No R3 manifest exists. Focused E3.22/E3.27/E3.28/E3.29 tests
now pass 28/28, including a regression that an exhausted quality repair also
hard-stops a shard.

### Next

Do not resume or rewrite E3.31. The current blocker is provider delivery
confirmation across R0/R1/R2, not the forced-planning, trace-bound Q1, HCV,
or tool-order contract. A future fresh lineage must first make a separately
validated transport-level change or use an externally verified healthy provider
route; it cannot replay E3.31 requests. Dynamic Top-3 RAG remains prohibited
until a new full Classifier lineage passes report, audit, and hard gate.

## 2026-09-15 18:05:51 CST - Current Micu SLB stability canary fails at no-data load

Tags: micu, slb, sol, provider-stability, no-data, no-replay, no-rag

### Result

An independent current-route canary was run after the E3.30/E3.31 freezes at
`outputs/artifacts/micu-slb-canary/v5-sol-post-e331-stability/`. It sent only
the fixed text request "Return exactly READY." to
`https://api-slb.micuapi.ai/v1` using `gpt-5.6-sol`: no image, label, candidate
card, RAG query, classifier card, or training datum was included. The immutable
contract requires two serial rounds of ten, 60-second requests and forbids
automatic replay.

Round 1 completed 4/10 valid deliveries and 6 `DeliveryUnresolved` events,
then fail-closed without starting round 2. Its report sets both
`generation_ready_for_e2_retry_v2=false` and
`generation_ready_for_dynamic_smoke=false`; the managed run exited 2. This
reproduces the exact provider delivery-confirmation class of failure under a
minimal payload, independently of the Classifier/RAG protocol.

### Next

Do not launch another E3.30/E3.31-style full collection on this current route.
Obtain a changed provider transport/endpoint or wait for external service
recovery, then run a fresh immutable no-data canary before authorizing any new
Classifier lineage. RAG, Reject, conversion, SFT, and training remain
prohibited.

## 2026-09-15 18:15:01 CST - User-specified Micu direct node fails isolated stability canary

Tags: micu, direct-node, sol, provider-stability, no-data, no-replay, no-rag

### Result

The user-specified `https://www.micuapi.ai/v1` node first passed read-only
reachability checks (site HTTP 200; `/v1/models` HTTP 401), then ran the
separate, canary-only managed configuration
`rag-micu-direct-stability-canary-v6`. It used the existing credential profile
and `gpt-5.6-sol`, but sent only the fixed text request `Return exactly READY.`
No image, label, candidate card, RAG query, classifier card, or training datum
was included.

Round 1 recorded 0/10 confirmed valid deliveries; all ten results are durable
`DeliveryUnresolved` outcomes. The contract therefore stopped before round 2,
with no automatic replay. The report is
`outputs/artifacts/micu-direct-canary/v6-sol-post-e331-stability/report.json`;
the managed run exited 2 at
`outputs/runs/rag/rag-micu-direct-stability-canary-v6/20260915T181406-b1af5ee4-a01/`.
The ledger records only unknown delivery. The initial manual endpoint comparison
used the wrong `yunwu` profile and is superseded. A credential-safe follow-up
using the exact `micu_slb` profile injected into this canary returned HTTP 403
from the direct node's authenticated `GET /v1/models`; the same key and request
form returned HTTP 200 from the existing SLB node and listed `gpt-5.6-sol`.
This identifies a direct-node API authorization/access-policy difference for
the shared key, without exposing it. The direct node's 0/10 confirmed chat
deliveries remain the deciding failure evidence.

### Next

Do not use this direct node for a fresh Classifier or RAG lineage. Preserve all
frozen E3.28/E3.30/E3.31 artifacts and wait for an externally verified healthy
provider route before another no-data canary. Reject, conversion, SFT, and
training remain prohibited.

## 2026-09-15 19:16:26 CST - Make future no-data canary failures safely diagnosable

Tags: micu, canary, delivery-accounting, no-replay, credentials

### Result

`src/agrinet/rag/micu_slb_canary.py` now copies only the durable ledger
exception class into a failed probe's `failure_type`, while preserving the
existing `DeliveryUnresolved` outcome and no-replay behavior. It intentionally
does not write exception messages, HTTP bodies, URLs, query strings, or
credentials. This permits a future canary to distinguish safe categories such
as `HTTPError` and `TimeoutError` without weakening the delivery contract.

`tests/test_credentials.py` adds a synthetic HTTP-403 regression: all ten
first-round failures are still terminal for that round, no second-round folder
is created, the summary exposes only `HTTPError`, and a deliberately
sensitive URL fragment is absent. Focused validation passed 8 tests, syntax
compilation, and `git diff --check`. Existing frozen canary artifacts were not
rewritten, and no provider request was sent for this code validation.

### Next

Use the improved summary only for a future newly authorized no-data canary on
a changed/healthy provider route. The direct node remains ineligible, and all
Classifier/RAG/Reject/conversion/SFT/training restrictions remain in force.

## 2026-09-15 19:17:50 CST - Provider-free E3 full-chain regression remains green

Tags: e322, e327, e328, e329, micu, regression, no-provider

### Result

The focused integration suite passed 36/36: credentials/canary safety plus the
E3.22 Classifier presample, E3.27 visual Top-3 RAG, E3.28 full Classifier,
and E3.29 dynamic full RAG contracts. This covers the Q1 repair and recovery
rules, frozen reuse provenance, immutable sharding, shared budgets, Classifier
hard-gate prohibition of RAG, dynamic `future_rag` derivation, and no-training
flags at their provider-free boundaries. `py_compile` and `git diff --check`
also pass.

No E3.28--E3.31 or canary process is active. The E3.31 terminal freeze remains
at `outputs/artifacts/e331-classifier-full-v1/campaign/frozen-shard-gate-failure.json`;
there is no new dynamic successor under `outputs/artifacts/e332-visual-top3-rag-full-v1/`.
No provider request was sent by this regression.

### Next

Wait for an external provider-route or authorization change, then run one fresh
no-data canary before creating any fresh Classifier lineage. Do not replay a
frozen lineage or create RAG/Reject/conversion/SFT/training work.

## 2026-09-15 19:20:37 CST - Guard direct canaries before chat-intent creation

Tags: micu, direct-node, authorization, canary, no-provider

### Result

The direct-node-only canary route now performs an authenticated `GET /models`
preflight with the same runtime credential before it creates its output root,
request ledger, or any chat-completion intent. The requested model must appear
as a string ID in the response. HTTP denial, timeout, malformed JSON, or a
missing model causes a concise safe failure without persisting a response body
or credential detail. The ordinary SLB canary remains unchanged and still
requires two serial 10/10 chat rounds for a stability pass.

Two new provider-free regressions verify that a denied direct preflight creates
neither output nor chat intent, and that a successful injected preflight is
followed by exactly 20 original-protocol calls. Focused credential/canary tests
pass 10/10, as do syntax compilation and `git diff --check`.

### Next

Do not rerun the current direct node: its known HTTP 403 will now be rejected
before chat spend. Wait for provider authorization or endpoint change, then use
one fresh direct-node canary; no formal Classifier/RAG lineage may use it unless
that canary passes.

## 2026-09-15 19:30:05 CST - Rule out GPG/profile mismatch for the direct-node denial

Tags: micu, gpg, credentials, direct-node, authorization

### Result

The local `apikey` helper successfully decrypted both `micu_main` (direct
`www.micuapi.ai`) and `micu_slb` profiles. Their nonreversible SHA-256 key
fingerprints match; no fingerprint or key value is persisted in this record.
The invoking environment contains neither `YUNWU_API_KEY` nor
`YUNWU_API_BASE_URL`, so `yunwu_environment` did not bypass GPG by selecting a
stale inherited override. This rules out local GPG failure, an incorrect local
secret, and environment shadowing as explanations for the direct endpoint's
HTTP 403.

The runtime previously loaded `micu_slb` even for the direct-node-only canary.
Although both current secrets are the same, that binding was made explicit:
`micu_main` is now selected only for `https://www.micuapi.ai/v1`, and all SLB
routes keep `micu_slb`. The direct config identifies its teacher as
`micu_main_canary_only`. Tests cover encrypted helper parsing and route-profile
selection; 11 focused credential/canary tests, syntax compilation, CLI show,
dry-run, and `git diff --check` pass. No provider request was sent.

### Next

The remaining cause is external direct-node authorization/access policy despite
the shared valid key. Ask the provider to inspect the direct-node HTTP 403 for
this account/key and its source-IP/WAF policy, then run a fresh no-data canary
only after they confirm a change.

## 2026-09-15 19:40:09 CST - Fresh SLB v6 canary remains below delivery gate

Tags: micu, slb, sol, provider-stability, no-data, no-replay, no-rag

### Result

At the user's request, a fresh registered `rag-micu-slb-stability-canary-v6`
was prepared, shown, dry-run, and launched once as managed PID `1242576`. It
used `https://api-slb.micuapi.ai/v1`, `gpt-5.6-sol`, and only the fixed text
request `Return exactly READY.`; no image, label, Classifier/RAG card, query,
or training datum was sent. The immutable configuration retained two serial
rounds of ten, 60-second timeout, and no automatic replay.

Round 1 completed 5/10 valid deliveries. Probes 4, 5, 6, 9, and 10 are durable
`DeliveryUnresolved` outcomes with safe `failure_type=UnknownTeacherDelivery`.
The contract fail-closed before round 2; the managed run exited 2. Artifact
consistency checks confirm report readiness flags are both false and the
round-2 directory is absent. The report is
`outputs/artifacts/micu-slb-canary/v6-sol-post-e331-stability/report.json`;
the run state is under
`outputs/runs/rag/rag-micu-slb-stability-canary-v6/20260915T193250-b1af5ee4-a01/`.

### Next

Do not begin a new full Classifier lineage on SLB: this 5/10 result is below
the required consecutive 20/20 confirmation gate. Preserve E3.28/E3.30/E3.31
and this canary; wait for a provider stability change before a new canary.
RAG, Reject, conversion, SFT, and training remain prohibited.

## 2026-09-15 20:01:17 CST - Validate bounded SLB delivery recovery and launch fresh E3.32

Tags: micu, slb, e332, classifier, delivery-recovery, no-replay, no-rag

### Result

Implemented a separate no-data `R0 → R1 → R2` SLB recovery canary. Each R1/R2
action creates a new provider intent in a separate ledger and carries only the
direct predecessor request ID; an ambiguous request is never replayed. It is
strictly bounded to ten R0 probes and at most one R1 plus one R2 successor per
unconfirmed probe. Any R2 unknown delivery is terminal and fail-closed. Thirteen
credential/canary regressions, syntax compilation, and diff checks pass.

The fresh managed v7 run exited 0. R0 had nine valid deliveries and one
`UnknownTeacherDelivery` on probe 7; a new R1 request directly bound to that
R0 request delivered valid `READY`; R2 was empty. The final 10-probe report is
`generation_ready_for_full_classifier=true`, retains no-replay and all
no-training flags, and is stored at
`outputs/artifacts/micu-slb-recovery-canary/v7-sol-post-e331/report.json`.

On this explicit recovery-canary evidence, E3.32 was created as a fresh
protocol/lineage, not a resume of E3.28/E3.30/E3.31. Provider-free prepare
passed: 525 unique rows = 32 read-only E3.22-v3 reuse + 493 fresh rows, 105
truth classes, six checkpoint bindings, 7×64+45 immutable shards, zero initial
provider intents, and false training flags. Relevant regression passed 19/19;
campaign dry-run and authenticated SLB `/models` preflight (HTTP 200, Sol
listed) passed.

E3.32 was launched once at managed PID `1250174`, run
`outputs/runs/rag/rag-e332-classifier-full-v1/20260915T195203-b1af5ee4-a01/`.
At this record it is volatile and running shard-00; bounded ledger inspection
has 26 trajectory ledgers, 100 intents, 96 confirmed deliveries, and no
unknown-delivery result. No shard outcome is final yet; no later shard or RAG
artifact has been created.

### Next

Monitor only this PID, its bounded logs, ledger, and immutable shard outcomes.
Allow later shards only after each completed shard has no terminal R2 delivery,
quality, or contract shortfall. Derive dynamic RAG only after E3.32 report,
independent audit, and hard gate all pass.

## 2026-09-15 20:22:46 CST - Freeze E3.32 on exhausted delivered Q1 quality repair

Tags: e332, classifier, q1, quality-contract, shard-gate, no-rag

### Result

E3.32's managed campaign ended with exit code 2 at
`outputs/runs/rag/rag-e332-classifier-full-v1/20260915T195203-b1af5ee4-a01/`.
The immutable shard gate is
`outputs/artifacts/e332-classifier-full-v1/campaign/frozen-shard-gate-failure.json`.
Only shard-00 exists; shard-01 and all dynamic RAG artifacts remain absent.

The bounded SLB recovery mechanism was not the failure mode: all 64 R0 rows
had confirmed delivery and both R1 and R2 manifests/outcomes were empty. R0
had 58 semantic-correct, one future-RAG, and five quality-reject outcomes.
Five trace-bound Q1 repairs were created with new predecessor-bound request
IDs; their final outcomes were three semantic-correct, one future-RAG, and one
quality-reject. The terminal shortfall is
`e35-known-N04017-0092a94364d27a0267a1`. Its Q1 request was delivered and its
private audit marked semantic correctness, but its public quality result was
still `fail`; the sole permitted Q1 was exhausted. It cannot be recast as a
delivery R1/R2 recovery or replayed.

### Next

Do not resume or modify E3.32. The next candidate must use a fresh protocol
and lineage that fixes this public Q1 quality-contract failure while retaining
the successful bounded delivery-recovery guard. No RAG, Reject, conversion,
SFT, or training is authorized.

## 2026-09-15 20:36:14 CST - Prepare and launch E3.33 calibrated-Q1 recovery lineage

Tags: e333, classifier, calibrated-q1, slb, delivery-recovery, no-rag

### Result

Created E3.33 as a fresh protocol and artifact root, preserving all frozen
E3.28--E3.32 artifacts. It uses the already-passed no-data SLB delivery design:
R0, then one new predecessor-bound R1 only after unknown delivery, followed by
at most one R2; no ambiguous request is replayed. Q1 now expressly excludes
classifier score/rank/name as visual evidence and requires calibrated
low/medium confidence with `INSUFFICIENT_EVIDENCE` when decisive traits are not
visibly resolvable.

Provider-free preparation at `outputs/artifacts/e333-classifier-full-v1/`
passed with 525 rows (32 read-only E3.22-v3 reuse plus 493 new), 105 classes,
six checkpoint bindings, 7×64+45 immutable shards, zero provider intents, and
all training flags false. Compilation, diff check, and focused regressions
passed 36/36. CLI show/dry-run passed. The authenticated SLB `/models`
preflight returned HTTP 200 and listed `gpt-5.6-sol`; no credential value was
recorded.

The campaign was launched once as managed PID `1263696`, run
`outputs/runs/rag/rag-e333-classifier-full-v1/20260915T203346-b1af5ee4-a01/`.
It is volatile and initially processing shard-00; do not infer completion from
the PID alone. RAG, Reject, conversion, SFT, and training remain unexecuted.

### Next

Monitor only the managed status, bounded logs, request/token ledgers, and
immutable shard outcomes. Stop on any terminal quality, contract, budget, or R2
delivery shortfall. Derive dynamic RAG only after the complete E3.33 Classifier
report, independent audit, and hard gate pass.

## 2026-09-15 21:46:29 CST - Freeze E3.34 local Q1 orchestration failure; launch E3.35

Tags: e334, e335, classifier, q1, lineage, no-rag

### Result

E3.33 correctly froze at shard-00: two delivered Q1 outputs remained quality
rejects, including the previously recurring `N04017` and `N04051` cases. The
public evidence showed two missing deterministic checks: Q1 needed exactly
three numbered visual observations and two genuine frozen-card candidate
rejection clauses. The shared Q1 implementation now enforces those checks
before private audit, and additionally treats generic, non-discriminating
symptom descriptions as insufficient for a closed-set assertion when decisive
visible evidence is unresolved.

E3.34 provider-free prepare passed the fixed 525-row source and its live
shard-00 delivered 254 provider results plus one safe unknown-delivery record.
It then exited 1 before a shard gate because an R0 parser/contract error without
a persisted public trajectory was incorrectly admitted into the trace-bound Q1
set. Three other Q1 generation requests had already delivered, but no Q1
outcome was written. E3.34 is retained unchanged and is not resumed or replayed.

The scheduler now permits Q1 only for a delivered R0 quality reject with both a
persisted parent path and matching parent SHA. An R0 error without that binding
is terminal and reaches the shard hard-stop directly. Compilation, diff check,
CLI show/dry-run, and 38 focused regressions passed. E3.35 provider-free prepare
again verified 525=32 read-only reuse+493 new rows, 105 classes, six checkpoints,
7×64+45 shards, zero initial intents, and false training flags. SLB model
preflight returned HTTP 200 with `gpt-5.6-sol`.

E3.35 was launched once as managed PID `1286637`, run
`outputs/runs/rag/rag-e335-classifier-full-v1/20260915T214200-b1af5ee4-a01/`.
At this record it is volatile in shard-00. No RAG, Reject, conversion, SFT, or
training has started.

### Next

Monitor only the E3.35 managed status, bounded ledgers, shard outcomes, and
freeze gate. Do not derive RAG unless the entire Classifier report, independent
artifact audit, and hard gate pass.

## 2026-09-15 22:04:00 CST - Freeze E3.35; launch E3.36 with bounded terminal-delivery exposure

Tags: e335, e336, classifier, slb, unknown-delivery, recovery, no-rag

### Result

E3.35 ended with exit code 2 at
`outputs/runs/rag/rag-e335-classifier-full-v1/20260915T214200-b1af5ee4-a01/`.
Its immutable shard-00 gate is
`outputs/artifacts/e335-classifier-full-v1/campaign/frozen-shard-gate-failure.json`.
The sole terminal shortfall was
`e35-known-N04030-05f737b16f7d264a0d20`: it exhausted the legal R0--R2
delivery-recovery chain and remained `unknown_delivery`.  The old E3.35 policy
permitted zero terminal unknown deliveries, so this is a policy gate rather
than a retry-orchestration replay opportunity.

Under the user's newly authorized threshold, a fresh E3.36 lineage was
prepared and launched.  Its immutable manifest records a terminal
unknown-delivery maximum fraction of 0.10, enforced independently per shard
and globally across the 493 new rows: 64-row shards permit at most 6, the
45-row shard at most 4, and the global limit is 49.  It still executes full
R0--R1--R2 recovery before a row can become terminal unknown.  Contract,
quality, private-leak, tool-order, and budget errors remain zero-tolerance.
Terminal unknowns remain explicit shortfalls and are excluded from the dynamic
RAG input; they are not relabeled as successful Classifier or RAG outcomes.

Provider-free preparation at `outputs/artifacts/e336-classifier-full-v1/`
verified 525 rows (32 E3.22-v3 reuse + 493 fresh), 105 classes, six
checkpoints, and 7x64+45 shards, with zero preparation intents and all training
flags false.  Focused regression tests passed 40/40, CLI show/dry-run passed,
and the managed campaign was launched once as PID `1294456`, run
`outputs/runs/rag/rag-e336-classifier-full-v1/20260915T220336-b1af5ee4-a01/`.
At the first bounded verification it was running, had nine confirmed provider
deliveries, and had no stderr.

### Next

Volatile: monitor E3.36's managed status, bounded ledgers, budget, and shard
outcomes before relying on it.  Do not launch a duplicate.  Do not derive RAG
until all Classifier shards, the E3.36 report, independent audit, and gate pass.
Reject, conversion, SFT, and training remain unexecuted.

## 2026-09-15 23:27:21 CST - Freeze E3.36 Option-Q1 contract conflict; launch E3.37

Tags: e336, e337, classifier, option, q1, slb, no-rag

### Result

E3.36 closed shards 00--02 safely: shard-00 had one terminal delivery unknown
(1/64, within its six-row limit); shard-01 had zero terminal unknowns after
R1 recovery; shard-02 had no delivery unknowns.  It froze at shard-03 with
exit code 2 and immutable gate
`outputs/artifacts/e336-classifier-full-v1/campaign/frozen-shard-gate-failure.json`.
The only shortfall was `e35-known-N05040-019f748e8bd456855614`.  Its R0 and
trace-bound Q1 were both delivered, but the Q1 output was deterministically
rejected for failing to compare every public Option choice.  The remaining
R0 delivery unknown recovered at R1, so delivery exposure was not the blocker.

The failure revealed a local contract mismatch: the shared Q1 prompt and
validator requested exactly two rejected alternatives for every question type,
while the Option trajectory contract requires comparison of OA/OB/OC/OD.  The
new E3.37 lineage fixes that mismatch without modifying E3.36: Option Q1 now
requires explicit A/B/C/D comparison and three visible-trait rejection lines
for all unselected public options; Open Q1 retains two.  Candidate validation
uses the actual public OA--OD list rather than a Top-3 card subset.  The 10%
terminal unknown-delivery policy and all other zero-tolerance gates remain
unchanged.

Compilation, diff check, CLI show/dry-run, and 41 focused tests passed.
Provider-free E3.37 prepare at `outputs/artifacts/e337-classifier-full-v1/`
again verified 525=32 reuse+493 new, 105 classes, six checkpoint bindings,
7x64+45 shards, zero preparation intents, and false training flags.  The
managed campaign was launched once as PID `1330411`, run
`outputs/runs/rag/rag-e337-classifier-full-v1/20260915T232502-b1af5ee4-a01/`.
At first bounded checks it was running, had 19 confirmed provider deliveries,
and had no stderr.

### Next

Volatile: monitor only the E3.37 PID, status, bounded ledger/token evidence,
and immutable shard outcomes.  Do not launch a duplicate or derive RAG until
the complete Classifier report, independent audit, and gate pass.  Reject,
conversion, SFT, and training remain unexecuted.

## 2026-09-16 00:01:43 CST - Correct E3.38 to sample-level terminal audit

Tags: e338, classifier, sample-level, shard-scheduling, rag-eligibility

### Observation

The prior E3.28--E3.37 controller treated a non-valid terminal within one
64-row execution shard as a reason to stop all later independent shards.  That
does not follow the recovery protocol: R0--R1--R2 binds retries, terminal
disposition, and downstream eligibility to individual `sample_id` lineages.
Shards only supply deterministic batching, the four-worker limit, immutable
checkpoint files, and transport-error resume boundaries.

### Decision

E3.38 is a fresh protocol that records every terminal shortfall at sample
level but completes all eight scheduling shards.  Only confirmed
`future_rag` sample IDs may enter the dynamic visual Top-3 RAG source.
`contract_shortfall`, `quality_reject`, budget shortfall, and terminal delivery
unknown samples remain in an explicit Classifier residual queue and cannot be
relabeled as RAG inputs.  Delivery exposure is computed globally over the 493
new rows under the user-authorized 10% maximum; it is not a per-shard gate.

The report separately records complete collection, safe RAG-input eligibility,
the residual queue, and global shortfall exposure.  The independent audit now
separates artifact/lineage integrity from global Classifier quality and no
longer adds a shard-local unknown threshold.  A partial RAG source additionally
requires the explicit audited partial-RAG-input gate; it is never authorized
merely because a shard completed.

### Evidence and next action

E3.38 provider-free preparation at
`outputs/artifacts/e338-classifier-full-v1/` passed: 525 rows = 32 immutable
E3.22-v3 reuse + 493 new rows, 105 classes, six checkpoint bindings,
7x64+45 scheduling shards, shared 4M/8k controls, zero preparation intents,
and all training flags false.  Its manifest explicitly sets
`shards_are_scheduling_only=true`; CLI dry-run, compilation, diff check, and
42 focused tests passed.  E3.37 remains volatile and was not altered or
duplicated.  Launch E3.38 only after E3.37 reaches a terminal state and its
artifacts have been inspected.
