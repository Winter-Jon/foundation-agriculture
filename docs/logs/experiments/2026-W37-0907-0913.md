## 2026-09-07 19:10:59 CST - Launch OpenAgri v3 Known-only ViT-B MAE comparison

Tags: open-agri-v3, vision, vit-base, mae, known-only, vitl-comparison

### Objective
Train and evaluate a ViT-B/16 model using the same Known-only disease/pest
classification protocol as the existing ViT-L route.

### Setup

- Config: `configs/experiments/vision/vision-openagri-v3-known-vitb-mae-v1.yaml`.
- Data: the independently built audit reports 107 Known classes, 142,726 train
  images, 547 Known-dev images, and 545 Known-only test images. MAE excludes
  all 1,831 dev/test SHA-256 values from 1,668,771 pretraining images.
- Model: `mae_vit_base_patch16_224`; the workflow now selects the MAE and
  classifier architecture from the experiment configuration, rather than
  assuming ViT-L.
- Resources: `CUDA_VISIBLE_DEVICES=4,5,6,7`, four A800 GPUs, local detached
  process. Per-GPU batch size is 64 (global batch 256).

### Validation

- The four-GPU 20-step MAE smoke completed successfully: reconstruction loss
  `1.0905182361602783`; encoder and full resume checkpoints were written under
  `outputs/artifacts/vision/openagri-v3-known-vitb-mae-v1/mae-smoke/`.
- The formal 100-epoch MAE process is running as
  `20260907T191004-fe65aa26-a01`; its status is `running` and it is resident
  only on GPUs 4--7.

### Next

After the formal encoder is complete, run the 50-epoch class-balanced
classifier fine-tune, select `model_best.pth.tar` by Known-dev Macro-F1, then
run the single Known-only private-test evaluation and compare it with ViT-L.

## 2026-09-09 01:20:00 CST - Raw Qwen3-VL-4B execution diagnostic completed

- Ran the original `models/Qwen3-VL-4B-Instruct` on GPU 1 through `scripts/vlm/diagnose_qwen3_vl4b_v2.py`. The process loaded successfully under `.venv_test` (Torch 2.13.0+cu130, Transformers 5.16.1), processed one local image, and generated 33 output tokens.
- Evidence: `outputs/artifacts/micu-classifier-hcv-v2/student-diagnostic/raw-4b.json`. Load time was 8.236 s, generation time 2.262 s, peak allocated memory 8,907,205,120 bytes on an NVIDIA A800-SXM4-80GB.
- This is an execution/resource diagnostic only; it has `training_eligible=false` and is not a labeled evaluation or distillation sample. The active goal remains incomplete because the frozen v3 dataset has zero classifier-independent Known fresh images for the required 32-image smoke.

## 2026-09-09 10:33:02 CST - Scheme B grouped OOF execution resumed

- The previously blocked fresh-image route was superseded by the user-selected Scheme B: three-fold near-duplicate-group out-of-fold classifier predictions. The existing fold-0 managed run remains active at `outputs/runs/vision/vision-openagri-v3-known-vitl-oof-v2/20260909T101843-fe65aa26-a01`; it was not duplicated.
- Fold-0 has written epoch-1 validation metrics and `model_best.pth.tar`; the managed status is still `running`, so these are intermediate artifacts and are not yet accepted as final fold output.
- Added `scripts/vision/convert_oof_predictions.py` and `scripts/vision/audit_oof_predictions.py` for provenance-bound Top-5 conversion and three-fold coverage/isolation checks. Added `--fold` override to the registered vision CLI, allowing folds 0--2 to use distinct artifact roots without serially editing the experiment YAML.
- Focused tests passed: `tests/test_convert_oof_predictions.py`, `tests/test_grouped_oof_manifests.py`, `tests/test_micu_classifier_hcv_v2.py`, `tests/test_classifier_distill.py`, `tests/test_classifier_ledger.py`, and CLI tests. `git diff --check` passed.
- Volatile state: fold-0 classifier training is still running on GPU 1. Next action is to wait for managed completion, evaluate the final best checkpoint on the held-out fold, convert it, then launch folds 1 and 2.

## 2026-09-09 10:50:00 CST - OOF folds parallelized and downstream source gated

- Fold-0 remains the original managed run `20260909T101843-fe65aa26-a01` on GPU 1; it has reached epoch 2/50 and remains `running`. Fold-1 `20260909T104516-fe65aa26-a01` and fold-2 `20260909T104522-fe65aa26-a01` were launched through the registered CLI on GPUs 2 and 3 respectively, with distinct fold artifact roots. All three GPUs show active classifier utilization.
- The CLI now accepts `--fold` and `--cuda-visible-devices` for grouped-OOF execution. Dry-runs for folds 1 and 2 matched the intended artifact paths and classifier command.
- Added the deterministic downstream builder `scripts/vision/build_oof_smoke_source.py`; it requires merged OOF predictions, frozen v3 class/image manifests, and historical exclusion inputs. It enforces Known-only rows, one near-duplicate group per smoke set, eight fixed cells x four images, private target-pattern assignment, and a complete exclusion sidecar.
- Updated the v2 execution contract to point at the OOF fold-0 checkpoint/training manifest and the v2 smoke artifact location; no readiness/live collection was run because final OOF provenance is not yet available.
- Validation: focused CLI, OOF-manifest, converter, source-builder, readiness, classifier-distill, ledger tests passed; `git diff --check` passed.
- Volatile state: all three classifier jobs are running. Downstream collection remains gated until all three final best checkpoints, held-out predictions, merged OOF audit, and 32-row source are present.

## 2026-09-09 11:06:58 CST - Scheme B OOF provenance and smoke gates hardened

- Registered `vision ... --operation oof-evaluate --fold N` now evaluates a finished fold's selected checkpoint on its `dev_known` held-out manifest; the command was dry-run verified without touching a running fold.
- OOF conversion/audit now checks fold identity, training-manifest hash and isolation, checkpoint hash, label-map hash, and ordered five-way prediction payloads. The v2 readiness contract now requires an accepted grouped-OOF audit and verifies that every 32-image source prediction exactly matches the audited merged OOF record.
- Source validation additionally requires byte-SHA correctness, one image/source/pHash group per smoke image, no v3 dev/test overlap, Known-only/non-simulated-Unknown samples, and exactly four entries in each Open/Option × EN/ZH × disease/pest cell.
- Validation: 59 focused tests passed across CLI, manifests, conversion, source construction, readiness, classifier-distill, and ledger modules; `git diff --check` passed.
- Volatile: fold-0 is at epoch 4/50 (latest held-out Macro-F1 0.8839); folds 1 and 2 have each written epoch 1. All three managed runs remain `running`.

## 2026-09-09 11:20:00 CST - Scheme B live parent collector gated and validated offline

- Added `src/agrinet/rag/micu_classifier_hcv_v2_collect.py` and registered `rag submit ... --operation micu-classifier-hcv-v2-collect`. The operation first requires v2 readiness, including the grouped OOF audit, source-to-merge binding, image isolation, and a healthy local v3 RAG service; it does not obtain Micu credentials before those checks pass.
- The collector projects only public fields into Micu input, exposes OOF Top-3/Top-5 only through actual classifier tool calls, invokes typed local visual/semantic RAG only, records provider/raw tool requests in the durable no-replay ledger, and enforces one classifier call, one expansion, cumulative RAG `0→1→3→5`, seven generation turns, and a terminal closure.
- Added a compact JSON/Markdown report generator at `scripts/report/summarize_micu_classifier_hcv_v2.py`. It reports OOF audit status, all parent trajectory states, private pattern allocation, G1/G2 count, and retains `training_eligible=false`.
- Validation: 66 focused tests passed across v2 collector, OOF conversion/source/audit, contracts, ledger, report helper, and CLI; `git diff --check` passed.
- Volatile: all three OOF folds remain active. Latest written states are fold-0 epoch 5 (Macro-F1 0.8871), folds 1 and 2 epoch 3 (0.8681 and 0.8749).

## 2026-09-09 11:35:00 CST - Parent request durability and derivation boundaries verified

- Parent request ledger records the actual public message sequence while replacing image data URLs with the image SHA reference; it contains neither private truth nor target pattern. OOF conversion now resolves Top-5 candidates to frozen English and Chinese display names for the teacher-facing classifier card, while retaining class codes only in provenance.
- Added isolated private-audit payload/parser plus G1/G2 preparation helpers. G1 receives only a completed public trace; G2 deterministically uses the prefix through the first completed tool result and skips parents without one. Private audit output is not wired back into parent generation.
- Validation: 67 focused tests passed and `git diff --check` passed.
- Volatile: managed OOF workers remain live. At the latest bounded check fold-0 is epoch 6/50 (0.8942), folds 1 and 2 are epoch 4/50 (0.8837, 0.8853); all three GPUs report active compute.

## 2026-09-09 12:04:39 CST - Scheme B derivation execution and global-budget gates completed offline

- The v2 collector now has durable, isolated execution paths for one private audit per parent, fixed-order one-parent-per-cell selection, one G1 public rewrite, and G2 continuation from the first actual public tool result. G2 reconstructs inherited controller state and executes any new tool calls rather than copying the parent suffix.
- Micu and local RAG have separate atomic, goal-wide ledgers at `outputs/artifacts/micu-classifier-hcv-v2/smoke-v1/global_micu_events.jsonl` (limit 340) and `global_rag_events.jsonl` (limit 200). Parent, audit, G1, and G2 requests share the Micu cap; public and private ledgers remain separate. All outputs retain `training_eligible=false`.
- The report helper now separates parent, private-audit, derivation, and shared-budget states. Focused collector/report/CLI tests passed (30 tests); both registered parent and derivation CLI dry-runs passed without obtaining credentials.
- Volatile: grouped OOF training remains the live gate. Latest bounded metrics are fold-0 epoch 9/50 (0.8898), fold-1 epoch 6/50 (0.8922), fold-2 epoch 6/50 (0.8984); all managed workers remain live. No Micu request has been sent.
- Environment observation: `.venv_test/bin/python` currently resolves to a missing/unexecutable uv Python target, so the later 32-image raw-Qwen diagnostic cannot be launched under its required environment until that project environment is restored. This is an environment-path issue, not a Qwen result or a reason to substitute `.venv`.

### Next

Wait for all three managed OOF folds to complete; evaluate, convert, merge and strictly audit them; construct the 32-row source; then start the local v3 RAG service and execute the registered parent collection.

## 2026-09-09 21:52:45 CST - Scheme B grouped OOF completed; smoke source and RAG readiness accepted

- All three grouped-OOF classifier managed runs finished successfully (`exit_code=0`) at epoch 50. Held-out `dev_known` prediction export also completed successfully for each fold. Prediction row counts exactly match their held-out manifests: fold 0 `47,839`, fold 1 `47,534`, fold 2 `47,353`. Final held-out Macro-F1 values were `0.909009`, `0.902635`, and `0.914261`, respectively.
- Converted fold results record selected-checkpoint and training-manifest SHA-256 provenance. The merged artifact is `outputs/artifacts/vision/openagri-v3-known-vitl-oof-v2/predictions_oof_v2.jsonl` with `142,726` rows.
- Strict audit initially exposed an implementation performance defect: the script rebuilt each fold's training identity sets and rehashed invariant artifacts per prediction. The incomplete audit process was terminated before it wrote output; the audit was patched to cache fold-invariant training identities and hashes while retaining every per-row provenance, overlap, and Top-5 check. Syntax, source-builder tests, and `git diff --check` passed.
- The resulting audit is accepted at `outputs/artifacts/vision/openagri-v3-known-vitl-oof-v2/oof_audit.json`: `ready=true`, `source_rows=merged_rows=142,726`, `unique_near_duplicate_groups=142,026`, and no errors.
- Built `outputs/artifacts/micu-classifier-hcv-v2/smoke-v1/source.jsonl`: exactly 32 unique-SHA Known rows, with four rows in every Open/Option × EN/ZH × disease/pest cell. All predictions are `out_of_fold`. Historical isolation blocked `7,629` SHA values plus related source/near-duplicate groups. Pattern allocation is natural OOF-derived (`P1=28`, `P2=2`, `P3=1`, `P4=1`), not a teacher instruction.
- The registered v3 RAG service is running at the prescribed local endpoint `127.0.0.1:8077`; subsequent readiness passed with no blockers and verified the expected `open_agri_v3_classes` / `open_agri_v3_images` collections.
- Parent Micu collection was not sent: local preflight failed while attempting `gpg --decrypt` of the configured `micu_slb` API-key store. No request, replay, or substitute teacher was used. This blocks parent traces, private audit, and G1/G2 until credentials are available.
- Started the independent original-Qwen source diagnostic with `.venv_test` through managed run `outputs/runs/vlm/vlm-qwen3vl4b-raw-diagnostic-v2/20260909T215229-5fc04488-a01`. It reads public source fields only, does not train/evaluate labels, and remains `training_eligible=false`.
- Volatile: 4B diagnostic is running; RAG service is running. Resume by checking these handles first, then resolve the existing Micu credential preflight and run one registered parent collection (never replay an unknown request).

## 2026-09-09 22:31:00 CST - Scheme B smoke executed; report is complete but 32-image coverage remains unresolved

- The raw source diagnostic completed under `.venv_test`: `32/32` public source rows, all status `complete`, original `models/Qwen3-VL-4B-Instruct`, GPU `cuda:0`, load `3.709 s`, total elapsed `104.065 s`. Evidence: `outputs/artifacts/micu-classifier-hcv-v2/smoke-v1/student-raw-4b/summary.json`; `training_eligible=false`.
- After Micu credentials were restored, the first parent collector revealed two separate runtime defects before full collection: a missing `_read_jsonl` helper (failed before creating any provider intent) and invalid reconstruction of native Chat Completions tool exchanges. The helper was added. The tool fix preserves the exact assistant `tool_calls` message and attaches the corresponding `tool_call_id` to the tool result; G2 prefix reconstruction now uses the same protocol. Focused collector/ledger/CLI tests passed (`43`); no parent intent was replayed.
- Two initial parent trajectories each have a delivered first tool call and a second request with `unknown_delivery`; their persisted request IDs are `ef557aaf4eef4e27a966dcad1226b0a9` and `fe00d88631b44e69aef43480f1a05f51`. They are reported as `unresolved_not_replayed`, per the no-replay contract. A third later parent trajectory (`oof-smoke-f89d485bad4df1b1370c`) has an additional `DeliveryUnresolved`; it was also not replayed. There is no supported provider-side delivery-status lookup in the current Micu integration.
- The repaired collector then completed `29` closed parent trajectories. Parent batch accounting is `29 closed`, `2 unresolved_not_replayed`, `1 not_closed`; reserved budget is `77` Micu and `20` local RAG at end of parent collection.
- The isolated derivation batch completed successfully. It audited all `29` closed parents (`21 accept`, `6 human_review`, `2 reject`), selected exactly one fixed-order accepted parent in each of the eight cells, and wrote `16` G1/G2 derivations. Of those, `15` closed and one G1 ended `DeliveryUnresolved`; no derivation request was replayed. Final total budgets: `144/340` Micu and `26/200` RAG.
- Final report artifacts: `outputs/artifacts/micu-classifier-hcv-v2/smoke-v1/report.json` and `report.md`. The report logic was corrected so `completion_ready` requires every source image to have a closed parent rather than merely 32 batch-status records. It correctly reports `oof_audit_ready=true`, `source_rows=32`, `closed_parent_count=29`, `complete_parent_coverage=false`, `completion_ready=false`, and `training_eligible=false`.
- Conclusion: the deliverable is a reproducible partial 32-image smoke, not a fully complete smoke. Do not fill the three missing source images with substitutions or retries. Completing 32/32 requires a provider-supported deterministic request-status reconciliation mechanism or explicit human authorization to retire/reissue those images under a new, separately versioned smoke package.

## 2026-09-10 00:11:00 CST - Authorized v2 replacement smoke completed with 32/32 parent coverage

- User explicitly authorized retirement and replacement of the three v1 unknown-delivery images. The v1 evidence and ledgers were preserved unchanged. `outputs/artifacts/micu-classifier-hcv-v2/smoke-v1/retired_unknown_delivery.jsonl` records the three retired sample IDs, SHA-256 identities, no-replay reason, and replacement authorization.
- Added `scripts/vision/build_oof_smoke_replacement_source.py`. It creates a new `smoke-v2` source by retaining exactly 29 v1 rows and deterministically replacing only the three retired images in their original cells, while excluding all historical identities plus the retired and retained image/source/exact-pHash groups. The v2 source validates to 32 unique image SHA/source/pHash groups and eight cells x four rows.
- Replacement mapping in `outputs/artifacts/micu-classifier-hcv-v2/smoke-v2/replacement_manifest.json`: two replacements in `open-en-disease` and one in `open-en-pest`. Every replacement retains audited `out_of_fold` prediction provenance. `training_eligible=false`.
- Added versioned v2 contract/experiment: `configs/sampling/micu-classifier-hcv-v2/contract-v2.yaml` and `configs/experiments/rag/rag-micu-classifier-hcv-v2-smoke-v2.yaml`. v2 readiness passed with no blockers against the original accepted OOF audit and the prescribed local v3 RAG collections.
- v2 collector now permits a `closed_referenced` result only when a v1 trajectory has exactly matching sample ID, image SHA, and `closed` status. The 29 v1 trajectories and parent audit verdicts are linked read-only into v2; no provider request or audit was repeated for them. All 3 replacement parent trajectories were newly collected and closed. V2 parent accounting is 29 `closed_referenced` + 3 locally `closed`, giving exact 32/32 parent coverage.
- V2 derivation referenced the 29 prior parent audits and executed new parent audits only for replacements. It selected one accepted parent in every fixed cell. G1/G2 produced 14 closed derivations; one G1 and one G2 remain `DeliveryUnresolved` and were not replayed. V2 reserved budgets are `49/340` Micu and `9/200` local RAG.
- The independent v2 raw Qwen diagnostic completed under `.venv_test`: 32 expected/32 recorded public source rows, all complete, original `models/Qwen3-VL-4B-Instruct`, `training_eligible=false`.
- Final v2 report: `outputs/artifacts/micu-classifier-hcv-v2/smoke-v2/report.json` and `report.md`. It reports `oof_audit_ready=true`, `source_rows=32`, `closed_parent_count=32` (`3` local + `29` referenced), `complete_parent_coverage=true`, `completion_ready=true`, and `training_eligible=false`. Focused report/collector/CLI/source tests passed and `git diff --check` passed.

## 2026-09-10 01:14:29 CST - Independent smoke-v2 data-quality audit: engineering pass, distillation gate fails

- Trigger: audit whether the current `smoke-v2` data is quality-qualified. This was a read-only audit; no teacher call, replay, training, or source replacement was performed. Worktree was already dirty from the v2 implementation.
- Source/provenance passed: rerunning `validate_smoke_source(...)` against the frozen v3 manifests and `predictions_oof_v2.jsonl` verified 32 byte-SHA-correct, unique image/source/near-duplicate groups; no dev/test overlap; all predictions are three-fold held-out OOF; and each of the eight fixed Open/Option x EN/ZH x disease/pest cells has four images. The upstream OOF audit remains `ready=true`, `142726/142726` rows, and no errors.
- Trajectory protocol passed: all 32 parent trajectories close with matching sample/image SHA. Actual action counts are 1--4 (contract maximum 7); classifier calls are 0--1; RAG calls are 0--2 (maximum 5). The retained evidence is composite lineage: 29 trajectories are immutable v1 symlink references and only 3 are newly collected under v2. This is valid recovery accounting, but it is not an independently newly collected 32-image sample.
- Private audit does not support training: only 23/32 parents are `accept` (71.9%); 6/32 are `human_review` and 3/32 are `reject`. All six human-review cases are Option questions whose public final is only a letter with no durable public option-to-class mapping. The three rejections are unsupported/conflicting answers. Therefore neither the full parent set nor the current G1/G2 outputs are a quality-qualified SFT set.
- Derivation quality is also insufficient: 8 fixed cells were selected, but only 14 of 16 requested G1/G2 derivations closed; two delivery-unknown results remain correctly unreplayed. Of the closed derivatives with audits, 10 are accepted and 3 rejected; one G1 output has no corresponding derivation audit. They are usable only as diagnostic artifacts, not supervision.
- Research coverage fails for a distillation claim: source allocation is P1=29, P2=2, P4=1; there is no P3/P5--P10 coverage, no formal Unknown, and no simulated Unknown. It can validate collection plumbing and easy candidate confirmation, but cannot establish candidate correction, candidate-outside-Top-5 discovery, uncertainty/stopping, routing, or disagreement-sampling value.
- Learnability/distribution limitation: teacher calls used temperature 0, but only sampled text/tool trajectories were saved; no teacher token probabilities/logits were collected. The outputs are heterogeneous (one-letter Option labels, class names, tool traces, and rewrites), so this cannot substantiate approximate teacher-distribution or unimodal-fit claims for Qwen3-VL-4B.
- Quality gate: engineering/protocol **pass**; provenance/isolation **pass**; training eligibility **fail** (also contractually `false`); research/pattern adequacy **fail**; possible accepted subset **conditional** only after stable answer-schema conversion, exclusion of rejected/unresolved records, and manual resolution of all six Option mappings.
- Evidence: `outputs/artifacts/micu-classifier-hcv-v2/smoke-v2/{source.jsonl,report.json,report.md,parent/summary.json,derivations/summary.json,replacement_manifest.json}`; `outputs/artifacts/vision/openagri-v3-known-vitl-oof-v2/oof_audit.json`. Next: retain this package as an engineering smoke; before any SFT, define a canonical answer representation and collect an independently sampled, audit-accepted, pattern-balanced exploration set.

## 2026-09-10 11:30:32 CST - E2 P6 classifiers and 160-image source accepted for live collection

### Objective

Establish the E2 data-process exploration inputs without starting SFT: three class-holdout P6 classifiers, independent Qwen/RAG pre-screen signals, and a balanced 80-targeted + 80-random source.

### Results

- All three 99-class P6 classifiers completed 50 epochs with exit code 0. Their best Known-dev Macro-F1 values are 0.913839 (group 0, epoch 2), 0.908347 (group 1, epoch 5), and 0.913503 (group 2, epoch 33).
- Best-checkpoint score-only evaluation completed for all held-out train-candidate images: 9,755 / 12,444 / 14,813 predictions. Each group excludes eight frozen Known classes; label-map overlap and held-out truth leakage into Top-5 are both zero.
- Qwen original-weight pre-screen and complete local AgriNet visual RAG pre-screen each completed 320/320 rows. RAG remains complete for P6 and may recover held-out classes.
- Frozen E2 source: `outputs/artifacts/micu-classifier-hcv-e2/exploration-v1/source.jsonl`, SHA-256 `b3f64909cad527842f576bdddaec1035f954581d1d32e5ce2b16e1a034a8962d`. It has 160 independent image/source/near-duplicate identities, eight cells x 20, arms 80 targeted + 80 random, and targeted P1--P10 quotas of eight each.
- P6 was increased before live collection from 11 to 40 rows (25% of E2), covering 16 of the 24 held-out classes across all three groups. This is adequate for process exploration but not a formal Unknown-generalization claim; missing classes are not duplicate-padded.
- Registered E2 readiness passed with no blockers: `outputs/artifacts/micu-classifier-hcv-e2/exploration-v1/20260910T032515Z-138e4315/readiness.json`.
- Validation: 15 focused tests passed; Python compilation and `git diff --check` passed. Every source/config/artifact remains `training_eligible=false`; SFT was not started.

### Current state

Volatile: the two-way concurrent 160-parent Micu collection is running as PID 1587111 under `outputs/runs/rag/rag-micu-classifier-hcv-e2-exploration-v1/20260910T112718-5fc04488-a01`. Existing public intents must never be replayed after unknown delivery.

### Next

Verify all 160 parent outcomes, run isolated parent audits with one observed primary pattern per image, derive G1/G2 for at most 20 accepted images and at most two per pattern, then generate the independent quality/cost/learnability audit. Do not start SFT.

## 2026-09-10 11:35:00 CST - Correct E2 P6 best-checkpoint metrics

### Note

The 11:30:32 record captured intermediate best values from an earlier bounded poll. Re-reading the complete 50-epoch histories gives final selected Known-dev best Macro-F1: group 0 `0.923327` at epoch 13, group 1 `0.917693` at epoch 10, and group 2 `0.922166` at epoch 23. Holdout prediction counts and zero-leakage conclusions are unchanged.

### Status

The correction is evidence-backed by each group’s `classifier/classifier_dev_metrics.json`; model selection continues to use `model_best.pth.tar`. No training or collection artifact was changed.

### Next

Continue monitoring the existing E2 parent PID; do not replay its two currently recorded unknown-delivery requests.

## 2026-09-10 17:23:09 CST - E2 160-image exploration closed; output remains non-training-qualified

Tags: micu, classifier, hcv, e2, data-process, retry

### Objective

Complete the planned E2 engineering/data-process exploration without starting SFT: account for 160 independent source images, perform isolated private audit and bounded G1/G2 derivations, and produce quality, cost, and learnability evidence.

### Results

- Parent collection `outputs/runs/rag/rag-micu-classifier-hcv-e2-exploration-v1/20260910T121119-5fc04488-a01` and audit/derivation collection `outputs/runs/rag/rag-micu-classifier-hcv-e2-exploration-v1/20260910T152225-5fc04488-a01` both exited 0. All 160 source IDs have exactly one parent accounting state: 70 `closed`, 17 `closed_existing`, 54 `not_closed`, and 19 `unresolved_not_replayed`; 87 usable closed parent trajectories exist.
- Of 84 eligible closed parents, isolated private audit returned 20 accept, 4 reject, 2 human-review, and 60 delivery-unresolved results. Observed primary patterns are P1=19, P4=1, P6=2, P7=2, P8=1, P9=1. Selection correctly retained only three accepted, tool-prefix-valid image groups (two P1 and one P4), within the cap of two per pattern and 20 total. G1 closed once; G2 closed three times.
- Final process metrics: 74 classifier calls, 59 RAG calls, mean 1.53 tool calls per closed parent, RAG truth-hit fraction 0.678, and 84 answered versus 3 insufficient-evidence stops. Micu ledger records 384 delivered, 4 invalid, and 126 unknown-delivery requests; 912,917 total tokens and mean latency 40.65 seconds.
- Final evidence is `outputs/artifacts/micu-classifier-hcv-e2/exploration-v1/audit/final.json` and `final.md`. It reports `completion_ready=true` and `training_eligible=false`. The source, parent, and derivation summaries are under the same artifact root.

### Issues

- Delivery instability, rather than audit/content rejection alone, prevented sufficient closed and audited coverage. The structured sidecar `outputs/artifacts/micu-classifier-hcv-e2/exploration-v1/audit/retry_pending.jsonl` has 131 rows: 130 retry-pending and one quarantined no-retry boundary record. Its scope inventory is 68 parent, 60 parent-audit, 2 G1, and 1 G2-audit records. Retries are not automatic: a future attempt must reconcile delivery state first, use a new version/batch/request ID, and preserve original ledgers.
- Three early P6 records exposed excluded-supervised-code provenance before the boundary fix. They remain preserved but quarantined in `public_boundary_exclusions.json`; they are excluded from qualified subsets, G1/G2, and retry.
- This is not a quality-qualified distillation corpus: G1/G2 coverage is only 1/3, observed patterns are sparse, teacher token logits are unavailable, and no SFT has started. The deterministic sampled teacher traces support process/learnability diagnostics only, not exact teacher-distribution matching or a student-quality claim.

### Next

Keep all E2 outputs training-ineligible. If a retry is authorized, create a separately versioned, state-reconciled retry batch with the configured future Micu concurrency of four; do not automatically replay, overwrite, or reuse original requests. E3 class-disjoint folds remain a separate unlaunched line.

## 2026-09-10 17:38:39 CST - E2 retry-v1 launched as a separate no-replay attempt

Tags: micu, classifier, hcv, e2, retry

### Note

After explicit user authorization, added `agrinet.rag.micu_classifier_hcv_e2_retry` and the registered `rag-micu-classifier-hcv-e2-retry-v1` experiment. The executor accepts only the 130 `retry_pending` rows from the final E2 sidecar, excludes the quarantined public-boundary row, requires the frozen source identities, requires no-automatic-replay and concurrency four, creates a fresh output root/manifest, and retains original request IDs solely as lineage. New provider calls receive new request IDs in new retry ledgers.

### Status

Focused retry/collector tests passed (22), Python compilation and `git diff --check` passed, and the registered dry-run reported 67 parent, 60 parent-audit, 2 G1, and 1 G2-audit retry records. Live retry run is volatile: PID 1700132, run directory `outputs/runs/rag/rag-micu-classifier-hcv-e2-retry-v1/20260910T173325-5fc04488-a01`, artifact root `outputs/artifacts/micu-classifier-hcv-e2/retry-v1`. Initial new ledgers confirm distinct request IDs. Early outcomes include new upstream unknown-delivery/invalid-response records, so retry-v1 is not yet a quality result.

### Next

Monitor this one managed run until terminal; do not launch a duplicate. Audit retry-v1 separately from the original E2 package and keep all outputs training-ineligible.

## 2026-09-10 18:29:59 CST - E2 retry-v1 terminal audit: complete accounting, no qualified corpus

Tags: micu, classifier, hcv, e2, retry, delivery

### Objective

Complete the user-authorized independently versioned retry batch, then prove its scope accounting, request isolation, and training boundary without modifying the original E2 package.

### Results

- Managed retry run `outputs/runs/rag/rag-micu-classifier-hcv-e2-retry-v1/20260910T173325-5fc04488-a01` exited 0. Its retry summary accounts for all 130 permitted sidecar records: parent 67 (4 closed, 63 not completed), parent-audit 60 (10 accept, 4 reject, 46 not completed), G1 2 (both not completed), and G2-audit 1 (not completed). The single original quarantined boundary record was excluded.
- Independent retry audit: `outputs/artifacts/micu-classifier-hcv-e2/retry-v1/audit/final.{json,md}`. It verifies expected and actual scope counts match, all 151 new retry intents have a terminal ledger result, all 151 new request IDs are unique, and their overlap with the original 130 request IDs is zero. The manifest records concurrency 4 and `automatic_replay_allowed=false`.
- New ledger outcomes are 39 delivered, 28 invalid-response, and 84 unknown-delivery. The retry therefore reaches terminal engineering accounting, but the upstream delivery limiter persists and qualified parent/audit/derivation coverage remains insufficient. All retry-v1 artifacts are `training_eligible=false`; SFT was not started.
- Validation: 23 focused retry/audit/collector tests passed; Python compilation and `git diff --check` passed.

### Conclusion

- Verified: the authorized retry scope is complete, isolated from original ledgers, and did not reuse request IDs or automatically replay.
- Verified: retry-v1 does not create a quality-qualified distillation corpus.
- Next: do not start another provider retry automatically. Any further retry needs explicit new authorization and a new versioned attempt; E3 remains separate and unlaunched.

## 2026-09-10 18:44:18 CST - Micu endpoint comparison selects authenticated SLB route for future E2 attempts

Tags: micu, endpoint, slb, e2, delivery

### Note

Compared the two user-supplied Micu paths using the same existing `micu_slb` credential, without sending images, labels, trajectories, or training data. The main path was tested only through `/v1/models`: it returned HTTP 403 on all three attempts. The SLB path returned HTTP 200 on all three `/v1/models` probes (approximately 0.5--0.7 seconds) and its list includes `gpt-5.6-terra`. The credential profile already resolves to SLB.

### Status

`configs/experiments/rag/rag-micu-classifier-hcv-e2-{exploration,retry}-v1.yaml` now explicitly specify `teacher_base_url: https://api-slb.micuapi.ai/v1`. The RAG CLI validates this E2 setting and injects it only into the child runtime environment, leaving the encrypted credential store and configs free of keys. Minimal no-image SLB chat probes returned HTTP 500, and one larger-minimal probe reached its timeout window before the observation transport disconnected; therefore the endpoint choice establishes authentication/routing, not a successful-generation guarantee.

### Next

Use SLB for any future explicitly authorized E2 attempt. Do not auto-retry the terminal retry-v1 failures; first require a fresh authorized version and recheck a minimal generation probe because service-side generation reliability remains an open risk.

## 2026-09-10 19:04:22 CST - SLB generation canary fails before E2 retry-v2 preflight

Tags: micu, slb, canary, delivery, e2

### Objective

Test the SLB generation surface independently of all E2 images, labels, candidates, RAG evidence, and training data before considering another E2 retry.

### Results

- Added the registered `rag-micu-slb-generation-canary-v1` operation. Its fixed contract is two serial rounds of ten independent, no-data `gpt-5.6-terra` requests; each has a durable new request ID and no automatic replay. The second round is permitted only when all ten first-round responses are strict valid `READY` completions.
- The managed run `outputs/runs/rag/rag-micu-slb-generation-canary-v1/20260910T185853-5fc04488-a01` terminated with exit 2, the intentional gate-fail status. Round 1 completed all 10 persisted intents: 1 valid delivered response and 9 `unknown_delivery`; all request IDs are unique and terminal, with zero unresolved intents. Round 2 was not created. Evidence: `outputs/artifacts/micu-slb-canary/v1/{round-1/summary.json,report.json}`.
- `report.json` records `generation_ready_for_e2_retry_v2=false`, `automatic_replay_allowed=false`, `contains_images=false`, `contains_labels=false`, and `training_eligible=false`. This isolates the blocker to the provider generation/delivery surface rather than E2 data, image payloads, classifier, RAG, prompts, or SFT.
- Validation: 27 focused tests passed; Python compilation and `git diff --check` passed.

### Next

Do not launch E2 retry-v2. Preserve the canary's request IDs and timestamps as supplier diagnostics; after the provider reports a generation-side repair, create a fresh versioned canary root and require two passing 10/10 rounds before any separately authorized small E2 preflight.

## 2026-09-10 19:21:06 CST - gpt-5.6-sol SLB canary improves but does not clear stability gate

Tags: micu, slb, gpt-5.6-sol, canary, delivery, e2

### Objective

Test the user-requested `gpt-5.6-sol` independently from the failed Terra canary, preserving the same endpoint, no-data boundary, independent-request ledger, and two-round 10/10 gate.

### Results

- Generalized the registered SLB canary to retain a permitted model name in its requests and reports, then created `rag-micu-slb-generation-canary-v2` with a separate `outputs/artifacts/micu-slb-canary/v2-sol` root. It only permits SLB-visible E2 teacher models (`gpt-5.6-terra`, `gpt-5.6-sol`); request IDs, ledgers, and output root are distinct from Terra.
- Managed Sol run `outputs/runs/rag/rag-micu-slb-generation-canary-v2/20260910T191434-5fc04488-a01` terminated with exit 2, the intended unmet-gate status. It completed 20/20 durable no-data intents with unique IDs and no unresolved ledger result. Round 1 passed 10/10 strict valid `READY` completions. Round 2 delivered 8/10; probes 6 and 8 are `unknown_delivery`.
- `outputs/artifacts/micu-slb-canary/v2-sol/report.json` therefore records `generation_ready_for_e2_retry_v2=false`; no E2 retry-v2 output or run directory exists. Sol is materially more available than Terra in this short canary (18/20 versus 1/10 valid), but is not stable enough for the predeclared consecutive-two-round gate. No image, label, E2 source, RAG evidence, original ledger, or SFT state was touched.
- Validation: 28 focused tests passed; Python compilation and `git diff --check` passed.

### Next

Do not relax the 10/10 × 2 gate based on the better first round. Give the supplier both model-specific canary summaries and terminal request ID/timestamp evidence; after a reported repair, run a fresh `gpt-5.6-sol` canary version before considering a separately authorized E2 retry-v2 preflight.

## 2026-09-10 20:01:24 CST - User-authorized Sol E2 retry-v2 preflight: terminal accounting, no qualified corpus

Tags: micu, slb, gpt-5.6-sol, hcv, classifier, e2, retry-v2, no-sft

### Objective

Under the explicit user-authorized risk exception following the failed Sol
readiness gate, execute only a separately versioned, 16-scope E2 preflight with
new IDs, a new ledger, concurrency one, no automatic replay, private auditing,
and a final delivery/quality audit.

### Execution and isolation

- Added the explicit selection manifest
  `configs/sampling/micu-classifier-hcv-e2/retry-v2-sol-preflight-selection.json`,
  which is a strict subset of the original retry sidecar: 8 parent, 5
  parent-audit, 2 G1, and 1 G2-audit scope. It excludes the quarantined
  boundary record, binds every row to its original request ID only for lineage,
  and records the failed 18/20 Sol canary as a risk exception.
- Registered `rag-micu-classifier-hcv-e2-retry-v2-sol-preflight-v1` with
  `gpt-5.6-sol`, `https://api-slb.micuapi.ai/v1`, concurrency one, a new
  output root, and `no-sft`/`no-replay` tags. The direct executor dry-run proved
  the 16-row scope partition and rejected non-Sol, non-one-concurrency, or
  non-manifest preflight attempts.
- Extended the collection primitive so an allowed teacher model is carried
  consistently in request payloads and fresh ledger bindings. Historical calls
  retain the Terra default, while the validator permits only Terra and Sol.

### Results

- Managed run
  `outputs/runs/rag/rag-micu-classifier-hcv-e2-retry-v2-sol-preflight-v1/20260910T193706-5fc04488-a01`
  finished with exit 0. It created 37 unique new provider intents, all with a
  terminal result: 34 delivered and 3 `unknown_delivery`. The three unknown
  outcomes were retained as terminal ledger evidence and were not replayed.
- The isolated retry-v2 audit at
  `outputs/artifacts/micu-classifier-hcv-e2/retry-v2-sol-preflight/audit/final.{json,md}`
  proves exact 16-scope coverage, 37/37 terminal intent accounting, uniqueness
  of every new request ID, and zero overlap with selected original request IDs.
  It also records Sol, SLB lineage, the explicit risk exception, concurrency one,
  and `automatic_replay_allowed=false`.
- Scope outcomes: parent 6 closed / 2 not completed; parent-audit 3 accept / 2
  reject; G1 1 closed / 1 not completed; G2-audit 1 accept. Thus all 16 scopes
  have terminal orchestration status, but not all produced usable artifacts.
- `training_eligible=false` and `sft_started=false` are explicit in the final
  audit and every generated trajectory/audit/derivation. No original E2, retry-v1
  or canary ledger was modified.
- Validation after the audit fix: 42 focused collector/retry/audit/contract tests
  passed with `PYTHONPATH=.`, and `git diff --check` passed.

### Conclusion and handoff

Sol materially improves delivery relative to Terra/retry-v1 in this bounded
preflight, but it does not produce a quality-qualified corpus: 3/37 provider
deliveries were unresolved, leaving two parent and one G1 scope incomplete. The
preflight fulfills its engineering/accounting purpose only. Do not start SFT,
do not automatically expand the batch, and do not replay the three failures. A
future expansion requires an explicit new authorization, a fresh versioned
selection and output root, and an evidence-based delivery gate decision.

## 2026-09-10 21:55:17 CST - E2 dynamic-route smoke source frozen; fresh Sol canary blocked locally

Tags: micu, sol, hcv, classifier, dynamic-routing, no-sft

### Goal

Implement the next gated E2 route: strict no-data Sol canary, then only if it
passes, 32 isolated images through Direct x3, Classifier x2, and Classifier+RAG
x1, followed by route selection and non-authorized student conversion.

### Verified state

- Added a separate dynamic controller and registered source, canary, collection,
  and conversion experiments. Direct provider requests are truly tool-free; the
  classifier and RAG rungs expose only their allowed real tools. Every attempt
  receives a separate ledger and private audit; a private verdict cannot enter a
  later teacher prompt.
- Frozen source: 32 images, 32 image groups, 4 per fixed cell, P1--P4 each 8,
  and zero overlap with exploration source on image/source/near-duplicate group.
  SHA audit passed 32/32. The available isolated half of the prescreen pool is
  wholly targeted; `random_arm_shortfall_recorded=true` prevents this smoke from
  being interpreted as the planned random-versus-disagreement comparison.
- Focused source/controller/collector/credential tests pass. The fresh canary
  submit did not start because `micu_slb` credentials require an interactive GPG
  private-key unlock. No canary artifact root, no image request, and no SFT was
  created.

### Resume

After unlocking the existing GPG key in an interactive terminal, run registered
`rag-micu-slb-dynamic-smoke-canary-v1`. Only a two-round strict 10/10 pass may
launch `rag-micu-classifier-hcv-e2-dynamic-smoke-collect-v1`; otherwise record
the terminal canary report and end this goal without image collection.

## 2026-09-10 22:00:21 CST - Dynamic-route Sol canary failed closed; no image collection

Tags: micu, slb, sol, canary, dynamic-routing, delivery, no-sft

### Result

After the interactive GPG unlock, the newly registered no-data Sol gate ran at
`outputs/runs/rag/rag-micu-slb-dynamic-smoke-canary-v1/20260910T215832-5fc04488-a01`
and ended with exit 2, the intended gate-fail status. Round 1 created ten fresh
independent ledgers and request IDs: 9 strict `READY` deliveries and one
`unknown_delivery`/`DeliveryUnresolved`. All ten intents have terminal results
and IDs are unique. Evidence is
`outputs/artifacts/micu-slb-canary/v3-sol-dynamic-smoke/{round-1/summary.json,report.json}`.

The 10/10 round condition failed, round 2 was not created, and the gate report
sets `generation_ready_for_e2_retry_v2=false`. No dynamic-route image collection
root exists, so no image, label, classifier card, RAG evidence, private audit,
student conversion, immutable freeze, or SFT was sent or created.

### Validation and next action

30 focused dynamic-controller/collector/audit/credential tests passed and
`git diff --check` passed. The provider generation surface remains below the
predeclared reliability gate; await an external provider stability change before
any separately authorized new canary version.

## 2026-09-10 22:29:58 CST - Dynamic-route visual CoT contracts implemented offline

Tags: micu, hcv, dynamic-routing, visual-cot, hermes, no-sft

### Result

The dynamic smoke controller now has a code-level, SHA-recorded canary gate:
only a matching `gpt-5.6-sol` SLB report with two independent 10/10 rounds and
`generation_ready_for_dynamic_smoke=true` can enter collection. The existing
v3 report remains a 9/10 failure, so the gate fails before any image request.

Accepted winners now require route-specific real tool use (Direct: none;
Classifier: predict; RAG: predict plus local RAG). A post-accept public rewrite
stage is registered to create a fixed four-section final CoT: visual
observations, candidate comparison, evidence, and uncertainty. It validates the
English/Chinese 1,400/900-character limits, three observations, explicit
nearby-class contrasts, frozen final answer, public projection and no private
or rank-as-evidence leakage. Option supervision writes the canonical class name
followed by its visible final letter.
For Direct, nearby-class names are supplied only from a SHA-matched frozen public
registry; absent or mismatched registry data fails before any rewrite request.
The actual local registry is
`datasets/AgriNet-1K/open_agri_v3/taxonomy/canonical_label_registry.jsonl`; its
SHA is `4fa642...00b8`, matching the dynamic source.

Conversion now emits Hermes `tool_call`/`tool` roles and verifies the complete
protocol instead of placing tool JSON in ordinary assistant text. Registered
collection, rewrite and conversion dry-runs resolve to the dynamic controller.

### Validation and next action

`py_compile`, `git diff --check`, and 43 focused dynamic-controller, Hermes,
collector and credential tests passed. No provider request, image collection,
rewrite, conversion or SFT was started by this implementation. After an external
service stability change, create a new immutable canary version; do not rerun or
overwrite the failed v3 canary.

## 2026-09-10 22:55:30 CST - New dynamic-route Sol canary v4 failed closed

Tags: micu, slb, sol, canary, dynamic-routing, delivery, no-sft

### Result

Created a new immutable no-data v4 canary root rather than reusing v3, then ran
`rag-micu-slb-dynamic-smoke-canary-v2`. Its first serial round persisted ten
unique terminal ledger results: eight strict `READY` deliveries and two
`DeliveryUnresolved` outcomes (probes 3 and 10). It therefore exited 2 as the
intended gate-fail state, did not create round 2, and writes
`generation_ready_for_dynamic_smoke=false` in
`outputs/artifacts/micu-slb-canary/v4-sol-dynamic-smoke/report.json`.

The v2 collection configuration points only at that v4 report and a distinct
`dynamic-smoke-v2/collection` output root. That root does not exist: no image,
classifier card, RAG call, audit, rewrite, conversion, or SFT was initiated.

### Next

Treat the current SLB generation service as below the hard reliability
threshold. Preserve v4 as terminal evidence; do not replay its two unresolved
probes or create another canary until an externally observable provider repair.

## 2026-09-10 23:50:32 CST - E3 adjacent class-fold DDP smoke passed; formal classifiers launched

Tags: open-agri-v3, e3, classifier, adjacent-classfold, ddp, a800

### Result

Verified the frozen E3 graph before launch: the source ranking SHA-256 remains
`a92a724a82d170cad97ba3b6b3c4a337650a20ce8f1382724488ccd4f8545676`; all
107 Known classes have a training-side directed same-domain witness; fold label
spaces are 71/71/72 and exclude their 36/36/35 held-out classes.

Added registered E3 fold experiments and a two-A800 DDP classifier path with
deterministic globally class-balanced sampling, exact no-padding distributed
validation, rank-0 checkpointing, per-device batch 32 and learning rate 5e-5.
The three 20-step smoke runs all exited 0 and produced the required model,
checkpoint, label map, and aggregated Known-dev metrics: fold 0 macro-F1
0.17593, fold 1 0.14083, fold 2 0.11975.

The gate therefore launched three independent 50-epoch formal runs at
`outputs/runs/vision/vision-openagri-v3-known-vitl-e3-fold{0,1,2}-v1/20260910T235014-5fc04488-a01`,
using GPU pairs 1,2; 3,4; and 5,6. All were observed running with high GPU
utilization immediately after launch. These runs use the completed formal
ViT-L MAE encoder. They report only Known-dev metrics for the training label
spaces; no held-out closed-set score, teacher collection, RAG fusion, or SFT is
authorized or started.

### Validation and next action

Focused adjacency and DDP sampler tests passed (5), configuration show/dry-run
resolved all three `torchrun --nproc-per-node 2` commands, and `git diff --check`
passed. Volatile: monitor each managed status and log to terminal completion;
then summarize best per-fold Known-dev metrics and checkpoint hashes.

## 2026-09-11 00:12:22 CST - Experimental E2 dynamic delivery recovery completes R0/R1/R2 accounting

Tags: micu, slb, gpt-5.6-sol, hcv, dynamic-routing, delivery-recovery, no-sft

### Objective

Test a separately lineaged R0/R1/R2 delivery-recovery controller for the frozen
32-image dynamic smoke. The experiment treats unconfirmed provider delivery as
an auditable replenishment gap, without rewriting the historical strict canary
result, reusing old request IDs, automatically replaying a request, or starting
SFT. It is an experiment record, not a revision to the formal dynamic-smoke
plan or its 10/10x2 readiness contract.

### Execution and result

- Recovery admission binds the v4 raw report SHA and accepts its single round
  of 8/10 strict READY responses with two unknown deliveries. The report still
  records `generation_ready_for_dynamic_smoke=false`; that historical field was
  neither changed nor treated as a strict pass.
- R0 manifest froze 32 independent image groups, four workers, and the existing
  340-Micu campaign budget. Managed collection run
  `outputs/runs/rag/rag-micu-classifier-hcv-e2-dynamic-recovery-r0-collect-v1/20260910T234350-5fc04488-a01`
  ended exit 0. Its ledger-backed outcomes contain 176 lifecycle events,
  reconciled into 175 logical scopes: every scope had confirmed delivery, so no
  provider delivery recovery item was eligible for R1.
- R1 and R2 were each created as immutable empty manifests and frozen empty
  summaries, rather than sending unnecessary provider calls. Final collection
  states over 32 image groups are 24 `selected`, 7 `quality_rejected`, 1
  `tool_shortfall`, and 0 `delivery_shortfall`. The tool shortfall is kept
  distinct from provider delivery and is not retryable as Micu replenishment.
- A conservative conversion gate was executed with the rewrite phase explicitly
  `not_started`: 0 student candidates admitted and all 32 groups excluded. Thus
  `training_eligible=false` and `sft_may_start=false` remain true. No rewrite,
  rewrite audit, conversion row, or SFT was launched.

### Evidence and validation

- Controller/tests: `src/agrinet/rag/micu_classifier_hcv_e2_dynamic_recovery.py`
  and `tests/test_micu_classifier_hcv_e2_dynamic_recovery.py`. The recovery
  suite covers v4 admission, immutable R0/R1/R2 lineage, audit-only recovery,
  terminal delivery shortfall, latest-attempt reconciliation, and conversion
  exclusion.
- Artifacts: `outputs/artifacts/micu-classifier-hcv-e2/dynamic-recovery-v1/`
  (`manifests/`, `rounds/`, `summaries/`, `reports/collection-final.json`, and
  `reports/conversion-gate.json`). The reconciled R0 summary is authoritative;
  the earlier `summaries/r0.json` retained a duplicate parent lifecycle record
  and is superseded by `summaries/r0-reconciled.json`, while raw outcomes remain
  unchanged.
- Validation after completion: 53 focused recovery/dynamic/collector/audit/Hermes
  tests passed; registered R0 `show` and `submit --dry-run` passed;
  `git diff --check` passed.

### Next

Do not auto-start rewrite, R1/R2 collection, conversion, or SFT. A later
explicit decision is required to authorize rewrite for the 24 collection
winners; any such phase must use the same immutable recovery lineage and admit
only rewrite-audit accepts through the conversion gate.

## 2026-09-11 00:22:42 CST - Clarify what multi-round delivery recovery solves

Tags: micu, delivery-recovery, dynamic-routing, reliability, no-sft

### Note

The R0/R1/R2 experiment solves the operational handling of unreliable provider
delivery, not the provider's underlying reliability. An `unknown_delivery`,
truncated response, invalid response, or incomplete ledger stops that image
group for its current round; only the same logical scope may receive a fresh-ID
attempt in the next frozen round, at most through R2. After that it is reported
as `delivery_shortfall`, never silently replayed, replaced with another image,
or converted into a quality rejection.

### Status

This campaign needed no replenishment: R0's 175 reconciled logical scopes all
had confirmed delivery, and R1/R2 were empty. This proves the recovery protocol
can complete a bounded, auditable collection despite v4's 8/10 canary outcome.
It does not prove the SLB service meets the original consecutive 10/10x2
stability requirement; `generation_ready_for_dynamic_smoke=false` remains the
correct historical strict-gate value.

### Next

Treat strict readiness and experimental recovery admission as distinct signals.
Do not infer broad provider stability or SFT authorization from the successful
recovery campaign; rewrite for the 24 selected groups remains a separate
explicit decision.

## 2026-09-11 00:55:51 CST - Rewrite recovery resolves one delivery gap but yields no qualified rows

Tags: micu, slb, gpt-5.6-sol, hcv, dynamic-routing, delivery-recovery, rewrite, no-sft

### Objective

Execute the separately authorized rewrite and isolated rewrite-audit phase for
the 24 selected collection winners, using the same R0/R1/R2 immutable-round
semantics and shared 340-intent Micu ledger. This is an experiment record, not
a change to the formal dynamic-smoke plan or its strict canary history.

### Result

- R0 used `rewrite/manifests/r0.json` and completed 24 independent rewrite
  requests. Twenty-three were confirmed delivered but failed the public rewrite
  contract (`quality_rejected`). One rewrite was `unknown_delivery`; because it
  lacked a closed rewrite, no audit was run for that group.
- The frozen R1 manifest contained exactly that one same-image, same-`rewrite`
  scope with a fresh request ID and predecessor lineage. Managed run
  `outputs/runs/rag/rag-micu-classifier-hcv-e2-dynamic-recovery-rewrite-r1-v1/20260911T005204-5fc04488-a01`
  exited 0. It confirmed delivery, but its rewrite also failed the public
  contract. It was not retried as a service-delivery issue.
- R2 was generated from the frozen R1 summary with zero work items and was
  frozen without a provider request. The campaign ledger is 242/340 intents.
- Final rewrite report: 24 `quality_rejected`, 0 `accepted`, 0
  `delivery_shortfall`, and 0 `tool_shortfall`. Consequently the conversion
  gate excluded all 32 collection groups (24 rewrite-quality rejects and eight
  collection groups without an eligible rewrite) and admitted 0 student rows.
  `training_eligible=false` and `sft_may_start=false`; no SFT was started.

### Evidence and implementation

- Immutable manifests/summaries: `outputs/artifacts/micu-classifier-hcv-e2/dynamic-recovery-v1/rewrite/{manifests,summaries}/`.
- Final evidence: `outputs/artifacts/micu-classifier-hcv-e2/dynamic-recovery-v1/rewrite/reports/final.json`
  and `outputs/artifacts/micu-classifier-hcv-e2/dynamic-recovery-v1/reports/conversion-gate-after-rewrite.json`.
- The controller adds `collect-round`, a shared-ledger R1 entrypoint, final
  rewrite accounting, and an audit-only recovery branch that reuses a closed
  rewrite instead of regenerating it if a future isolated audit is unresolved.
  Focused recovery/dynamic tests passed (17); `git diff --check` passed.

### Conclusion and next action

The protocol resolved the only uncertain rewrite delivery as designed, but it
cannot turn a confirmed content-quality failure into a training candidate. The
strict v4 canary remains `generation_ready_for_dynamic_smoke=false`. Do not
start SFT or broaden provider traffic; any new content-generation strategy needs
separate authorization and a new experiment lineage.

## 2026-09-11 01:23:07 CST - E2/E3 training-eligibility loop initialized

Tags: e2, e3, rewrite, contract-patch, training-eligibility, no-sft

### Result

- Created the append-only operational memory at
  `docs/loops/e2-e3-training-eligibility-v1/`. Its contract lock binds the E2
  exploration and E3 class-fold contracts to their SHA-256 values; the original
  contracts, strict canary history, old ledgers/request IDs, and SFT authority
  are explicit non-modifiable boundaries.
- Validated patch `0001-rewrite-structure-v2`, which applies only to future
  E2 lineages. It replaces free-form future rewrite generation with enumerable
  observations/comparisons before local rendering into the fixed public four
  sections. It does not relabel any historical failure as accepted.
- Offline analysis of all 24 recovery rewrites reports 24 missing observation
  lists, 24 missing comparison lists, and 15 Direct-route tool-boundary leaks.
  Thus the immediate E2 blocker is prompt/validator schema mismatch, not an
  unresolved provider delivery.
- Three E3 formal classifier workers remain live; no training process was
  duplicated. E2 provider work is deferred until E3 completion and cross-fold
  aggregation.

### Validation and next action

Focused loop, dynamic smoke, recovery, rewrite recovery, and E3 fold tests
passed (21). The next loop action is to monitor the three managed E3 runs to
terminal state, then evaluate and aggregate them. Training eligibility remains
false and SFT remains unauthorized.

### Qualification gate update

The loop now includes an explicit final data-qualification check: it binds a
frozen source and conversion-gate SHA, requires exactly 32 unique accepted rows
and four rows in each Open/Option x EN/ZH x disease/pest cell, and never enables
SFT itself. Applied to the completed recovery campaign it records zero accepted
rows and rejects both requirements. Focused tests now pass (27).

The next E2 risk-exception pilot is also prepared offline only: its preflight
requires a terminal E3 aggregate, the SHA-bound rewrite-v2 patch, and a new
eight-cell source. The source freezer rejects every overlap with declared
historical image/source/near-duplicate groups. It deliberately reports
`provider_request_authorized=false`; no candidate pool was frozen and no
provider request was made. Focused tests now pass (29).

E3 aggregation was hardened before the formal classifiers finish: it now rejects
any fold where held-out classes leak into the classifier label map or training
manifest, or held-out images overlap training images. A read-only preflight of
the frozen E3 artifacts confirms label spaces 71/71/72, held-out class counts
36/36/35, and zero class-label/image overlap. It does not create a false
closed-set holdout accuracy claim; the classifier cannot emit excluded classes.
The aggregate additionally binds the frozen RAG bridge witnesses: all 36/36/35
held-out classes have at least one training-side neighbour in their own fold.

Before terminal aggregation, E3 now requires a separate one-time Known-only
test evaluation for each selected best checkpoint. The registered
`e3-evaluate-test-known` dry-run resolves to each fold's `model_best.pth.tar`
and `test_known` split; it is intentionally deferred until the current formal
training runs finish. This keeps checkpoint selection (Known-dev), final
Known-only evaluation, and class-excluded generalization evidence distinct.

The E3 aggregate's complete-evidence path is now regression-tested as well as
its rejection paths: it can emit `generalization_evidence_complete=true` only
when all three terminal folds have 50-epoch selection evidence, independent
Known-only test metrics, checkpoint/label provenance, held-out isolation, and
all frozen RAG witnesses.

During the E3 wait, rewrite-v2 was completed offline for future lineages. The
R0 planner now must write the approved patch path and SHA into its immutable
manifest, and the collector rechecks that binding before using the structured
prompt/validator. V2 requires three distinct candidate comparisons as well as
three observations before local rendering. Historical v1 output and reports are
unchanged; no new source, provider request, conversion, or SFT was created.

## 2026-09-11 10:14:20 CST - E2 rewrite-v2 eight-cell pilot launched

Tags: e2, pilot, rewrite-v2, delivery-recovery, e3-running, no-sft

### Result

- The user authorized a narrow sampling validation before E3 completes. Added
  append-only patch `0002-e3-running-pilot-risk-exception.json`, parent-bound
  to rewrite patch `0001`; it preserves the historical strict canary, prior
  ledgers/request IDs, historical contracts, and SFT prohibition.
- Froze `2026-09-11-e2-rewrite-v2-pilot-source.json` from the 320-row
  prescreen pool. It has eight fresh, unique image/source/near-duplicate groups
  and exactly one row in each fixed cell, excluding both historical E2 sources.
- Generalized the recovery controller only for an explicitly balanced eight-row
  pilot: its manifests bind `source_rows_expected=8` and `pilot=true`; the
  formal 32-row source and four-per-cell training qualification are unchanged.
- Preflight records E3 as incomplete, `strict_canary_overridden=false`,
  `training_eligible=false`, and `sft_may_start=false`. It permits this one
  user-authorized new-lineage provider pilot, not automatic replay.
- R0 was dry-run successfully and then started as local managed run
  `outputs/runs/rag/rag-micu-classifier-hcv-e2-rewrite-v2-pilot-r0-collect-v1/20260911T021420-5fc04488-a01`
  (PID 1886919). Its separate output root is
  `outputs/artifacts/micu-classifier-hcv-e2/rewrite-v2-pilot-v1/rounds/r0`.

### Validation and next action

Relevant smoke, collection recovery, rewrite recovery, loop, E3 CLI, and
adjacent-fold tests passed (37); `git diff --check` passed for the edited
implementation/configuration files. At record time R0 remained running and had
not written `outcomes.json`; no quality or delivery result is claimed. Freeze
R0 only after the managed run reaches a verified terminal status, then generate
R1 exclusively from delivery-pending scopes. E3 folds remain volatile/running.

## 2026-09-11 10:37:40 CST - E3 aggregate complete; E2 rewrite-v2 pilot closes and formal R0 begins

Tags: e3, e2, rewrite-v2, formal-campaign, delivery-recovery, no-sft

### Result

- All E3 folds reached successful 50-epoch terminal state. Independent
  `test_known` evaluation for each selected checkpoint also succeeded. The
  immutable aggregate verifies 36/36/35 held-out classes, 47,174/48,057/47,495
  held-out images, no held-out label/image leakage, and a training-side RAG
  witness for every held-out class. Mean best Known-dev macro-F1 is 0.94318;
  the final Known-only macro-F1 values are 0.86943, 0.90816, and 0.88138.
- The E2 eight-cell rewrite-v2 pilot completed collection recovery with five
  selected collection winners and no terminal delivery/tool shortfall. Its
  single uncertain R0 private audit was recovered in R1 without regenerating
  the parent; R2 was explicitly empty.
- Of five v2 rewrite attempts, two completed both public rewrite validation and
  independent private rewrite-audit acceptance. Conversion contains those two
  unique candidate rows; three are confirmed quality rejections. This repairs
  the historical rewrite schema mismatch but does not satisfy the immutable
  32-row/four-per-cell qualification bar. Training eligibility and SFT remain
  false.
- Added/validated append-only patch `0003-e3-verified-formal-e2-campaign` and
  froze a new 32-row source with four fresh isolated image/source/near-duplicate
  groups per fixed cell. It excludes historical E2 sources and the pilot source.
- The formal R0 collection dry-run passed and began as managed local run
  `outputs/runs/rag/rag-micu-classifier-hcv-e2-rewrite-v2-formal-r0-collect-v1/20260911T023740-5fc04488-a01`
  (PID 1898649).

### Next action

Verify the formal R0 terminal status and freeze its summary. Generate R1/R2
only from delivery-pending scopes, then run rewrite-v2 exclusively on unique
collection winners. The conversion gate and 32-row qualification remain the
only route to potential data eligibility; no automation may start SFT.

## 2026-09-11 11:15:24 CST - E2 v2 formal closes; image-bound v3 repair begins

Tags: e2, rewrite-v2, rewrite-v3, image-binding, quality-analysis, no-sft

### Result

- The fresh 32-row rewrite-v2 formal collection completed all recovery rounds:
  27 selected groups and 5 confirmed quality rejections, with zero terminal
  delivery or local-tool shortfall. Its R1/R2 manifests were empty by design.
- All 27 v2 rewrite scopes were delivered. Twenty-one passed local rewrite
  validation; ten passed their independent private rewrite audit and conversion.
  The formal qualification artifact records 10 accepted rows, not 32, and lacks
  four accepted rows in every fixed cell. It remains training-ineligible and
  SFT remains disabled.
- Root-cause audit separates content quality from provider reliability: six local
  rejections were exclusively public reasoning length overflow. Private audit
  reasons repeatedly identify claims that no image was seen or that the fixed
  answer was followed. The v2 rewrite request supplied public JSON context but
  omitted an image attachment.
- Implemented and offline-tested future-only rewrite-v3. It attaches the frozen
  source image to rewrite generation, uses exact compact observation/comparison
  fields, and locally rejects non-visual or fixed-final-as-evidence claims.
  Existing v2 ledgers, contracts, strict canary history, and quality outcomes
  remain immutable.
- Patch 0004 and a fully isolated 32-row v3 source were validated. V3 R0
  collection was dry-run and launched as managed run
  outputs/runs/rag/rag-micu-classifier-hcv-e2-rewrite-v3-formal-r0-collect-v1/20260911T031524-5fc04488-a01
  (PID 1910742). At this record time it is live and has confirmed substantial
  ledger progress; do not duplicate it.

### Validation and next action

All 39 targeted E2/E3 tests pass, including image-bound v3 prompt/validator,
delivery recovery, conversion qualification, and E3 aggregate paths. Wait for
v3 R0 terminal status, freeze its summary, and generate R1/R2 only from its
unconfirmed delivery scopes. V3 rewrite may be planned only from final unique
collection winners.

## 2026-09-11 05:19:35 CST - E2 image-bound v3 completion R0 frozen; R1 recovery running

Tags: e2, rewrite-v3, completion-lineage, r0, r1, delivery-recovery, no-sft

### Result

- A distinct, identity-isolated 32-row P1-P4 completion lineage completed its
  R0 collection as managed local run
  `outputs/runs/rag/rag-micu-classifier-hcv-e2-rewrite-v3-completion-r0-collect-v1/20260911T042142-5fc04488-a01`
  with verified exit code 0. Its immutable source SHA is
  `e3962e2e17086ba78e210219fa98c624af8e1df27747a62cf6eaa0ea192a900d`.
- The frozen R0 summary at
  `outputs/artifacts/micu-classifier-hcv-e2/rewrite-v3-completion-v1/summaries/r0.json`
  reports 127 delivered scopes and 17 `unknown_delivery` scopes: 12 parent
  generations and 5 private parent audits. Confirmed quality rejects and human
  review outcomes remain excluded from delivery recovery.
- The immutable R1 manifest has exactly those 17 work items. Audit-only items
  reuse a closed public parent; parent recovery uses a fresh attempt/output root
  and preserves predecessor lineage. No old request ID or ledger is reused.
- R1 was launched as managed local run
  `outputs/runs/rag/rag-micu-classifier-hcv-e2-rewrite-v3-completion-r1-collect-v1/20260911T051237-5fc04488-a01`
  (PID 1947261). It was live at record time. Training eligibility and SFT
  permission remain false.

### Next action

Verify R1 reaches a terminal status and freeze it; generate R2 only from its
remaining unconfirmed delivery scopes. Do not plan rewrite until collection R2
and collection final are both frozen.

## 2026-09-11 05:52:00 CST - E2 image-bound v3 completion lineage closed; qualification remains false

Tags: e2, rewrite-v3, completion-lineage, delivery-recovery, qualification, no-sft

### Result

- Completion collection R0/R1/R2 all reached managed exit code 0. R0 had 17
  unconfirmed provider-delivery scopes, R1 had 7, and R2 confirmed all final
  recovery scopes. The frozen collection final has 18 selected winners and 14
  confirmed quality rejections, with no terminal delivery or local-tool
  shortfall.
- A planner defect surfaced only because a selected route could be closed in a
  recovery round: rewrite planning had consulted only one collection summary.
  It was fixed to read immutable R0/R1/R2 sibling summaries, preserving exact
  winner parent lineage. A regression test covers a winner closed in R1.
- The image-bound v3 rewrite R0 processed all 18 collection winners: every
  rewrite passed local validation, 16 independent private audits accepted, one
  rejected, and one audit was unconfirmed. Audit-only R1 reused its closed
  rewrite and returned a confirmed reject; explicit empty R2 issued zero new
  provider intents.
- Conversion has 16 unique accepted rows. Formal qualification is false because
  the frozen standard requires exactly 32 and four per fixed cell; observed
  counts are 3/3/1/2/0/2/2/3 across the fixed cells. Both
  `training_eligible` and `sft_may_start` remain false.

### Next action

Read-only audit whether another fully identity-isolated 32-row source remains.
Do not reuse this campaign's image, source, or near-duplicate groups and do not
replay confirmed quality rejects.

## 2026-09-11 05:54:00 CST - Frozen E2 prescreen pool is exhausted for another formal lineage

Tags: e2, source-exhaustion, identity-isolation, qualification, no-sft

### Result

- A read-only identity audit of
  `outputs/artifacts/micu-classifier-hcv-e2/prescreen/source-320.jsonl` excluded
  dynamic smoke, exploration, v2 pilot, v2 formal, v3 formal, and v3 completion
  sources across image group, source group, and near-duplicate group.
- Only 24 candidates remain, all identity-unique. Every fixed cell has exactly
  three remaining candidates, whereas the immutable campaign source contract
  requires four per cell (32 total). The frozen pool therefore cannot yield
  another legitimate formal source without reuse or a contract violation.
- This is source exhaustion, separate from provider delivery. It does not allow
  any replay of confirmed quality rejects. A new independently identity-audited
  pool and an append-only future-only provenance patch are required before a new
  campaign can be considered.
- The targeted E2 dynamic/rewrite/qualification and E3 regression suite passed
  42 tests after the recovery-summary winner-lineage fix. Training eligibility
  and SFT permission remain false.

### Next action

Preserve this exhausted pool as read-only. If a new source pool is built, audit
it against all six existing E2 lineages before freezing any new campaign source.

## 2026-09-11 06:07:00 CST - Rebuilt OOF prescreen-v2 is isolated; fresh v3 R0 begins

Tags: e2, prescreen-v2, oof, identity-isolation, rewrite-v3, delivery-recovery

### Result

- The original 320-row pool remains exhausted and read-only. A new pool was
  rebuilt from `predictions_oof_v2.jsonl`, the canonical registry, and the
  frozen 107-class OOF label map. The pool builder now directly excludes compact
  JSON/JSONL sources on image/source/near-duplicate identity, while retaining
  compatibility with its historical class-split input.
- `outputs/artifacts/micu-classifier-hcv-e2/prescreen-v2/source-320.jsonl` has
  320 unique values for each identity type, 40 rows in every fixed cell, and
  zero three-key overlap with all six prior E2 lineages.
- Patch `0006-rebuilt-oof-prescreen-v2-lineage` passed contract-lock validation.
  It preserves historical contracts/canary/ledgers/SFT prohibition and records
  rebuilt OOF provenance. The newly frozen source has 32 identity-isolated rows
  and four rows per fixed cell.
- Fresh collection R0 started as managed local run
  `outputs/runs/rag/rag-micu-classifier-hcv-e2-rewrite-v3-prescreen-v2-r0-collect-v1/20260911T060417-5fc04488-a01`
  (PID 1965244). It was live and writing new campaign ledger intents at record
  time; no outcome is yet claimed.

### Next action

Verify R0 terminal state before freezing it. Generate R1/R2 only from its
unconfirmed delivery scopes, and retain all prior campaign identities as blocked.

## 2026-09-11 16:38:00 CST - Sixth collection closed; seventh lower-pattern R0 started

Tags: e2, prescreen-v2, sampling-validation, quality-shortfall, lower-pattern, delivery-recovery

### Result

- Sixth stable-order collection completed with managed R0 exit 0, 86 delivered
  parent and 86 delivered audit outcomes, then explicit zero-request R1/R2
  closures. Its frozen final has 27 selected and five confirmed quality
  rejections, so the exact-32 phase barrier prevents rewrite and conversion.
- Read-only rejection sampling found quality evidence rather than transport
  uncertainty: classifier-led answers conflicted with image/private truth,
  including one adult-image versus larval-label mismatch. No rejection is
  replayable.
- The rebuilt-pool history gives directional pattern evidence (P2 18/19, P3
  8/11, P4 10/13, P5 5/6, P6 30/31 selected). Patch `0012` passed contract
  validation and permits only a future-only lower-pattern ordering test.
- The seventh source is frozen at 32 rows, four per fixed cell, and 32 unique
  image/source/near-duplicate identities. Its R0 is the managed local run
  `outputs/runs/rag/rag-micu-classifier-hcv-e2-rewrite-v3-prescreen-v2-seventh-r0-collect-v1/20260911T083654-5fc04488-a01`
  (PID 2026400 at last verification); its four new ledgers exist.

### Next action

Treat the seventh R0 as volatile: inspect its terminal status before freezing
anything. Generate R1 and R2 only from frozen predecessor summaries. Training
eligibility and SFT permission remain false.

## 2026-09-11 17:00:00 CST - Seventh collection closed below barrier; eighth R0 started

Tags: e2, prescreen-v2, lower-pattern, quality-shortfall, delivery-recovery

### Result

- Seventh R0 reached managed exit 0 with 87 delivered parents and 87 delivered
  audits. Its R1/R2 manifests were empty and their managed zero-request
  closures completed with exit 0. The final has 31 selected and one confirmed
  quality rejection, so rewrite is blocked by the exact-32 phase barrier.
- The rejected P3 open-en disease item was a healthy potato leaf repeatedly
  mistaken for a healthy soybean leaf; all confirmed routes were quality
  rejected. It is not replayable delivery work.
- Patch `0013` passed contract validation. Its eighth source has 32 rows, four
  per fixed cell, 32 unique values for every identity key, and lower-pattern
  ordering (24 P3, 8 P4). The sole eighth R0 run is
  `outputs/runs/rag/rag-micu-classifier-hcv-e2-rewrite-v3-prescreen-v2-eighth-r0-collect-v1/20260911T090022-5fc04488-a01`
  (PID 2036383 at last verification).

### Next action

Verify the eighth R0 reaches a terminal managed status before freezing it;
derive R1/R2 exclusively from frozen summaries. Training eligibility and SFT
permission remain false.

## 2026-09-11 09:23:03 CST - Eighth collection closes below barrier after one delivery recovery

Tags: e2, prescreen-v2, lower-pattern, quality-shortfall, delivery-recovery

### Result

- Eighth R0 completed with 77 delivered parent outcomes and 77 delivered private
  audits, except for one RAG parent marked `unknown_delivery`. Its frozen R1
  manifest contained exactly that scope; the fresh R1 parent and audit both
  delivered. R2 was the explicit zero-request closure.
- The final collection report is 26 selected and six confirmed quality
  rejections, with no delivery-pending or tool shortfall. The exact-32 phase
  barrier blocks rewrite, conversion, qualification, and SFT.
- The six rejected groups exhausted their confirmed route attempts and have no
  unresolved scope; they are terminal quality evidence, not retry work.
- A read-only exclusion audit of all 13 prior E2 source lineages finds 64 fresh
  prescreen-v2 identities remaining, eight per fixed cell (P4=20, P5=35,
  P6=9). This is capacity evidence only; it does not authorize a new campaign.

### Next action

Keep the eighth lineage immutable and blocked at collection. If continuing E2,
first create and validate a future-only provenance patch for a new source drawn
solely from the audited 64 identities. Training eligibility and SFT permission
remain false.

## 2026-09-11 09:29:03 CST - Ninth identity-isolated R0 starts from audited remaining pool

Tags: e2, prescreen-v2, remaining-pattern, delivery-recovery, sampling-validation

### Result

- Patch `0014-prescreen-v2-ninth-remaining-pattern-v3-lineage` passed
  contract-lock validation. It preserves the historical contract, strict canary
  history, old ledgers/request IDs, and SFT prohibition.
- The ninth frozen source has 32 rows, four per fixed cell, and 32 distinct
  image/source/near-duplicate identities. It excludes all 13 earlier E2 source
  lineages and selects remaining frozen patterns deterministically: P4=20 and
  P5=12. Its SHA-256 is
  `65e8a4fc5f5763e413733f0f2bf0ffa26e4bb09f63ae29f953015a5ff7fb12d1`.
- The R0 manifest contains 32 work items, four workers, a shared 340-intent
  Micu cap, recovery-canary admission, and false replay/training flags.
- `show`, `submit --dry-run`, `git diff --check`, and the focused E2/E3 loop,
  recovery, and rewrite tests all passed (29 tests). The managed R0 run
  `20260911T092809-5fc04488-a01` is live; at verification it had 9 closed
  parent trajectories and 7 private audits.

### Next action

Treat ninth R0 as volatile. Freeze it only after its managed terminal status
and `outcomes.json` are present; derive R1 and R2 exclusively from frozen
predecessor summaries. Training eligibility and SFT permission remain false.

## 2026-09-11 09:53:43 CST - Ninth collection closes with terminal delivery and tool shortfalls

Tags: e2, prescreen-v2, remaining-pattern, delivery-recovery, quality-shortfall, tool-shortfall

### Result

- Ninth R0 closed 87 delivered parents and 86 delivered private audits. R1
  recovered its RAG audit-only scope against the closed parent, but its fresh
  Classifier parent remained `unknown_delivery`; R2 issued its allowed second
  and final parent attempt, which also remained `unknown_delivery`.
- The frozen collection final reports 24 selected, six quality rejections, one
  local tool shortfall, and one terminal delivery shortfall. The 32-winner
  phase barrier blocks rewrite, conversion, qualification, and SFT.
- The quality-rejected groups have no unresolved scope; the delivery shortfall
  has exhausted both permitted recovery attempts. Neither is replayable.
- Read-only three-key exclusion over all 14 source lineages leaves exactly 32
  independent candidates, four per fixed cell, P5=23 and P6=9. It is capacity
  evidence only and requires a new future-only patch before any final lineage.

### Next action

Keep the ninth campaign immutable and blocked at collection. Before an optional
final lineage, validate a new provenance patch and freeze the remaining 32-row
source without changing contracts or enabling SFT.

## 2026-09-11 10:22:56 CST - Tenth final prescreen-v2 collection closes below barrier

Tags: e2, prescreen-v2, final-lineage, delivery-recovery, sampling-validation

### Result

- The managed tenth R2 run `20260911T102015-5fc04488-a01` exited 0 and wrote its
  outcomes. Its sole parent recovery and sole audit-only recovery were both
  `unknown_delivery` / `DeliveryUnresolved` after their second permitted fresh
  attempt. No third attempt was issued.
- The frozen collection final is 27 selected, three quality-rejected, and two
  delivery-shortfall groups. Per-cell selected counts are 1/3/4/3/4/4/4/4 in
  fixed-cell order open-en-disease, open-en-pest, open-zh-disease,
  open-zh-pest, option-en-disease, option-en-pest, option-zh-disease,
  option-zh-pest; the exact-32 barrier is not met.
- A deterministic one-winner-per-nonempty-cell sample (eight rows) verified
  matching source sample identity, accepted private audit, and recorded
  route-contract pass for every sampled winner. This is integrity evidence for
  selected rows, not authorization to bypass the missing five rows.
- The rebuilt prescreen-v2 pool is exhausted after this final identity-isolated
  source. Rewrite, conversion, qualification, and SFT remain blocked. Focused
  E2/E3 recovery regression suite: 29 passed.

### Next action

Keep all ten prescreen-v2 campaigns immutable. Do not create another source or
replay shortfalls. A future E2 attempt needs a newly independently audited pool
and future-only provenance authorization; until then `training_eligible=false`
and `sft_may_start=false`.

## 2026-09-11 11:34:37 CST - P6-first prescreen-v3 clears collection but misses rewrite qualification

Tags: e2, prescreen-v3, p6, rewrite-v3, delivery-recovery, qualification

### Result

- The independently frozen P6-first source completed collection R0/R1/R2 with
  32 selected winners. R0 confirmed 70 parent and 70 private audit deliveries;
  R1/R2 were formal zero-request closures.
- Image-bound rewrite-v3 closed at 28 accepted groups and four terminal quality
  rejections. One R0 rewrite `unknown_delivery` recovered in its sole R1 fresh
  attempt; R2 was empty. There are no rewrite delivery or tool shortfalls.
- Conversion gate has 28 unique rows; qualification is false because open/en/
  disease and open/zh/disease each have 3 rows and option/en/pest has 2. SFT
  remains forbidden.
- Future recovery code now recomputes route contract on audit-only recovery.
  Focused E2/E3 recovery, rewrite, and eligibility tests passed (31 tests).

### Next action

Preserve the P6-first campaign as immutable. Do not replay its quality rejects.
 Build and audit a new three-key-independent OOF pool before any future-only
 source or provider request; eligibility and SFT permission remain false.

## 2026-09-11 11:54:29 CST - Prescreen-v4 P6-first R0 sampling checkpoint

Tags: e2, prescreen-v4, p6, collection-r0, sampling-validation, delivery-recovery

### Result

- Submitted exactly one managed R0 collection run,
  `20260911T114114-5fc04488-a01` (PID 2097871), against the independently
  frozen 32-image v4 source and its immutable R0 manifest.
- At this checkpoint the process is live; its raw attempt root contains 64
  closed parent trajectories and 64 confirmed private audits: 31 accept, 32
  reject, and one human review. These are confirmed quality decisions, not
  delivery failures.
- `rounds/r0/outcomes.json` is not present yet, so no R0 summary, winner
  selection, replenishment manifest, rewrite, conversion, qualification, or
  SFT decision has been made.

### Next action

Wait for the managed run's terminal status and outcomes, then freeze the R0
summary exactly once. Generate R1 solely from that frozen summary; preserve
all current and historical ledgers, requests, contracts, and no-SFT flags.

## 2026-09-11 12:00:00 CST - Prescreen-v4 P6-first collection closes with one quality shortfall

Tags: e2, prescreen-v4, p6, collection, sampling-validation, quality-shortfall

### Result

- The single managed R0 run exited 0 and closed 66 parent plus 66 private
  parent-audit scopes. Its frozen summary contains no unresolved delivery
  scopes; R1 and R2 were therefore formal zero-request closures.
- The final collection report contains 31 selected groups and one terminal
  quality rejection (`e2-prescreen-e8ae0d1bdda288165b6f`), after all six of
  that group's prescribed route slots had confirmed delivery. There is no
  delivery or tool shortfall.
- The immutable exact-32 winner barrier is not met. No rewrite, conversion,
  qualification, or SFT action is authorized; `training_eligible=false` and
  `sft_may_start=false` remain in force.

### Next action

Keep the v4 lineage immutable and do not replay its quality rejection. Before
any further E2 collection, perform a new offline three-key candidate-pool
availability audit across every prior source lineage, then seek a separate
future-only provenance patch only if that audit supports a complete source.

## 2026-09-11 12:07:18 CST - Prescreen-v4 P1-first independent R0 started

Tags: e2, prescreen-v4, p1, collection-r0, sampling-validation, delivery-recovery

### Setup

- A read-only all-prior three-key availability audit found 48 remaining v4
  candidates, six per fixed cell, after excluding 19 prior sources including
  the closed P6-first source. Patch `0019-prescreen-v4-p1-first-rewrite-v3-lineage`
  passed contract-lock validation.
- The immutable P1-first source has 32 unique image/source/near-duplicate
  identities, exactly four rows per fixed cell, and only P1 candidates. It
  retains `automatic_replay_allowed=false`, `training_eligible=false`, and
  `sft_may_start=false`.
- One managed R0 submission is live as `20260911T120718-5fc04488-a01`
  (PID 2106278). It uses a fresh campaign root, manifest, request ledgers, and
  output paths; no rewrite, conversion, or SFT process has started.

### Next action

Verify the managed R0 terminal status and outcomes before freezing its summary.
Generate R1 only from that frozen summary; do not replay confirmed quality
outcomes or reuse any P6-first request identity.

## 2026-09-11 12:21:59 CST - Prescreen-v4 P1-first collection passes and rewrite R0 started

Tags: e2, prescreen-v4, p1, collection, rewrite-v3, image-bound, delivery-recovery

### Result

- P1-first collection R0 exited 0 with 65 delivered parent and 65 delivered
  parent-audit scopes. R1/R2 were immutable zero-request closures, and the
  final report has exactly 32 selected winners.
- Its rewrite manifest binds only those frozen winners, the image-bound compact
  observation/comparison-v3 schema, and the immutable schema patch `0004`;
  source-lineage authorization remains in patch `0019`.
- One managed rewrite R0 is live as `20260911T122159-5fc04488-a01`
  (PID 2111218). Conversion, qualification, and SFT remain disabled pending
  closed rewrite R0/R1/R2 plus accepted private rewrite audits.

### Next action

Verify the managed rewrite R0 terminal status and outcomes, freeze it, and
derive rewrite R1 solely from that summary. Any confirmed rewrite quality
rejection remains terminal; only unresolved delivery scopes may enter R1/R2.

## 2026-09-11 12:30:00 CST - Prescreen-v4 P1-first reaches formal training eligibility

Tags: e2, prescreen-v4, p1, rewrite-v3, conversion, qualification, no-sft

### Result

- Collection closed at 32 selected winners: R0 had 65 delivered parent and 65
  delivered private parent-audit scopes, while R1/R2 were zero-request
  closures.
- Image-bound rewrite-v3 closed at 32 accepted groups with 32 independently
  accepted private rewrite audits; rewrite R1/R2 were zero-request closures.
  There were no delivery, tool, route-contract, or quality shortfalls.
- Conversion accepts 32 unique rows and excludes none. Formal qualification has
  four rows in all eight fixed cells, no errors, and
  `training_eligible=true`. Focused E2/E3 regression suite: 31 passed.
- This does not authorize or start SFT. The immutable contract locks preserve
  `sft_may_start=false`; no training process was launched.

### Next action

Preserve the P1-first campaign and qualification artifacts as immutable
evidence. Any proposed SFT requires a separate explicit authorization decision
and must not be inferred from training eligibility alone.

## 2026-09-11 12:35:00 CST - P1-first converted-row spot check supports qualification

Tags: e2, prescreen-v4, p1, sampling-validation, rewrite-v3, conversion

### Result

- Read-only spot checks covered four accepted rows across open/option, English/
  Chinese, disease/pest cells. Each had an image-bound rewrite with no local
  validator errors, an independent private rewrite-audit `accept`, and a
  generated final consistent with the frozen private truth or correct option.
- The checked examples were tomato early blight (option B), a cricket-class
  pest, pomegranate anthracnose, and yellow stem borer (option D). Their
  visible image cues matched the public observations and uncertainty bounds.
- This qualitative check supports, but does not replace, the formal conversion
  gate: it already admits 32 unique rows, excludes none, and qualification has
  four accepted rows in every fixed cell. No SFT authorization changes.
