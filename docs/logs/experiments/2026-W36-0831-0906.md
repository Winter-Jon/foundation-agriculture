# Experiments — 2026-W36 (0831–0906)

## 2026-08-31 15:43:11 CST - 对照 M1 与当前 terminal-v5 Direct 数据及 Direct-618 子集

Tags: m1-direct, terminal-v5, data-distribution, direct-618, diagnosis

### Objective
在同一 Direct-618 manifest 上对 M1 与当前可用 Direct-only checkpoint 做数据分布和细分评测对照，区分已验证的差异与仍待消融的归因。

### Setup
- M1 training data: `outputs/vlm_sft/disease_pest_large/sft_messages_en_zh.jsonl`。
- Current data: `outputs/artifacts/datasets/m1-direct-current-hcv-v1/terminal-v5-m1-comparison-v1/`；4,076 行、1,019 图像、216 类，保留已审计的 M1-style reasoning renderer。
- Current checkpoint: tail `epoch-2/checkpoint-64`，由 terminal-v5 M1-like epoch-4 checkpoint 继续以常数 LR `5e-7` 训练两 epoch。
- Evaluation: native DP8 Direct-618、618/618 唯一预测、10,000 paired bootstrap。证据为 `outputs/runs/vlm/vlm-sft-qwen3vl4b-m1-direct-current-hcv-terminal-v5-m1like-tail-e2-lr5e7-8gpu-v1/20260831T140359-2ef993b1-a01/artifacts/epoch-2-checkpoint-64/formal-direct/artifacts/summary.json`。

### Results
- M1：2,114 行 / 1,077 图像 / 217 类，所有样本均为 Open；所有最终答案为类别名。
- Current：4,076 行 / 1,019 图像 / 216 类；每图固定四个相关视图（中英 × Open/Option），各 2,038 行。37 类低于五图、合计短 66 图，N04080 未出现。
- Current 的 Option 占一半行数，但其最终 A--D 字符只占极小 token mass；英语 reasoning 占 assistant 字符的 76.11%，中英行数平衡并不等于训练 token 平衡。
- Direct-618：current 51.62%，M1 67.64%，raw base 40.61%。current 对 raw 的 Open 提升显著（28.80% vs 3.88%），但 Option 仍低于 raw（74.43% vs 77.35%）。
- 对 M1 最大主要差距为 Open（-16.02 pp）、中文（-20.13 pp）、disease（-19.48 pp）与 Option（-15.86 pp）。交叉切片中 English disease Open 为 -20.91 pp，Chinese disease Option 为 -26.21 pp；English pest Open 接近（-2.22 pp），但样本量仅 45。

### Conclusion
- Verified: 早期 compact-converter 的信息压缩已被长链 renderer 修复；terminal artifact 的旧 interim 3,264 行逐字未变，新加入 812 行，不存在转换时覆盖或重标旧样本。
- Verified: 当前与 M1 的主要结构性差异是独立视觉样本更少、类别覆盖未完成、四视图强相关、静态 hard-negative 复用，以及把随机位置的单字母 Option closure 混入长 reasoning 目标。
- Assumed: Option 终端字母得到的直接 loss 权重过低，且随机候选位置加重了“识别语义类→映射到字母”的学习难度；这与低训练 loss 但 Option 低于 raw 的现象一致。
- Unknown: 这些因素各自的因果贡献尚未被完全隔离；不能仅由本对照断言补采图像质量差或 loss 未收敛。

### Next
优先做不改图像或真值的可控消融：同一 terminal 数据下比较 Open-only、当前四视图、以及加强 Option 答案闭合监督的派生视图；另以相同预算比较旧 interim 与 terminal 增量，以量化新增补采的净效应。

## 2026-08-31 16:49:57 CST - 生成不泄漏的 Direct 题型引导派生数据

Tags: m1-direct, conversion, task-guidance, option-closure, privacy

### Note
新增 `m1_comparison_task_guidance_v2` renderer 和受管操作 `m1-direct-terminal-reconvert-task-guidance-v2`。它仅依据 public `language` 与 `question_type` 在 `<think>` 开头加入固定任务说明：Open 要输出一个规范类别名；Option 要在题目给出的候选中选择，并在末尾只输出一个字母。它不消费或写入 private truth、类别 code、正确字母、字母—候选映射、图像路径或教师/audit 元数据。

### Status
已生成 `outputs/artifacts/datasets/m1-direct-current-hcv-v1/terminal-v5-m1-comparison-task-guidance-v2/`：4,076 行、1,019 图像、`training_authorized=true`、`quota_complete=false`。审计确认它与 v1 长链 artifact 的 sample ID 集和 image hash 完全一致，且 assistant 文本的 code、字母映射和本地路径扫描均为 0。47 个 M1 Direct/CLI 回归测试、`py_compile` 与 `git diff --check` 通过。

### Next
使用该 artifact 作为不改变图像或标签的 Option closure 引导消融来源；训练配置应独立版本化，并与 v1 保持相同预算作配对评测。

## 2026-08-31 17:00:30 CST - 修正题型引导至 user prompt 的 v3 派生数据

Tags: m1-direct, conversion, user-prompt, task-guidance, privacy

### Note
根据审阅反馈，v2 将题型约束写入 assistant `<think>` 的位置不正确。新增 `m1_comparison_user_guidance_v3` renderer 与 `m1-direct-terminal-reconvert-user-guidance-v3` 操作：Open/Option 的固定题型和输出格式说明现在只在 student user prompt；assistant `<think>` 仅保留视觉观察、候选比较、知识核验和不确定性。

### Status
已生成 `outputs/artifacts/datasets/m1-direct-current-hcv-v1/terminal-v5-m1-comparison-user-guidance-v3/`：4,076 行 / 1,019 图像，`training_authorized=true`。与 v1 sample ID/image hash 集严格一致；四种中英 Open/Option prompt 都包含 user-side guidance，`<think>` 无 Task/任务行。私有 code、字母映射和本地路径扫描均为 0。48 个 M1 Direct/CLI 测试、`py_compile` 与 `git diff --check` 通过。v1/v2 均未改写。

### Next
以 v3 作为正确的题型引导消融数据来源，使用与 v1 相同的训练预算和 Direct-618 协议比较 Option closure 是否改善。

## 2026-08-31 17:11:44 CST - 启动 v3 的严格 M1 策略 Direct-only SFT

Tags: m1-direct, user-guidance-v3, exact-m1, sft, direct-only

### Setup
- Data: `outputs/artifacts/datasets/m1-direct-current-hcv-v1/terminal-v5-m1-comparison-user-guidance-v3/data.jsonl`，4,076 行 / 1,019 图像。
- Strategy matched to historic M1: raw `Qwen3-VL-4B-Instruct` initialization, full parameter, 4 GPUs, per-device batch 1, gradient accumulation 16 (global batch 64), 5 epochs, LR `1e-5`, cosine, 5% warmup, bf16, SDPA/Liger, ZeRO-3, max length 2,048 and `delete` truncation.
- Registered experiment: `vlm-sft-qwen3vl4b-m1-direct-current-hcv-terminal-v5-user-guidance-v3-m1exact-4gpu-v1`; managed run `outputs/runs/vlm/vlm-sft-qwen3vl4b-m1-direct-current-hcv-terminal-v5-user-guidance-v3-m1exact-4gpu-v1/20260831T171144-2ef993b1-a01/`, PID 2944177.

### Status
Volatile: running. Startup log confirms `--nproc_per_node 4`, `--gradient_accumulation_steps 16`, `--max_length 2048`, `--truncation_strategy delete`, five epochs and 100% trainable parameters. DeepSpeed explicitly owns accumulation 16. The current data has 4,076 rows, so this five-epoch run contains 320 optimizer steps; historical M1 has 2,114 rows and therefore 165 steps under the same global batch 64.

### Next
Wait for managed SFT completion, verify trainer state/checkpoints, then run the unchanged native DP8 Direct-618 protocol per epoch checkpoint before choosing any model.

## 2026-08-31 17:22:54 CST - 在 GPU 4--7 启动历史 M1 原始数据独立复现

Tags: m1, reproduction, direct-only, qwen-base, four-gpu

### Setup
- Data: original `outputs/vlm_sft/disease_pest_large/sft_messages_en_zh.jsonl`, verified 2,114 rows, SHA-256 `9aa8026e2a5b759dac823139efe59377329f3d9ba437fa26ee3e0561585724f0`.
- Config: raw `Qwen3-VL-4B-Instruct`; full parameter; GPUs 4--7; per-device batch 1; accumulation 16/global batch 64; 5 epochs; `1e-5` cosine/5% warmup; max length 2,048/`delete`; bf16, SDPA/Liger and ZeRO-3.
- Managed run: `outputs/runs/vlm/vlm-sft-qwen3vl4b-m1-direct-reproduction-4gpu-v1/20260831T172254-2ef993b1-a01/`, PID 2946082.

### Status
Volatile: running concurrently with v3 only on the non-overlapping GPU set 4--7. Startup command confirms 4 distributed ranks, the original M1 JSONL and all declared M1 hyperparameters. This run intentionally does not launch an evaluation while v3 occupies GPUs 0--3; native DP8 Direct-618 can run after both SFT jobs release their training devices.

### Next
Verify both jobs independently through trainer-state completion. After device availability, evaluate the reproduced M1 checkpoint against the historical M1 checkpoint and v3 under the same Direct-618 manifest.

## 2026-08-31 18:50:57 CST - M1 重训在同 manifest 的原生 DP4 Direct-618 评测完成

Tags: m1, reproduction, direct-618, dp4, paired-bootstrap

### Setup
对重训 M1 `checkpoint-165` 在 GPU 4--7 执行 TP=1/DP=4 的原生 Direct-618；manifest、`max_new_tokens=2048`、scorer 和 paired bootstrap 均保持正式路线不变。候选、历史 M1 checkpoint 与 raw base 三条路由各有 618 条唯一预测、零错误行。

### Results
- 重训 M1: 60.84% overall；Open 39.16%，Option 82.52%，disease 69.48%，pest 41.67%。
- 同次重跑的历史 M1: 66.99%；重训相对差 -6.15 pp，95% CI [-9.71, -2.59]。
- 同次 raw base: 40.29%；重训相对增益 +20.55 pp，95% CI [+16.50, +24.60]。
- known: 51.45%（历史 M1 57.85%，-6.40 pp；raw 35.76%，+15.70 pp）；unknown: 72.63%（历史 M1 78.47%，-5.84 pp；raw 45.99%，+26.64 pp）。

### Conclusion
- Verified: 同一原始 2,114 行数据和相同主要训练参数可稳定重建对 raw base 的大幅 Direct 增益。
- Verified: 此次独立重训没有逐点复现历史 M1 checkpoint；差距在同次三路评测中仍显著。
- Unknown: 差距来自训练随机性、数据顺序、软件/依赖版本、硬件执行或历史 checkpoint 训练细节的哪一部分，尚未隔离。不能把 DP4 与此前 DP8 单独比较当作唯一因果证据；本次配对比较以内置同次历史 M1 为准。

### Next
等待 v3 严格-M1 SFT 完成后，用同一 DP4 路线对 v3 checkpoint 做并列评测；随后将重训 M1 作为新、同执行环境的参考基线。

## 2026-08-31 19:29:29 CST - 诊断 v3 严格 M1 终点低于 M1 的主要原因

Tags: m1-direct, user-guidance-v3, diagnosis, option-closure, data-distribution

### Evidence
- v3 `checkpoint-320` 的同 manifest TP=1/DP=4 Direct-618 为 49.19%，相对同次历史 M1 67.48% 的 paired delta 为 -18.28 pp（95% CI [-22.49,-14.08]），相对 raw 40.45% 为 +8.74 pp（95% CI [+4.69,+12.78]）。Open 25.24% 而 raw 3.88%；Option 73.14% 低于 raw 77.02%。
- 同一执行环境的原始 M1 重训为 60.84%，因此即使考虑独立重训相对历史 M1 的约 -6.15 pp 再现性差异，v3 仍比该同环境数据参考低 11.65 pp。
- v3 有 4,076 行但只有 1,019 独立图像，且每图恰好四个相关视图；M1 有 2,114 行和 1,077 独立图像。v3 一半最终答案是 A--D 字母（2,038 行），M1 的 2,114 个最终答案全部是类别名。
- 2,048/`delete` 不会截去 v3 的答案：`delete` 会整条排除超长样本；v3 assistant target token 中位数 521、P99 684、最大 809，均远小于 2,048。

### Conclusion
- Verified: 题型说明移至 user prompt 没有在终点解决 Option closure；当前 long reasoning + 随机字母答案的混合监督仍使 Option 低于 raw base。
- Verified: 仅提高 SFT 行数不等于提高视觉容量；四视图复用同一图像和静态候选，不能替代 M1 的独立视觉多样性。
- Assumed: 在长 `<think>` 后仅用一个字母作答案的 token-level 梯度过弱，且随机候选顺序要求每题重新完成语义类别到字母的映射；这同时解释了较高的训练 loss 平台与 Option 回退。
- Unknown: v3 的早期 checkpoint 是否优于终点仍待进行中的五 checkpoint DP4 队列确认；v1（无 user guidance）在完全相同 4-GPU/2,048/delete 预算下的对照尚未执行，因此不能把全部差异单独归因于 v3 prompt。

### Next
完成 v3 全 checkpoint 队列；随后按相同严格 M1 预算执行 v1 baseline 与 Open-only/Option-only 视图消融。优先用这些结果决定是修复 Option loss/closure，还是先补独立图像与 hard-negative 多样性。

## 2026-08-31 20:11:09 CST - 启动 terminal-v5 v3 Open-only 严格 M1 消融

Tags: m1-direct, terminal-v5, user-guidance-v3, open-only, controlled-ablation, sft

### Objective
直接检验四视图训练中 Option 的长推理加随机单字母闭合，是否是当前 Direct 能力低于 M1 的主要干扰因素。

### Controlled data derivation
- 新不可变 artifact：`outputs/artifacts/datasets/m1-direct-current-hcv-v1/terminal-v5-m1-comparison-user-guidance-v3-open-only-v1/`。
- 它只筛选已审计 v3 父数据中 `metadata.question_type == open` 的行；不重新采样、不重渲染、不改变 image、user prompt、assistant reasoning 或 canonical answer。
- 验证：2,038 行、1,019 张独立图像；Open/en/disease=669、Open/en/pest=350、Open/zh/disease=669、Open/zh/pest=350；`training_authorized=true`，SHA-256 为 `7c357416a18b947c8ea182a49b719794a6b4a4dd70df82ccf0f55d73e358fa42`。
- 新派生器 `m1-direct-terminal-derive-open-only-v1` 与其行级契约测试已通过；`tests/test_m1_direct_collection.py`、`tests/test_cli.py`、`py_compile` 和 `git diff --check` 均通过。

### Training
- Registered experiment：`vlm-sft-qwen3vl4b-m1-direct-current-hcv-terminal-v5-user-guidance-v3-open-only-m1exact-4gpu-v1`。
- Managed run：`outputs/runs/vlm/vlm-sft-qwen3vl4b-m1-direct-current-hcv-terminal-v5-user-guidance-v3-open-only-m1exact-4gpu-v1/20260831T201004-2ef993b1-a01/`，PID `3015495`。
- 配置严格沿用 M1：Qwen3-VL-4B base、full parameter、4 GPU、global batch 64、5 epoch、LR `1e-5` cosine/5% warmup、2,048/delete、bf16/SDPA/Liger/ZeRO-3。数据量对应 160 optimizer steps。启动检查显示 4 个训练 rank 正常，DeepSpeed 使用 accumulation 16，无 OOM/NCCL 异常。

### Next
训练后在相同 Direct-618 TP=1/DP=4 路线评测全部 epoch checkpoint，并同时报告 Open 与 Option；不能只因训练集为 Open-only 而省略 Option 泛化评测。

## 2026-08-31 21:36:00 CST - Open-only 完成：Option 并非 Open 退化的主要根因

Tags: m1-direct, open-only, four-view, ablation, direct-618

### Result
Open-only 队列完成五个 epoch checkpoint 的原生 TP=1/DP=4 Direct-618 评测。最佳为 epoch 3 / checkpoint 96：overall 50.81%，Open 25.57%，Option 76.05%。

在相同测试协议下，四视图 user-guidance-v3 的最佳 epoch 3 / checkpoint 192 为 overall 51.46%，Open 29.13%，Option 73.79%。因此移除 Option 后，overall -0.65 pp、Open -3.56 pp、Option +2.26 pp。两种条件的最佳点均在 epoch 3 左右，不支持“仅增加训练轮数即可修复”的解释。

同环境 M1 原始数据重训为 overall 60.84%、Open 39.16%、Option 82.52%；历史 M1 checkpoint 同次为 66.99%、44.66%、89.32%。Open-only 仍分别落后重训 M1 10.03/13.59/6.47 pp（overall/Open/Option）。

### Conclusion
- Verified: Option 样本不是当前 Open 能力落后 M1 的主要原因；移除它会损失 Open 的正迁移。
- Verified: Option 闭合仍有任务权衡：Open-only 的 Option 略高于四视图，但仍低于 M1 重训且未超过同次 raw base 的 76.70%。
- Next: 优先验证独立图像容量、类别覆盖和 hard-negative 多样性；若要继续研究 Option，应做带匹配 token 预算的答案闭合结构消融，而非简单删除 Option。

## 2026-08-31 21:45:00 CST - Direct M1 差距的机制复核：相似类是候选原因而非已证实主因

Tags: m1-direct, hard-negatives, class-coverage, mechanism-diagnosis, direct-618

### Verified checks
- 图像泄漏不是解释：对 M1 的 1,077 张训练图与 Direct-618 的 309 张唯一评测图重新计算 SHA-256，交集为 0；当前数据同样按 hash 隔离。
- Open-only 的独立图像数量与 M1 接近（1,019 对 1,077），不能把约 10pp 的总体差距简单归为图像总数少 58 张。真正差异是当前类别配额不完整：216 类中 180 类有 5 图，21 类有 4 图、9 类有 3 图、2 类有 2 图、4 类有 1 图；Direct-618 中 126/618 行来自不足 5 图的类别，另有 2 行来自完全未训练的 `N04080`。
- 当前 Open-only 的每条图像都保留三相似类比较；与 M1 一样，解析出的比较集合均为四个候选。当前每个真实类实际有 2--10 个候选集合（来自不同图像/补采），故“当前每类只固定复用一组 hard negative”的旧表述不成立。
- 候选质量尚未验证：当前 183 个类别被用作负例，最高频负例在 1,019 图中出现 126 次，多个高频负例跨很多目标类复用。M1 的候选集合来自另一旧数据/生成链路，不能由集合数量直接判断谁更 hard 或更贴近 Direct-618 的真实混淆。
- M1 与当前都有长候选比较、知识核验和规范答案；链路结构并非有无相似类的二元差别。M1 assistant 文本中位长度 1,579 字符，当前 Open-only 为 1,055 字符；该长度差异是潜在机制但未做长度匹配消融，不能视为质量优劣的证明。

### Inference
相似类别可能造成差距的准确机制是：当前静态 top-3 图文相似负例未必覆盖评测时模型实际混淆的近邻，且高频负例可能形成偏置；不是其候选集合数量必然少。要证实它，需要用训练/评测预测构建 confusion-aware negatives，并在图像、类配额、token 预算固定后与现有 joint-similar negatives 作配对消融。

### Other high-priority mechanisms
1. 不完整类别覆盖与每类图像不均衡；这是已量化的训练信息缺口。
2. 图像的视觉分布/拍摄条件与 Direct-618 的距离；当前仅证明 hash 不重叠，尚未量化域距离或类内形态覆盖。
3. 当前教师链中具体的视觉冲突是否对每张图都足够可见和判别；审计保证格式与可见性，不保证它是对测试混淆最有学习价值的反事实。
4. 中英链路与答案别名的语言分布；当前 Open-only 中英文行数平衡，但 M1 的语言构成、名称规范和文本长度不完全相同。
5. 优化数据顺序/软件再现性可解释约 6.15pp（M1 重训对历史 M1），但不能解释当前对同环境 M1 重训仍约 10pp 的差距。

### Next
优先做按类别配额和 Direct-618 混淆固定的 hard-negative 消融，且保留 Open+Option；随后再做 teacher reasoning 长度/判别性配对消融。

## 2026-08-31 20:37:15 CST - 汇总 Formal-618 三个里程碑的分子集结果

Tags: milestones, formal-618, direct, rag, subset-analysis

### Note
按同一 618-ID manifest 重算 M1 Direct bridge、M2 Hermes RAG 与 M3 strict Manual-JSON RAG 的 Known/Unknown、Open/Option、Disease/Pest、语言及三维交叉子集。结果写入 `docs/results/FORMAL618_MILESTONE_SUBSET_COMPARISON.md`。

### Status
Verified: 同 manifest 部署对比为 M1 66.99% (414/618)、M2 58.58% (362/618)、M3 60.52% (374/618)。M3 比 M2 +1.94 pp、比 M1 -6.47 pp；其主要 M1 差距是 Pest (-11.98 pp)、Option (-10.68 pp) 和 Known × Option × Pest (-29.41 pp)。M1 的 63.11% 历史 Direct 协议主里程碑分数保留为独立口径，未与 bridge 分数混用。

### Next
将该表用作后续候选的 618 子集汇报模板；对小样本 Unknown × Pest 格在新正式结果中补充 paired CI 后再作路线选择。

## 2026-08-31 20:37:15 CST - 审计 M1 Direct 自生成候选的可观测召回

Tags: m1-direct, formal-618, candidate-recall, audit

### Note
从 M1 Direct bridge 的 618 条 `scored.jsonl` 输出中，仅解析 `Candidate Analysis` / `候选分析` 段的明确项目符号候选列表；真实标签的英文名、中文名或 code 出现在该列表即计为 candidate hit。

### Status
Verified: 282/618（45.63%）输出含可解析候选列表。条件候选召回为 180/282 = 63.83%；将无候选列表严格计为未召回时，端到端可观测候选召回为 180/618 = 29.13%。候选命中时最终准确率为 180/180 = 100.00%；已列候选但未命中时为 5/102 = 4.90%。该指标不能与 M3 的 tool-return truth-hit recall 直接等同，因为 Direct 的候选文字是最终自由生成输出的一部分，且 336 条没有可解析列表。

### Next
若要与 RAG 的检索 recall 严格横比，需要在 Direct 推理时强制输出独立、固定 schema 的 top-k candidate ledger，再在最终答案前审计命中。

## 2026-09-01 01:13:43 CST - 最新历史 M1 独立复现的同协议 DP8 Direct-618 复测

Tags: m1-direct, reproduction, direct-618, dp8, reproducibility

### Objective
在刚完成的历史 M1 checkpoint 复测框架中，使用完全相同的 Direct-618 manifest、TP=1/DP=8、`max_new_tokens=2048`、当前 scorer 和 paired bootstrap，重新评估最新独立 M1 复现 checkpoint，并同轮刷新历史 M1 与 raw base 对照。

### Setup
- Candidate checkpoint: `outputs/vlm_sft/qwen3_vl_4b_m1_direct_reproduction_4gpu/v0-20260831-172308/checkpoint-165`（完整 5 epoch / 165 steps）。
- Historical M1 checkpoint: `outputs/vlm_sft/qwen3_vl_4b_disease_pest_full_all_e5_len2048_liger_lr1e5_final/v0-20260531-231355/checkpoint-165`。
- Manifest: `outputs/vlm_eval/qwen3_vl_4b_rag_sft/bounded_latest_sft_rag_test_20260804/manifest.jsonl`，618 条唯一 ID，SHA-256 `80d79f3ac4860d17ac3612265254240b79d29babf310c94d1c0b3b86d5fc0508`。
- Output: `outputs/runs/vlm/vlm-direct-m1-reproduction-reevaluation-formal618-dp8-v1/20260901T000000-2ef993b1-a01/artifacts/summary.json`。

### Results
- Latest reproduction: 60.84% (376/618); Open 39.48%, Option 82.20%.
- Same-run historical M1: 67.15% (415/618); Open 45.31%, Option 89.00%.
- Same-run raw base: 40.29% (249/618); Open 3.88%, Option 76.70%.
- Reproduction vs historical M1: -6.31 pp, paired bootstrap 95% CI [-9.87, -2.59] (10,000 samples; seed 20260819).
- Reproduction vs raw base: +20.55 pp, paired bootstrap 95% CI [+16.50, +24.60].
- All three routes: 618 predictions, 618 unique IDs, 0 explicit errors, 0 protocol errors, and 0 unparseable outputs; per-row correct counts equal metric numerators.

### Conclusion
- Verified: the latest M1 reproduction remains materially below the historical M1 checkpoint under the same DP8/2048 framework, while preserving a significant improvement over raw base.
- Relation to prior results: the new 60.84% matches the prior DP4 reproduction point estimate exactly, while same-run historical M1 varies within its previously observed sampling band (67.15% here).
- Unknown: the root cause of the reproduction gap (training stochasticity, ordering, software, hardware, or unrecovered historical training detail) is not isolated by this evaluation.

### Next
Use this DP8 result as the reproducibility reference; do not promote the reproduction as a replacement historical-M1 milestone without an experiment isolating the training-side gap.

## 2026-09-01 01:13:43 CST - 历史 M1 未逐点复现的训练侧工件审计

Tags: m1-direct, reproduction, training-reproducibility, environment-audit

### Objective
定位最新 M1 独立复现（60.84%）低于历史 M1（同轮 67.15%）的可验证训练侧差异。

### Verified checks
- 输入数据未发现差异：两者均声明 `outputs/vlm_sft/disease_pest_large/sft_messages_en_zh.jsonl`；复现冻结记录为 2,114 行、SHA-256 `9aa8026e2a5b759dac823139efe59377329f3d9ba437fa26ee3e0561585724f0`。当前文件仍为相同 2,114 行；1,077 个图像引用均存在。
- 有效训练参数一致：两个 checkpoint 的 `args.json` 均记录 seed/data_seed 42、dataset shuffle、4 GPU global batch 64（batch 1 × accumulation 16 × 4）、5 epochs/165 steps、full tuning、bf16、SDPA、Liger、ZeRO-3、max length 2,048、delete 截断、AdamW fused（weight decay 0.1，betas 0.9/0.95）、cosine LR 1e-5、warmup 0.05。
- 学习率逐步一致：165 个 step 的 LR 值逐点相同；两者都完整达到 step 165 / epoch 5。保存策略不同（历史按 step，复现按 epoch），但均为 model-only 保存，不能解释训练过程中已出现的分叉。
- 分叉从首步即出现：step 1 loss 为历史 2.137685、复现 2.156307；其后曲线持续不同。复现末步 loss 更低（0.141339 vs 0.158935），但 Direct-618 泛化低 6.31 pp，因此差距不是未收敛或末端保存失败。
- 已证实的环境版本差异：历史 checkpoint `config.json` 写入 `transformers_version=5.9.0`；复现为 `5.8.1`。当前复现环境还记录 torch 2.11.0、DeepSpeed 0.18.9、accelerate 1.13.0、Liger 0.8.0、ms-swift 4.3.0.dev0。历史作业没有保留完整 Python/CUDA/驱动/DeepSpeed/Liger 版本快照或可执行命令。
- 代码差异不是已知 Direct-SFT 改动：从导入 ms-swift 基线到当前，仅检出 manual-json tool-call 模板的局部修改；它不适用于 `template=qwen3_vl` 的单轮 Direct 数据。

### Conclusion
- Verified: 数据、核心超参、步数、学习率日程和当前 Direct-SFT 代码路径不足以解释差距；两条优化轨迹在第一步已经分叉。
- Most likely, unisolated: 历史与复现的依赖栈（至少 transformers 5.9.0 vs 5.8.1）及其与 CUDA/DeepSpeed/数据加载多进程的交互，和/或历史首批 shuffle/worker RNG 状态。
- Not supported: 将 6.31 pp 归因为评测框架、少训练、不同数据文件、截断策略、LR 日程或 checkpoint 保存策略。
- Unknown: 没有保存历史的完整环境锁、启动命令、GPU/CUDA 指纹和每 epoch 样本顺序，当前不能把差距归因到其中某一项。

### Next
创建最小隔离矩阵：固定当前数据与 4 GPU 运行形态，仅切换 transformers 5.8.1/5.9.0；同时记录依赖 lock、GPU/CUDA 指纹、每 epoch 采样顺序和每步 RNG 状态。先以短程（例如 10--20 steps）比对 step-1/step-10 loss 和权重哈希，再决定是否进行完整 165-step 对照。

## 2026-09-01 01:59:21 CST - Transformers 5.9.0 隔离环境预检阻止 M1 双训练队列

Tags: m1-direct, transformers-5.9.0, environment-audit, zero3, preflight

### Objective
在不改动当前 `.venv`、历史数据或 terminal-v5 v3 freeze 的前提下，验证 Transformers 5.9.0 是否可作为历史 M1 复现与 v3 对照的隔离训练环境；只有两条 2-step ZeRO-3 smoke SFT 均通过后才启动 tmux 正式队列。

### Setup
- Isolated environment: `.venv_test` with Transformers 5.9.0, Torch 2.11.0+cu130, DeepSpeed 0.18.9, Accelerate 1.13.0, ms-swift 4.3.0.dev0, qwen-vl-utils 0.0.14 and baseline-matched Liger 0.8.0.
- Inputs: historical 2,114-row M1 JSONL (`9aa802…724f0`) and immutable v3 4,076-row JSONL (`7e07d1…1adee`).
- Evidence root: `outputs/artifacts/environments/m1-transformers590-v1/`. Smoke outputs are isolated under `outputs/runs/vlm/m1-transformers590-preflight-v1/`.

### Results
- Passed: imports (including Qwen processor), CUDA visibility, 4-rank NCCL all-reduce, real one-row template encoding for both datasets, and both formal command dry-runs with non-overlapping 0--3 / 4--7 GPU groups.
- Initial smoke configs were rejected before optimization due to an unsupported temporary `_name_or_path` field; retry1 removed it.
- Retry1 reached the first backward pass on both datasets but failed with the same `torch.utils.checkpoint.CheckpointError` under ZeRO-3 + gradient checkpointing: forward saved non-empty tensors while recomputation received empty tensors.
- Retry2 changed only Liger 0.8.2 to baseline 0.8.0 while retaining Transformers 5.9.0; both branches reproduced the identical error. No optimizer step, checkpoint or `trainer_state.json` was written.
- No `m1_transformers590` tmux session exists and no formal SFT run/evaluation was started.

### Conclusion
- Verified: this isolated 5.9.0 stack is not compatible with the required current full-SFT ZeRO-3/gradient-checkpointing path.
- Verified: the failure is shared by historical and v3 data, and persists after the only necessary package adjustment tested (Liger back to 0.8.0).
- Unknown: whether a compatible ms-swift/Transformers patch or a different minimum dependency adjustment can preserve the required M1 strategy.

### Next
Keep the formal queue blocked. Before any fresh authorization or formal launch, reproduce the error in a minimal upstream configuration and identify a version/patch that passes two optimizer steps with ZeRO-3 and gradient checkpointing while retaining Transformers 5.9.0.

## 2026-09-01 11:01:00 CST - 重建最新 ms-swift 环境仍未通过 M1 ZeRO-3 smoke

Tags: m1-direct, ms-swift, transformers, zero3, environment

### Objective
按最新 PyPI `ms-swift` 及其解析的 Transformers 版本重建隔离 `.venv_test`，重新验证之前的 5.9.0 失败是否由旧 ms-swift 造成。

### Setup
- Previous `.venv_test` preserved as `.venv_test_tf590_failed_20260901`; project `.venv` unchanged.
- Latest resolved stack: ms-swift 4.5.2, Transformers 5.16.1, Torch 2.11.0+cu130, torchvision 0.26.0, DeepSpeed 0.18.9, Accelerate 1.14.0, Liger 0.8.2, qwen-vl-utils 0.0.14.
- Evidence: `outputs/artifacts/environments/m1-ms-swift-latest-v1/`.

### Results
- Passed: `uv pip check`, imports, CUDA visibility, 4-rank NCCL all-reduce, and real template encoding for the 2,114-row historical JSONL and 4,076-row v3 JSONL.
- Both 2-step SFT smoke branches reached training backward then failed with the same `torch.utils.checkpoint.CheckpointError` as the 5.9.0 stack: ZeRO-3 recomputation observed zero-sized parameter tensors.
- No optimizer step, checkpoint or `trainer_state.json` was produced. No formal tmux session, formal SFT run or Direct-618 evaluation was started.

### Conclusion
- Verified: upgrading ms-swift from local 4.3.0.dev0 to PyPI 4.5.2 and using its Transformers 5.16.1 dependency does not resolve the required ZeRO-3 + gradient-checkpointing runtime incompatibility on this Torch/DeepSpeed stack.
- Unknown: whether the minimal compatible change is a newer DeepSpeed/Torch build or an upstream code fix.

### Next
Keep formal work blocked. Test a tightly scoped DeepSpeed/Torch compatibility update only if it preserves the historic M1 training semantics, and require both two-step smoke branches to pass before creating any formal queue.

## 2026-09-01 15:39:04 CST - 最新 ms-swift 双 M1 训练与 Direct-618 六 checkpoint 评测完成

Tags: m1-direct, ms-swift, transformers, zero3, direct-618, reproducibility

### Objective
在通过 Torch/DeepSpeed 兼容更新的隔离环境中，以最新 PyPI `ms-swift` 重新完成历史 M1 数据和 terminal-v5 user-guidance v3 数据的 4-GPU、5-epoch ZeRO-3 full-SFT，并对各自 epoch 1/3/5 checkpoint 执行一致的 Direct-618 DP8 正式评测。

### Environment and training
- Isolated training environment: `.venv_test` with `ms-swift==4.5.2`, `transformers==5.16.1`, `torch==2.13.0+cu130`, `deepspeed==0.19.6`, `accelerate==1.14.0`, `liger-kernel==0.8.2`; `uv pip check` passed. Full evidence: `outputs/artifacts/environments/m1-ms-swift-latest-v1/`.
- Both two-step preflight SFT runs passed before launch. Formal historical run completed 165/165 steps at `outputs/vlm_sft/qwen3_vl_4b_m1_latest_ms_swift_reproduction_4gpu/v0-20260901-112453/`; formal v3 run completed 320/320 at `outputs/vlm_sft/qwen3_vl_4b_m1_latest_ms_swift_v3_4gpu/v0-20260901-112453/`.
- Both use the required M1 semantics: full SFT, bf16/SDPA/Liger/ZeRO-3, 4 GPUs, per-device batch 1, accumulation 16, global batch 64, 5 epochs, LR 1e-5 cosine with 5% warmup, length 2048/delete truncation, and seed/data_seed 42.

### Evaluation acceptance
- Manifest: `outputs/vlm_eval/qwen3_vl_4b_rag_sft/bounded_latest_sft_rag_test_20260804/manifest.jsonl`, SHA-256 `80d79f3ac4860d17ac3612265254240b79d29babf310c94d1c0b3b86d5fc0508`.
- All six evaluations use native TP=1/DP=8, concurrency 64, `max_new_tokens=2048`, retries 2, and paired bootstrap (10,000 samples, seed 20260819).
- Every candidate, historical-M1, and raw-base route passed: 618 predictions, 618 unique IDs, 0 explicit errors, 0 protocol errors, and 0 unparseable outputs. Summaries: `outputs/runs/vlm/m1-latest-ms-swift-queue-v1/evaluations/`.

### Results

| Run / epoch | Candidate | Historical M1 | Raw base | Candidate vs M1 (95% CI, pp) | Candidate vs raw base (95% CI, pp) |
|---|---:|---:|---:|---:|---:|
| Historical data e1 (ckpt 33) | 57.93% | 67.80% | 40.13% | -9.87 [-13.27, -6.47] | +17.80 [+14.40, +21.36] |
| Historical data e3 (ckpt 99) | 64.40% | 67.15% | 40.29% | -2.75 [-5.99, +0.49] | +24.11 [+20.06, +28.16] |
| Historical data e5 (ckpt 165) | 64.72% | 67.31% | 40.45% | -2.59 [-5.83, +0.49] | +24.27 [+20.39, +28.16] |
| v3 data e1 (ckpt 64) | 42.88% | 67.31% | 40.61% | -24.43 [-28.64, -20.39] | +2.27 [-1.62, +5.99] |
| v3 data e3 (ckpt 192) | 50.00% | 66.67% | 40.61% | -16.67 [-20.71, -12.46] | +9.39 [+5.18, +13.43] |
| v3 data e5 (ckpt 320) | 52.27% | 67.48% | 40.61% | -15.21 [-19.58, -10.84] | +11.65 [+7.44, +15.86] |

### Queue recovery
- The initial tmux evaluation worker completed historical e1 then exited fail-closed because its post-evaluation assertion used the obsolete `route.metrics.total` field. The produced summary was valid; the correct schema is `route.metrics.overall.count`.
- `scripts/vlm/run_m1_latest_ms_swift_evals.sh` now validates the actual schema plus zero unparseable/protocol errors and skips only already-complete, validated summaries on resume. The remaining five evaluations then completed sequentially without overwriting e1 evidence.

### Conclusion
- Historical-data reproduction under latest ms-swift substantially improves over raw base (+24.27 pp at epoch 5) but remains 2.59 pp below historical M1; the paired 95% CI includes zero at epochs 3 and 5, so this run does not establish a remaining nonzero gap under the refreshed environment.
- The v3 run improves steadily from epoch 1 to 5 and is significantly above raw base by epoch 3/5, but remains significantly below the historical-M1 comparator. It is a same-environment data baseline; its difference from historical M1 must not be attributed to dependencies alone.

### Next
Use the six immutable summaries as the completed evaluation evidence. If further work is desired, analyze training/data effects explicitly rather than treating either v3-vs-M1 difference or the residual historical-data gap as a single-package causal result.

## 2026-09-01 15:39:04 CST - v3 派生为 M1 system-format 的 v4 数据视图

Tags: m1-direct, prompt-ablation, v3, v4, system-prompt

### Objective
为隔离 v3 与历史 M1 的 system/user prompt 职责差异，从冻结 v3 数据派生一个仅改变提示分工的 v4 视图：system 使用历史 M1 的 `<think>`/`<answer>` 格式契约，v3 的农业诊断助手身份文本移入 user。

### Result
- New immutable artifact: `outputs/artifacts/datasets/m1-direct-current-hcv-v1/terminal-v5-m1-comparison-user-guidance-v4-m1-system-format/`.
- v4 system: historical-M1 English output-format contract, including required `<think>...</think><answer>...</answer>` structure.
- v4 user begins with `<image>`, then `You are an agricultural visual diagnosis assistant.`, followed by the unchanged v3 open/option task instruction and candidates.
- Parent v3 remains unchanged. v4 preserves all 4,076 sample IDs and order, 1,019 image hashes, assistant targets, language, question type and audited source metadata. Parent data SHA-256: `7e07d106611a2ce931728283d503f5ea7dd364d121e5c86b6e32f1907bb1adee`; v4 data SHA-256: `af67c60d4b10581b6e4ee56876ef715852a1b5ec0378a645cbbcb4622cc313fd`.
- Conversion script: `scripts/vlm/derive_m1_v3_m1_system_format_v4.py`; it is fail-closed and refuses to overwrite an existing derived artifact.

### Next
Treat v4 as a prompt-only ablation against v3. No v4 training has been started; create a new authorization/configuration before a formal SFT or Direct-618 evaluation.

## 2026-09-01 16:36:00 CST - 简洁 v4 system-format 8 卡训练已启动

Tags: m1-direct, v4, system-prompt, latest-ms-swift, eight-gpu, running

### Prompt decision
- The first untrained long historical-M1 system-format preview was retained at `outputs/artifacts/datasets/m1-direct-current-hcv-v1/terminal-v5-m1-comparison-user-guidance-v4-m1-system-format-superseded-preview/`.
- The user-approved formal v4 instead uses the concise consistent contract: `You are an agricultural visual diagnosis assistant. Reason from the image, then respond exactly as: <think>concise diagnostic reasoning</think><answer>final answer</answer>`. The v3 user task wording remains unchanged, so its canonical-name/option-letter restrictions apply specifically to the existing task text while system defines the outer response shape.
- Formal v4 artifact: `outputs/artifacts/datasets/m1-direct-current-hcv-v1/terminal-v5-m1-comparison-user-guidance-v4-m1-system-format/`, 4,076 rows / 1,019 images, SHA-256 `85465926d4723b153ae17bb50551af943aaea85e26268cc11efbff280992cb98`. Images, sample order, assistant targets, language and question type are unchanged from v3.

### Preflight and launch
- Fresh single-use authorization: `outputs/artifacts/authorizations/m1-ms-swift-latest-v4-m1-system-format-8gpu-v1/terminal-v5-v4.json`. Registered dry-run passed.
- Eight-GPU ZeRO-3 smoke: 2/2 steps, losses 2.803 and 2.793, checkpoint-2 written under `outputs/runs/vlm/m1-latest-ms-swift-v4-8gpu-preflight-v1/smoke/model/v0-20260901-163250/`; no CUDA, DeepSpeed, data, checkpointing, or prompt-format failure.
- Formal training started in tmux session `m1_latest_ms_swift_v4_8gpu`, via `scripts/vlm/run_m1_latest_ms_swift_v4_m1_system_format_8gpu.sh`. It uses latest `.venv_test`, full SFT/bf16/SDPA/Liger/ZeRO-3, 8 GPUs, batch 1, accumulation 8 (global batch 64), 5 epochs / 320 expected steps, LR 1e-5 cosine, warmup 5%, max length 2048/delete, and seed/data_seed 42.
- Confirmed stable through steps 1--3: step-1 loss 2.803, grad norm 23.17, all 8 GPUs active, and no errors. Volatile: running job; confirm status and final checkpoint-320 before relying on results.

### Next
After successful step-320 completion, formally evaluate checkpoints 64, 192 and 320 with the existing Direct-618 DP8 protocol; do not run that evaluation concurrently with training.

## 2026-09-01 17:50:03 CST - v4 简洁 system-format 8 卡训练完成并进入正式评测

Tags: m1-direct, v4, system-prompt, latest-ms-swift, eight-gpu, direct-618

### Training result
- The v4 prompt-only 8-GPU run completed successfully: 320/320 optimizer steps, epoch 5.0, exit code 0. Final training loss 0.8186.
- Checkpoints were written at steps 64, 128, 192, 256 and 320 under `outputs/vlm_sft/qwen3_vl_4b_m1_latest_ms_swift_v4_m1_system_format_8gpu/v0-20260901-163603/`; final trainer state verifies global_step 320.
- No CUDA OOM, DeepSpeed, data loading, checkpointing or training-process error was found.

### Evaluation
- After GPU release, started tmux session `m1_latest_ms_swift_v4_8gpu_eval` for sequential Direct-618 DP8 evaluation of checkpoint 64 (epoch 1), 192 (epoch 3) and 320 (epoch 5).
- Script: `scripts/vlm/run_m1_latest_ms_swift_v4_m1_system_format_evals.sh`. It validates manifest SHA `80d79f…0508`, all 618 rows, unique IDs, and zero unparseable/protocol errors for candidate, historical-M1 and raw-base routes; completed summaries are immutable and resumable.

### Next
Wait for all three formal summaries, then compare the v4 prompt-only ablation with the completed v3 checkpoints under the same Direct-618 protocol.

## 2026-09-01 19:19:42 CST - 修复 Direct-618 最终答案评分并派生重评分

Tags: direct-618, evaluation, scoring, m1, v3, v4

### Objective
修复“正式结果不合理”的判分机制，同时保留原始预测和旧评分产物，不重跑任一模型推理。

### Changes
- normalize_answers.py 新增版本化 final-answer-strict-v2：答案只可来自 <answer>、显式最终答案标记或第一条非空行；Open 禁用子串包含匹配，Option 不再从完整候选比较推理中搜索选项/类别。
- 三个 formal launcher 均显式使用严格策略；新的汇总 schema 标记为 final-answer-v2。
- 新增 scripts/vlm/rescore_direct_formal618_final_answer_v2.py，拒绝覆盖目标目录，并将每个来源 prediction 的路径和 SHA-256 记录进派生 summary。
- 增加回归测试：错误首行而推理中提及金标必须判错、<answer>/显式答案优先、Option 不可由候选表中的金标字母误判。

### Results
- 对 historical、v3、v4 的 9 个 checkpoint 生成独立 v2-derived 证据；每条 route 都有 618 行、manifest SHA 80d79f…0508、0 protocol errors、0 unparseable，并完成 10,000 次/seed 20260819 paired bootstrap。
- v3 e5：candidate 52.27%，同轮 M1 52.43%，raw base 40.45%，candidate vs M1 -0.16 pp；旧版同一对比为 -15.21 pp，差异主要来自 M1 长推理中被旧版 contained_match 误计的候选标签。
- v4 e5：candidate 35.92%，同轮 M1 52.27%，raw base 40.61%，candidate vs M1 -16.34 pp；因此 v4 的弱表现仍是真实模型/训练提示问题，不能归因于旧评分缺陷。
- 派生根：outputs/runs/vlm/m1-latest-ms-swift-queue-v1/evaluations-final-answer-v2-20260901-r2/ 与 outputs/runs/vlm/vlm-sft-qwen3vl4b-m1-latest-ms-swift-v4-m1-system-format-8gpu-v1/evaluations-final-answer-v2-20260901-r2/。

### Conclusion
- Verified: 旧 legacy-contained-v1 不能再作为带有长候选比较生成的最终诊断准确率使用；历史旧汇总保留以便审计，但与 v2 结果不可混表。
- Verified: v3 与 M1 的原先大差距主要为评测伪差；v4 与 M1 的差距在严格口径下仍存在。
- Compatibility / scope: 新策略仅改变答案评分，不改变 manifest、模型输出、请求参数、bootstrap 或训练数据。

### Next
使用 final-answer-strict-v2 重新审视 v4 的 Open 与 Option 错例；若继续训练或评测，禁止引用 legacy-contained-v1 的 accuracy 作为正式结论。

## 2026-09-01 19:40:00 CST - v4 恢复训练 system prompt 的 Direct-618 控制评测

Tags: direct-618, v4, system-prompt, controlled-replay

### Objective
检验 v4 的低分是否主要由训练时 system prompt 在既有 Direct 评测请求中缺失造成。

### Setup
- Checkpoint: outputs/vlm_sft/qwen3_vl_4b_m1_latest_ms_swift_v4_m1_system_format_8gpu/v0-20260901-163603/checkpoint-320。
- 与既有 v4 e5 相同的 manifest（SHA 80d79f…0508）、TP=1/DP=8、并发 64、temperature 0、max_new_tokens=2048、retries 2 和 final-answer-strict-v2。
- 唯一推理输入改变：在每个 user+image 请求前增加训练 v4 system prompt，要求以 think 和 answer 标签返回。
- 仅重放 candidate；不重跑 M1/raw base，因为它们没有用该 v4 system 训练。新 evaluator 的 optional system-prompt 参数进入 request fingerprint，防止与无 system snapshot 混用。

### Results
- 完整 618/618，0 explicit/protocol errors，0 unparseable；临时 DP8 service 已停止。
- 恢复 system 后：54.21%（335/618），Open 32.36%，Option 76.05%；617/618 输出 answer 标签。
- 相同 checkpoint 的无 system 严格结果为 35.92%（222/618），paired delta +18.28 pp，10,000 bootstrap 95% CI [+14.24,+22.33]。
- 同一无 system 原评测中的 M1/raw base 分别为 52.27% / 40.61%；它们不与此控制值作无条件横向排名，因为输入 prompt 已不同。
- Evidence: outputs/runs/vlm/vlm-sft-qwen3vl4b-m1-latest-ms-swift-v4-m1-system-format-8gpu-v1/evaluations-system-v4-restored-e5-20260901/summary.json。

### Conclusion
- Verified: v4 的主要低分来源是正式评测遗漏了其训练时 system 条件；补回该条件不但恢复格式，而且在同一 checkpoint 上显著提升最终答案准确率。
- Unknown: 剩余的格式与训练数据影响需要使用同一 system 条件下的 M1/v3 对照，或同卡数训练消融后再归因。

### Next
将 v4 的正式后续评测固定为显式 v4 system prompt；若需要严格横向比较 v3、v4、M1，应为每个模型声明并固定各自的训练兼容 prompt 条件。

## 2026-09-01 19:45:00 CST - v4 恢复 system 后与 v3、修正 M1 的配对比较

Tags: direct-618, v3, v4, m1, paired-bootstrap

### Setup
- 同一 Direct-618 manifest 与 final-answer-strict-v2 评分。
- Candidate: v4 e5 checkpoint-320，加回其训练 system prompt，335/618。
- Baselines: v3 e5 checkpoint-320 与同一 v3 formal route 中的历史 M1；二者均为既有无 system 原始请求的严格重评分。
- 使用 10,000 paired bootstrap，seed 20260819；比较结果仅说明这两个已声明输入条件下的表现，不能视为完全相同 prompt 下的因果排序。

### Results
| Route | Overall | Open | Option |
|---|---:|---:|---:|
| v4 e5, restored v4 system | 54.21% (335/618) | 32.36% | 76.05% |
| v3 e5, no system | 52.27% (323/618) | 31.07% | 73.46% |
| Historical M1, no system | 52.43% (324/618) | 27.83% | 77.02% |

- v4 restored vs v3: +1.94 pp, 95% CI [-1.46,+5.34]; 271 both-correct, 64 v4-only, 52 v3-only.
- v4 restored vs M1: +1.78 pp, 95% CI [-2.75,+6.15]; 233 both-correct, 102 v4-only, 91 M1-only.
- v4 restored has higher Open than M1 (+4.53 pp), while M1 retains a small Option advantage (+0.97 pp); neither overall paired interval excludes zero.
- Evidence: outputs/runs/vlm/vlm-sft-qwen3vl4b-m1-latest-ms-swift-v4-m1-system-format-8gpu-v1/evaluations-system-v4-restored-e5-20260901/artifacts/system_restored_vs_v3_no_system_paired_review.json and system_restored_vs_m1_no_system_paired_review.json.

### Conclusion
- Verified: under the measured prompt conditions, restored-system v4, v3 e5 and corrected M1 e5 are statistically indistinguishable in overall Direct-618 accuracy.
- Compatibility / scope: this is not a same-prompt three-way ranking; the result establishes that the apparent former v4 collapse disappears once its required training system condition is restored.

## 2026-09-01 20:57:07 CST - Formal-618 当前评测入口迁移

Tags: formal-618, rag, m1, v4, final-answer-strict-v2, archive

### Objective
将当前评测入口统一到严格最终答案评分，避免旧 contained-match 分数和旧报告继续作为当前结论。

### Results
- 新的唯一入口：`docs/results/CURRENT_FORMAL618_EVALUATION.md`。
- 统一结果：RAG checkpoint-232 58.41%，修正 M1 checkpoint-165 52.43%，恢复训练 system prompt 的 v4 checkpoint-320 54.21%。
- 配对结果：RAG - 修正 M1 为 +5.99pp，95% CI [+1.62,+10.36]；RAG - v4 为 +4.21pp，95% CI [-0.49,+8.90]。
- 所有结果均使用 618 条固定 manifest（SHA `80d79f…0508`）、`final-answer-strict-v2` 和 10,000 次 paired bootstrap（seed `20260901`）。

### Archive
- 已将旧 current/milestone 报告移至 `docs/archive/results/`：`CURRENT_RAG_VS_M1_DIRECT_618_REPORT.md`、`EPOCH6_RAG_MILESTONE_COMPARISON.md`、`FORMAL618_MILESTONE_SUBSET_COMPARISON.md`、`MILESTONE_EXPERIMENTS.md`。
- 原始预测、评测运行和评分产物未删除；仅停止将其作为当前入口。

### Next
后续 Formal-618 结论和报告只更新 `docs/results/CURRENT_FORMAL618_EVALUATION.md`，并明确声明每条路线的 prompt 条件。
