## 2026-08-24 04:01:09 CST - Micu HCV v7 retention-aware freeze and SFT start

- Goal: repair the final Micu HCV training-data gate, authorize the immutable v7 artifact, and start the registered 8-GPU SFT run.
- Root cause: `swift_role_alternation` rejected the valid `system -> user` prefix present in the native frozen rows; the HCV tool sequence itself remained strict and valid.
- Change: `tools/rag_distill/augment_hcv_terminal_rejection.py` now allows the native `system -> user` prefix while retaining fail-closed checks for all other transitions. Regression coverage was added in `tests/test_hcv_terminal_rejection.py`.
- Verification: `tests/test_hcv_terminal_rejection.py tests/test_hcv_v7_retention_freeze.py` passed (8 tests); `git diff --check` passed.
- Artifact: `outputs/artifacts/datasets/agrinet-hcv-manual-json-v7-five-turn-retention-aware/`, 2,864 rows, `training_authorized=true`, data SHA-256 `00cce90e639b0e30e4f6d698141f79b53f3ff4650a3b9eca146d4a56f1c0d24f`.
- SFT: registered experiment `vlm-sft-qwen3vl4b-hcv-manual-json-five-turn-terminal-rejection-v7-8gpu-v1`; run `outputs/runs/vlm/vlm-sft-qwen3vl4b-hcv-manual-json-five-turn-terminal-rejection-v7-8gpu-v1/20260824T035852-2ef993b1-a01/`; PID 1362220. First completed step: loss 2.71, token accuracy 0.5712, learning rate 6.3e-7 during warmup; process still running and no OOM/data errors observed.
- Next action: let the 6-epoch SFT finish, then resolve epoch 2/4/6 checkpoints and run strict 64-row smoke and formal Direct/RAG DP8 gates.

## 2026-08-24 04:18:00 CST - Evaluation queue registered behind active SFT

- The SFT parent remains running at the verified checkpoint queue dependency path; no second training job was launched.
- Added `configs/experiments/vlm/vlm-hcv-v7-checkpoint-eval-queue-v1.yaml`, a `SKIP_TRAIN=1` queue that waits for the parent run, reuses `v0-20260824-035908`, resolves actual checkpoint steps from `trainer_state.json`, and evaluates epoch 2/4/6.
- Dry-run confirmed native DP8 settings, strict five-turn protocol, 64-row smoke, and no training command. CLI/config tests passed.
- Queue run: `outputs/runs/vlm/vlm-hcv-v7-checkpoint-eval-queue-v1/20260824T040848-2ef993b1-a01/`; status is running and currently waiting for the parent SFT completion.
- At the latest check, SFT was `8/144`, loss `2.2809`, token accuracy `0.6174`, LR `5e-6`; GPUs 0--7 were previously confirmed saturated without runtime errors.

## 2026-08-24 04:27:00 CST - First SFT epoch checkpoint verified

- Parent SFT reached `global_step=24/144`, `epoch=1.0`; latest loss `1.1736`, token accuracy `0.7260`.
- `checkpoint-24` is complete with both model shards, tokenizer/processor files, and `trainer_state.json` reporting `global_step=24`, `max_steps=144`, `epoch=1.0`.
- No traceback, OOM, NCCL, NaN, or Inf signatures were found in the parent run logs.
- The post-training evaluation queue remains correctly waiting for the parent `complete` status; no SGLang evaluator process has started.

## 2026-08-24 06:55:00 CST - Micu v7 SFT completed and epoch-2 DP8 evaluation diagnosis

- The Micu HCV v7 SFT completed successfully on eight GPUs: `global_step=144/144`, `epoch=6.0`, exit code `0`. The final checkpoint is `outputs/vlm_sft/qwen3_vl_4b_hcv_manual_json_five_turn_terminal_rejection_v7_8gpu/v0-20260824-035908/checkpoint-144/`, with two model shards, tokenizer/processor files, and a matching `trainer_state.json`.
- The registered checkpoint queue correctly resolved epoch 2/4/6 to steps `48/96/144` and entered native SGLang `TP=1, DP=8` smoke/formal evaluation sequentially.
- Epoch-2 smoke passed the strict protocol gate: 64 rows, zero unparseable rows, zero invalid tool calls, zero malformed tool-call attempts, zero post-budget attempts, and zero terminal-closure failures.
- Epoch-2 Direct formal completed with 618 rows. Candidate accuracy was `52.10%`; it improved over raw-base by `+11.49 pp` (95% paired bootstrap CI `[+7.93, +15.21]`), but remained below M1 by `-15.05 pp` (95% CI `[-18.93, -11.33]`).
- Epoch-2 RAG produced all 618 prediction rows but was correctly rejected by fail-closed normalization because two rows emitted another malformed/noncanonical tool call after terminal-answer correction: `zh_N05028_P00677_open` and `en_N04100_P00009_open`. Each has `terminal_closure_failed=1`, `malformed_tool_call_attempts=2`, and an explicit evaluation error. These rows must remain failures under `invalid_tool_call_policy=strict`; they must not be silently converted into answers or scored.
- The queue preserved the rejected RAG artifacts and continued to epoch-4 smoke. Volatile state: epoch-4 smoke/formal is still running; verify its artifacts before relying on it.
- Next action: inspect epoch-4 and epoch-6 strict smoke/formal gates, and only promote a checkpoint if all 618-row and paired-bootstrap requirements pass.

## 2026-08-24 06:56:00 CST - v7 checkpoint queue completed with strict rejections

- The checkpoint queue ended with `phase=complete` after processing epoch 2, 4, and 6; this denotes workflow completion, not promotion.
- Epoch 4 smoke was rejected before formal evaluation because four of 64 rows emitted repeated noncanonical tool calls after the five-turn budget and terminal-answer correction. The affected rows were `en_N05064_P00715_open`, `en_N05070_P00543_option`, `zh_N04038_P00913_open`, and `en_N05036_P00439_option`; each had `post_budget_tool_attempts=2` and `terminal_closure_failed=1`.
- Epoch 6 smoke produced 64 rows but failed the strict gate on five rows: `en_N04047_P00927_open`, `zh_N04088_P00125_open`, `zh_N05007_P02159_open`, `zh_N04038_P00913_open`, and `zh_N05044_P00839_option`. Each had `post_budget_tool_attempts=2`, `terminal_closure_failed=1`, and `max_tool_turns_exceeded=1`; the queue therefore created no formal artifacts and no epoch-6 metric or paired-bootstrap claim is valid.
- Current v7 promotion state: no checkpoint promoted. Epoch 2 has valid Direct formal evidence but its RAG formal is rejected due to two strict protocol-error rows; epochs 4 and 6 fail smoke.
- The strict evaluator behavior is intentional: malformed/noncanonical calls after terminal correction are explicit failures and are not converted into answer rows.
- Next action: diagnose and repair the remaining terminal-state behavior in the model/evaluator contract before launching another formal run; do not summarize v7 as a successful RAG experiment.

## 2026-08-24 07:28:00 CST - v8 invalid-terminal repair training started

- The repaired evaluator was validated by targeted tests, including the mixed-generation queued-call regression. The second 64-row smoke with queue clearing passed with 64 unique rows, zero error rows, and zero terminal-closure failures. A third smoke with a terminal-only `max_new_tokens=128` cap reduced the failure count from five to one but still exposed one genuine malformed follow-up; strict evaluation remains fail-closed.
- The v7 checkpoint-144 Direct formal was complete and valid (candidate/M1/raw-base each 618 rows), but candidate Direct was `-18.45 pp` versus M1 (95% CI `[-22.33, -14.56]`) and `+8.58 pp` versus raw-base (95% CI `[+5.02, +12.14]`). Candidate RAG formal produced 618 rows but was rejected by strict normalization on four explicit terminal protocol errors; no raw-base RAG or paired RAG review was generated.
- Added `tools/rag_distill/augment_hcv_invalid_terminal.py` and `tests/test_hcv_invalid_terminal.py`. The new immutable v8 dataset extends the audited v7 artifact with 64 balanced label-blind malformed-tool terminal rows (8 per task cell), for 3,056 total rows. Validation authorizes training; data SHA-256 is `4ffd26233cf8ebe350602a03076525a6f3154d485deb2ad9000ae04b3d5110f`.
- Registered and launched v8 SFT on eight GPUs: run `outputs/runs/vlm/vlm-sft-qwen3vl4b-hcv-terminal-repair-v8-8gpu-v1/20260824T072424-2ef993b1-a01/`, PID 1447481. The first completed step is `global_step=1/144`, loss `2.698`, token accuracy `0.5739`; no OOM/NCCL/NaN/Inf/data errors observed. Volatile: training is running.
- Next action: let v8 SFT complete, verify checkpoint-144, run repaired strict 64-row smoke, and only then run formal Direct/RAG DP8 gates.

## 2026-08-24 08:35:00 CST - Micu evaluator regression and v8 training continuation

- Confirmed the active route remains the Micu HCV v8 terminal-repair SFT, not a legacy evaluation queue: parent run `outputs/runs/vlm/vlm-sft-qwen3vl4b-hcv-terminal-repair-v8-8gpu-v1/20260824T072424-2ef993b1-a01/`, PID `1447481`.
- Training reached `global_step=8/144`, `epoch=0.3351`; latest loss `2.311`, token accuracy `0.5790`, learning rate `5e-6`. GPU 0--7 were all at 100% utilization during the check; no OOM, NCCL, NaN, Inf, traceback, or data-read error was observed.
- Regression verification passed: `tests/test_rag_eval_blind.py`, `tests/test_hcv_invalid_terminal.py`, `tests/test_hcv_terminal_closure.py`, `tests/test_hcv_terminal_rejection.py`, and `tests/test_formal_eval_runtime.py` (44 tests).
- Volatile state: SFT is still running; no v8 evaluation has been launched.
- Next action: wait for the same parent run to reach `global_step=144/144`, verify its final checkpoint and exit status, then run the strict native DP8 smoke before any formal Direct/RAG evaluation.

## 2026-08-24 08:52:00 CST - Micu v8 evaluation queue registered behind SFT

- Added `configs/experiments/vlm/vlm-hcv-v8-terminal-repair-checkpoint-eval-queue-v1.yaml`, isolated from the superseded v7 queue and pointing to the active v8 SFT parent and v8 validation artifact.
- Dry-run verified the queue uses `SKIP_TRAIN=1`, waits for the parent run, resolves epoch `2/4/6` checkpoints, and declares native `TP=1, DP=8`, Direct concurrency `64`, RAG concurrency `24`, `max_tool_turns=5`, and strict invalid-tool policy.
- Detached queue submitted as run `outputs/runs/vlm/vlm-hcv-v8-terminal-repair-checkpoint-eval-queue-v1/20260824T074222-2ef993b1-a01/`, PID `1451107`. It is currently waiting for the still-running SFT parent; no SGLang evaluator has started and no duplicate training was launched.
- Volatile state: parent SFT remains at the early training stage; verify both parent completion and queue dependency state before relying on any evaluation artifacts.

## 2026-08-24 09:00:00 CST - Micu v8 epoch-1 checkpoint integrity verified

- Parent SFT reached `global_step=24/144`, `epoch=1.0`; the intermediate artifact `outputs/vlm_sft/qwen3_vl_4b_hcv_manual_json_terminal_repair_v8_8gpu/v0-20260824-072440/checkpoint-24/` is complete.
- Verified both model shards, tokenizer/processor/config files, and `trainer_state.json` (`global_step=24`, `max_steps=144`, `epoch=1.0`). No OOM, NCCL, NaN, Inf, traceback, or error signature was found in the parent logs.
- This is an intermediate integrity checkpoint only; promotion and formal evaluation remain gated on the final `checkpoint-144`.

## 2026-08-24 09:08:00 CST - Micu v8 training process ownership check

- The active parent reached `global_step=29/144`, latest loss `0.9452`, token accuracy `0.7645`; GPU utilization remained high and the parent status stayed `running`.
- A process inspection briefly showed newer `swift/cli/sft.py` descendants, but `nvidia-smi` confirmed only one 8-rank model set holding model memory. The newer descendants are per-rank `pt_data_worker` DataLoader processes under the existing rank, not a duplicate training launch.
- The separate RAG service remains the only other GPU consumer; the v8 evaluation queue is still waiting for the SFT parent and has produced no evaluator artifacts.

## 2026-08-24 10:02:00 CST - Micu v8 epoch-2 checkpoint integrity verified

- Parent SFT reached `global_step=48/144`, `epoch=2.0`; latest logged loss was `0.778`, token accuracy `0.7925`.
- Verified `checkpoint-48` contains both model safetensors shards, tokenizer, processor/preprocessor configuration, model config, and `trainer_state.json` with `global_step=48`, `max_steps=144`, `epoch=2`.
- No OOM, NCCL, NaN, Inf, or traceback signatures were found. The evaluation queue remains waiting for final parent completion; no v8 smoke/formal artifacts exist yet.

## 2026-08-24 11:45:48 CST - Micu v8 strict diagnosis and v9 launch

- v8 SFT and its checkpoint queue completed, but no checkpoint was promoted. Epoch 2 Direct was `-17.9612 pp` versus M1 (95% paired bootstrap CI `[-21.6828, -14.2395]`) and `+9.0615 pp` versus raw-base (CI `[+5.5016, +12.6214]`).
- Strict RAG protocol evidence shows v8's `invalid_terminal_v1` augmentation placed a truncated bare JSON request in an `assistant` completion position. This is a data-construction defect that directly explains the recurring `malformed_bare_json` events. Terminal closure itself was successful (`terminal_closure_failed=0`), so the strict evaluator was not loosened.
- Added `tools/rag_distill/build_hcv_v9_protocol_retention.py` and changed `tools/rag_distill/augment_hcv_invalid_terminal.py` so the rejected-tool state is context-only and the malformed request is never an assistant target. Added regression coverage; 32 focused tests passed.
- Built immutable v9 artifact `outputs/artifacts/datasets/agrinet-hcv-manual-json-v9-protocol-retention/`: 5,296 rows, image-isolated/native roles, `no_invalid_assistant_supervision=true`, Direct token fraction `0.41661`, `training_authorized=true`, SHA-256 `b070dabb33c1d6c50033b9685e10ad41e329db44b5e27579e4868f411f11d5aa`.
- Registered v9 as a conservative M1-preserving one-epoch, LR `1e-6`, eight-GPU SFT followed by a native DP8 strict queue. Run `outputs/runs/vlm/vlm-hcv-v9-protocol-retention-eval-queue-v1/20260824T114447-2ef993b1-a01/` is running; initial logs show one 8-rank training launch and GPUs 0--7 at high utilization, with no OOM/NCCL/NaN/Inf/traceback signature. Volatile: training is still running.
- Next action: wait for the v9 checkpoint, verify loss and checkpoint integrity, then require the 64-row strict smoke before any formal Direct/RAG/bootstrap claim.
## 2026-08-24 12:56:00 CST - HCV v10 SFT completed; strict DP8 smoke rejected

- v10 SFT completed normally on eight GPUs with exit code `0`: `global_step=42`, `epoch=1`, final logged loss `1.877`, token accuracy `0.6178`. The verified checkpoint is `outputs/vlm_sft/qwen3_vl_4b_hcv_manual_json_v10_protocol_safe_8gpu/v0-20260824-120307/checkpoint-42/`; both model shards, tokenizer/processor files, and `trainer_state.json` are present.
- The original v10 queue only completed the training sub-stage and produced no evaluation artifacts. A recovery queue was therefore launched without retraining at `outputs/runs/vlm/vlm-hcv-v10-protocol-safe-eval-recovery-v1/20260824T125327-2ef993b1-a01/`, reusing the checkpoint above with native SGLang `TP=1, DP=8`.
- The strict 64-row smoke completed with 64 unique IDs but failed on `12/64` rows (`18.75%`). Every failure was `model emitted a tool call despite terminal-answer correction`; traces show malformed XML tool output followed by bare-JSON tool output. The queue wrote `skipped_formal.json` and correctly did not launch 618-row formal Direct/RAG or bootstrap.
- This is not a service, coverage, or missing-row failure. It is a model/protocol-contract failure: v10 has no formal promotion result. Next action is to repair the trainable terminal-closure representation and re-audit the resulting dataset before any new SFT; do not relax the strict gate or treat v10 as a formal result.

## 2026-08-24 14:03:00 CST - v11 strict smoke diagnosis: data-state coverage is primary, convergence is secondary

- v11 SFT completed successfully from M1 checkpoint-18 on eight GPUs: `global_step=46/46`, epoch `1`, final step loss `1.910`, aggregate train loss `2.171`, final token accuracy `0.6046`, exit code `0`. The verified checkpoint is `outputs/vlm_sft/qwen3_vl_4b_hcv_manual_json_v11_terminal_closure_8gpu/v0-20260824-130627/checkpoint-46/`.
- The v11 64-row native DP8 strict smoke completed with exact 64 unique IDs but failed on `9/64` rows. Every failure was `model emitted a tool call despite terminal-answer correction`; the first invalid forms were six `malformed_xml_envelope` and three `malformed_bare_json`. These failures occurred before any successful retrieval (`tool_turns=0` in the error records), followed by a repeated or mixed tool attempt after terminal correction.
- Dataset audit shows v11 added 560 rows, balanced 70 per cell, all with valid native role sequence `system>user>assistant>tool_call>tool>assistant>tool_call>tool>assistant`, no malformed/invalid assistant supervision, and a final assistant answer. Thus the added data is clean, but it teaches a valid budget-rejection context and final closure; it does not directly supervise the deployed invalid-first-call -> terminal-only correction state, because malformed tool calls cannot be placed in assistant training targets under the strict data contract.
- Diagnosis: the dominant blocker is training/evaluation state coverage and the residual XML/bare-JSON format prior, not a service or manifest defect. Convergence is also incomplete in the optimization sense (only one epoch; loss still fluctuates around `1.83--1.91` at the end), so additional training may help after the state representation is repaired, but more epochs on unchanged v11 data alone are not a sufficient fix.
- Decision: keep strict evaluation unchanged; do not promote v11 and do not launch formal 618 evaluation. Next repair must add a native, valid supervision representation for invalid-call recovery/terminal-only closure or align the runtime state to the exact trainable message sequence, then run a fresh SFT and require a zero-error 64-row smoke.

## 2026-08-24 18:24:30 CST - checkpoint-184 formal evaluation completed with strict RAG rejection

- The manually stopped six-epoch training run produced a verified intermediate `checkpoint-184` (`global_step=184`, `epoch=4`, `max_steps=276`); evaluation used the explicit `step:184` resolver and native SGLang `TP=1, DP=8` on GPUs 0--7. The evaluation run exited `0`, but workflow completion does not imply promotion.
- Strict 64-row smoke passed, and Direct formal completed with exactly 618 rows for candidate, M1, and raw-base. Candidate Direct accuracy was `52.7508%`, versus M1 `67.4757%` (`-14.7249 pp`, paired 95% CI `[-18.1230, -11.3269]`) and raw-base `40.6149%` (`+12.1359 pp`, CI `[+8.4142, +16.0194]`).
- Candidate RAG generated all 618 prediction rows, but strict normalization rejected four explicit protocol-error rows: `zh_N05036_P03243_open`, `en_N04028_P00115_option`, `en_N04071_P00029_open`, and `en_N04107_P00080_option`. All emitted a tool call after terminal-answer correction; the first had repeated noncanonical calls, `max_tool_turns_exceeded=1`, and `terminal_closure_failed=1`, while the other three had `invalid_hermes_tool_call` and `terminal_closure_failed=1`. No RAG metrics, raw-base RAG comparison, or RAG bootstrap claim is valid.
- The DP8 service was healthy (`/model_info` HTTP 200) and exposed eight DP scheduler processes; the failure is therefore a model/protocol state-coverage issue, not a GPU/service/manifest problem. The active evaluator includes the native terminal-context alignment fix; this result must not be compared as a pure training-only change to the older smoke.
- Promotion state: no checkpoint promoted. Next action: diagnose and repair the remaining invalid-first-call to terminal-only state transition, then require zero explicit protocol errors on strict smoke before another formal RAG run.

## 2026-08-24 18:27:10 CST - checkpoint-184 RAG宽松诊断统计（非正式）

- 按用户要求对 candidate RAG 的 618 条预测做宽松诊断计分：仅移除 4 条记录中的显式 `error` 字段，未修改其实际预测内容；因此这 4 条均被计为错误。结果保存在临时目录 `/tmp/hcv184-rag-diagnostic.mErEar/`，未覆盖正式产物。
- 宽松整体准确率为 `53.0744%`（328/618）；open `40.1294%`（124/309），option `66.0194%`（204/309）。known `47.0930%`（162/344），unknown `60.5839%`（166/274）；pest `32.8125%`（63/192），disease `62.2066%`（265/426）。
- 该统计仅用于回答当前模型能力，不构成正式 RAG 指标或 promotion evidence：4 条协议错误仍然违反 strict gate，且本 run 没有 raw-base RAG 与 paired bootstrap 结果。

## 2026-08-24 18:29:40 CST - checkpoint-184 收敛性判断

- `checkpoint-184` 训练状态为 `global_step=184/276`、`epoch=4/6`，因此训练在计划完成前被人工停止。无独立验证集 loss 或 best checkpoint 选择，不能声称已经收敛。
- 训练 loss 按 epoch 均值从 epoch 1 的约 `1.247` 降至 epoch 2 的 `0.961`、epoch 3 的 `0.833`；checkpoint-184 最后记录为 `0.799`。loss 仍有下降趋势，说明继续训练可能带来收益。
- 但 RAG 的 4 条失败是 terminal correction 后再次非法调用，属于协议状态覆盖/生成行为问题；Direct 相对 M1 的 `-14.72 pp` 也明显超出少量随机波动。当前结论是“未充分收敛可能是性能不足的贡献因素，但不是已证实的唯一根因”。

## 2026-08-24 18:44:00 CST - HCV v11 LR=5e-6 从 checkpoint-18 重新训练启动

- 为验证更大学习率是否改善未充分收敛与 Direct 保持问题，新建独立 v2 配置；初始化固定为 M1 `checkpoint-18`，不是从 checkpoint-184 续训。
- 除学习率外保持上一轮设置不变：v11 数据、6 epoch、per-device batch 1、gradient accumulation 16、8 GPU（0--7）、相同 manual_json/DP8 后续评测协议。学习率由 `1e-6` 改为 `5e-6`。
- 配置：`configs/vlm/qwen3_vl_4b_hcv_manual_json_v11_terminal_closure_8gpu_sft_e6_lr5e6.yaml`；实验登记：`configs/experiments/vlm/vlm-sft-qwen3vl4b-hcv-v11-terminal-closure-8gpu-e6-lr5e6-v1.yaml`。
- 已通过 `agrinet vlm submit ... --dry-run`，实际训练已启动：run `outputs/runs/vlm/vlm-sft-qwen3vl4b-hcv-v11-terminal-closure-8gpu-e6-lr5e6-v1/20260824T184331-2ef993b1-a01/`，PID `1588922`。启动命令已确认传入 `--learning_rate 5e-06`、`--num_train_epochs 6`、`--nproc_per_node 8`、`--master_port 29705`。
- 启动阶段状态为 `running`，8 个训练 rank 已建立，未见 OOM/NCCL/NaN/Inf/traceback。Volatile：训练尚未完成，后续需验证 loss 曲线、epoch checkpoint 完整性，再排队 smoke/formal 评测。

## 2026-08-25 00:02:55 CST - HCV v11 LR=5e-6 六 epoch 训练完成

- 训练 run `outputs/runs/vlm/vlm-sft-qwen3vl4b-hcv-v11-terminal-closure-8gpu-e6-lr5e6-v1/20260824T184331-2ef993b1-a01/` 正常完成，exit code `0`；训练目录为 `outputs/vlm_sft/qwen3_vl_4b_hcv_manual_json_v11_terminal_closure_8gpu_e6_lr5e6/v0-20260824-184347/`。
- 最终状态为 `global_step=276/276`、`epoch=6`，checkpoint `46/92/138/184/230/276` 均已保存；最终 checkpoint-276 含两个模型 shard、tokenizer、processor、配置和 trainer state。
- 训练 loss 单调地大幅下降：epoch 均值约为 `1.555`、`0.558`、`0.202`、`0.094`、`0.054`、`0.036`，最后 step loss `0.0289`，总 train loss `0.4108`。无 OOM/NCCL/NaN/Inf/traceback。
- 这表明训练优化已充分收敛，但本配置没有独立验证 split；极低 training loss 同时提高了过拟合/Direct 保留退化的风险，不能据此作泛化或 RAG 晋级结论。下一步应优先在多个 epoch checkpoint 上做严格 smoke，选出协议合格且 Direct retention 最好的候选后再做 618 条正式评测。

## 2026-08-25 12:44:16 CST - HCV v11 LR=5e-6 多 checkpoint 严格 DP8 评测启动

- 新建并验证配置 `configs/experiments/vlm/vlm-hcv-v11-lr5e6-multicheckpoint-eval-v1.yaml`，复用完成的 5e-6 训练目录，显式评测 `step:92/138/184/230/276`（epoch 2--6）；不会重新训练。
- 每个 checkpoint 串行执行：64 条 strict RAG smoke（`max_tool_turns=5`）→ 仅 smoke 合格时执行 618 条 Direct → 618 条 RAG → 10,000 次 paired bootstrap 与 promotion gate。所有服务均使用原生 SGLang `TP=1, DP=8`，Direct/RAG 并发分别为 64/24。
- Dry-run 已确认包含 `SKIP_TRAIN=1`、正确训练目录和五个显式 step。后台队列为 `outputs/runs/vlm/vlm-hcv-v11-lr5e6-multicheckpoint-eval-v1/20260825T124416-2ef993b1-a01/`（PID `1662125`）；启动时 GPU 0--7 容量检查通过，当前正在 checkpoint-92 的 smoke 服务阶段。

## 2026-08-25 13:09:43 CST - HCV v11 LR=5e-6 多 checkpoint 严格 DP8 队列完成

- 队列正常结束（exit `0`），按 step `92/138/184/230/276` 串行运行；所有 SGLang 服务均以 native `TP=1, DP=8` 启动。工作流完成不表示任一 checkpoint 晋级。
- Strict 64-row smoke：step-92 有 1 个 invalid/malformed call（`1.56%`）而跳过 formal；step-138 完整生成 64 条预测但有 1 个 explicit terminal-closure error，strict normalization 拒绝评分；step-184、230、276 均通过 smoke。step-230 的单次 post-budget attempt 被 terminal closure 正常处理，未构成 invalid call。
- 完整 Direct（618 条）只对 smoke 通过的 step-184/230/276 生成：accuracy 分别为 `46.76%`、`46.76%`、`46.44%`；相对 M1 的 paired delta 为 `-21.52`、`-21.04`、`-21.04 pp`，均不满足 Direct retention gate。相对 raw-base 仅为 `+6.15`、`+6.47`、`+6.15 pp`。
- 三个 checkpoint 均生成 618 条 candidate RAG 预测后被 strict formal 拒绝，故没有正式 RAG/raw-base paired claim。宽松诊断（移除 explicit error 字段但原预测仍照常计错）RAG accuracy：step-184 `58.09%`（open `42.72%`，option `73.46%`），step-230 `56.63%`（`41.42%`，`71.84%`），step-276 `56.96%`（`43.04%`，`70.87%`）。
- RAG protocol traces呈现后期稳定性退化：step-184 有 6 invalid rows、1 explicit error、6 malformed attempts；step-230 为 9/2/11；step-276 为 11/0/11（invalid rows/explicit errors/malformed attempts）。step-276 虽没有显式 error，仍因 invalid-tool rate `1.78%`、11 次 malformed attempts 被 strict gate 拒绝。
- 一个队列健壮性缺陷已修复并由 focused tests 通过：当 strict normalization 因协议错误没有写出 smoke metrics 时，`validate_smoke` 现在显式返回失败，而不会再随后抛出误导性的 `FileNotFoundError`。未放宽任何 strict gate，也未改变已有结果。
- 结论：LR=5e-6 确实使训练 loss 快速收敛，但没有改善 Direct retention，且 RAG 的全量协议稳定性仍不足；step-184 是当前这轮中宽松 RAG 能力最高的 checkpoint，但不是可晋级模型。

## 2026-08-25 13:18:00 CST - Direct 能力保护策略

- 基于 5e-6 的多 checkpoint 结果，训练 loss 的充分收敛没有带来 Direct 保持（step-184/230/276 Direct 仅约 46.4--46.8%，比 M1 低约 21 pp），因此不能只通过延长训练或继续调低学习率解决。
- 后续数据路线应采用 token-weighted Direct anchor replay：使用来自 M1 的、与正式评测一致的简洁 image→answer Direct 样本（覆盖 open/option、en/zh、disease/pest、known/unknown），按 token 计而非按行数确保 Direct assistant token 占训练监督的约 50--60%；RAG 轨迹作为其余 40--50%，并保持 label-blind 与 public-evidence 约束。
- 训练采用两阶段：先用较保守学习率、短时 Direct+RAG 混合 SFT 并在每个 epoch smoke Direct；只有 Direct retention 通过后，才增加 terminal-closure/RAG 专项样本。最终 checkpoint 按 Direct retention 与 RAG protocol 双 gate 选择，而不按训练 loss 选择。
- 该策略仍需通过一个新的、不可变数据 artifact 和消融实验验证；当前 v11 结果不作为可晋级模型。

## 2026-08-25 13:47:52 CST - HCV v12 Direct anchor 混合训练启动

- 按“仅混用数据、不引入额外模型侧方法”执行 Direct retention 消融。新构造器 `tools/rag_distill/build_hcv_v12_direct_anchor_mix.py` 保留 v11 的 5,856 行和原始顺序，仅复制零工具调用的 Direct anchors；新增单测通过。
- 冻结 artifact：`outputs/artifacts/agrinet-hcv-manual-json-v12-direct-anchor-mix/`，共 14,816 行；4,480 个 Direct anchor 以总 factor 3 出现，新增 8,960 条具独立 provenance 的 replay 行。token proxy 下 Direct 占比从 v11 的 `32.18%` 提升至 `58.74%`（下限 55%），无新增工具轨迹，所有不变量和授权通过，SHA-256 `70c7751c6dbde0c5b314f357af6c4a69a944bf64fd9e288f6c63b23f39dfbc69`。
- 注册并 dry-run 验证 2 epoch、LR `1e-6`、8 GPU 从 M1 checkpoint-18 重训；配置为 `configs/vlm/qwen3_vl_4b_hcv_manual_json_v12_direct_anchor_mix_8gpu_e2_sft.yaml`，实验为 `configs/experiments/vlm/vlm-sft-qwen3vl4b-hcv-v12-direct-anchor-mix-8gpu-e2-v1.yaml`。
- 训练已后台启动：`outputs/runs/vlm/vlm-sft-qwen3vl4b-hcv-v12-direct-anchor-mix-8gpu-e2-v1/20260825T134752-2ef993b1-a01/`，PID `1749609`。已确认 8 个 training rank、M1 checkpoint-18、v12 数据、LR 1e-6 及 2 epoch 参数；启动阶段无 OOM/NCCL/NaN/Inf/traceback。
- 下一步：完成后先对两个 epoch checkpoint 运行 Direct/RAG strict smoke；仅在 Direct retention 明显改善时进入 618 条全量评测。

## 2026-08-25 14:28:34 CST - 标准 4B SFT 参数登记并从 Qwen base 重启

- 用户指定当前 4B 模型的标准 SFT 参数：初始化 `models/Qwen3-VL-4B-Instruct`、3 epoch、per-device batch size 2、gradient accumulation 4、learning rate `5e-6`。该组参数已显式登记为新实验的 `standard_4b_parameters`。
- 先前 v12 M1 初始化的 2 epoch/batch1/acc16/LR1e-6 run 被人工停止，状态 `failed`、exit `143`；这是主动替换配置，历史日志/未完成输出保留，不删除、不作为实验结果。
- 新配置：`configs/vlm/qwen3_vl_4b_hcv_manual_json_v12_direct_anchor_mix_base_8gpu_e3_sft.yaml`；新实验：`configs/experiments/vlm/vlm-sft-qwen3vl4b-hcv-v12-direct-anchor-mix-base-8gpu-e3-v1.yaml`。数据仍为已冻结的 v12 Direct-anchor mix，未增加任何模型侧方法。
- 已通过 `agrinet` show/dry-run，启动命令确认包含 Qwen base、3 epoch、batch 2、accumulation 4、LR 5e-6、8 rank 和 master port 29713。后台 run：`outputs/runs/vlm/vlm-sft-qwen3vl4b-hcv-v12-direct-anchor-mix-base-8gpu-e3-v1/20260825T142834-2ef993b1-a01/`，PID `1753927`。启动阶段正常，无 OOM/NCCL/NaN/Inf/traceback。

## 2026-08-25 16:22:04 CST - 标准参数 v12 训练进展与 checkpoint 评测队列

- 标准参数 Qwen-base v12 训练仍在运行，检查时约 `123/696` step（约 18%）；最近 loss 从约 `0.356` 降至 `0.186`，8 卡均为 100% 利用率、显存约 73--77 GiB，未见 OOM/NCCL/NaN/Inf/traceback。首个 epoch checkpoint 尚未生成。
- 已注册并 dry-run 验证训练后依赖队列：`configs/experiments/vlm/vlm-hcv-v12-direct-anchor-mix-base-e3-checkpoint-eval-v1.yaml`。它严格等待本训练 run 变为 complete，不会启动重复 SFT；之后串行评测 step `232/464/696`（3 个 epoch checkpoint），每个 checkpoint 走 64-row strict smoke → gated Direct/RAG 618 → paired bootstrap。
- 评测队列已 detached：`outputs/runs/vlm/vlm-hcv-v12-direct-anchor-mix-base-e3-checkpoint-eval-v1/20260825T162204-2ef993b1-a01/`，PID `1763037`。当前为等待训练依赖状态，不占评测 GPU。

## 2026-08-25 18:22:15 CST - v12 标准 4B 训练 OOM 定位、allocator 恢复与评测重排

- 原标准 run `outputs/runs/vlm/vlm-sft-qwen3vl4b-hcv-v12-direct-anchor-mix-base-8gpu-e3-v1/20260825T142834-2ef993b1-a01/` 在约 step 188/696 失败（exit `1`）。首个根因是 rank 5 backward 的 CUDA OOM：需申请 `17.34 GiB`，GPU 尚余 `10.20 GiB`，其中 PyTorch 已保留未分配 `13.04 GiB`；未发现 NaN、Inf、NCCL 或数据解析错误。变长长轨迹与 allocator 碎片叠加是已验证的失败机制。该残缺输出无 epoch checkpoint，不可恢复或评测。
- 恢复只改变分配器策略，不改变用户指定的模型、数据或优化超参数。`local_launch.pytorch_cuda_alloc_conf` 已接入 `src/agrinet/cli/vlm.py` 并由单测覆盖；新训练显式传递 `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`。仍为 Qwen3-VL-4B base、v12 冻结数据、3 epoch、batch `2`、gradient accumulation `4`、LR `5e-6`、8 GPU，且保持 `max_length=16384`。
- 已验证的新配置/实验：`configs/vlm/qwen3_vl_4b_hcv_manual_json_v12_direct_anchor_mix_base_8gpu_e3_allocator_sft.yaml` 和 `configs/experiments/vlm/vlm-sft-qwen3vl4b-hcv-v12-direct-anchor-mix-base-8gpu-e3-allocator-v1.yaml`；focused `tests/test_vlm_adapters.py` 通过，`agrinet` show/dry-run 正确显示 allocator 环境变量。新训练 run 为 `outputs/runs/vlm/vlm-sft-qwen3vl4b-hcv-v12-direct-anchor-mix-base-8gpu-e3-allocator-v1/20260825T182017-2ef993b1-a01/`（PID `1771417`），启动后 8 卡均在计算。
- 首次新评测队列误因缺少 `wait_for_run_dir` 进入脚本内训练分支，已在其早期显式终止（exit `143`）；主训练未被中断。随后已修正依赖，并启动新队列 `outputs/runs/vlm/vlm-hcv-v12-direct-anchor-mix-base-e3-allocator-checkpoint-eval-v1/20260825T182159-2ef993b1-a01/`（PID `1773952`）。它等待新训练 status `complete` 后才串行运行 step `232/464/696` 的 strict 64-row smoke、618-row Direct/RAG 和 10,000 次 paired bootstrap。
- 下一步：先验证 allocator 恢复训练跨越原 OOM 区间并完成三个 epoch checkpoint；严格 smoke 未通过则不生成正式分数。

## 2026-08-25 18:28:54 CST - 仅保留 M1/M2/M3 checkpoint 的存储清理

- 用户授权清理 `outputs/vlm_sft/` 下所有非里程碑 checkpoint。清理前核验 157 个 checkpoint；`docs/results/MILESTONE_EXPERIMENTS.md` 定义的 M1/M2/M3 三个 checkpoint 被逐一路径白名单保护，正在运行的 v12 allocator 恢复输出根也被排除。
- 已不可恢复删除 154 个非里程碑 checkpoint；训练日志、评测产物、配置、数据冻结件和 run 目录均未删除。清理后仅保留 M1 `...full_all_e5_len2048_liger_lr1e5_final/.../checkpoint-165`、M2 `...hermes_long_direct_blind_rag_1to1_e5/.../checkpoint-175`、M3 `...manual_json_8gpu_b2_e6_lr5e6_rerun/.../checkpoint-72`。三者均通过 `config.json` 与模型 shard 存在性检查。
- 磁盘可用空间由约 `3.4 TiB` 增至 `4.7 TiB`（约释放 `1.3 TiB`）。当前 v12 allocator 恢复训练仍为 `running`，未受清理影响；未来写出的候选 checkpoint 将按其评测流程管理，不自动视为里程碑。
- Next: 完成 v12 训练与严格评测；只有经用户明确指定为新的里程碑后，才作为长期 checkpoint 保留。

## 2026-08-25 22:50:01 CST - v12 epoch-1 checkpoint-232 strict smoke 结果

- 用户要求在 loss 已明显下降后提前评测。训练在完整写出 `checkpoint-232`（`global_step=232`、`epoch=1.0`）后主动停止，父训练 run 的 exit `143` 仅表示人工停止，不是 checkpoint 损坏。评测使用独立的 `SKIP_TRAIN=1` 队列：`outputs/runs/vlm/vlm-hcv-v12-direct-anchor-mix-base-epoch1-checkpoint232-eval-v1/20260825T221748-2ef993b1-a01/`。
- 原生 SGLang 服务启动成功并返回 `/model_info` 200，使用 `TP=1, DP=8`；64 条 smoke 预测全部生成、ID 覆盖完整且 `unparseable_rate=0`。宽松诊断准确率为 `31/64=48.4375%`，Open `37.5%`，Option `59.375%`；这些不是正式指标。
- Strict smoke gate 拒绝：`invalid_tool_call_rate=0.53125`（34/64 malformed tool-call attempts），但 `terminal_closure_failed=0`。因此队列正确写出 `skipped_formal.json`，没有运行 618 条 Direct、618 条 RAG 或 paired bootstrap；当前不存在 checkpoint-232 的正式 Direct/RAG 结果。
- 结论：allocator 修复解决了训练阶段的中段 OOM，但 epoch-1 模型的 serving 协议稳定性仍不达标，且 smoke 的宽松准确率低于可晋级要求。继续延长训练是否降低 malformed-call 率尚未验证；若需要正式指标，必须先获得 strict smoke 零协议错误的 checkpoint。
- Evidence: `.../artifacts/epoch-0-checkpoint-232/smoke/metrics.json`、`skipped_formal.json`、`services/candidate/service_status.json`。

## 2026-08-25 23:02:59 CST - 全量优先评测策略与 checkpoint-232 重评测启动

- 用户决定不再让 64 条预评测阻断全量结果。`scripts/vlm/run_manual_json_e6_checkpoint_queue.sh` 现支持声明式 `FULL_EVAL_WITHOUT_SMOKE=1`：启用时直接执行 618 条 Direct 与 618 条 strict RAG。RAG 的严格协议/覆盖校验和 promotion gate 不变；若失败，完整预测与拒绝原因仍保留，但不会伪装成正式指标。focused tests (`tests/test_formal_eval_runtime.py`、`tests/test_vlm_adapters.py`) 共 20 项通过。
- 新的 checkpoint-232 队列为 `outputs/runs/vlm/vlm-hcv-v12-direct-anchor-mix-base-checkpoint232-full-eval-v2/20260825T230259-2ef993b1-a01/`，显式 `SKIP_TRAIN=1`、`CHECKPOINT_SPECS=step:232`、`FULL_EVAL_WITHOUT_SMOKE=1`、native SGLang `TP=1/DP=8`。启动后状态已进入 `formal`，不再创建/消费 64-row smoke。
- 存储清理的约束：此前用户授权的清理已经不可恢复删除了所有非里程碑历史 checkpoint，因此本轮可以重新全量评测的非里程碑候选仅为 checkpoint-232；M1/M2/M3 仍作为已保留的比较里程碑。
- Next: 待该队列写完全量 Direct/RAG 预测，分别报告严格可接受的指标或“全量诊断、正式拒绝”的协议结果。

## 2026-08-25 23:10:21 CST - checkpoint-232 全量 Direct/RAG 评测结果

- 全量队列 `outputs/runs/vlm/vlm-hcv-v12-direct-anchor-mix-base-checkpoint232-full-eval-v2/20260825T230259-2ef993b1-a01/` 正常完成（exit `0`），完全跳过 64-row smoke，使用 native SGLang `TP=1/DP=8`。
- Direct 正式覆盖与评分通过：candidate 618/618，accuracy `39.1586%`；Open `5.8252%`，Option `72.4919%`；disease `40.3756%`，pest `36.4583%`；English `46.4516%`，Chinese `31.8182%`。相对 M1 Direct `67.4757%`，paired delta `-28.3172 pp`，95% CI `[-32.5243,-24.1100] pp`；相对 raw-base Direct `40.4531%`，delta `-1.2945 pp`，95% CI `[-4.5307,+1.9417] pp`。Direct 不满足 retention gate。
- Direct 的独立维度 paired review 还显示：相对 M1，known `-19.1860 pp`、unknown `-39.7810 pp`；相对 raw-base，known `+3.1977 pp`、unknown `-6.9343 pp`。这些切片不能替代整体 gate。
- RAG 全量生成 618/618 行，但有 4 条显式严格协议错误（均为 terminal-answer correction 后再次发出 tool call）：`zh_N05062_P03378_option`、`zh_N05025_P01399_open`、`zh_N04106_P00115_open`、`zh_N04119_P01565_option`。因此没有写出 RAG metrics/summary，也没有生成正式 RAG paired bootstrap；完整 predictions 保留用于诊断，`formal_rejected.json` 标记 `rag_formal_protocol_or_runtime_failed`。
- 结论：全量优先策略已按设计生效：Direct 即使不经过 smoke 也得到完整正式结果；RAG 即使协议失败也保留全量轨迹，但严格拒绝进入正式统计。checkpoint-232 不是里程碑候选。
- Evidence: `.../formal-direct/artifacts/summary.json`、`candidate_vs_m1_direct_paired_review.json`、`candidate_vs_raw_base_direct_paired_review.json`、`.../formal/artifacts/candidate_rag/predictions.jsonl`、`formal_rejected.json`。

## 2026-08-25 23:51:11 CST - 统一 RAG 宽松计分协议固化

- 用户明确规定：以后 RAG 默认采用单一的全量宽松计分，不区分“宽松分数”和“严格分数”。所有 manifest 对齐的 618 条均进入 accuracy 与 paired bootstrap；协议错误行强制计为错误，并在 `metrics.json` 的 `protocol_errors` 与 `hermes_protocol` 中单独统计。协议错误不再阻止 metrics、paired review 或 bootstrap。
- `normalize_answers.py` 已改为默认全量计分；协议错误仍输出 `protocol_error=true`，且不会因错误文本匹配标签而计对。RAG formal runner 已移除 strict validation 阻断，先完成 candidate/raw-base 两条路线，再统一计分、paired review 和 summary。运行时的非法调用处理仍保持原有 bounded terminal policy，但它不再改变评分协议。
- 已对 checkpoint-232 的历史全量预测补齐统一结果：candidate RAG `53.0744%`，raw-base RAG `52.2654%`，paired delta `+0.8091 pp`，95% CI `[-2.4272,+4.2071] pp`。candidate 协议错误 `4/618=0.6472%`；raw-base `5/618=0.8091%`。这些错误已计入整体准确率，bootstrap 已正常生成。
- 新 summary schema 为 `agrinet.rag-formal-evaluation-native-sglang-dp8/v4`，显式记录 `scoring_protocol.version=full-coverage-with-protocol-errors/v1` 与 `protocol_errors_block_metrics_or_bootstrap=false`。后续实验均按此协议解释 RAG 指标。

## 2026-08-26 00:13:53 CST - checkpoint-232 统计归因

- 配对 618 样本分析确认 Direct 相对 raw-base 的总体 `-1.29 pp`（95% CI `[-4.53,+1.94] pp`）不是均匀退化：English `+5.81 pp`（`[+1.94,+9.68]`），Chinese `-8.44 pp`（`[-13.64,-3.57]`）。主要负项是 Chinese Option disease `-22.33 pp`（103 rows，5 gains/28 losses）和 Chinese Option pest `-15.69 pp`（51 rows，5/13）；English Option pest 反而 `+26.67 pp`（45 rows，14/2）。这指向中英文本/Option 对齐的非对称泛化失配，而不是整体视觉能力均匀下降。
- 相对 M1 的 Direct `-28.32 pp`（正式 10,000 bootstrap CI `[-32.52,-24.11]`）主要由 Open `-40.13 pp`（5 gains/129 losses）驱动；最大单元为 English Open disease `-57.27 pp`（1/64 gains/losses）与 Chinese Open disease `-41.75 pp`（0/43）。这证明从 Qwen base 的 v12 checkpoint-232 没有重建 M1 的开放类别命名能力；不能用“仅缺少若干训练 step”解释。
- RAG 相对 raw-base `+0.81 pp`（10,000 bootstrap CI `[-2.43,+4.21]`），仅 58 gains 对 53 losses。Open delta `0.00 pp`，Option `+1.62 pp`；病害 `-0.23 pp`、虫害 `+3.12 pp`，所有主要切片的 2,000-resample诊断 CI 均包含零。结论是当前 RAG 没有已证实的稳定提升。
- 协议错误不是主因：candidate 的 4/618 protocol-error 行即便全部修正，accuracy 上限也只增 `0.65 pp`；其中两条对应 raw-base 本来正确、两条 raw-base 也错误。真正限制在于样本级决策收益不足与 Direct/Open 语义能力未保留，而非少量 terminal error。
- Evidence: checkpoint-232 Direct/RAG `scored.jsonl`、paired reviews、以及本次固定 seed 的 2,000 次分层配对诊断（正式 overall CI 仍取产物中的 10,000 次 bootstrap）。

## 2026-08-26 00:16:xx CST - 4B 默认学习率下调至 2e-6

- 用户决定将后续 4B HCV SFT 的默认学习率由 `5e-6` 下调至 `2e-6`。历史配置和 checkpoint-232 保持不变，以保留其可复现性；新默认不追溯修改已完成 run。
- 新规范入口：`configs/vlm/qwen3_vl_4b_hcv_manual_json_v12_direct_anchor_mix_base_8gpu_standard_lr2e6_sft.yaml` 与 `configs/experiments/vlm/vlm-sft-qwen3vl4b-hcv-v12-direct-anchor-mix-base-8gpu-standard-lr2e6-v1.yaml`。其余标准参数不变：Qwen3-VL-4B base、v12 immutable data、3 epoch、batch 2、gradient accumulation 4、8 GPU、`expandable_segments:True`。
- `agrinet` show/dry-run 已验证启动命令明确传递 `learning_rate: 2.0e-6` 和 allocator 配置。该登记为新训练默认；未启动新的训练。

## 2026-08-26 01:30:52 CST - v12 4B LR=2e-6 默认训练启动

- 按新默认重新从 `models/Qwen3-VL-4B-Instruct` 开始 v12 Direct-anchor SFT。run：`outputs/runs/vlm/vlm-sft-qwen3vl4b-hcv-v12-direct-anchor-mix-base-8gpu-standard-lr2e6-v1/20260826T013052-2ef993b1-a01/`，PID `1833193`。
- 已核验实际运行参数：3 epoch、per-device batch 2、gradient accumulation 4、learning rate `2e-6`、8 GPU（0--7）以及 `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`。启动时 GPU 容量可用，训练 status 为 `running`；无 OOM/NCCL/traceback。
- 后续评测遵循当前统一协议：直接全量 618 条 Direct/RAG，RAG 协议错误计错并单独报告，但不阻止 metrics 和 paired bootstrap。

## 2026-08-26 14:08:51 CST - v12 LR=2e-6 训练完成并启动三 checkpoint 全量评测

- LR=`2e-6` v12 训练正常完成（exit `0`），无 OOM/NCCL/NaN/Inf/traceback。输出目录为 `outputs/vlm_sft/qwen3_vl_4b_hcv_manual_json_v12_direct_anchor_mix_base_8gpu_standard_lr2e6/v0-20260826-013108/`；三个 epoch checkpoint `232/464/696` 均经 trainer state 与两份 model shard 存在性核验。
- Epoch 末步训练 loss 分别为 checkpoint-232 `0.15845`、checkpoint-464 `0.04524`、checkpoint-696 `0.10052`；这是训练 token loss，不能据此替代泛化选择。
- 用户要求多 checkpoint 评测。已启动串行全量队列 `outputs/runs/vlm/vlm-hcv-v12-direct-anchor-mix-base-lr2e6-multicheckpoint-full-eval-v1/20260826T140851-2ef993b1-a01/`，显式 `SKIP_TRAIN=1`、`FULL_EVAL_WITHOUT_SMOKE=1`、checkpoint specs `232/464/696`。每个 checkpoint 顺序执行 618 条 Direct 与 618 条 RAG、统一全量协议错误计错和 10,000 paired bootstrap；队列目前位于 checkpoint-232 的 formal 阶段。

## 2026-08-26 14:40:00 CST - v12 LR=2e-6 三 checkpoint 全量评测完成

- 原始队列在 checkpoint-232 之后以 exit `1` 停止，根因是 promotion gate 的预期拒绝触发了 shell 的 `ERR` trap，并非服务或评测失败。已将 gate 置于 `if ! ...; then` 中；拒绝仅记录证据，不再中断剩余 checkpoint。focused runtime tests（13 项）、`bash -n` 与 `git diff --check` 均通过。
- 恢复队列 `outputs/runs/vlm/vlm-hcv-v12-direct-anchor-mix-base-lr2e6-multicheckpoint-full-eval-recovery-v1/20260826T143708-2ef993b1-a01/` 正常完成（exit `0`），复用 checkpoint-232 预测并完成 checkpoint-464/696。所有候选 Direct 与 RAG 预测均为 618 行；每条路线均生成 metrics、paired review 与 promotion gate 证据。
- Direct（candidate / raw-base / M1）：checkpoint-232 为 `34.47% / 40.45% / 67.80%`，checkpoint-464 为 `35.76% / 40.78% / 67.48%`，checkpoint-696 为 `34.95% / 40.45% / 66.83%`。相对 raw-base 的配对差分别为 `-5.99`、`-5.02`、`-5.50 pp`；Direct retention 在三个 checkpoint 均失败。
- RAG（candidate / raw-base）：checkpoint-232 为 `58.41% / 52.59%`（`+5.83 pp`, 95% CI `[+2.75,+8.90]`），checkpoint-464 为 `58.25% / 51.94%`（`+6.31 pp`, `[+3.24,+9.55]`），checkpoint-696 为 `57.61% / 51.78%`（`+5.83 pp`, `[+2.59,+9.06]`）。三个 checkpoint 的 RAG 增益均通过正向 paired-effect gate，最佳点为 checkpoint-464。
- 统一 RAG 计分下的候选协议错误分别为 `2/618`、`3/618`、`5/618`，均已作为错误计入分母；它们不构成 Direct 退化的解释。三个 promotion gate 均拒绝，原因均为 Direct 非劣与主要子群检查失败。
- Evidence: 原 artifact root `outputs/runs/vlm/vlm-hcv-v12-direct-anchor-mix-base-lr2e6-multicheckpoint-full-eval-v1/20260826T140851-2ef993b1-a01/artifacts/`；每个 `epoch-0-checkpoint-{232,464,696}` 下的 `formal-direct/artifacts/summary.json`、`formal/artifacts/summary.json` 与 `promotion_gate.json`。

## 2026-08-26 15:49:51 CST - v12 Direct-only 训练消融启动

- 为定位 v12 的 Direct 退化是否由 RAG/工具轨迹混合监督导致，启动受控 Direct-only 消融。它保持 Qwen3-VL-4B base 初始化、8 GPU、3 epoch、batch `2`、gradient accumulation `4`、LR `2e-6`、max length `16384` 与 ZeRO-3 不变，仅移除全部工具轨迹。
- 新冻结数据 artifact `outputs/artifacts/agrinet-hcv-manual-json-v12-direct-only/` 从 v12 immutable mix 中稳定抽取原始零工具调用 Direct anchors：4,480 行、SHA-256 `4c40a6a73962586b1e85a10ba27172b936d5615bef07ae0ae5f22fb2fad8a639`。验证了 ID 唯一、单图、零工具调用、且不含 v12 replay 副本。
- 训练实验/配置：`configs/experiments/vlm/vlm-sft-qwen3vl4b-hcv-v12-direct-only-base-8gpu-lr2e6-v1.yaml` 与 `configs/vlm/qwen3_vl_4b_hcv_manual_json_v12_direct_only_base_8gpu_lr2e6_sft.yaml`。run：`outputs/runs/vlm/vlm-sft-qwen3vl4b-hcv-v12-direct-only-base-8gpu-lr2e6-v1/20260826T154951-2ef993b1-a01/`（volatile: running）。启动已确认 8 rank 计算、210 step、无 OOM/NCCL/NaN/Inf。
- Next: 训练完成后只对 final epoch checkpoint 运行原生 SGLang `TP=1, DP=8` 的 618 条 Direct 评测，与 M1 和 raw base 作 10,000 次 paired bootstrap；不运行 RAG。

## 2026-08-26 22:20:00 CST - v12 Direct-only final checkpoint Direct 评测

- Direct-only 训练 run 正常完成（exit `0`），完成 210 step/3 epoch，并写出 checkpoint `70/140/210`。final checkpoint 为 `outputs/vlm_sft/qwen3_vl_4b_hcv_manual_json_v12_direct_only_base_8gpu_lr2e6/v0-20260826-155006/checkpoint-210`。
- 618-row Direct-only formal evaluation 正常完成（exit `0`），原生 SGLang `TP=1, DP=8`，candidate、M1、raw-base 各有 618/618 条唯一预测。candidate Direct 为 `36.89%`，raw-base 为 `40.45%`，paired delta `-3.56 pp`（95% CI `[-6.63,-0.65]`）；相对 M1 `66.83%` 为 `-29.94 pp`（`[-33.98,-26.05]`）。
- 去除全部 RAG/工具轨迹相对混合 v12 LR=2e-6 epoch-3 Direct（`34.95%`）提升约 `+1.94 pp`，表明轨迹混合确有负迁移，但仍不足以恢复 raw-base。相对 raw-base：Open `+3.88 pp`、pest `+1.04 pp`、known `+0.58 pp`；残余退化为 unknown `-8.76 pp`、Option `-11.00 pp`、disease `-5.63 pp`。
- 结论：当前 Direct 失败不只是 RAG 格式干扰，更关键是 Direct-only 的训练数据/任务分布没有覆盖或强化 unknown Option disease 的泛化能力。不要将 checkpoint-210 作为里程碑或主模型。
- Evidence: `outputs/runs/vlm/vlm-hcv-v12-direct-only-base-lr2e6-checkpoint210-formal-direct-v1/formal618-native-dp8-20260826-220410/artifacts/summary.json`。

## 2026-08-26 22:30:00 CST - Direct-only 与 M1 差距归因

- 618 条配对结果中，M1 独自正确 202 条，Direct-only 独自正确 17 条，净差 185 条（`-29.94 pp`）；这是预测质量差异，不是覆盖、解析或服务错误。三条路线均为 618/618 完整预测。
- 类别词表覆盖是已验证的主因。以评测 152 个唯一标签的任一规范别名是否出现在训练 assistant 文本中统计：M1 覆盖 `147/152`（96.7%），Direct-only 仅 `74/152`（48.7%）。Direct-only 在 known disease/pest 的覆盖约为 78.2%/75.3%，在 unknown disease/pest 仅 16.7%/36.4%；M1 在各四个 cell 为 90.6%--100%。
- 这与结果对齐：Direct-only 相对 M1 在 unseen-label unknown disease Open 为 `-60.00 pp`（105 rows），在 seen-label unknown disease Open 仍为 `-57.14 pp`（21 rows）；Option 同样在 unseen-label unknown disease 低 `-21.90 pp`。因此未知标签缺失解释最大部分，但已见类别的区分监督也不足。
- 监督密度也显著不同。M1 为 2,114 条、约 4.24M 序列字符（约 2,004 字符/样本），均为显式视觉观察、多候选对比、排除与答案的 `<think>/<answer>` 轨迹；Direct-only 为 4,480 条、约 3.22M 字符（约 718 字符/样本），以固定选项选择与短 Direct 答案为主。更多行并未带来更多的类别级判别知识。
- 消除 RAG 轨迹后只带来约 `+1.94 pp`（34.95%→36.89%），说明 RAG 混合是次要负迁移项。主问题是 v12 Direct 数据在长尾类别、病害、Option 对比和中英文类别表达上的知识覆盖/辨析质量不足，且从 Qwen base 重新训练没有继承 M1 已形成的 Direct 类别空间。
- Evidence: Direct formal summary `outputs/runs/vlm/vlm-hcv-v12-direct-only-base-lr2e6-checkpoint210-formal-direct-v1/formal618-native-dp8-20260826-220410/artifacts/summary.json`; M1 training data `outputs/vlm_sft/disease_pest_large/sft_messages_en_zh.jsonl`; Direct-only freeze `outputs/artifacts/agrinet-hcv-manual-json-v12-direct-only/data.jsonl`.

## 2026-08-27 04:29:40 CST - M1 Direct 两级 gate 首轮结果

Tags: m1-direct, hcv, data-quality, blind-audit

- 容量 preflight 已重新通过：217 类、195,232 个唯一源图、193,180 个隔离后可用图，N05053 与同域 hard-negative 均无短缺，`plan_ready=true`。证据为 `outputs/artifacts/datasets/m1-direct-current-hcv-v1/preflight_report.json`。
- Level-1 使用 2 张图、两域各一张、每图四视图；N05053 的 `needs_id` 补充图被强制纳入私有计划。v2 暴露答案格式与单帧事实 prompt 缺陷；v3 修复后 8/8 接受，teacher/auditor/reasoning 均 8/8，零错误，正式 gate 通过。证据为 `reports/sample_preflight_gate.json` 与 `outputs/runs/data/data-m1-direct-current-hcv-v1/sample-preflight-v3/`。
- Level-2 首轮使用 64 个全新唯一图像，八格各 8 条，与 Level-1 零 hash 重叠；128 次 teacher/auditor 调用均明确完成，无 unknown delivery。结果仅 45/64 接受，teacher 64/64 正确、reasoning 64/64 完整，但 auditor 仅 59/64 与私有真值一致，18 条视觉事实审核失败。正式 gate 因格子通过率、整体接受率和 auditor accuracy 失败。
- 拒绝主要来自宿主断言、不可见微观结构、静态图动作/进展、物质成因和近邻物种不可区分；不得放宽预定义阈值，也不得复用首轮 pilot 图像。`m1-direct-plan` 仍由失败的 Level-2 gate 硬阻断，全量 4,340 条采集与 SFT 均未授权。
- Next: 收紧 teacher v3 为纯单帧描述契约，把首轮 64 图纳入 contacted 排除集，从未联系隔离池构建全新八格 pilot；只有新 pilot gate 通过才生成 full plan。

## 2026-08-27 10:05:07 CST - M1 Direct 循环修复与 Level-2 v6 结果

Tags: m1-direct, transport-recovery, blind-audit, quality-gate

- 修复了恢复循环的两个根因：已收到但不可解析的 provider 响应不再误标为 unknown；异常退出会从 request checkpoint 重建 partial ledger，覆盖陈旧的上一轮 ledger。真正网络未知交付仍禁止自动重放。相关测试 30 项通过。
- v4 的历史 unknown 无法事后确认，按 fail-closed 归档；其 partial ledger 为 29 known、1 unknown、34 not-attempted，gate 失败。所有计划图像在远程调用前原子登记到 contacted ledger。
- 经全新 image-screen v4/v5 形成 v5 pilot。v5 64 对调用均已知，49/64 accepted；12 个 `all_candidates_compared=false` 实际都有四个非空候选比较，确认 auditor check 职责歧义。prompt 已明确把结构覆盖与视觉支持分别归入不同 checks，gate 阈值未修改。
- 再经全新 image-screen v6/v7 形成 v6 pilot。v6 的 64 对 teacher/auditor 全部已知，57/64 accepted，zero-tolerance、teacher accuracy、blind-auditor accuracy 和 reasoning checks 均通过；拒绝仅为 7 条实质 auditor checks（5 visual facts、2 leakage、1 invisible facts，可重叠），不再出现 comparison-coverage 误判。
- Level-2 v6 仍失败：Open/en/pest 5/8、Open/zh/pest 6/8；其余六格通过。整体 57/64=89.06%，低于预定义 90%，不得启动 full collection、freeze 或 SFT。证据：`outputs/artifacts/datasets/m1-direct-current-hcv-v1/reports/stratified_pilot_gate.round6.failed.json` 和 `outputs/runs/data/data-m1-direct-current-hcv-v1/stratified-pilot-v6-screened/`。
- Next: 先针对 pest Open 的可见事实支持、标签泄漏与不可见事实做新的小样本 preflight；该 gate 通过后才允许再跑完整八格 Level-2 pilot。

## 2026-08-27 12:59:56 CST - M1 Direct Open/pest targeted preflight v1 中断归档

Tags: m1-direct, transport-recovery, open-pest, fail-closed

- 为定位 Level-2 v6 的 Open/en/pest 与 Open/zh/pest 失败，新增独立且版本化的 diagnostic gate：每格 8 条、两格均要求 8/8 接受，teacher、盲审和 reasoning 均要求 100%，并保持 unknown、泄漏、协议、格式与不可见事实的零容忍。它明确不能替代原 eight-cell Level-2 gate。
- 新 builder 只从未联系、隔离合格的 pest 图像构造 16 条 Open/en+zh 计划。其 public/private manifest 严格分离；public 不含 class code、真值、正确 option 或路径。计划报告为 `outputs/artifacts/datasets/m1-direct-current-hcv-v1/reports/open_pest_preflight_plan.json`，确认 16 个唯一图像、与既有 contacted 集合零重叠。
- `run_m1_direct_collection.py` 现在会在首个远程 POST 前原子登记整个 plan 的 image hashes；同一 round 仅可幂等恢复，任何跨 round 的 hash 重用均被拒绝。runner 同时将遗留的 `in_progress` checkpoint 先持久化为 `unknown`，禁止盲目重放。
- v1 的远程会话在第 8 个 teacher 请求期间中断，不能确认 provider 是否已接收该请求。因此 16 个图像均已登记并退役；partial ledger 记录 1 条 `unknown_delivery`、其余未完成项为 `not_attempted`，targeted gate 失败。这是 transport/交付未知状态，不是质量 gate 的通过或失败证据。
- 验证：`PYTHONPATH=. .venv/bin/pytest -q tests/test_m1_direct_collection.py tests/test_m1_direct_gates.py tests/test_m1_direct_runner.py tests/test_cli.py` 为 31 passed；计划生成与 public/private boundary 检查均通过。
- Next: 从该 contacted ledger 排除的全新隔离 pest 图像创建 Open/pest targeted preflight v2；只有 v2 完整通过后才启动全新的 image-screen 和完整 8-cell Level-2 pilot。

## 2026-08-27 15:01:30 CST - Open/pest 候选可区分性 screen v1/v2 均未达 gate

Tags: m1-direct, open-pest, distinguishability, fail-closed

- 为避免用 prompt 强行通过肉眼不可区分的同域近邻类，实现了独立、无真值 candidate-distinguishability screen。公开请求只包含图像、随机候选和公开知识；私有 gate 才将独立选择同真实标签对齐。必须同时满足：高置信、图像质量通过、至少三条可见事实，以及对其余三个候选各有一条具体可见冲突。
- v1 和 v2 均为 32 个新、唯一、隔离合格图像（Open/en/pest 16、Open/zh/pest 16），并在第一个远程请求前把全体 hash 原子登记到 `isolation/pilot_contacted.jsonl`。两轮均完成 32/32 checkpoint，无 unknown、泄漏、协议、格式或不可见事实硬错误。
- v1 screen gate：en 7/16、zh 7/16，14/32；v2：en 8/16、zh 5/16，13/32。两轮均未达到每格至少 8 个可提升样本的冻结要求，故没有生成 teacher/auditor v7 计划。累计错误仅为 `screen_choice_mismatch`（27）与 `not_visually_distinguishable`（28，可重叠），表明失败来自可见候选区分而非 provider 交付。
- 为防止绕过屏障，只有通过私有 screen gate 的样本才能从 screen ledger 显式、原子地提升到 v7；任何其他跨阶段或跨轮 hash 重用都被拒绝。八格 pilot/image-screen gate 也显式要求 v7 targeted gate 通过。
- 代码与 gate：`tools/rag_distill/run_m1_direct_image_screen.py`、`src/agrinet/data/m1_direct_gates.py`、`src/agrinet/data/m1_direct_runner.py`、`configs/sampling/m1-direct-open-pest-distinguishability-screen-gates-v1.yaml`。证据：`reports/open_pest_screen_v1_gate.json`、`outputs/runs/data/data-m1-direct-current-hcv-v1/open-pest-screen-v1/`、`outputs/runs/data/data-m1-direct-current-hcv-v1/open-pest-screen-v2/`。
- Validation: focused M1 Direct suite passed 36 tests; `git diff --check` passed.
- Next: 在相同 gate 下只从仍未联系的隔离图像建立 v3 screen；若任一 cell 仍不足 8，停止在 screen 阶段，不启动 v7 或 Level-2。

## 2026-08-27 15:38:31 CST - screened v7 preflight 因未知 auditor 交付 fail-closed

Tags: m1-direct, transport-recovery, open-pest, fail-closed

- 扩容后的 screen v3 完成 64/64 个确定 checkpoint，Open/en/pest 有 14 个、Open/zh/pest 有 15 个可提升样本；预定义私有 screen gate 通过，零 unknown、泄漏、协议、格式或不可见事实硬错误。仅从这些通过样本确定性构建了 v7 的 8+8 teacher/auditor preflight。
- v7 在首条 English 样本的 auditor POST 收到 `RemoteDisconnected`，故交付状态为 unknown；runner 写出完整 partial ledger：1 条 `unknown_delivery`、15 条 `not_attempted`。没有重放该请求，也没有复用或替换 v7 已提升的 16 个 hash。
- 这是 transport 终态而非质量 gate 证据，v7 strict gate 必然失败。恢复必须从全新、未联系图像建立独立 screen，再从其私有通过样本构建新的 preflight；不得把 v3 剩余或 v7 计划图像跨轮复用。
- Evidence: `outputs/runs/data/data-m1-direct-current-hcv-v1/open-pest-screen-v3/`, `reports/open_pest_screen_v3_gate.json`, `outputs/runs/data/data-m1-direct-current-hcv-v1/open-pest-preflight-v7/`, and `private/open_pest_preflight_v7_ledger.jsonl`.

## 2026-08-27 18:00:00 CST - 八格候选筛选 v1/v2 完整失败归档

Tags: m1-direct, level-2, distinguishability, transport-recovery, fail-closed

- 八格 Level-2 的 label-blind candidate-distinguishability screen v1 已完成 128/128 个检查点，但 gate 未通过：38 个候选合格、50 条 `unknown_delivery`；每格通过数为 Open/en/disease 8、Open/en/pest 8、Open/zh/disease 3、Open/zh/pest 10、Option/en/disease 6、Option/en/pest 3、Option/zh/disease 0、Option/zh/pest 0。所有 v1 图像已在 `isolation/pilot_contacted.jsonl` 中退役。
- 运行器修复为逐样本写入 ledger，并在未知交付后继续同一预登记 round 中的其它独立样本；未知请求仍不可重放。恢复后的 v1 完整账本在 `private/stratified_screen_ledger.jsonl`，gate 为 `reports/stratified_screen_gate.json`。
- 使用全新、与所有历史 contacted hash 零重叠的 128 张图像建立 v2。v2 完成 128/128 个检查点，55 个候选合格，但 4 条 `unknown_delivery` 触发零容忍；Open/en/disease、Open/zh/disease、Open/zh/pest、Option/en/disease 分别仅 3、5、6、7 个合格候选。证据：`private/stratified_screen_v2_ledger.jsonl` 与 `reports/stratified_screen_v2_gate.json`。
- 因而 formal eight-cell teacher/auditor pilot、4,340-row full collection、freeze、SFT 和 618-row evaluation 都未获授权，且没有启动。v3 将仅从仍未联系的隔离图像选择，采用独立 config/plan/ledger，并将候选覆盖扩为每格 32；最低 8 个通过与全部零容忍项不变。
- Validation: image-screen recovery patch passed `py_compile`, `git diff --check`, and the focused M1 Direct suite (`39 passed`) before v2 launch.

## 2026-08-27 20:10:34 CST - 扩容八格 screen v3 通过；formal pilot 因 Option 审计契约缺陷失败

Tags: m1-direct, level-2, option-contract, blind-audit, fail-closed

- 为避免 v1/v2 的候选覆盖不足，v3 在不放宽任何质量阈值或零容忍项的前提下，把每格候选数从 16 扩至 32。256 张图像均与历史 contacted hash 零重叠，公开清单无真值/正确字母/本地路径，私有对齐完整。
- v3 screen 完成 256/256 个确定交付、零 hard error、113 个通过样本，八格通过数依次为 14、19、8、15、14、13、11、19；`reports/stratified_screen_v3_gate.json` 为 `passed=true`。仅从其私有通过集合确定性提升 64 张（八格各 8）至 formal pilot。
- formal pilot 的第一次启动在 contacted-ledger 的本地 promotion-lineage 校验处停止（0 teacher/0 auditor 远程请求）；原因是命令仍声明 screen v1。修复为显式 v3 晋级后，64 teacher + 64 label-blind auditor 调用全部以确定状态完成。
- Level-2 formal gate 仍失败：30/64 接受，要求的总体接受率为 90%。Open/en/disease、Open/en/pest、Open/zh/pest 均为 8/8，Open/zh/disease 为 6/8；四个 Option 格子均为 0/8。所有交付、teacher accuracy、blind-auditor accuracy、reasoning checks 和 zero-tolerance 均通过。证据：`reports/stratified_pilot_gate.json`、`private/stratified_pilot_ledger.jsonl`、`outputs/runs/data/data-m1-direct-current-hcv-v1/stratified-pilot-v7-screened/`。
- 根因确认是 Option 审计 prompt 的格式契约错误：Option 的 teacher 正确答案按 Direct 契约只输出字母，且为防止泄漏，label-blind auditor 不接收该字母；但 prompt 错误要求 `answer_under_audit` 必须复制候选名称，致使所有 Option 行 `format_complete=false`。修复后的 prompt 规定 Option 中该字段为空是预期行为，格式检查应审 reasoning 结构而非缺失字母。回归：`39 passed`。
- 此外 Open/zh/disease 保留两条真实拒绝：一条将静态图中的病斑描述为“扩展”，另一条在 Tomato mosaic virus vs Yellow Leaf Curl Virus 的独立选择上分歧。它们必须由全新隔离的样本筛选和更严格的单帧可见事实约束处理。已有 formal-pilot 图像均已联系/退役，不得重评、重放或复用。
- Next: 建立新的、仅使用未联系隔离图像的修复验证链。它必须首先验证 Option auditor format 修复及 Open/zh/disease 单帧事实约束，然后才可重新进行 eight-cell formal pilot；full collection、freeze、SFT 和 618-row evaluation 均继续未授权。

## 2026-08-27 21:07:42 CST - v4 筛选恢复链与 v8 正式 pilot 路由就绪

Tags: m1-direct, level-2, option-contract, transport-recovery

- 修复 Option 盲审格式契约后，已建立全新 v4 eight-cell distinguishability screen：256 个未联系且隔离合格图像（每格 32）。其 public/private manifest 边界、唯一 image/sample ID 和 contacted-hash 零重叠均已验证。收集为 volatile 本地 managed run：`outputs/runs/data/data-m1-direct-current-hcv-v1/20260827T201834-2ef993b1-a01/`；截至本记录写入时，132/256 条 ledger 已以确定 checkpoint 持久化，未见 provider/protocol 错误。
- 新增 `m1-direct-pilot-v8-plan`、`m1-direct-pilot-v8-collect`、`m1-direct-pilot-v8-gate`。v8 plan 只可从通过的 v4 screen 私有 `passed_rows` 提升；v8 collection 明确声明 prior stage `stratified_distinguishability_screen` / round `v4`，使用独立的 v8 manifest、ledger、run 路径与 repaired Option blind-auditor prompt。full plan 的前置 gate 也改为 v8，而非已失败且已退役的 v7。
- 验证：focused M1 Direct suite 为 40 passed，`py_compile src/agrinet/cli/data.py` 与 `git diff --check` 均通过。
- Next: 等待 v4 达到 256 个终态 checkpoint；只在 `stratified_screen_v4_gate.json` 通过后运行 v8 plan/collect/gate。若 v4 有未知交付或任一 gate 不通过，所有 v4 图像保持 retired，禁止提升或重放。

## 2026-08-27 21:48:54 CST - v4 完整失败归档；启动全新 v5 screen

Tags: m1-direct, level-2, transport-recovery, fail-closed

- v4 收集器已正常结束并写完 256/256 个唯一 checkpoint：255 known、1 unknown。正式 gate `reports/stratified_screen_v4_gate.json` 为 `passed=false`；硬错误为 `unknown_delivery`，且 Open/en/disease 仅 7 个合格图像，低于每格 8 的预定义下限。总计 102 个通过的 screen 样本不具备晋级资格。v4 的 256 张图像均已作为 contact round `v4` 登记并 retire；未知请求未重放。
- 新增 v5 screen 的独立 config/output/ledger/CLI 路由，沿用完全相同的 32/格、每格至少 8 与 zero-tolerance 条件。v8 promotion 改为只能使用通过的 v5 私有 `passed_rows`，其 collection 也只允许 `stratified_distinguishability_screen/v5` lineage。
- v5 plan 通过本地审计：256 个唯一 image/sample ID、八格各 32、public manifest 零真值/正确字母/本地路径字段、与 contacted ledger 零 hash 重叠。唯一 detached collector 为 PID `2173711`，run 目录 `outputs/runs/data/data-m1-direct-current-hcv-v1/20260827T214854-2ef993b1-a01/`；本记录时为 17/256 known checkpoint，状态 volatile。
- Validation: focused M1 Direct suite 40 passed，`py_compile` 与 `git diff --check` 通过。
- Next: 只监控 v5 已登记 run。完成后运行 `m1-direct-pilot-screen-v5-gate`；仅在该 gate 通过时才创建 v8 formal pilot。

## 2026-08-28 01:07:48 CST - v8 formal pilot gate 失败并发现中文候选规范歧义

Tags: m1-direct, level-2, blind-audit, catalog-contract, fail-closed

- v8 formal pilot 的唯一受管采集进程正常结束（exit 0）；私有 ledger 为 64/64 行、全部 known delivery，teacher 与 label-blind auditor 的私有真值选择均为 64/64 正确，且无 zero-tolerance hard error。证据：`outputs/runs/data/data-m1-direct-current-hcv-v1/20260828T002250-2ef993b1-a01/`、`outputs/artifacts/datasets/m1-direct-current-hcv-v1/private/stratified_pilot_v8_ledger.jsonl`。
- 预定义 formal gate 未通过：56/64 accepted（87.5%，低于 90%）；Open/zh/pest 与 Option/en/pest 均为 6/8，低于每格 7。八个拒绝均为 auditor checks，不是 transport failure。正式报告：`outputs/artifacts/datasets/m1-direct-current-hcv-v1/reports/stratified_pilot_v8_gate.json`。因此 full 4,340-row plan、freeze、SFT 和 618-row evaluation 继续禁止。v8 的 64 张图像保持 contacted/retired，不得重放或复用。
- 拒绝审计显示三项可修复的生成契约问题：从单帧 cropped/obscured 像素推断未见结构或材料、在 Option reasoning 中关联正确字母与类别、以及中文名称不能唯一表达候选。teacher prompt 已加强前两项约束，聚焦 M1 Direct tests 为 `40 passed`，`git diff --check` 通过。
- 本地 catalog audit 发现 217 类中有 8 个真实 hard-negative candidate sets 含重复 `chinese_name`（包括 `豆荚螟`、`番茄早疫病`、`樱桃健康叶片`、`石榴炭疽病`、`葡萄褐斑病`）。这使相关中文 Open 的只输出名称契约及中文 Option 的候选展示不可唯一。planner 已增加 fail-closed 校验，遇到任何重复中文候选名立即拒绝构建；该校验尚未改变 catalog 名称或重写 hard negatives。
- Next: 在开始新的全新隔离 Level-1 preflight、screen 或 pilot 之前，确定六组重名类别的规范中文显示名（或批准保留名称并重选受影响 hard negatives）。随后只从未 contacted 的隔离图像建立新的修复验证链。

## 2026-08-28 01:24:45 CST - 中文候选唯一性修复后 repair-v9 preflight 仍因视觉分歧失败

Tags: m1-direct, level-1, candidate-distinguishability, fail-closed

- 从相同 `wiki_siglip2_joint_text_image_top12` 图文联合排序中，为 8 个含重复中文候选名的类跳过重复显示名候选并保留最靠前的三个中文唯一同域负类；未改类目真值或配额。重建 preflight 后为 217 类、193,180 个隔离可用图像、每类至少 5 张、217/217 三个 hard negatives 完整、4,340-row 容量完整；8 项选择的审计记录为 `reports/hard_negative_zh_uniqueness_repairs.jsonl`。
- 新增 planner fail-closed 规则：中文 candidate set 含重复显示名时直接拒绝构建。修复 prompt 同时禁止从裁切/遮挡像素推断未见结构/材料，禁止 Option reasoning 关联答案字母与类别。聚焦 M1 Direct suite 为 48 passed，`git diff --check` 通过。
- 从未 contacted 的隔离池生成 repair-v9：Open/Option × en/zh × disease/pest 八格各一张，8 个唯一 hash、public/private 对齐且 public 无真值、correct letter 或本地路径。唯一采集进程 exit 0，8/8 known delivery。
- repair-v9 的严格逐样本 gate 未通过：7/8 accepted，teacher 8/8 正确、reasoning checks 8/8 通过；唯一失败为 `option/zh/pest` 的 `auditor_choice_mismatch`。图像为外观相近蛾类，teacher 按私有真值选 `agrotis ypsilon`，label-blind auditor 独立选 `ostrinia furnacalis`；双方审计视觉事实、结构比较和泄漏检查均合格。这是单帧可区分性失败，而非 transport、中文名称或格式失败。证据：`reports/repair_preflight_v9_gate.json`、`private/repair_preflight_v9_ledger.jsonl`、`outputs/runs/data/data-m1-direct-current-hcv-v1/repair-preflight-v9/`。
- Next: repair-v9 的八个图均 retire。建立只针对 `option/zh/pest` 的全新 label-blind distinguishability screen（预设 32 candidates、至少 8 通过、zero-tolerance 不变），仅从 screen-passed 图像中补做该格的 Level-1 preflight；不得随机重抽或进入新的 eight-cell pilot。

## 2026-08-28 03:37:05 CST - v7 screen 通过；v9 formal pilot 因挂起 POST fail-closed

Tags: m1-direct, level-2, transport-recovery, fail-closed

- 新的 eight-cell v7 distinguishability screen 已终态通过：256/256 known delivery、109 passed、各格通过数为 17/20/12/15/10/14/10/11，且 zero-tolerance 项（unknown、leakage、protocol、format、invisible fact）均为零。报告：`outputs/artifacts/datasets/m1-direct-current-hcv-v1/reports/stratified_screen_v7_gate.json`。
- 只从 v7 private `passed_rows` 提升的 v9 formal pilot 有 64 张唯一图像（八格各 8），public/private 对齐且仅保留 v7 promotion lineage。22 个 teacher 与 22 个 label-blind auditor 请求已确认完成；第 23 个 teacher POST 在 provider polling 中超过配置的 300 秒仍未返回。终止受管进程后，将该 durable `in_progress` checkpoint 规范化为 `unknown_delivery`；其余 41 行为 `not_attempted`。v9 gate 因 unknown delivery 和配额不足严格失败，`passed=false`。
- 已修复 collection runner：每个 POST 现在有 SIGALRM wall-clock deadline，超时进入既有的 unknown/no-replay checkpoint 路径；partial-state writer 也会将残留 `in_progress` checkpoint 原子转换为 `unknown`，再写出完整 64 行 private ledger。验证：focused M1 Direct suite 46 passed、`py_compile` 与 `git diff --check` 通过。
- Evidence: `outputs/runs/data/data-m1-direct-current-hcv-v1/stratified-pilot-v9/`, `private/stratified_pilot_v9_ledger.jsonl`, `reports/stratified_pilot_v9_gate.json`, `tools/rag_distill/run_m1_direct_collection.py`, `tests/test_m1_direct_runner.py`.
- Next: v9 的全部 64 个 hash 已登记为 `stratified_pilot/v9`，不得重放或降级复用。新 formal pilot 必须先使用仍未联系的隔离图像完成独立的八格 screen，并在其通过后仅提升该 screen 的 passed rows；full collection、freeze、SFT 和 DP8 evaluation 仍未授权。

## 2026-08-28 04:19:02 CST - v8 screen 出现 unknown；下一恢复链已版本化

Tags: m1-direct, level-2, transport-recovery, fail-closed

- 正在运行的 `stratified_screen_v8` 已持久化 114/256 个 checkpoint，其中 112 known、2 `unknown_delivery`、47 passed。任一 unknown 均触发零容忍，因此该轮不会具备晋级资格；collector 仍须完成其预登记的 256 个样本以留下完整 contacted/retired 审计。Volatile run: `outputs/runs/data/data-m1-direct-current-hcv-v1/20260828T035029-2ef993b1-a01/`; ledger: `private/stratified_screen_v8_ledger.jsonl`.
- 在不启动任何额外远程请求的前提下，已版本化下一条恢复链：`m1-direct-pilot-screen-v9-{plan,collect,gate}` 仅接受 v8 的终态失败 gate，并从仍未联系的隔离容量创建独立 256-image / eight-cell plan；`m1-direct-pilot-v10-{plan,collect,gate}` 仅从通过 v9 gate 的私有 `passed_rows` 提升，并强制 prior round 为 v9。全量 Direct plan 的 formal-pilot 前置条件同步改为 v10 gate。
- 验证：`py_compile src/agrinet/cli/data.py`、两个新 operation 的 `--dry-run` 和 focused M1 Direct suite 均通过（47 passed）。
- Next: 等 v8 collector 终态后先运行并归档 v8 gate；只有 gate 明确为 failed 才可建立 v9 screen。不得提前执行 v9 plan、重放 unknown 请求，或启动 full collection、freeze、SFT、DP8 evaluation。

## 2026-08-28 04:58:37 CST - v8 screen 失败归档并启动全新 v9 screen

Tags: m1-direct, level-2, transport-recovery, fail-closed

- v8 collector 已完成 256/256 个唯一预登记 sample checkpoint，私有 ledger 为 254 `known`、2 `unknown`、114 passed；八格均有 32 个终态行。受管 run 以 exit 1 结束，随后独立 gate 也按预定义零容忍条件失败：`all_cells_pass=true`、`exact_attempts=true`、`unique_sample_ids=true`，但 `zero_tolerance=false`。证据：`outputs/artifacts/datasets/m1-direct-current-hcv-v1/private/stratified_screen_v8_ledger.jsonl`、`reports/stratified_screen_v8_gate.json`、`outputs/runs/data/data-m1-direct-current-hcv-v1/20260828T035029-2ef993b1-a01/`。未知请求未重放，v8 图像继续 retired。
- v8 终态失败后，v9 plan 已创建并经本地 launch audit：public/private 各 256 行、sample ID 完全对齐、256 个唯一 image hash、八格各 32；public 没有 `class_code`、`correct_option`、truth 或 query path；相对 contacted ledger 和 v8 plan 的 image-hash overlap 均为 0。
- 已启动唯一的 detached v9 collector：PID `2228208`，run `outputs/runs/data/data-m1-direct-current-hcv-v1/20260828T045808-2ef993b1-a01/`，ledger `private/stratified_screen_v9_ledger.jsonl`。Volatile: 运行状态必须在后续操作前重新检查。
- Next: 仅监控 v9 直到 256 个预登记样本全部成为终态；运行 `m1-direct-pilot-screen-v9-gate`。只有该 gate 为 passed 才能创建并收集 v10 formal pilot；否则保持全部 v9 图像 retired。

## 2026-08-28 05:24:00 CST - M1 Direct gate 改为拒绝式 90% cohort 验收

Tags: m1-direct, acceptance-gate, audit-retention

- 用户明确将 gate 政策改为拒绝式验收：`unknown_delivery`、泄漏、协议/格式错误和不可见事实仍然拒绝对应样本、保留其私有审计记录并永久排除出训练，但不再因单条拒绝自动否决完成的 cohort。正式 pilot 以固定分母、每格最低条件和整体接受率大于 90% 判定；整数 64-row formal cohort 最少需要 58 条 accepted（90.625%）。
- 新增 `configs/sampling/m1-direct-acceptance-gates-v2.yaml`，用于后续 formal pilot/full-route 的 rejection accounting；`m1_direct_gates.py` 现在报告 `rejection_reasons`，并以这些拒绝从 accepted numerator 中排除。新增 `configs/sampling/m1-direct-stratified-distinguishability-screen-gates-v3.yaml`，为未来 256-row screen 加入 known-delivery 和 acceptance 两项 >90% cohort 条件，同时保留每格至少 8 个可提升候选。
- v9 已在旧不可变 v2 screen gate 下启动，不能事后替换其绑定策略；其终态仍须按启动时的 gate 审计。新策略只适用于后续明确绑定 v3 的 screen 及 formal pilot。全量 freeze 的完整类别配额、SFT 训练输入隔离和私有真值分离不变。
- Validation: `PYTHONPATH=. .venv/bin/pytest -q tests/test_m1_direct_gates.py tests/test_m1_direct_collection.py tests/test_m1_direct_runner.py tests/test_cli.py` 为 50 passed；`py_compile` 与 `git diff --check` 通过。
- Next: 继续唯一的 v9 collector；终态后按其冻结 gate 出具报告。若需要新的 replacement screen，必须显式绑定 v3 gate，并使用仍未联系的隔离图像。

## 2026-08-28 05:31:00 CST - 拒绝式 cohort gate 的严格大于 90% 边界已实现

Tags: m1-direct, acceptance-gate, reproducibility

- 用户确认阈值为“超过 90%”，而非“至少 90%”。gate validator 新增显式 strict-rate 语义；future formal pilot 与 future screen 的配置均启用它。64 行 pilot 的可接受最小整数为 58（90.625%）；256 行 screen 的可接受最小整数为 231（90.234%）。
- 任一 rejection-error 仍使相应行从 accepted numerator 移除，并保留在私有 ledger；它不会单独构成 cohort veto。每格最低 accepted、教师/盲审正确率、reasoning 检查、泄漏与协议约束仍按预定义 gate 验收。
- Validation: `PYTHONPATH=. .venv/bin/pytest -q tests/test_m1_direct_gates.py tests/test_m1_direct_collection.py tests/test_m1_direct_runner.py tests/test_cli.py` 为 50 passed；`py_compile src/agrinet/data/m1_direct_gates.py` 和 `git diff --check` 通过。
- Volatile: 正在运行的 v9 screen 启动时绑定旧的不可变 v2 screen policy，不能事后替换；它须终态后按原 gate 报告。新的 rejection-based policy 适用于后续显式绑定版本。

## 2026-08-28 06:02:37 CST - v9 eight-cell screen 通过；v10 formal pilot 已启动

Tags: m1-direct, level-2, formal-pilot, screen-promotion

- v9 screen collector 已正常写满 256 个唯一终态样本，全部 `known` delivery。immutable v2 gate 报告 `passed=true`：八格均为 32 attempts，screen-passed 数为 Open/en/disease=13、Open/en/pest=14、Open/zh/disease=15、Open/zh/pest=17、Option/en/disease=11、Option/en/pest=14、Option/zh/disease=12、Option/zh/pest=19，总计 115。hard errors 与 rejection reasons 均为空。证据：`outputs/artifacts/datasets/m1-direct-current-hcv-v1/reports/stratified_screen_v9_gate.json`。
- 已运行 v10 promotion：只从 v9 private `passed_rows` 挑选，生成 64 public + 64 private 行、64 个唯一 image hash、八格各 8，ID 严格对齐且 public 顶层无 class code、correct option、truth name、query path、audit 或本地路径。`screen_lineage` 记录 gate schema 与 gate passed 状态；gate hash 在 64 行中一致。证据：`public/stratified_pilot_v10_teacher_plan.jsonl`、`private/stratified_pilot_v10_truth_alignment.jsonl`、`reports/stratified_pilot_v10_plan.json`。
- 已提交唯一 detached v10 formal-pilot collector：PID `2237639`，run `outputs/runs/data/data-m1-direct-current-hcv-v1/20260828T060237-2ef993b1-a01/`。它使用 rejection-based `m1-direct-acceptance-gates-v2.yaml`；64 行 fixed cohort 必须至少 58 accepted（严格超过 90%）并满足每格 7/8。Volatile: 完成前不得启动 full plan、freeze、SFT 或 DP8 evaluation。

## 2026-08-28 06:46:12 CST - v10 formal pilot 通过；全量 Direct 计划已审计

Tags: m1-direct, formal-pilot, full-plan, acceptance-gate

- v10 collector 已以 exit 0 结束；private ledger 为 64 个唯一且确定交付的样本。预定义 rejection-based gate 为 `passed=true`：61/64 accepted（95.3125%，严格超过 90%），八格 accepted 分别为 7、7、8、7、8、8、8、8；teacher accuracy、blind-auditor accuracy、reasoning 检查、唯一 ID 和 cohort-level 条件均通过。拒绝仍仅保留在私有 ledger，不进入训练。证据：`outputs/artifacts/datasets/m1-direct-current-hcv-v1/reports/stratified_pilot_v10_gate.json`、`private/stratified_pilot_v10_ledger.jsonl`、`outputs/runs/data/data-m1-direct-current-hcv-v1/20260828T060237-2ef993b1-a01/status.json`。
- v10 gate 通过后才运行 `m1-direct-plan`。全量 public/private manifests 各 4,340 行、ID 完全对齐、1,085 个唯一 image hash（每张四视图）、217 类各五张；八格容量为 disease 各 725、pest 各 360。public manifest 未含 truth/code/correct-option/query path，并与 formal 618、历史 Direct、当前 RAG、contacted/retired hash 均为零重叠。所有行统一绑定 `m1-direct-acceptance-gates-v2.yaml` digest 和 teacher/auditor v5。证据：`public/teacher_plan.jsonl`、`private/truth_alignment.jsonl`、`reports/plan.json`。
- Next: 启动唯一 full-v1 可恢复 collector。每张图在首次 POST 前即登记 contacted/retired；未确定交付不重放。collection 结束后先审计 private ledger 的 rejected/accepted 状态并建立失败补采计划；在 217×5×4 的完整 accepted quota 和 freeze gate 通过之前，SFT 与 618-row evaluation 均不授权。

## 2026-08-28 06:49:15 CST - 全量启动隔离缺口修复并开始 full-v1 收集

Tags: m1-direct, isolation, full-collection, transport-recovery

- 第一次 full-v1 提交在首次远程请求之前于 contacted registration 被安全阻断：静态 `contacted_or_retired` 快照与 append-only `pilot_contacted.jsonl` 存在时间差，计划中有 22 个已联系 hash。该 run 没有 teacher/auditor checkpoint、没有新注册 hash，也没有任何远程调用；其 managed 状态为 failed/exit 1，保留为 preflight 证据。
- planner 已修复为在 full-plan 阶段合并运行期 `pilot_contacted` ledger 作为隔离输入，并增加回归测试。重新生成的 plan 仍为 217 类、1,085 unique images、4,340 rows，但相对 runtime contacted、formal evaluation、historical training 与 current RAG 均为 zero hash overlap。focused M1 Direct tests 为 51 passed，`py_compile` 与 `git diff --check` 通过。
- 修复后唯一 full-v1 collector 已启动：run `outputs/runs/data/data-m1-direct-current-hcv-v1/20260828T064915-2ef993b1-a01/`，PID `2244888`。启动后确认 1,085 个 full-v1 hash 已在首次 POST 前登记，teacher 已有 2 complete/1 in_progress，auditor 已有 2 complete。Volatile：最终 ledger 只有收集完成后才写入，不得据此提前判断接受率。
- Next: 仅监控该 managed run 和 durable `full-v1/{teacher,auditor}` checkpoints。结束后先审计完整 private ledger、拒绝原因与完整配额；若有拒绝，则从未联系隔离图像建立补采链，禁止冻结、SFT 或 DP8 evaluation 直至完整 217×5 accepted image quota 达成。

## 2026-08-28 07:25:08 CST - full-v1 中间采集状态保持可恢复

Tags: m1-direct, full-collection, transport-recovery

- full-v1 managed collector 仍在运行（PID `2244888`）。当前 durable checkpoints 为 teacher 100 complete、7 unknown、1 in_progress；auditor 100 complete。7 条 unknown 继续保持 `do_not_replay_without_provider_resolution`，最终 ledger 尚未生成。
- 已完成配对样本的只读早期对齐仅用于趋势观察，不作为 freeze 或接受率证据；正式判断必须等待 4,340 个计划样本终态并由 private ledger 原子固化。
- Next: 继续监控同一 run；终态后先执行 full audit，再按 rejected image 的原类别构建补采，未通过完整 freeze gate 前不启动 SFT 或 618-row DP8 evaluation。

## 2026-08-28 07:46:13 CST - full audit 对拒绝式 unknown 终态账本开放补采审计

Tags: m1-direct, rejection-based-gate, transport-recovery

- 用户确认 cohort 不是零容忍：固定分母接受率必须严格 `>90%`，而 known-invalid/unknown delivery 只拒绝对应样本、私有保留且永久排除训练。64-row pilot 的下限为 58；256-row screen 的下限为 231。
- 已修复 `m1-direct-full-audit` 的终态判定：当 full-v1 的所有 4,340 个计划 sample 都有唯一的 `known` 或 `unknown` ledger 行时，允许生成私有 audit/replenishment 证据；未知行仍不会进入 accepted staging。audit 永远写入 `freeze_authorized=false`，因此不能直接放行 freeze 或 SFT。
- Validation: `PYTHONPATH=. .venv/bin/pytest -q tests/test_m1_direct_collection.py tests/test_m1_direct_runner.py tests/test_m1_direct_gates.py tests/test_cli.py` 通过（54 passed）；`py_compile src/agrinet/cli/data.py src/agrinet/data/m1_direct_collection.py` 与 `git diff --check` 通过。新增回归测试覆盖 full audit 接收含 unknown 的完整终态 ledger，但仍要求 replenishment 且禁止 freeze。
- Volatile: 唯一 full-v1 collector（PID `2244888`）仍在运行；截至本记录的 durable checkpoint 为 teacher 119 complete/7 unknown，auditor 118 complete/1 in-progress，尚无最终 ledger 或 collection summary。
- Next: 仅继续该 collector。终态后核验完整 ledger 与 exact ID coverage，运行 corrected full audit，再按被拒绝 image group 的同类新图执行补采。

## 2026-08-28 07:53:15 CST - 补采合并与类别配额校验闭环补全

Tags: m1-direct, replenishment, freeze-contract

- 增加 `m1-direct-replenishment-v1-audit-merge`：在初始 full-v1 与同类补采均终态后，私下合并审核结果，只将四视图全通过的完整图像组写入 staging；含拒绝或 unknown 的图像组整体淘汰，且始终输出 `freeze_authorized=false`。
- freeze 新增私有 sample-to-class 对齐校验，要求恰好 217 个类别且每类恰好 5 张图像，防止仅凭总行数误通过。
- Validation: focused M1 Direct suite `56 passed`，`py_compile` 与 `git diff --check` 通过；replenishment merge 已加入 `agrinet data submit` dry-run 注册检查。
- Volatile: full-v1 collector 仍运行（PID `2244888`）。最近检查为 teacher 130 complete、8 unknown、1 in-progress；auditor 130 complete；尚无终态 ledger。
- Next: 等待 full-v1 完成，运行 full audit → replenishment plan/collect → replenishment audit-merge；若仍有拒绝则继续同类补采，直到 1,085 个完整图像组满足 freeze。

## 2026-08-28 07:55:00 CST - full-v1 auditor unknown 按逐样本拒绝保留

Tags: m1-direct, full-collection, rejection-based-gate

- 最新只读检查显示 full-v1 teacher 为 195 complete、8 unknown、1 in-progress；auditor 为 194 complete、1 unknown。该 auditor unknown 与 teacher unknown 一样保持 no-replay，并在最终私有 ledger 中作为拒绝样本处理。
- collector 仍为唯一运行实例（PID `2244888`），尚无最终 ledger、run report 或 collection summary；未启动任何新远程请求。
- Next: 继续等待 4,340 条请求终态，随后执行完整 ledger 校验、私有 audit 和同类整组补采。

## 2026-08-28 09:00:00 CST - full-v1 仍在唯一可恢复 collector 中运行

Tags: m1-direct, full-collection, transport-recovery

- 只读复核确认没有重复 collector、没有重放 unknown 请求，也没有提前创建 full ledger。最近状态为 teacher 266 complete、9 unknown、1 in-progress；auditor 264 complete、2 unknown。主进程 PID `2244888`、collection 子进程 PID `2245025` 仍存活并保持网络连接。
- unknown 请求继续遵循 no-replay 规则；其对应图像组在终态后会按原类别整组拒绝，并由 replenishment plan 使用新的同类隔离图像替代。
- 已验证的数据侧实现与下游 route 均保持 gate 阻断：full audit 需要 4,340 个唯一 terminal ledger 行和 runtime contacted 覆盖；freeze 需要 217 类×5 图×4 视图及 zero-overlap；SFT queue 需要 immutable `training_authorized=true`。
- Next: 不干预正在运行的 collector；终态后按 full audit → v1 replenishment → vN continuation → freeze 顺序推进，所有大 JSONL 继续使用原子、分块写入。

## 2026-08-28 08:46:13 CST - 多轮同类补采已具备可恢复闭环

Tags: m1-direct, replenishment, rejection-based-gate, transport-recovery

- 将用户确认的拒绝式 cohort 政策保持为当前不可回退约束：单条 `unknown_delivery`、泄漏、协议/格式、不可见事实或 reasoning 缺陷只拒绝该样本并私有留痕；已完成的固定分母 cohort 以严格 `>90%` 接受率及预定义格子/质量 gate 判断，不采用全 cohort 零容忍。
- 补齐 v2+ 的通用操作：`m1-direct-replenishment-next-plan` 只以已完成 private audit/merge 的历史轮次计算短缺；新增 `m1-direct-replenishment-next-collect` 与 `m1-direct-replenishment-next-audit-merge`。每轮仍只补同类、全新、未联系图像的四视图组；audit 会重新聚合 full-v1 与全部既往补采，仅将四视图均 accepted 的组写入 staging。
- Validation: `PYTHONPATH=. .venv/bin/pytest -q tests/test_m1_direct_collection.py tests/test_m1_direct_runner.py tests/test_m1_direct_gates.py tests/test_cli.py` 为 57 passed；`py_compile src/agrinet/cli/data.py src/agrinet/data/m1_direct_collection.py`、`git diff --check` 和三个 next-operation 的 managed submit dry-run 均通过。
- Volatile: 唯一 full-v1 collector（PID `2244888`）继续运行；本记录时 durable checkpoint 为 teacher 228 complete/8 unknown/1 in-progress，auditor 226 complete/2 unknown，最终 ledger 尚不存在。
- Next: 不重启 collector。其终态后先执行 full audit，再按 v1 → vN 进行整组同类补采与私有 merge，直至完整 217×5 quota 通过 freeze；随后才允许 Direct-only SFT 和 formal DP8 evaluation。

## 2026-08-28 08:55:38 CST - Direct-only SFT 与三 checkpoint Direct 评测路由已声明

Tags: m1-direct, direct-only, sft, formal-evaluation

- 新增 freeze-gated Direct-only SFT 配置：`configs/vlm/qwen3_vl_4b_m1_direct_current_hcv_8gpu_sft.yaml`，固定从 `models/Qwen3-VL-4B-Instruct` 初始化，8 GPU、3 epoch、per-device batch 2、gradient accumulation 4、LR `2e-6`、ZeRO-3、`max_length=16384`，数据源仅允许不可变 `m1-direct` frozen artifact。
- 新增注册实验与队列脚本：`vlm-sft-qwen3vl4b-m1-direct-current-hcv-8gpu-v1` / `run_m1_direct_checkpoint_queue.sh`。队列启动前强制读取 frozen `validation.json` 的 `training_authorized=true`，然后仅执行 Direct native DP8 的三个 epoch checkpoint 618-row 路线；每个 checkpoint 使用 10,000 次 paired bootstrap，并对 raw-base 与 M1 生成独立证据。
- 新增 `validate_m1_direct_promotion_gate.py`，要求整体不低于 raw-base、raw-base paired CI 下界不低于 -3pp、known/unknown、Open/Option、disease/pest 六个主要切片退化不超过 -3pp，并确认 M1 comparison evidence 存在。
- 新增 freeze authorization preflight：注册 VLM `train` 与 M1 Direct queue 均拒绝缺失或未授权的 immutable validation artifact。当前 frozen artifact 尚不存在，因此 queue dry-run 被正确拒绝，SFT 尚未启动。
- Validation: M1/data/VLM focused suites 共 68 tests passed；queue shell syntax、Python compile、`git diff --check` 均通过。
- Volatile: full-v1 collector PID `2244888` 仍运行；最新 durable checkpoint 为 teacher 243 complete/8 unknown/1 in-progress、auditor 241 complete/2 unknown。
- Next: 等待 full-v1 终态，完成 audit → same-class multi-round replenishment → freeze；仅在 freeze authorization 成立后启动此 Direct-only SFT queue。

## 2026-08-28 11:58:35 CST - 拒绝式 >90% cohort 政策再次确认并持久化

Tags: m1-direct, acceptance-gate, rejection-based-policy

- 用户再次确认：不得采用全 cohort 的完全零容忍。`unknown_delivery`、泄漏、协议/格式错误、不可见事实和 reasoning 缺陷只拒绝相应样本，私有保留审计证据并排除出训练；不因单条拒绝否决已完成 cohort。
- 当前 active goal 与 `configs/sampling/m1-direct-acceptance-gates-v2.yaml` 已一致固化该决策：`stratified_pilot` 的固定分母整体接受率必须严格 `> 0.90`，64-row cohort 因而至少接受 58 条；其 `zero_tolerance` 为空，拒绝原因通过 `rejection_errors` 逐行处理。
- Volatile: 只读检查时唯一 full-v1 collector 仍存活，durable checkpoints 为 teacher 567 complete、9 unknown、1 in_progress；auditor 564 complete、3 unknown。未知请求不重放，等该 run 写出完整终态账本后按原类别整组补采。
- Next: 保持同一 rejection-based 规则，继续 full audit → 同类补采 → immutable freeze；freeze 前不启动 SFT 或 Direct-618 evaluation。

## 2026-08-28 14:17:00 CST - 后续 M1 Direct 大批量采集启用受限多线程

Tags: m1-direct, collection, concurrency, transport-recovery

- 根据用户要求，M1 Direct 的 future full/replenishment collector 现在以 8 个独立样本 worker 并发执行。每个 worker 仍严格串行执行同一 sample 的 teacher → label-blind auditor；不同 sample 只共享只读 plan 与各自独立、原子 checkpoint。
- `unknown` checkpoint 的 no-replay 语义不变：worker 只会读取 confirmed checkpoint；请求发送前写 `in_progress`，不确定交付写为 `unknown`，之后不会再次调度该 sample。并发 request 的 signal wall-clock guard 仅在主线程启用，worker 仍使用 HTTP timeout，避免 process-global signal 相互干扰。
- active full-v1 不能中途切换并发；中断会将当前 `in_progress` 请求保守退休为 unknown。因此其继续按已启动的单 worker 进程终态化。后续 v1/vN same-class replenishment 自动从 config 取得 `collection_workers: 8`。
- Validation: `PYTHONPATH=. .venv/bin/pytest -q tests/test_m1_direct_runner.py tests/test_cli.py tests/test_m1_direct_collection.py tests/test_m1_direct_gates.py` passed (62 tests); `py_compile` for runner/collector/CLI and `git diff --check` passed. New runner tests assert bounded two-worker overlap and reject nonpositive worker counts.
- Next: wait for the active full-v1 terminal ledger; execute full audit, then launch any required same-class replacement rounds with the new bounded worker setting.

## 2026-08-28 14:20:00 CST - full-v1 已完成配对的只读质量信号偏低

Tags: m1-direct, quality-observation, full-collection, rejection-based-policy

- 对 active full-v1 durable checkpoints 做只读 interim alignment：1,335 个已有 complete teacher + complete auditor 的配对中，1,083 accepted、252 rejected，view-level acceptance 为 81.12%。这不是终态 cohort gate：未完成与 unknown delivery 没有纳入分母，且当前计划顺序只覆盖 disease 四格，pest 尚未观察。
- 已观察到的 disease slices：Open/en 82.09%、Open/zh 84.48%、Option/en 77.18%、Option/zh 80.72%。拒绝主要为 `auditor_choice_mismatch` 193 和 `auditor_checks_failed` 131（可重叠）；teacher-answer mismatch 尚未出现在该中间 report。
- 在 325 个四视图均已完成的 image groups 中，197 组四视图全 accepted（60.62%）。这意味着若该信号延续，全量路线将需要较多同类整组 replenishment；不能据此冻结或声称 >90% 收集质量。
- Evidence was computed read-only from `full-v1/{teacher,auditor}` checkpoints against the locked public/private plan, with no output artifacts or checkpoint changes. Next: let the active run terminalize; inspect the full ledger and rejection distribution before deciding whether any prompt/auditor policy adjustment is warranted for future replenishment.

## 2026-08-29 14:13:05 CST - full-v1 与 v1 补采已审计；v2 被同类新图容量预检阻断

Tags: m1-direct, replenishment, capacity-audit, freeze-gate

- full-v1 private audit 已完成：3,408/4,340 view 通过，932 view 拒绝或未知；冻结仍未授权。v1 补采随后以 8 workers 正常完成 1,960/1,960 teacher 与 auditor checkpoint，私有合并后共有 816 个四视图完整通过的图像组（3,264 staging rows）；仍有 759 个部分/拒绝图像组被整体丢弃。
- `m1-direct-replenishment-next-plan` 已在任何新的 teacher POST 前 fail-closed：下一轮需要的新同类隔离图像容量缺口为 `N04036:4`、`N04074:3`、`N04097:3`、`N05053:2`。因此未创建 v2 collection plan 或启动远程收集。
- 为恢复容量，已只读检索可许可外部候选并下载到临时 review 路径；未将这些候选接入任何正式 plan，也未将其标记为 contacted。N05053 当前公开田间来源只新增一个多图 `needs_id` occurrence，若要求独立 occurrence 不能填满两张缺口；馆藏标本图像则不应未经显式路线决策混入田间识别数据。
- 另外修复了一项可恢复性错误：若 v1 的 immutable plan 已原子登记、但在首次 POST 前因凭据/endpoint 启动失败，精确同一 stage+round 的重试现在获准；不同 hash 或不同 round 仍会拒绝。Focused tests：54 passed，`py_compile` 与 `git diff --check` 通过。
- Next: 对四个缺口类别的外部候选完成逐图来源、hash isolation、可见性与标签/症状适配 preflight；只把通过项加入新的同类 pool。若任一类别无法达到其 shortfall，保持 fail-closed，禁止 freeze 和 SFT。

## 2026-08-29 22:48:35 CST - 用户授权先行 Direct-only interim SFT，短缺类延期补采

Tags: m1-direct, interim-sft, user-authorized-exception, todo

- 用户明确要求：N04036 柑橘炭疽病缺 4 张、N04074 辣椒青枯病缺 3 张、N04097 草莓裂果病缺 3 张的后续补采暂不阻塞，先用当前已通过完整四视图审计的 816 个图像组（3,264 条 Direct student rows）进行 SFT；后续再做同类补采。
- 新增 `m1-direct-interim-authorize`，生成独立且不可变的 `outputs/artifacts/datasets/m1-direct-current-hcv-v1/interim-sft/`。该 artifact 明确记录 `quota_complete=false`、每类缺口、regular freeze 未通过、零 isolation overlap、student contract 通过和数据 hash；不得把它标称为常规 217×5 freeze。
- 新增 interim VLM experiment/config，仍从 `models/Qwen3-VL-4B-Instruct` 使用 8 GPU、3 epoch、batch 2、gradient accumulation 4、LR `2e-6`、ZeRO-3、`max_length=16384`，仅使用 Direct 数据；SFT queue 启动前强制校验 authorization type。
- 已启动 managed SFT：run `20260829T224835-2ef993b1-a01`，PID `2463994`；当前状态为 running，训练日志显示 153 steps、step 1 完成、8 GPU utilization 100%，无 RAG 数据混入。
- TODO（用户要求后续处理）：待 interim SFT 与 Direct-618 三 checkpoint 评测完成后，再为 `N04036:4`、`N04074:3`、`N04097:3`、以及此前记录的 `N05053:2` 继续寻找全新、隔离、视觉适配的同类图片；最多按原先 v2–v5 补采上限执行。不得重放 unknown 或使用已拒绝候选。
- Validation: focused M1/data/VLM suites 66 passed；interim authorization artifact rows=3,264/images=816，data SHA-256=`a38adc4bcf123ca9bbbc3e5eb3804c798ab4a793141257aec1c26a6b76900832`。

## 2026-08-30 01:33:27 CST - Interim Direct-only SFT 与三轮 Direct-618 评测完成，全部拒绝 promotion

Tags: m1-direct, interim-sft, direct-only, formal-evaluation, promotion-gate

### Objective
在用户授权的、不完整但已审计的 816 个四视图图像组上，验证 Direct 数据本身能否重建 Direct 能力；不混入任何 RAG 轨迹。

### Setup
- Data: `outputs/artifacts/datasets/m1-direct-current-hcv-v1/interim-sft/`，3,264 rows / 816 images；`quota_complete=false`，仅由用户的 interim authorization 放行。
- Model/config: `models/Qwen3-VL-4B-Instruct`，8 GPU、3 epoch、batch 2、gradient accumulation 4、LR `2e-6`、ZeRO-3、`max_length=16384`。
- Run: `outputs/runs/vlm/vlm-sft-qwen3vl4b-m1-direct-current-hcv-interim-8gpu-v1/20260829T224835-2ef993b1-a01/`，status `complete`，exit 0。
- Evaluation: 每个 epoch checkpoint 均执行 native DP8 Direct-618；每个结果 618 个唯一预测，并与 raw base 和 M1 各做 10,000 次 paired bootstrap。

### Results
| Checkpoint | vs raw-base Δ pp (95% CI) | vs M1 Δ pp (95% CI) | Promotion |
| --- | ---: | ---: | --- |
| epoch 1 / 51 | -4.05 [-6.96, -1.13] | -30.58 [-34.63, -26.54] | rejected |
| epoch 2 / 102 | -3.72 [-6.63, -0.97] | -30.10 [-34.14, -26.05] | rejected |
| epoch 3 / 153 | -3.56 [-6.47, -0.65] | -29.61 [-33.66, -25.57] | rejected |

epoch 3 is the least-negative candidate, but it still violates all configured raw-base gates: overall non-inferiority, bootstrap lower-bound floor, and major-subgroup threshold. Its largest raw-base regression is Option (-9.06 pp); disease is -4.23 pp and unknown is -4.01 pp. All exact evidence is retained under each `artifacts/epoch-*/formal-direct/artifacts/summary.json` and `promotion_gate.json`.

### Conclusion
- Verified: training and all three formal Direct-only evaluations completed; all yielded exact coverage and metric artifacts.
- Verified: no checkpoint is authorized for promotion, release, or RAG mixing. A complete queue with exit 0 is the expected terminal state for this rejected experimental conclusion.
- Unknown: whether a later regular, quota-complete freeze can recover Direct performance; this interim result is not evidence that it will.

### Next
Return to same-class replenishment capacity preflight. The immediate planner blockers remain new, isolated candidate capacity for N04036, N04074, N04097, and N05053; retain the existing no-replay and five-round ceiling rules.

## 2026-08-30 02:05:00 CST - M1 与 interim Direct 数据链路对比：监督转换存在结构性信息压缩

Tags: m1-direct, data-audit, supervision-chain, root-cause-analysis

### Note
对真实训练 JSONL、current converter 与相同 current-Direct-618 评测进行了只读对比。M1 有 2,114 行 / 1,077 个图像 / 217 个类别；interim 有 3,264 行，但只有 816 个图像、208 个有样本的类别，93 类达到 5 图，124 类仍有缺口（269 个图像组）。两者图像 hash 为零重叠，符合隔离约束。

M1 的每个学生目标是长比较链：全部 2,114 行带有 Visual Observation 与 Candidate Analysis，1,067 行带 Knowledge Verification；assistant 文本中位 1,579 字符。interim 的最终 assistant target 中位为 190 字符，其中源 `student_reasoning` 中位仅 154 字符。尽管 raw teacher `reasoning` 对每个 accepted row 都有至少 3 条视觉证据和恰好 4 个候选比较，`audit_and_convert` 只写入 `student_reasoning`（或空缺时拼接 visual evidence），没有把 `candidate_comparisons`、`knowledge_verification` 或 `uncertainty` 送入 SFT assistant text。

M1 legacy SFT 不含单字母 A--D assistant target；current data 则一半（1,632/3,264）是随机候选顺序下的单字母 Option 目标。current hard negatives 本身满足 217 类、同域、非自身、每类 3 个的合同，但其集合是 class-level static set，四视图复用；审计通过并不验证这些短 student targets 是否承载 M1 级的比较监督。

### Status
Verified: 数据链路存在足以解释 Open recognition 未重建的重大差异。当前 checkpoint-153 的 Open 为 5.83%，而同一 bridge 的 M1 为 43.69%；Option 为 67.96% 对 89.32%。这不是纯数据因果消融：M1 使用 5 epoch / LR `1e-5`，interim 使用 3 epoch / LR `2e-6`，因此优化预算仍是独立混杂因素。

### Next
在任何新 SFT 前，对同一 816 图做 contract-preserving 长链转换消融：把已审计的四类完整比较、安全地渲染进 `<think>`，保持 student prompt 无真值并重做泄漏审计；再与 M1-like training budget 分别和联合对照。另对每类跨图轮换 hard negatives，而非只复用 3 个 class-level negatives。

## 2026-08-30 03:00:47 CST - 已修复 Direct 长链转换并生成可追溯 M1-comparison 派生产物

Tags: m1-direct, conversion-repair, derived-artifact, privacy, reproducibility

- 根因修复：`audit_and_convert` 新增显式 `m1_comparison_v1` renderer，保留默认 compact 行为以免改变既有路线。新 renderer 只消费已由 teacher/auditor 使用的结构化 reasoning，按学生语言写入至少三条视觉观察、四个公开候选的逐项比较、知识核验和不确定性；Option `<answer>` 仍只保留正确字母，reasoning 中拒绝 A--D 映射。
- 安全：renderer 对 private truth 字段、类别 code、局部路径、option mapping fail-closed；转换命令要求新旧 sample ID 与 image hash 完全一致，并再次执行 student contract、完整四视图、私有 class alignment 和 isolation authorization。
- 新不可变 artifact：`outputs/artifacts/datasets/m1-direct-current-hcv-v1/interim-sft-m1-comparison-v1/`，3,264 rows / 816 images，`training_authorized=true`、`quota_complete=false`。其 parent 为原 `interim-sft/data.jsonl`（SHA-256 `a38adc4bcf123ca9bbbc3e5eb3804c798ab4a793141257aec1c26a6b76900832`）；旧 artifact 未修改。
- 验证：新旧 sample ID 和 image-hash 集合严格相同；3,264/3,264 targets 均含 M1 式 section；assistant 字符数中位数从约 190 提升至 1,565；private-token 与 option-mapping 扫描均为 0。Focused suites 72 passed，`py_compile` 与 `git diff --check` 通过。
- Next: 该数据仅授权为用户已声明的、仍有类别短缺的 interim Direct-only SFT 消融；若启动新的训练，必须使用新的 artifact 和独立 output/run ID。常规 full freeze 仍受 N04036/N04074/N04097/N05053 的同类图像补采阻断。

## 2026-08-30 06:55:07 CST - 长比较链 + M1 类优化预算 Direct-only SFT 完成

Tags: m1-direct, comparison-chain, m1-like-optimization, direct-only, formal-evaluation

- Setup: 从 `models/Qwen3-VL-4B-Instruct` 在 `interim-sft-m1-comparison-v1` 的 3,264 行 / 816 图像上进行 8 GPU 全参数 SFT；5 epoch、batch 1、gradient accumulation 16、LR `1e-5`、ZeRO-3、max length 16,384。该预算匹配历史 M1 的 epoch/LR/batch/accumulation 核心设置，但输入仍是类别不完整的 interim 数据。
- Run `outputs/runs/vlm/vlm-sft-qwen3vl4b-m1-direct-current-hcv-interim-comparison-m1like-8gpu-v1/20260830T040540-2ef993b1-a01/` 已 exit 0。五个 checkpoint 都完成 native DP8 Direct-618，均有 618 条预测、10,000 paired bootstrap、零 protocol errors。

| checkpoint | overall | Open | Option | vs raw-base pp (95% CI) | vs M1 pp (95% CI) | raw-base promotion gate |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| epoch 1 / 26 | 47.57% | 9.71% | 85.44% | +7.28 [+3.88, +10.68] | -20.06 [-23.95, -16.02] | pass |
| epoch 2 / 52 | 52.91% | 24.60% | 81.23% | +12.46 [+8.74, +16.18] | -14.56 [-18.77, -10.36] | pass |
| epoch 3 / 78 | 60.03% | 31.72% | 88.35% | +19.58 [+15.86, +23.30] | -7.44 [-11.65, -3.40] | pass |
| epoch 4 / 104 | 59.55% | 33.66% | 85.44% | +19.26 [+15.37, +22.98] | -7.61 [-11.81, -3.40] | pass |
| epoch 5 / 130 | **60.68%** | **34.30%** | 87.06% | **+20.39 [+16.34, +24.27]** | **-6.63 [-10.84, -2.59]** | pass |

- Conclusion: 相比旧 compact interim 的最佳 Open 5.83%、Option 67.96%、且低于 raw base 的结果，长链转换结合 M1 类预算使 Open 提升至 34.30%、Option 至 87.06%，epoch-5 对 raw base 为显著正增益。该结果验证监督链压缩是先前失败的重要原因。
- Caution: 当前 promotion gate 的定义只要求相对 raw base 非劣与主要切片不超过退化阈值，五个 checkpoint 都通过；但 epoch-5 仍显著低于 M1 6.63 pp，且数据仅覆盖 208/217 类、816/1,085 图像。因此该 checkpoint 可作为该 interim ablation 的最佳候选，不能被表述为已恢复 M1 或替代完整 freeze。
- Next: 保留 epoch-5/130 为当前 ablation 选择；继续同类新图容量预检与补采，之后以完整冻结数据验证是否能弥补其相对 M1 的剩余差距。

## 2026-08-30 07:05:00 CST - epoch-5 与 M1 的分 subset 对比

- 在相同 618 条 Direct manifest 上，epoch-5/checkpoint-130 为 60.68%，M1 为 67.31%，总体差距 `-6.63 pp`（95% CI `[-10.84,-2.59]`）。
- 主要差距集中在 English 和 Open：English `-12.26 pp`（56.45% vs 68.71%），Open `-11.00 pp`（34.30% vs 45.31%）；中文几乎持平（`-0.97 pp`），Option 也接近（`-2.27 pp`）。
- Domain 切片显示 disease `-10.80 pp`（62.68% vs 73.47%），而 pest 反而高于 M1 `+2.60 pp`（56.25% vs 53.65%）。
- 最明显的交叉格子是 English disease Open：`-28.18 pp`（32.73% vs 60.91%）；English pest Open 持平；Chinese pest Open `+3.92 pp`，Chinese pest Option `+9.80 pp`。
- known/unknown 切片：known `-3.78 pp`，unknown `-10.22 pp`。
- 解释：长链转换已显著修复整体和 Open，但剩余差距不是均匀的；优先表现为英文 disease Open、unknown Open 以及 disease 总体。该结果仍受 interim 类别/图像覆盖不完整影响，不能单独归因于优化或语言因素。

## 2026-08-30 10:10:09 CST - 按请求启动补采前的 v2 容量预检仍被阻断

Tags: m1-direct, replenishment, capacity-preflight, fail-closed

- 已执行 `agrinet data submit data-m1-direct-current-hcv-v1 --operation m1-direct-replenishment-next-plan`。该操作只读取既有 pool、历史 ledger 与隔离集合；在构建 immutable v2 plan 前 fail-closed，未写入 v2 plan，未向 teacher/auditor 发 POST，未登记新 contacted hash。
- 当前 fresh、isolated、同类候选仍不足：`N04036` Citrus Anthracnose 缺 4，`N04074` Pepper Bacterial Wilt 缺 3，`N04097` Strawberry Fruit Cracking 缺 3。错误证据来自 `build_replenishment_plan`: `insufficient fresh isolated replenishment images`。
- N05053 不再是本轮 planner blocker（已有 supplemental pool 可用），但其他三个 disease 类均没有通过 source-and-visual preflight 的新候选；已拒绝候选继续保留在私有 retired/preflight ledger，不能重放。
- Next: 需要先获得这三个类别合计 10 张新的、明确同类来源与可见症状适配的图像，并完成 hash isolation 与逐图 preflight；随后重新运行 v2 plan，才有权启动 8-worker teacher/auditor collection。

## 2026-08-30 10:17:36 CST - 已授权分批补采并启动 v2 的 8-worker 收集

Tags: m1-direct, replenishment, capacity-partial, parallel-collection

- 用户选择先补齐所有已有 fresh isolated capacity 的类别，同时保留缺源类别的严格阻断。精确审计显示当前是 124 个类别低于 5 张（非先前口头的 94 个）；需补 269 个完整图像组。其中 121 类、258 图像组可在现有 pool 中完整同类补齐；N04036/N04074/N04097 仍分别缺 4/3/3 张新候选。
- 实现了 opt-in `allow_capacity_partial_replenishment`：只有某类别的整个 same-class shortfall 有足够新隔离候选时才进入 plan；被容量阻断的类只写进 `capacity_blocked_shortages`，不降低最终 full-freeze 配额，也不允许部分图像组。新增回归测试覆盖该边界。
- `replenishment_v2` immutable plan 已生成：1,032 public/private-aligned views、258 unique images、121 collectable classes、query image paths 均存在；阻断类没有写入 plan。计划与私有 alignment 通过 ID 一致性检查。
- 已按用户指定的并行策略启动 8-worker teacher→auditor collection：managed run `outputs/runs/data/data-m1-direct-current-hcv-v1/20260830T101736-2ef993b1-a01/`，PID 2616374。其 terminal audit/merge 后才可建立后续 round；未知交付保持未接受且不会重放。
- Validation before launch: 44 focused collection/CLI tests passed; `py_compile` and `git diff --check` passed.

## 2026-08-30 10:24:00 CST - 三个容量阻断类的人工 review 批准已受限登记

Tags: m1-direct, manual-review, replenishment, isolation

- 用户检阅 `output/review_images/` 后确认 N04036/N04074/N04097 的候选图像可用，并授权纳入正常补采。执行范围限定为未联系、未退休、未进既有 public plan 的图像；不解除 unknown/rejected remote 请求的不可重放约束。
- 登记了 10 条审阅批准：N04036 4、N04074 3、N04097 3。N04097 另有 1 张尚未联系的原始 pool 图像，因此其 4 张缺口可由这 3 条批准加该原始候选共同覆盖。所有 10 条批准均属于原 `candidate_preflight_rejected` 集，且与 contacted/retired 集交集均为 0。
- 新的、范围锁定的私有审阅记录为 `private/user_review_approved_uncontacted_v1.jsonl`。planner 仅可从 `candidate_preflight_rejected` 排除集中移除该精确 hash 集；formal/history/RAG/contacted/retired 的隔离集合继续生效。
- v2 已作为 immutable registered collection 正在运行，不能事后修改其 plan。v2 完成并 private audit/merge 后，v3 planner 将把这些批准候选作为正常同类候选纳入；届时仍使用 8 workers。
- Validation: approval hashes=10，均在原 preflight-rejected 集，contacted overlap=0、retired overlap=0；focused collection/CLI tests passed，`py_compile` 和 diff check 通过。
