# Experiments 2026-W33

## 2026-08-11 01:35:00 CST - Round090 Open-only diagnostic and baseline

- Goal: validate the Open answer-contract repair before authorizing any formal 618 evaluation.
- Round090 checkpoint: `outputs/vlm_sft/qwen3_vl_4b_rag_sft_round090_open_repair/v0-20260811-011309/checkpoint-2`.
- Diagnostic: `outputs/runs/vlm/vlm-sft-round090-open-diagnostic8`; 8/8 rows, forced retrieval off, 8 autonomous Milvus successes, 0 forced calls, 0 unparseable, 0 language mismatches, accuracy 0/8.
- Same-manifest checkpoint-165 baseline: `outputs/runs/vlm/vlm-sft-round090-open-baseline8`; identical 0/8 accuracy with the same tool/protocol health.
- Reviews: `outputs/experiments/rag_sft_iteration/reviews/round090_open_diagnostic8_review.json` and `round090_open_baseline8_review.json`.
- Decision: protocol smoke gate passes, answer-quality gate fails. Do not run formal 618, expand SFT, or claim improvement. Next action is to redesign evidence anchoring / Open answer supervision and run a smaller discriminative pilot.

## 2026-08-11 01:42:00 CST - Round091 Open discrimination pilot

- Plan: `outputs/experiments/rag_sft_iteration/rounds/round_0001/plan/round091_open_discrimination/plan.jsonl`; four fresh images, balanced across English/Chinese and disease/pest, excluding Round090 frozen images.
- Artifact: `outputs/experiments/rag_sft_iteration/candidates/round091-open-discrimination`; teacher preflight passed on `https://api3.wlai.vip/v1`, tmux session `rag_pilot_round091` exited normally.
- Result: 0 accepted / 4 rejected; 6 successful Milvus calls, 0 tool failures, 0 protocol parse errors, 0 language mismatches. Every rejection was `final_name_mismatch` plus `target_not_in_retrieved_evidence`.
- Review: `outputs/experiments/rag_sft_iteration/reviews/round091_open_discrimination_review.json`.
- Decision: reject all; no SFT or freeze. Round091 demonstrates that visual preflight eligibility does not guarantee the teacher's actual query recalls the target. Round092 Oracle recall diagnostic was planned at `outputs/experiments/rag_sft_iteration/rounds/round_0001/plan/round092_oracle_recall/plan.jsonl`; it remains diagnostic-only and must not relax evidence gates.

## 2026-08-11 02:03:00 CST - Round092 Oracle recall diagnostic

- Artifact: `outputs/experiments/rag_sft_iteration/candidates/round092-oracle-recall`; 4 balanced Open rows, Oracle route only, completed in tmux `rag_pilot_round092`.
- Result: 2 accepted / 2 rejected; 11 successful Milvus calls, 0 retrieval failures, 0 protocol validation errors. Apple Rot and Cherry Normal leaf were recalled and passed. cicadella viridis and cosmopolites sordidus were absent from actual top-k evidence and were rejected; the latter also failed the Chinese final contract.
- Review: `outputs/experiments/rag_sft_iteration/reviews/round092_oracle_recall_review.json`.
- Decision: diagnostic-only; do not SFT or freeze the two accepted Oracle rows. Next sampling must use a route-aware recovery pool where actual target recall is verified before Blind generation, while preserving Oracle/Blind isolation.

## 2026-08-11 02:01:00 CST - Round093 Blind recall retry

- Artifact: `outputs/experiments/rag_sft_iteration/candidates/round093-blind-recall`; two targets carried forward from Round092's actually recalled pool.
- Result: 1 accepted / 1 rejected; 4 successful Milvus calls, 0 protocol errors, 0 language mismatches. Apple Rot passed Blind evidence grounding; Cherry Normal leaf was not recalled in actual Blind top-k and was rejected.
- Review: `outputs/experiments/rag_sft_iteration/reviews/round093_blind_recall_review.json`.
- Decision: retain the one accepted row as a future candidate only; no SFT/freeze because the dose is unbalanced and far below the training gate. Continue minimal route-aware recovery sampling.

## 2026-08-11 02:12:00 CST - Round094 preflight-eligible Blind batch

- Plan: `outputs/experiments/rag_sft_iteration/rounds/round_0001/plan/round094_blind_preflight_v2/plan.jsonl`; four fresh visual-preflight-eligible rows across language/domain cells.
- Artifact: `outputs/experiments/rag_sft_iteration/candidates/round094-blind-preflight-v2`; tmux `rag_pilot_round094` exited 0.
- Result: 3 accepted / 1 rejected; 6 successful Milvus calls, 0 protocol errors, 0 language mismatches; semantic validator passed all 3 accepted rows.
- Review: `outputs/experiments/rag_sft_iteration/reviews/round094_blind_preflight_v2_review.json`.
- Decision: retain three strict Blind rows as candidates; no SFT/freeze. Total new strict rows remain below the 16-row training gate; next batch should prioritize the missing English disease cell.

## 2026-08-11 02:21:00 CST - Round095 Blind recall batch

- Plan: `outputs/experiments/rag_sft_iteration/rounds/round_0001/plan/round095_blind_recall/plan.jsonl`; four fresh images across English/Chinese and disease/pest.
- Artifact: `outputs/experiments/rag_sft_iteration/candidates/round095-blind-recall`; tmux `rag_pilot_round095` exited 0.
- Result: 2 accepted / 2 rejected; 6 successful Milvus calls, 0 protocol errors, 0 language mismatches. Rejections were one missing `<think>/<answer>` contract and one final-name mismatch after evidence drift.
- Review: `outputs/experiments/rag_sft_iteration/reviews/round095_blind_recall_review.json`.
- Decision: retain two strict rows only; no SFT/freeze. One rank-1 target with formatting failure is eligible only for a separate formatting retry, not automatic promotion.

## 2026-08-11 02:36:00 CST - Round096 Blind batch

- Plan: `outputs/experiments/rag_sft_iteration/rounds/round_0001/plan/round096_blind_batch/plan.jsonl`; eight fresh English Open rows, four disease and four pest, all from the completed visual-recall audit.
- Artifact: `outputs/experiments/rag_sft_iteration/candidates/round096-blind-batch`; tmux `rag_pilot_round096` exited 0.
- Result: 7 accepted / 1 rejected; 14 successful Milvus calls, 0 protocol errors, 0 language mismatches; semantic validator passed all 7 accepted rows.
- Cross-round accumulation: 13 unique strict rows from Rounds093-096, zero duplicate query images.
- Review: `outputs/experiments/rag_sft_iteration/reviews/round096_blind_batch_review.json`.
- Decision: no SFT/freeze yet. Accumulation is still Open/Blind-only; next step must add the smallest missing Chinese/Option coverage and then run a freeze gate review.

## 2026-08-11 02:48:00 CST - Round097 Option coverage and candidate freeze

- Option pilot: `outputs/experiments/rag_sft_iteration/candidates/round097-option-pilot`; 3/4 accepted, 1 rejected for missing `<think>/<answer>`, semantic validation passed.
- Candidate artifact: `outputs/artifacts/datasets/agrinet-rag-sft-round097-candidate-freeze`; 17 unique images after selecting 15 Blind and 2 reviewed Oracle rows, with no image overlap. Basic artifact validation passed (final-label accuracy 1.0, zero leakage/errors).
- Gate review: `outputs/experiments/rag_sft_iteration/reviews/round097_candidate_freeze_gate_report.json`.
- Decision: candidate freeze only, not a formal training freeze. It has 17/48 required RAG rows, 3 Option rows all B, no Direct mixture, and unbalanced quotas. No SFT or formal evaluation is authorized.

## 2026-08-11 18:30:04 CST - Round098 fresh Option coverage rejected on answer contract

- Objective: add A/C/D Option coverage without reusing prior accepted/frozen or evaluation images. The static plan has six fresh Blind rows, 6/6 unique images, A/C/D = 2/2/2, English/Chinese = 3/3, disease/pest = 3/3; it excludes 122 historical accepted/freeze images and 468 evaluation images.
- Transport recovery: the original `https://yunwu.ai/v1` run rejected all six before any Milvus call because first teacher requests were network-unreachable. The artifact remains at `outputs/experiments/rag_sft_iteration/candidates/round098-option-pilot/` as non-training audit evidence.
- API3 preflight passed endpoint, real-image, and manual JSON tool-call checks. The bounded API3 retry (`rag_pilot_round098_api3`) made 10/10 successful Milvus calls and initially accepted 2/6.
- Strict re-audit found both apparent accepts answered their Option prompts with canonical class names rather than the required single A-D letter. The prompt and validators were aligned to enforce this hard contract; post-fix validation reports two Option-answer errors, so strict accepted rows are 0/6. Evidence and Blind-boundary audit was otherwise healthy for the rescinded rows; this does not permit post-hoc answer rewriting.
- Review: `outputs/experiments/rag_sft_iteration/reviews/round098_option_coverage_review.json`. Candidate freeze remains unchanged at 17 rows; `training_authorized=false` and `formal_eval_authorized=false`.
- Next: first run a one-image, fresh-image prompt/contract smoke using the repaired Option rule; only then consider another bounded coverage pilot.

## 2026-08-14 23:05:25 CST - Round100 Option format retry preserved syntax but lost semantics

- Objective: verify the one-shot Blind-safe Option answer-format retry on the fresh Round100 sample before any larger coverage Pilot. Unknown-delivery checks found no prior Round100 process, tmux session, dynamic artifact, stable log, or exit marker.
- Preflight: the single API3 real-image preflight passed with `reachable=true`, `visual_reachable=true`, and `manual_tool_call=true`. The narrow compile, three target tests, and targeted `git diff --check` also passed.
- Artifact: `outputs/experiments/rag_sft_iteration/candidates/round100-option-contract-smoke/pilot`; stable log and exit marker are under `outputs/runs/rag/rag-sft-round100-option-contract-smoke/logs/`. The managed tmux run exited 0.
- Result: 0 accepted / 1 rejected; 2/2 Milvus calls succeeded in the required visual -> balanced order, and both ranked target `N04062 / Grape shot hole` first. The first final answer was the semantically correct class name; exactly one format retry then returned legal letter `A`, while the public correct option was `D`. Rejection reasons were `option_answer_mismatch` and `final_name_mismatch`.
- Boundary audit: the retry prompt did not expose the private correct letter, requested no further tool call, and no further tool call occurred. Artifact leakage checks passed. Protocol validation failed only because the accepted artifact is empty; semantic validation was therefore not applicable.
- Decision: Round100 fails the acceptance gate. The bounded retry fixes output syntax but does not reliably map the selected class to its public option letter. Do not expand sampling, SFT, or formal evaluation. Next inspect and repair the public-choice mapping instruction/state supplied to the format retry, then run at most one new single-sample smoke.

## 2026-08-14 23:44:08 CST - Round101 validated public choice mapping and exposed mixed raw response

- Objective: repair Round100 by giving the Blind teacher and one-shot format retry the same public A-D choices as the student, while preserving the teacher's previous public class selection and withholding the private correct option. The shared public-question helper and regression tests were added in tools/rag_distill/run_pilot.py and tests/test_sft_recovery.py; the smoke builder now uses a round-correct target_id.
- Setup: Round101 selected fresh image datasets/AgriNet-1K/all/N04062/N04062_P00005.jpg, excluded 588 historical/evaluation images, and remained training_authorized=false / formal_eval_authorized=false. One API3 real-image preflight passed all three reachability signals, followed by exactly one single-sample tmux Pilot.
- Artifact result: outputs/experiments/rag_sft_iteration/candidates/round101-option-contract-smoke/pilot exited 0 with 1 accepted / 0 rejected, 2/2 successful visual -> balanced calls, target top-1 twice, final answer D, protocol validator 0 errors, and semantic validator 1/1 valid. No format retry occurred because the first final answer was already D.
- Strict raw audit: the provider's first raw response mixed a manual JSON tool call with a premature think/answer suffix. The old parser extracted the JSON prefix and produced a clean student trajectory, so the artifact validators passed; this is parser normalization, not strict teacher protocol compliance. Round101 is diagnostic-only and must not enter the candidate freeze.
- Hardening: manual JSON tool calls now require the entire assistant content to be exactly one JSON object. Mixed JSON-plus-answer content yields zero tool calls under the hardened parser. Full tests/test_sft_recovery.py passes 13/13, including public-choice isolation and mixed-response rejection tests.
- Decision: public choice mapping is now present and Blind-safe, but the real format-retry branch remains unexercised because Round101 answered D immediately. Do not run another smoke in this loop, expand sampling, SFT, or formal evaluation. Next add an explicit runtime rejection reason for mixed manual-tool content and a deterministic mocked end-to-end retry test before considering one final real smoke.

## 2026-08-14 23:50:11 CST - Harden mixed tool rejection and verify Option retry offline

- Change: run_sample now rejects a provider message with reason mixed_manual_tool_content when it contains a manual tool JSON object plus any non-whitespace suffix. It no longer silently derives a clean trajectory from that malformed raw response.
- Deterministic validation: a mocked run_sample test forces one valid tool call, a class-name final answer Target, and a corrected public-letter answer D. It verifies that the retry repeats A/B/C/D public choices, preserves the previous public class selection, and contains neither the correct_option field name nor a correct-answer hint.
- Result: tools/rag_distill/run_pilot.py and tests/test_sft_recovery.py compile; full tests/test_sft_recovery.py passes 15/15, including a run_sample-level assertion for the explicit rejection reason; targeted diff check passes. No additional API request or smoke was run.
- Decision: keep mixed manual-tool content as an immediate hard reject. Retrying and retaining only a cleaned continuation would hide a real provider protocol violation from the derived student trajectory. The next API action, if continued, is at most one final real Option smoke under this strict policy. Training and formal evaluation remain unauthorized.

## 2026-08-14 23:52:57 CST - Round102 stopped at strict manual-tool preflight

- Setup: Round102 selected fresh Blind Option image datasets/AgriNet-1K/all/N04062/N04062_P00006.jpg, excluded 589 historical/evaluation images, retained public correct option D, and remained non-trainable/non-evaluable.
- Result: the one allowed API3 real-image preflight reached the provider but exited 1 because the hardened parser found no single whole-message agrinet_rag_search JSON call. No Pilot was launched; no Round102 dynamic artifact, stable Pilot log, exit marker, tmux session, or Python process exists.
- Unknown: because preflight does not persist the raw provider response, this run cannot distinguish mixed JSON-plus-answer from a pure final answer or another malformed response. The preflight error path now reports mixed content explicitly on future runs when detectable.
- Validation: full tests/test_sft_recovery.py remains 15/15 and targeted diff check passes after the diagnostic improvement.
- Decision: strict preflight gate failed, so do not retry Round102, run another smoke, expand Option sampling, SFT, or formal evaluation. Next make preflight transport evidence persist safely without credentials or image payloads, then inspect one future bounded preflight before authorizing any Pilot.

## 2026-08-15 00:32:02 CST - Round110 passes strict Blind Option acceptance gate

- Objective: verify the phase-separated manual-tool protocol and one-shot retrieval-budget finalization with the new `gpt-5.6-luna` teacher on one fresh Blind Option sample. Unknown-delivery inspection found the already-completed run and avoided a duplicate launch.
- Setup: `N04062_P00013.jpg`, image SHA-256 `520950e6384cde36b9eb3b1bb2072c240b6dd97cbcfd0bfb4eda698b073204db`, public correct option `D`, temperature 0, maximum three tool turns. The strict bounded-evidence preflight passed endpoint, real-image, and whole-message manual-tool checks.
- Result: `outputs/experiments/rag_sft_iteration/candidates/round110-option-contract-smoke/pilot` contains 1 accepted / 0 rejected row. All three retrievals succeeded in `visual -> balanced -> visual` order, with `N04062 / Grape shot hole` at rank 1 each time. Artifact validation reports zero errors and semantic validation reports `semantic_valid=true`.
- Raw audit: the first three provider responses were single whole-message `agrinet_rag_search` JSON calls. After the third call, the model requested more retrieval; the runner sent one public retrieval-budget finalization prompt, executed no fourth tool call, and the fourth provider response ended with exactly `<answer>D</answer>`. The answer body contains no class name.
- Blind/leakage audit: API-visible messages contain no private `correct_option`, final-label, class-code, target-id, source-sample-id, sample-id, or image-SHA field/value. Artifact checks pass query-image-first, no-base64, no-system-message, and no-visible-class-code constraints. The query image occurs only in the two equivalent Round110 plan copies and not in earlier plans; the on-disk SHA matches the plan. No post-hoc response rewrite was used.
- Decision: Round110 is the first strictly acceptable Option protocol sample and satisfies this optimization goal's stop condition. Preserve it as a candidate, but do not expand sampling, start SFT, or run formal evaluation without a separately reviewed coverage plan and authorization.

## 2026-08-15 08:40:00 CST - Round111 coverage Pilot isolates finalization and retrieval failures

- Setup: one bounded four-row Blind Option Pilot with `gpt-5.6-luna`, temperature 0, four distinct classes, A/B/C/D = 1 each, English/Chinese = 2/2, disease/pest = 2/2, and four fresh images after excluding 1,499 historically exposed or evaluation images. Strict real-image preflight passed before the Pilot.
- Result: `outputs/experiments/rag_sft_iteration/candidates/round111-option-coverage/pilot` exited 0 with 1 accepted / 3 rejected and 12/12 successful Milvus calls. The retained D/Chinese/pest row has target rank 1 in all three calls, exact `<answer>D</answer>`, clean protocol validation, and `semantic_valid=true`. All raw tool responses were pure whole-message JSON and all API-visible private-boundary checks were clean.
- A/English/disease: final letter A was correct and target evidence appeared at rank 2, but the teacher used unpunctuated headings (`Evidence` rather than `Evidence:`). The strict parser therefore rejected `missing_final_fields` and `final_answer_not_evidence_anchored`; an offline heading-normalization diagnostic confirms the existing evidence anchor would otherwise pass. No historical output was rewritten or retained.
- C/English/pest: final letter C was correct and target ranked first three times, but the same heading-format failure occurred and the reasoning did not quote an exact returned class name. Even an offline heading-normalization diagnostic leaves the evidence-anchor gate false.
- B/Chinese/disease: target `N04071` was absent from all three retrieval result sets and the model selected A; this is a genuine retrieval/semantic failure (`option_answer_mismatch`, `final_name_mismatch`, `target_not_in_retrieved_evidence`), not a formatting issue.
- Repair: the public budget-finalization prompt now requires exact colon-terminated field labels and requires `Evidence:` / `证据：` to quote at least one exact class name already returned by retrieval. The validator was not relaxed. Full recovery tests pass 26/26.
- Decision: Round111 fails the 4/4 coverage gate, so no strict freeze or SFT is allowed. Retain only the D row. Next run one bounded fresh A/C repair smoke; handle B separately as retrieval hardness rather than mixing it into the protocol repair.

## 2026-08-15 09:21:01 CST - Build teacher-free 31-row expansion approval plan

- Re-ran `build_current_direct_candidates.py` and `build_strict_candidate_view.py`: Direct is 32/32 current-contract valid with zero image overlap; strict RAG remains 19 rows, 17 quota-usable, exact deficit 31.
- Unknown-delivery check found no running Pilot/SFT process; Round114 remains the latest completed Pilot. No teacher request was retried.
- Built `tools/rag_distill/build_rag_expansion_plan.py`, which uses the historical Milvus preflight audit only after excluding exposed plan/candidate images, evaluation images, and current RAG/Direct images. It selected 31 unique fresh, preflight-eligible targets across all 12 quota cells with no shortfall.
- Approval package: `outputs/experiments/rag_sft_iteration/approval/rag_expansion_plan_v1/`; four shards contain 8/8/8/7 target rows and request at most 64 teacher attempts. The package is teacher-free and does not authorize SFT or formal 618 evaluation.
- State/review records now use `rag_rows_missing=31` and record the active teacher as the user-specified Yunwu relay ID `gpt-5.6-luna`.
- Decision: pause for explicit material-sampling approval; after approval, confirm local preflight, run one shard, inspect unknown delivery and strict acceptance, then regenerate the candidate gate before proceeding.

## 2026-08-15 09:05:00 CST - Rounds112-114 close Option protocol gaps and stop at corpus-size gate

- Round112 tested the exact-field and exact-retrieval-anchor repair on fresh A/C images. A passed with target rank 1 three times. C answered correctly and cited target evidence but put all field labels on one line, so it was strictly rejected for `missing_final_fields`; the validator was not relaxed and the historical row was not rewritten.
- The public finalization prompt was minimally tightened to require `Evidence:`, `Rejected alternatives:`, and `Uncertainty:` each at the start of its own line. Round113 then passed 1/1 on a fresh C image with 3/3 successful target-rank-1 calls, exact `<answer>C</answer>`, all required fields, clean Blind boundary, and semantic validation.
- Round114 tested the remaining B, Chinese, and route-coverage gaps on two fresh images: Chinese disease Blind and Chinese pest Oracle. Both passed strictly with exact `<answer>B</answer>`, 5/5 successful target-rank-1 calls, zero protocol/semantic errors, and clean student-visible private boundary.
- Current-contract strict view: `outputs/experiments/rag_sft_iteration/strict_candidate_view_v1/rag.current_contract.jsonl` contains 19 reviewed RAG rows; `direct.reviewed_candidates.jsonl` contains 32 historical Direct candidates. All RAG sample IDs and all RAG/Direct images are unique, RAG-Direct overlap is zero, and evaluation-image overlap is zero. A/B/C/D plus Blind/Oracle routes are now represented.
- Gate: 31 quota-usable RAG rows are still missing from the required 48 (19 rows exist, but two are surplus in already-covered cells). `training_authorized=false` and `formal_eval_authorized=false`; no freeze or SFT was launched. Adding 31 quota-usable rows is a material sampling expansion, so the automatic loop pauses for approval as required. Review package: `outputs/experiments/rag_sft_iteration/reviews/round113_114_protocol_coverage_review.json`.

## 2026-08-15 10:02:00 CST - Round115 stop-correction Pilot fails stability gate

- Setup/result: fresh four-cell Blind `stop_correction` Pilot with `gpt-5.6-luna` completed normally (exit 0), 4/4 strict rejected, 12/12 Milvus calls successful, and 0 training rows added.
- Rejection audit: 1 row lacked final fields and an evidence anchor; 2 rows attempted another tool call after the retrieval budget-finalization turn; 1 Chinese row was semantically `class_changed` while the request expected `evidence_confirmed`. These are distinct protocol/semantic causes, not a retrieval-transport outage.
- Repair: strengthened the budget-finalization and final-contract prompts, separated final-contract retry accounting from correction retry accounting, and made the Chinese correction-type retry emit the exact validator marker. `tests/test_sft_recovery.py` passes 31/31 and `py_compile` passes. Validators and hard gates were not relaxed.
- Decision: retain raw/rejected traces; do not release the 30-attempt calibration, freeze, SFT, or formal 618 evaluation. After a new unknown-delivery and teacher preflight check, run one fresh four-cell targeted Pilot to test the repair; only nonzero strict acceptance without a repeated systemic hard-gate failure can open the calibration review.
- Evidence: `outputs/experiments/rag_sft_iteration/candidates/round115-stop-correction-pilot/pilot/`; `outputs/runs/rag/rag-sft-round115-stop-correction-pilot/logs/`; state entry `round115`.

## 2026-08-15 10:18:00 CST - Round116 Pilot stopped with unknown teacher delivery

- Setup: fresh four-cell Blind stop-correction repair plan; static audit passed with four unique images and training unauthorized.
- Preflight: API3 endpoint, real-image reachability, and whole-message manual-tool response passed for gpt-5.6-luna. Evidence: outputs/runs/rag/rag-sft-round116-stop-correction-pilot/logs/preflight_report.json.
- Delivery: the sole Pilot process remained blocked during the first teacher request for more than twice the configured 180-second timeout. It was terminated after six minutes; no trajectory, accepted/rejected JSONL, or exit marker was written. This is unknown delivery, not zero acceptance.
- Decision: do not retry Round116 or release calibration until provider/request status is independently resolved. Bulk distillation, freeze/SFT, and formal 618 remain unauthorized.

## 2026-08-15 14:07:34 CST - Recheck confirms Round116 remains unknown delivery

- Unknown-delivery recheck: no `run_pilot`, Round116, or `rag-sft` process remains; the output directory still contains only the static plan/audit, preflight report, preflight exit code, and short stable pilot log. No trajectory, accepted/rejected JSONL, or Pilot exit marker appeared.
- State: `outputs/experiments/rag_sft_iteration/state.json` remains `round116.pilot_status=terminated_unknown_delivery`, `accepted=null`, `rejected=null`, `exit_marker=false`, `trajectory_artifacts=false`; `teacher_model=gpt-5.6-luna`, `training_authorized=false`, `full_eval_authorized=false`.
- Decision: stop the Codex-side invalid wait/retry loop and do not issue a duplicate teacher request. Round116 is neither success nor failure; provider/request status must be independently resolved before any repeat. Bulk distillation, freeze/SFT, and formal 618 remain paused.
- Next action: after independent delivery resolution, rerun only a newly bounded, authorized Pilot if still warranted; otherwise preserve the unknown-delivery record and revise the plan.

## 2026-08-15 14:12:00 CST - Mark historical handoff Round100 steps as superseded

- Documentation correction: `docs/handoff_20260812.md` now has a current-state overlay pointing to `START_HERE`, the weekly log, and `state.json`; its detailed Round100 commands remain historical and must not be replayed.
- Reason: the live state has advanced through Rounds110-116, and Round116 is unresolved unknown delivery. This prevents a future continuation from mistaking the old Round100 section for the next runnable action.
- Validation: the overlay contains no credentials or new runtime instruction; no teacher request, training run, or evaluation was started.

## 2026-08-15 14:25:00 CST - Make ambiguous teacher delivery fail closed

- Local runner change: ambiguous POST outcomes (empty/invalid body, URL/socket failure, or direct timeout) now raise `UnknownTeacherDelivery` and bypass automatic retries.
- HTTP 408, 429, and 5xx teacher responses now follow the same fail-closed path; only deterministic client-side 4xx errors remain ordinary runtime failures.
- Future batch artifacts preserve `unknown_delivery` separately from ordinary rejection and return exit code 2; no validator or protocol gate was relaxed.
- Validation: `tests/test_sft_recovery.py` passes 34/34; compile and diff checks pass. No remote request was issued.
- Decision: this removes the local duplicate-request path exposed by Round116, but does not resolve Round116 itself. Provider/request status remains the next external gate.

## 2026-08-15 14:42:00 CST - Rebuild strict gate and correct current expansion deficit

- Ran the authoritative builder as `.venv/bin/python -m tools.rag_distill.build_strict_candidate_view`; it reproduced 19 strict RAG rows and 32 current-contract Direct rows with the existing gate hash and no authorization changes.
- The gate reports 17 quota-usable RAG rows because two rows are surplus in already-covered standard English/Open cells; the required deficit is therefore 31 quota-usable rows, not 29. Cell-level deficits remain the source of truth.
- Corrected the current `START_HERE` and weekly-log wording from 29 to 31. Historical records were left unchanged.
- Decision: no freeze, SFT, calibration release, or teacher request; material 31-row expansion still requires explicit approval, and Round116 remains unknown delivery.

## 2026-08-15 15:05:00 CST - Add interruption-safe Pilot status marker

- The local runner now atomically writes run_status.json; SIGTERM/SIGINT produce terminated_unknown_delivery with delivery_status=unknown before exit. Preflight and normal completion also write explicit terminal states.
- Validation: tests/test_sft_recovery.py passes 36/36; compile and diff checks pass. No remote action was started.
- Decision: this improves future auditability but does not change Round116's existing unknown-delivery status or authorize a retry.

## 2026-08-15 15:22:00 CST - Repair expansion plan model provenance

- Rebuilt the teacher-free expansion package after adding explicit `teacher_model=gpt-5.6-luna` to its report. It still selects 31 targets and requests at most 64 attempts, but remains non-authorizing.
- Validation: builder completed with no shortfalls; recovery tests 36/36; no remote request.
- Decision: the plan is auditable for the intended model, but explicit material-sampling approval is still required before preflight or teacher sampling.

## 2026-08-15 15:45:00 CST - Round117 shard 1 interrupted after partial execution

- Approval gate: user explicitly approved the next step; the first reviewed shard was limited to 8 targets and at most 16 teacher attempts. One real-image API3 preflight passed with `gpt-5.6-luna` (`reachable`, `visual_reachable`, and `manual_tool_call` all true).
- Local repair: approval targets use `source_sample_id`; the runner now hydrates sources indexed by either `sample_id` or `source_sample_id`, with a safe candidate-index fallback. Recovery tests pass 37/37.
- Execution: three targets were observed strictly accepted with 3/3 retrievals each; one target was observed rejected after one retrieval; the fifth teacher request remained unresolved. The bounded process was interrupted at exit code 130 before batch JSONL finalization.
- Evidence: `outputs/runs/rag/rag-sft-round117-shard1.log`, `outputs/experiments/rag_sft_iteration/candidates/round117-shard1/run_status.json`, and state entry `round117_shard1`.
- Decision: mark the shard `terminated_unknown_delivery`; do not promote the three observed accepts, do not retry the interrupted target, and stop remaining shards. No training rows were added; no SFT or formal evaluation was started.

## 2026-08-15 16:05:00 CST - Preserve completed rows during bounded interruption

- Added non-trainable per-sample progress checkpoints to the local runner. Each completed sample now has raw trace, retrieval ledger, accepted/rejected evidence, and an atomic status marker before the next teacher request begins.
- Validation: recovery tests 38/38, compile and diff checks pass; no remote request.
- Decision: do not retrofit or promote Round117's partial log-only observations; use the new checkpoint path only for future newly authorized shards.

## 2026-08-15 16:20:00 CST - Keep progress counts in interruption status

- Fixed the interrupt handler to preserve the latest progress metadata when switching a run to `terminated_unknown_delivery`; partial rows remain explicitly non-trainable.
- Validation: recovery tests 39/39, compile and diff checks pass. No remote request.
- Decision: Round117 remains unchanged; use the fix on future newly authorized runs.

## 2026-08-15 14:30:40 CST - Recheck confirms external gate remains unresolved

- No `run_pilot`, Round116, or `rag-sft` process remains; Round116 output still has only static plan/audit and preflight evidence, with no provider request status, trajectory, accepted/rejected JSONL, or exit marker.
- Current strict gate remains 19 RAG rows / 17 quota-usable, 32 Direct, and 31 quota-usable RAG rows short; training and formal evaluation remain unauthorized.
- The 31-target package remains `teacher_free_plan_ready_for_approval`, with `gpt-5.6-luna`, a 64-attempt cap, and both SFT/evaluation authorization flags false.
- Decision: the same external gate has persisted across repeated continuation checks. Stop Codex-side polling/retry; resume only after independent provider/request resolution or explicit material-sampling approval.

## 2026-08-15 15:27:02 CST - Synchronize latest unknown-delivery gate

- Recheck found no Round116/117/`run_pilot`/`rag-sft` process and no new provider request ID, receipt, trajectory, or final batch JSONL. Round117 shard 1 remains `terminated_unknown_delivery` with exit 130 after the fifth request; 3 observed accepts and 1 observed rejection remain log-only and non-trainable.
- Synchronized `outputs/experiments/rag_sft_iteration/state.json`, `docs/handoff_20260812.md`, and `docs/logs/START_HERE.md` so Round117 is the latest external gate. Strict corpus remains 19 RAG / 17 quota-usable plus 32 Direct; training and formal 618 remain unauthorized.
- Decision: do not replay Round116 or Round117, do not start remaining shards, calibration, freeze, SFT, or formal evaluation. Resume only after independent provider/request resolution or new explicit authorization, using a new run ID.
- Evidence: `outputs/runs/rag/rag-sft-round117-shard1.log`; `outputs/experiments/rag_sft_iteration/candidates/round117-shard1/run_status.json`; `outputs/experiments/rag_sft_iteration/state.json`.

## 2026-08-15 15:38:00 CST - Round118 shard 2 completes as a strict negative result

- Authorized shard 2 completed normally: 8/8 targets finished, 0 accepted, 8 rejected, 0 unknown delivery. The outer shell reported a read-only-variable error only after the Pilot wrote its terminal `completed` marker and final artifacts.
- Seven Option targets were rejected locally with `option sample requires four named public candidates`; no teacher sampling or retrieval was performed for those rows. The eighth stop-correction target made 2 successful retrieval calls but failed `final_name_mismatch`. No row is trainable.
- Root cause: the teacher-free expansion builder copied retrieval preflight rows without reconstructing the public Option candidate contract or stop-correction type. The builder now enriches Option rows from the audited catalog, sets `final_label`/`final_label_zh`/`correct_option`, sets `desired_correction_type=evidence_confirmed`, and fails closed on missing contracts.
- Rebuilding the teacher-free package changed the deterministic target snapshot. Round118 is therefore retained against its pre-rebuild source hash, is not replayed, and future shards must use the rebuilt package plus a new preflight.
- Evidence: `outputs/experiments/rag_sft_iteration/candidates/round118-shard2/pilot/manifest.json`; `outputs/experiments/rag_sft_iteration/candidates/round118-shard2/pilot/progress/rejected_trajectories.jsonl`; `outputs/runs/rag/rag-sft-round118-shard2/logs/pilot.stdout`.
- Next: run no further shard until its new-package preflight is reviewed; SFT, freeze, calibration release, and formal 618 remain unauthorized.

## 2026-08-15 15:46:00 CST - Round120 shard 2 stops on unknown teacher delivery

- The rebuilt package passed a fresh real-image preflight (`gpt-5.6-luna`, API3 reachable, visual reachable, whole-message manual tool call). The single authorized teacher request then remained blocked beyond the configured 180-second timeout; the bounded process was stopped with SIGINT before target completion.
- Runner state is `terminated_unknown_delivery`, exit 130, one attempt started, zero completed samples, no trajectory or training row, and no residual process. The request must not be replayed without independent provider/request resolution.
- Round119 is retained separately as a failed linkage preflight with no teacher sampling; Round118 remains a complete strict negative result.
- Evidence: `outputs/runs/rag/rag-sft-round120-shard2/logs/preflight_report.json`; `outputs/experiments/rag_sft_iteration/candidates/round120-shard2/pilot/run_status.json`; `outputs/rag_sft_iteration/state.json`.
- Next: stop Codex-side retries. Resolve the provider request state or obtain new explicit authorization before any new run; do not start remaining shards, freeze, SFT, or formal 618.

## 2026-08-15 16:25:00 CST - Terra stop-correction ablation fails finalization hard gate

- Switched the active future-sampling model to `gpt-5.6-terra` without changing any historical Luna artifact. The rebuilt 31-target approval package remains teacher-free, 31 targets, and capped at 64 attempts; Option targets now carry four public named candidates.
- Terra passed API3 real-image, vision, and whole-message manual-tool preflight. Two fresh Blind English stop-correction Pilots, each on a distinct target and new run ID, completed normally with 3/3 successful Milvus retrievals but were rejected for `tool_call_after_budget_finalization`.
- A public Blind-safe repair requiring the final response to begin with `<think>` and forbidding a JSON/tool-call prefix was tested on the second Pilot; the rejection was unchanged. Recovery tests remain 39/39 and validators were not relaxed.
- Decision: retain both negative artifacts and do not expand Terra stop-correction sampling. Round116/117/120 unknown requests remain non-replayable. Freeze, SFT, and formal 618 remain unauthorized.
- Evidence: `outputs/runs/rag/rag-sft-round121-terra-preflight/logs/preflight_report.json`; `outputs/experiments/rag_sft_iteration/candidates/round122-terra-shard3-pilot/pilot/`; `outputs/experiments/rag_sft_iteration/candidates/round123-terra-finalization-repair-pilot/pilot/`.

## 2026-08-15 16:42:00 CST - Split RAG SFT into standard-first and complete milestones

- Replaced the single 48+32 training sequence with two explicit gates. Stage A is an intermediate, standard-only SFT ablation: 32 balanced standard RAG plus 32 current-contract Direct. It requires a unique immutable freeze and separate SFT approval, and cannot request formal 618.
- Stage B retains the complete requirement: 48 balanced RAG plus 32 Direct, adding 16 stop-correction rows only after that family independently passes its Pilot gate. Only Stage B can later request formal 618 after matched forced-off diagnostics.
- Current Stage A state is 17/32 quota-usable standard RAG, so 15 standard Blind rows remain. The rebuilt approval package now writes `standard_targets.jsonl` (15) and `deferred_stop_correction_targets.jsonl` (16); the latter is intentionally not released while Terra repeats the budget-finalization hard failure.
- Validation: rebuilt strict gate and teacher-free package; Python compilation, recovery tests 39/39, JSON/YAML parsing, and diff checks pass. No sampling, freeze, SFT, or evaluation was started.
- Evidence: `outputs/experiments/rag_sft_iteration/reviews/strict_candidate_view_v1_gate.json`; `outputs/experiments/rag_sft_iteration/approval/rag_expansion_plan_v1/report.json`; `configs/experiments/data/data-rag-sft-iteration-control-v1.yaml`.

## 2026-08-15 16:50:00 CST - Make the standard milestone result-gated

- Stage A now explicitly stops after its separately approved standard-only 32+32 SFT and forced-retrieval-off matched diagnostic versus checkpoint-165. It must record a result-driven decision before any subsequent collection or training action.
- Decision branches are constrained: protocol failure repairs standard protocol; insufficient autonomous tool use adds Blind routing examples; insufficient answer quality adds high-evidence weak-cell standard rows; Direct regression increases validated Direct replay; a clear positive result requests direction rather than auto-starting stop-correction.
- Stage B, complete 48+32 SFT, and formal 618 are never automatic consequences of a Stage A result. They require an explicit post-review decision, an independently stable stop-correction Pilot family, a new immutable freeze, and their own approvals.
- Validation: JSON/YAML state and control configuration remain parseable; no sampling, SFT, freeze, or evaluation was started.

## 2026-08-15 17:05:00 CST - Correct Stage A to a complete standard milestone

- Corrected the two-stage design: Stage A is a complete standard milestone, not a low-value or partial SFT ablation. At an immutable 32 balanced standard RAG + 32 Direct freeze it receives one separately approved checkpoint-165 SFT plus forced-retrieval-off protocol smoke, balanced matched subgroup diagnostic, checkpoint-165 comparison, and a milestone review package.
- The Stage-A result remains decision-gated: it drives the next data/training revision and never automatically starts stop-correction or Stage B. If Stage-A gates are strong, formal 618 may be separately requested; it is not auto-started.
- Stage B remains a later complete 48+32 milestone after stop-correction independently stabilizes. No sampling, freeze, SFT, or evaluation was launched by this correction.

## 2026-08-15 19:05:00 CST - Round124/125 standard Pilot isolates Terra finalization failure

- Pre-run audit found no active Pilot process. Rounds116, 117 shard 1, and 120 shard 2 remain unknown delivery and were not replayed.
- Round124 used a fresh Stage-A Blind English disease Option target (`N04070_P00002`, public answer B, preflight rank 1 / score 0.95). Its default `yunwu.ai/v1` strict real-image preflight failed before visual reachability; it is retained as a complete endpoint-routing failure and was not retried.
- Round125 used a new run ID and the previously verified API3 endpoint. Strict real-image preflight passed. The bounded one-sample Pilot completed normally: 3 retrieval calls, 0 accepted, 1 rejected, 0 unknown delivery, and no training row.
- The strict rejection was `tool_call_after_budget_finalization`. This reproduces Terra's stop-correction failure in a `standard` trajectory, so it is a teacher/protocol hard failure rather than a stop-correction-only or retrieval-quality issue.
- Decision: retain all raw/rejected evidence, suspend Terra from further sampling, and run only a new-ID Luna API3 standard preflight followed by one bounded Pilot if it passes. No freeze, SFT, batch release, or formal evaluation is authorized.
- Evidence: `outputs/runs/rag/rag-sft-round124-standard-option-en-disease-preflight/logs/preflight_report.json`; `outputs/runs/rag/rag-sft-round125-standard-option-en-disease-api3-preflight/logs/preflight_report.json`; `outputs/experiments/rag_sft_iteration/candidates/round125-standard-option-en-disease-pilot/pilot/`.

## 2026-08-15 19:15:00 CST - Round126 Luna standard Pilot passes strict acceptance

- A fresh Blind English disease Option target (`N04006_P00003`, public answer B, preflight rank 1 / score 0.92) passed Luna/API3 strict real-image preflight.
- The one-sample standard Pilot completed normally: 1/1 accepted, 2/2 successful retrievals (`visual`, `balanced`), exact final Option answer B, zero unknown delivery, and no protocol, semantic, leakage, or image-isolation error.
- Independent artifact validation passed with retrieval success 1.0 and final-label accuracy 1.0. The row is now included in the current-contract strict view.
- Rebuilt gate: 20 strict RAG total, 18/32 Stage-A quota usable, 14 standard deficits, and 32/32 Direct. The remaining 30-target approval package is Luna-tagged and still non-authorizing.
- Decision: retain the accepted row and negative Terra evidence. Do not release adaptive sampling yet; use another new-ID Luna standard Pilot in a different deficit cell before requesting bulk release.

## 2026-08-15 19:25:00 CST - Round127 isolates Luna Chinese Open finalization instability

- A fresh Blind Chinese Open disease target (`N04013_P00001`, rank 1 / score 0.89) passed Luna/API3 strict real-image preflight.
- The bounded Pilot completed normally with 3/3 successful Milvus retrievals, 0 accepted, 1 rejected, and zero unknown delivery. The strict reason was `tool_call_after_budget_finalization`; no training row was added.
- Flow review: visual retrieval triggered the allowed automatic balanced follow-up, the model used the third available retrieval, then requested another tool call even after the public budget-finalization prompt. This is a finalization-state instability across Chinese/Open, not a transport, evidence-rank, or private-label failure.
- Decision: stop teacher sampling. Before a new target is requested, repair and offline-test the third-retrieval finalization state/prompt, then use a new-ID Luna/API3 preflight and one bounded Chinese Open repair Pilot. Adaptive sampling, freeze, SFT, and formal evaluation remain unauthorized.

## 2026-08-15 19:35:00 CST - Round128 shows finalization prompt repair is ineffective in vivo

- The strengthened finalization prompt passed an offline state-machine regression and a fresh Luna/API3 real-image preflight passed on a second Chinese Open disease target (`N04055_P00001`).
- The bounded repair Pilot completed normally with 3/3 successful retrievals, 0 accepted, 1 rejected, and zero unknown delivery. Its trace confirms the strengthened `工具已经永久关闭` finalization prompt was delivered.
- The teacher nevertheless made another tool call, producing the same strict `tool_call_after_budget_finalization` rejection as Round127. No training row was added.
- Decision: treat this as a repeated teacher/trajectory hard failure, not a prompt-only issue. Stop Chinese Open and all adaptive sampling; retain the negative evidence and require an explicit teacher/trajectory design revision before any further standard Pilot. Freeze, SFT, and formal evaluation remain unauthorized.

## 2026-08-15 20:15:00 CST - Round129 reaches closed terminal path but hits provider-format rejection

- New target `N05025_P00001` was a fresh Blind Chinese Open pest reserve (visual preflight rank 1 / score 0.83), distinct from the Round127/128 images. New-ID Luna/API3 strict real-image preflight passed.
- The one-sample Pilot completed with 3/3 successful retrievals, 0 accepted, 1 rejected, and zero unknown delivery. The closed final-answer state was entered and no fourth retrieval was executed.
- The terminal provider response was an invalid-format JSON error. Strict validation rejected it for missing final tags/fields, final-name evidence anchoring, and Chinese final language; no training row was added.
- Review found a local implementation defect: the new final-only session was not passed as `final_only=True`, so the provider still received the first-turn JSON response mode. This run is complete negative evidence and must not be replayed; it is not evidence that the closed trajectory is ineffective.
- Evidence: `outputs/runs/rag/rag-sft-round129-standard-open-zh-pest-luna-closed-preflight/logs/preflight_report.json`; `outputs/experiments/rag_sft_iteration/candidates/round129-standard-open-zh-pest-luna-closed-pilot/`.
- Next: repair and regression-test the terminal provider payload, then use another fresh target and new IDs for a strict preflight plus one bounded Pilot.

## 2026-08-15 20:35:00 CST - Round130 rejects a Blind semantic mismatch before budget closure

- Fresh Blind Chinese Open pest target `N05034_P00005` passed new-ID Luna/API3 strict real-image preflight (visual rank 2 / score 0.79). The bounded Pilot completed with 0 accepted, 1 rejected, and zero unknown delivery.
- The teacher made one successful visual retrieval and emitted a complete Chinese evidence-grounded final answer rather than continuing to the retrieval budget. It was rejected only for `final_name_mismatch`; no protocol, language, evidence-anchor, Blind-boundary, or tool-budget error occurred.
- This is ordinary Blind rejection evidence, not a closed-finalization result: `closed_finalization_used=false`. The answer was not privately rewritten and the target must not be replayed.
- Evidence: `outputs/runs/rag/rag-sft-round130-standard-open-zh-pest-luna-closed-preflight/logs/preflight_report.json`; `outputs/experiments/rag_sft_iteration/candidates/round130-standard-open-zh-pest-luna-closed-pilot/`.
- Next: use at most one higher-margin unused target to validate the corrected closed terminal path; keep adaptive sampling closed.

## 2026-08-15 20:45:00 CST - Round131 restores strict Blind Chinese Open acceptance

- Fresh Blind Chinese Open pest target `N05055_P00004` passed new-ID Luna/API3 strict real-image preflight (visual rank 2 / score 0.87). The bounded Pilot completed with 1 accepted, 0 rejected, and zero unknown delivery.
- The accepted trajectory used two successful Milvus calls (`visual`, `balanced`) and a Chinese evidence-grounded final answer. Independent artifact validation reported retrieval success 1.0, final-label accuracy 1.0, zero errors, and no visible class codes, system messages, or base64 payloads.
- This is positive evidence for the normal Chinese Open Blind trajectory. It ended after two retrievals, so `closed_finalization_used=false`: it does **not** yet verify the corrected budget-closure terminal branch in vivo.
- Evidence: `outputs/runs/rag/rag-sft-round131-standard-open-zh-pest-luna-closed-preflight/logs/preflight_report.json`; `outputs/experiments/rag_sft_iteration/candidates/round131-standard-open-zh-pest-luna-closed-pilot/`.
- Decision: retain the accepted candidate for the strict view only after the normal current-contract rebuild; do not release adaptive sampling or freeze/SFT. Return to Stage-A strategy review with the three bounded Round129-131 outcomes.

## 2026-08-15 21:05:00 CST - Rebuild a complete Stage-A-only fresh reserve

- Rebuilt the current-contract strict view after Round131: 21 RAG rows, 19/32 Stage-A quota-usable rows, 32/32 Direct rows, and 13 strict standard deficits. No freeze or SFT gate changed.
- The prior target list included completed Round127-131 images and was stale. The active planner now covers only the eight Stage-A standard cells and excludes deferred stop-correction work.
- The historic preflight audit supplied 12 isolated standard targets. One candidate supplement was rejected because it overlapped Direct replay. A catalog-level local Milvus preflight then examined six fully isolated Chinese Open/pest images, found four eligible, and selected N05008_P00001 (visual rank 1 / score 0.89) as the remaining reserve.
- The regenerated standard target list contains exactly 13 fresh isolated targets, one per remaining deficit position. Its images are unique and have zero overlap with current RAG, current Direct, or discovered evaluation images. It remains approval-only, with SFT and formal evaluation unauthorized.
- Evidence: outputs/experiments/rag_sft_iteration/approval/rag_expansion_plan_v1/report.json and the catalog supplement preflight artifacts in the same directory.
- Next: review per-cell Pilot stability using Round126/131 and the known finalization failures, then run only a bounded approved Pilot; do not release adaptive sampling.

## 2026-08-15 21:25:00 CST - Round132 validates closed terminal Option trajectory

- Fresh Blind Chinese Option disease target N04101_P00003 passed new-ID Luna/API3 strict real-image preflight (visual rank 1 / score 0.81). The bounded Pilot completed with 1 accepted, 0 rejected, and zero unknown delivery.
- The trajectory made three successful calls (visual, balanced, balanced), entered the closed final-answer state after a post-budget tool request, and returned a valid Chinese Option response with public answer A. No fourth retrieval executed.
- Independent artifact validation reported 3/3 retrieval success, final-label accuracy 1.0, zero errors, and no visible class codes, system messages, or base64 payloads. This is the first in-vivo positive validation of the repaired closed terminal branch.
- Rebuilt strict view: 22 RAG total, 20/32 Stage-A quota-usable, 32/32 Direct. Remaining standard deficits are Chinese Open disease 2, Chinese Open pest 2, English Option disease 1, English Option pest 3, Chinese Option disease 2, and Chinese Option pest 2. Freeze, SFT, and formal evaluation remain unauthorized.
- Evidence: outputs/runs/rag/rag-sft-round132-standard-option-zh-disease-luna-preflight/logs/preflight_report.json and outputs/experiments/rag_sft_iteration/candidates/round132-standard-option-zh-disease-luna-pilot/.
- Decision: retain Round132 in the current-contract strict view. This establishes a two-cell Luna stability basis across English Option and Chinese Option, but does not release adaptive sampling; next Pilot should cover the remaining high-deficit English Option pest or Chinese Option pest cell.

## 2026-08-15 21:40:00 CST - Round133 validates closed terminal Option trajectory in pest domain

- Fresh Blind English Option pest target N05020_P00004 passed new-ID Luna/API3 strict real-image preflight (visual rank 1 / score 0.83). The bounded Pilot completed with 1 accepted, 0 rejected, and zero unknown delivery.
- The trajectory made three successful calls (visual, balanced, visual), entered the closed final-answer state after a post-budget tool request, and returned a valid English Option response with public answer A. No fourth retrieval executed.
- Independent artifact validation reported 3/3 retrieval success, final-label accuracy 1.0, zero errors, and no visible class codes, system messages, or base64 payloads.
- Rebuilt strict view: 23 RAG total, 21/32 Stage-A quota-usable, 32/32 Direct. Remaining standard deficits are Chinese Open disease 2, Chinese Open pest 2, English Option disease 1, English Option pest 2, Chinese Option disease 2, and Chinese Option pest 2. Freeze, SFT, and formal evaluation remain unauthorized.
- Evidence: outputs/runs/rag/rag-sft-round133-standard-option-en-pest-luna-preflight/logs/preflight_report.json and outputs/experiments/rag_sft_iteration/candidates/round133-standard-option-en-pest-luna-pilot/.
- Decision: retain Round133 in the current-contract strict view. The closed final-answer trajectory is now accepted in both Chinese disease Option and English pest Option cells. Before requesting adaptive rejection sampling, run one fresh Chinese Option/pest Pilot and review all Option-cell yields.

## 2026-08-15 21:55:00 CST - Round134 completes Option-family Pilot coverage

- Fresh Blind Chinese Option pest target N05058_P00001 passed new-ID Luna/API3 strict real-image preflight (visual rank 1 / score 0.81). The bounded Pilot completed with 1 accepted, 0 rejected, and zero unknown delivery.
- The trajectory made three successful calls (visual, balanced, visual), entered the closed final-answer state after a post-budget tool request, and returned a valid Chinese Option response with public answer A. No fourth retrieval executed.
- Independent artifact validation reported 3/3 retrieval success, final-label accuracy 1.0, zero errors, and no visible class codes, system messages, or base64 payloads.
- Rebuilt strict view: 24 RAG total, 22/32 Stage-A quota-usable, 32/32 Direct. Remaining standard deficits are Chinese Open disease 2, Chinese Open pest 2, English Option disease 1, English Option pest 2, Chinese Option disease 2, and Chinese Option pest 1. Freeze, SFT, and formal evaluation remain unauthorized.
- Evidence: outputs/runs/rag/rag-sft-round134-standard-option-zh-pest-luna-preflight/logs/preflight_report.json and outputs/experiments/rag_sft_iteration/candidates/round134-standard-option-zh-pest-luna-pilot/.
- Decision: retain Round134 in the current-contract strict view. Luna now has one strict accepted Blind Option Pilot in all four language/domain cells, with in-vivo closed-terminal acceptance in three. This supports a future Stage-A-only calibration-release review, not automatic bulk sampling.

## 2026-08-15 22:10:00 CST - Build Stage-A-only calibration approval package

- Rebuilt the active rejection-sampling planner from the current 24-RAG/32-Direct strict view. It no longer includes stop-correction or a 48-row target: it serves only the 32+32 Stage-A milestone.
- The package contains ten fresh, isolated, preflight-eligible targets for the ten remaining standard deficits. It materializes 30 possible candidate attempts and a first calibration release of ten independent attempts, one per target; every item remains approval-only and sampling authorization is false.
- The Option-family stability review is positive: Rounds126, 132, 133, and 134 each strictly accepted one Blind Option trajectory across all English/Chinese by disease/pest cells, with zero unknown delivery. Rounds132-134 also exercise the closed terminal branch successfully.
- Open-family calibration remains not ready: Round131 passed Chinese Open, but Rounds127/128 recorded concentrated Chinese Open post-budget failures before the terminal-trajectory repair. The plan therefore does not license Open release without a separate review decision.
- Evidence: outputs/experiments/rag_sft_iteration/approval/bulk_distill_plan_v1/report.json.
- Next: obtain explicit Stage-A calibration approval scoped to Option cells, then repeat current real-image preflight for each released target and audit every attempt before releasing any reserve or Open cell.

## 2026-08-15 22:20:00 CST - Separate Option-ready and Open-deferred calibration releases

- The approval-only first release is now materialized as two disjoint files: six Option candidates in `calibration_option_ready_attempts.jsonl` and four Open candidates in `calibration_open_deferred_attempts.jsonl`. The six Option rows exactly match the remaining Option deficits (EN disease 1, EN pest 2, ZH disease 2, ZH pest 1); the four Open rows remain the Chinese disease/pest deficits.
- This is an authorization-boundary clarification, not a sampling decision: both files remain `sampling_authorized=false`. It prevents an Option-only approval from implicitly releasing the Open family while retaining a complete 10-target Stage-A reserve.
- Evidence: `outputs/experiments/rag_sft_iteration/approval/bulk_distill_plan_v1/report.json`; rebuilt with `python -m tools.rag_distill.build_bulk_distill_plan` and checked for type/cell separation.
- Next: only upon explicit Option-scoped approval, preflight and run the six Option first attempts one at a time with per-attempt audit; Open needs a separate strategy decision and authorization.

## 2026-08-15 22:30:00 CST - Enforce calibration scope before teacher access

- `run_pilot.py` now rejects every `approval_only` plan row unless the caller supplies a matching explicit authorization scope. The sole active scope is `stage_a_option_calibration`; it additionally requires standard, Blind, Option, non-reserve, candidate-index-1 rows. The check happens before credentials are resolved or a provider request is possible.
- The generated release files carry distinct scopes: all six Option candidates use `stage_a_option_calibration`; all four deferred Open candidates use `deferred_open_strategy_review` and cannot pass the active runner scope.
- This is a fail-closed execution guard, not authorization. No sampling was started and `sampling_authorized` remains false.
- Evidence: `tools/rag_distill/run_pilot.py`, `outputs/experiments/rag_sft_iteration/approval/bulk_distill_plan_v1/`; the focused recovery/RAG/language/manual-tool suite passed 47 tests.

## 2026-08-15 22:40:00 CST - Make within-cell Stage-A target selection reproducibly random

- The Stage-A expansion builder now allocates targets strictly by remaining quota deficit, then applies an independent uniform shuffle inside every fresh, preflight-eligible cell with seed `20260815`. Preflight eligibility remains a hard inclusion gate, but rank and retrieval score no longer act as an unrecorded preference for teacher collection.
- The rebuilt reserve still contains all ten needed targets with unique images and no shortfall. Its per-cell pool/selection counts are: Chinese Open disease 7/2, Chinese Open pest 2/2, English Option disease 8/1, English Option pest 5/2, Chinese Option disease 4/2, Chinese Option pest 3/1. The six Option candidates and four deferred Open candidates were rebuilt from this reserve.
- Static checks confirmed the 10 target contracts, Blind label boundary, Option A/B/C/D mappings, and image uniqueness; no teacher request was made.
- Evidence: `tools/rag_distill/build_rag_expansion_plan.py`, `outputs/experiments/rag_sft_iteration/approval/rag_expansion_plan_v1/report.json`.

## 2026-08-15 21:46:52 CST - Round135 Option calibration preflight stopped before teacher delivery

- User authorized the scoped Option calibration release. After the required no-process/unknown-delivery audit, Round135 selected the first current Option target (`rag_option-en-disease_N04119_N04119_P00004`) and launched its one-target real-image preflight.
- The configured Luna/API3 endpoint failed at TCP connection setup with `Network is unreachable`; the runner never reached endpoint preflight success, image transmission, or a teacher completion. The emitted status is `preflight_failed`, `delivery_status=complete`, `reachable=false`, `visual_reachable=false`, and `manual_tool_call=false`.
- Decision: this is a provider-connectivity prerequisite failure rather than unknown delivery or a rejection. Do not replay the failed preflight or launch another target while the endpoint remains unreachable; preserve the Round135 output and wait for independent connectivity restoration before a new-ID preflight. No Pilot, accepted/rejected trajectory, corpus mutation, freeze, SFT, or evaluation occurred.
- Evidence: `outputs/experiments/rag_sft_iteration/candidates/round135-standard-option-en-disease-luna-preflight/run_status.json` and `outputs/runs/rag/rag-sft-round135-standard-option-en-disease-luna-preflight/logs/preflight_report.json`.

## 2026-08-15 21:53:32 CST - Round136 Option Pilot rejects a final-answer contract violation

- Independent TCP checks found `https://api3.wlai.vip` reachable, so the original first Option target received a new-ID Round136 real-image preflight at `https://api3.wlai.vip/v1`. Endpoint, image reachability, and its single manual tool call all passed.
- The one-target Pilot completed with known delivery and three successful retrievals, but yielded 0 accepted / 1 rejected. Its final response used the valid Option letter `D` with evidence and alternatives, yet omitted the required `Predicted class name:` field in `<think>`; the strict validator rejected it as `missing_final_fields`.
- Decision: preserve the complete rejected trace without rewriting it or relaxing the validator. This is one protocol hard-gate rejection, not unknown delivery or a semantic/retrieval failure; do not replay the same target. Continue only with a fresh independent authorized target to determine whether the failure is concentrated.
- Evidence: `outputs/experiments/rag_sft_iteration/candidates/round136-standard-option-en-disease-luna-preflight/`, `outputs/experiments/rag_sft_iteration/candidates/round136-standard-option-en-disease-luna-pilot/`.

## 2026-08-15 22:00:00 CST - Repair first-attempt scope metadata collision

- A new-target Round137 preflight was stopped locally before credential resolution or network access because the authorization guard conflated two unrelated fields: a source candidate's `reserve` provenance and its teacher `candidate_index`. A separate planner issue also allowed selected-target position to overwrite `candidate_index`.
- The planner now records `target_selection_order` separately and emits `candidate_index=1` for every selected first attempt. The runner permits source-pool reserve provenance but still rejects every non-first teacher attempt; all 6 Option calibration rows pass scope validation after regeneration.
- This repairs authorization routing only. It does not relax the strict final contract, Blind boundary, retrieval validation, or acceptance policy. The recovery suite passed 36 tests; no Round137 teacher request was issued.

## 2026-08-15 21:55:00 CST - Round138 stops on unknown API3 teacher delivery

- After the first successful API3 real-image preflight and the local first-attempt scope repair, Round138 used a new English Option/pest target. Its first teacher completion POST returned HTTP 502 from the API3 nginx proxy before any retrieval call was recorded.
- The runner correctly recorded `completed_with_unknown_delivery`, `delivery_status=unknown`, 0 accepted, 1 non-trainable rejected trace, and 0 retrieval calls. An HTTP 502 after POST is ambiguous: it cannot establish that the upstream provider did not receive or process the request.
- Decision: do not replay Round138, do not send the same target to another endpoint, and do not start later calibration targets. Pause the authorized collection loop until independent provider/request resolution or new explicit authorization. Preserve its raw trace solely as unknown-delivery evidence; it is not training data.
- Evidence: `outputs/experiments/rag_sft_iteration/candidates/round138-standard-option-en-pest-luna-pilot/run_status.json`, `manifest.json`, and `traces/rejected_trajectories.jsonl`.

## 2026-08-15 22:05:00 CST - APIPlus rejects current credential; Round140 preflight becomes unknown delivery

- Per user direction to use an available endpoint, TCP probes found APIPlus and API3 reachable while the main endpoint was unreachable. The next independent Option/pest target was first tested as Round139 at APIPlus, where the endpoint preflight returned HTTP 403. Its preflight status is complete failure; it did not reach the real-image request or start a Pilot.
- The same untouched target then received a new-ID Round140 preflight at previously verified API3. Endpoint reachability succeeded, but the real-image POST waited the full 180 seconds then timed out while reading the response. The runner classified it as `UnknownTeacherDelivery`; its preflight status is `preflight_failed`, `delivery_status=unknown`, and no Pilot was launched.
- Decision: APIPlus is not usable with the current credential, and the Round140 target is non-replayable. Together with Round138's HTTP 502 unknown delivery, pause all further teacher requests rather than endpoint-hopping through the remaining calibration targets. No accepted data, quota change, freeze, SFT, or evaluation occurred.
- Evidence: `outputs/experiments/rag_sft_iteration/candidates/round139-standard-option-en-pest-luna-preflight/`, `outputs/runs/rag/rag-sft-round139-standard-option-en-pest-luna-preflight/logs/preflight_report.json`, `outputs/experiments/rag_sft_iteration/candidates/round140-standard-option-en-pest-luna-preflight/run_status.json`, and `outputs/runs/rag/rag-sft-round140-standard-option-en-pest-luna-preflight/logs/preflight_report.json`.

## 2026-08-15 22:12:00 CST - Round141 restores strict Option calibration acceptance at API3

- Under the user's bounded retry direction, a fresh untouched Chinese Option/disease target received a new-ID API3 real-image preflight. Endpoint, image reachability, and the single manual tool call passed. The bounded Pilot then completed with known delivery, 1 accepted, 0 rejected, and three retrieval calls.
- Strict artifact validation reported retrieval success 1.0, final-label accuracy 1.0, zero errors, and clean image/system/class-code boundaries. Its accepted artifact was explicitly added to the strict-view source list before rebuilding the gate.
- Updated current-contract state: 25 RAG rows total, 23/32 Stage-A quota-usable, and 32/32 Direct. Remaining standard deficits are Chinese Open disease 2, Chinese Open pest 2, English Option disease 1, English Option pest 2, Chinese Option disease 1, and Chinese Option pest 1. Rebuilt Stage-A reserve has 9 fresh targets (5 Option first attempts and 4 deferred Open).
- Decision: retain Round141 as strict Blind evidence. It does not erase Round138/Round140 unknown delivery evidence; their specific targets remain non-replayable. Freeze, SFT, and formal evaluation remain unauthorized.
- Evidence: `outputs/runs/rag/rag-sft-round141-standard-option-zh-disease-luna-preflight/logs/preflight_report.json`, `outputs/experiments/rag_sft_iteration/candidates/round141-standard-option-zh-disease-luna-pilot/`, and `outputs/experiments/rag_sft_iteration/reviews/strict_candidate_view_v1_gate.json`.
