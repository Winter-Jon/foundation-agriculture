# Handoffs - 2026-W25 (0615-0621)

## 2026-06-16 11:06:13 CST - Visual-observation candidate follow-up handoff

### Context
The user requested that after the first search, the model should explicitly propose a series of candidates from its own Visual Observation and the first retrieval results, then search again. The self-proposed candidates must differ slightly from exact database names instead of copying exact class names directly.

### Current State
- Current prompt version: `agrinet_rag_toolcall_v6_visual_observation_candidate_followup`.
- Active runner: `src/agrinet/rag/distill/run_pilot.py`.
- Student conversion: `src/agrinet/rag/distill/convert_to_student_sft.py`.
- Smoke coverage: `src/agrinet/rag/distill/name_eval_smoke.py`.
- No Slurm rerun has been launched yet after this code change.
- `src/agrinet/rag/distill/` is currently an untracked directory in git, so `git diff` does not show these file changes unless the directory is added or diffed manually as untracked content.

### Implementation Notes
- The teacher prompt now requires the first visual retrieval to be followed by a visible candidate-hypothesis step before any subsequent search.
- The automatic visual-follow-up path now builds descriptive candidates from retrieved class names with host/organ/symptom abstractions. For example, exact names such as `Cherry Normal leaf`, `Apple Black Rot`, and `Grape Leaf Blight` are converted into descriptive variants rather than copied verbatim into self-proposed candidates.
- The inserted pre-tool assistant message contains these visible headings: `Visual Observation:`, `Retrieved visual results:`, and `Self-proposed candidates:`.
- The follow-up query uses `Compare candidates: ...` with descriptive candidate phrases and `retrieval_type=balanced`, keeping `image=query_image`.
- `convert_to_student_sft.py` now preserves this richer candidate pre-tool think block during conversion instead of replacing it with a generic `pre_tool_think()` template.
- Exact `name` lookup remains restricted to exact names already copied from real tool evidence; unsupported private target names remain blocked by the existing leakage gates.

### Validation
- `python -m agrinet.rag.distill.name_eval_smoke` passed.
- `python -m compileall tools/rag_distill` passed.
- The smoke test now verifies that generated candidates do not normalize to the exact retrieved names, the follow-up query does not directly include exact names such as `Cherry Normal leaf` or `Apple Black Rot`, and the candidate think block contains `Visual Observation:` and `Self-proposed candidates:`.

### Recommended Next Steps
1. Run a small Slurm pilot with the updated v6 runner and inspect whether accepted trajectories contain the intended candidate step before the second search.
2. Check that validation still passes on `agent_sft.student.jsonl` after conversion and that the preserved candidate think block is acceptable for ms-swift Agent-format training.
3. Continue treating older supplemented-evidence artifacts and candidate-name-verification artifacts as debugging references, not current strict-policy training data.
4. If the descriptive candidate generator is too coarse, expand the host/symptom token mapping using observed AgriNet class names rather than loosening exact-name leakage rules.

## 2026-06-15 09:02:42 CST - Neutral-candidate RAG distillation handoff

### Context
The current active policy is stricter than the earlier candidate-name-verification run: the model must not write an unsupported exact database name before a `name` tool call. It should use neutral English candidate descriptions until a real tool response returns an exact class name; only then can `name` lookup verify that exact name.

### Current State
- Current prompt version: `agrinet_rag_toolcall_v5_neutral_candidate_retrieved_name_only_private_gt`.
- Current runner: `src/agrinet/rag/distill/run_pilot.py`.
- Current schema: `src/agrinet/rag/distill/schema.py`.
- Current smoke: `src/agrinet/rag/distill/name_eval_smoke.py`.
- Latest strict artifact: `outputs/rag_distill/agrinet_rag_toolcall_v5_neutral_candidate_rationale_clean_rerun_20260615_084745`.
- Latest strict Slurm logs: `slurm/rag-toolcall-v5-pilot5_19470.out`, `slurm/rag-toolcall-v5-pilot5_19470.err`.
- Slurm job `19470`: generation completed, but Slurm state is `FAILED`, `ExitCode=1:0`, because validation rejects empty accepted artifacts.
- Validation: `accepted_count=0`, `rejected_count=5`, `retrieval_call_count=14`, `retrieval_success_rate=1.0`, `error_count=1`, `errors=["accepted artifact is empty"]`.

### Implementation Notes
- `name_query_allowed()` now permits only exact names already present in real retrieved evidence.
- Automatic private-label `name` verification has been removed. The deterministic follow-up now uses neutral visible terms through `balanced` retrieval instead of an unsupported exact name.
- `clamp_tool_args()` scrubs both `query` and `rationale`, because tool-call rationale is student-visible and can leak target-like words too.
- `pre_tool_think()` for `name` says the name is copied from retrieved evidence.
- `src/agrinet/rag/distill/name_eval_smoke.py` checks first-turn name blocking, unsupported post-retrieval name blocking, constructed-evidence rejection, and rationale scrubbing.

### Current Conclusion
The latest strict run is safety-correct but not training-useful: it produced no accepted SFT rows. The earlier job `19468` remains a useful baseline for debugging acceptance, but it is not recommended under the current stricter policy because it used exact private candidate names before those exact names appeared in evidence.

### Recommended Next Steps
1. Add teacher API retry-on-timeout; four rejected rows in `19470` included timeout-related `runtime_error` reasons.
2. Improve retrieval for neutral healthy/normal leaf queries so `Cherry Normal leaf` can be found without exact-name leakage.
3. Keep `name` retrieval restricted to exact class names copied from tool evidence.
4. If a future run produces accepted rows, validate and scan both `query` and `rationale`, not just query text.

## 2026-06-15 11:31:39 CST - Teacher-retry strict rerun handoff

### Context
The user asked to rerun the strict neutral-candidate samples. The existing Slurm job `19471` was still running after the local monitoring command was interrupted, so it was allowed to finish instead of submitting a duplicate job on the same RAG port.

### Current State
- Code now includes teacher retry handling in `src/agrinet/rag/distill/run_pilot.py`.
- Latest rerun artifact: `outputs/rag_distill/agrinet_rag_toolcall_v5_neutral_candidate_teacher_retry_rerun_20260615_091212`.
- Slurm logs: `slurm/rag-toolcall-v5-pilot5_19471.out`, `slurm/rag-toolcall-v5-pilot5_19471.err`.
- Slurm result: `FAILED`, `ExitCode=1:0`, elapsed `02:14:50`, because validation rejects empty accepted artifacts.
- Validation: `accepted_count=0`, `rejected_count=5`, `retrieval_call_count=21`, `retrieval_success_rate=1.0`, `error_count=1`, `errors=["accepted artifact is empty"]`.
- Rejected reasons: `runtime_error=4`, `target_not_in_retrieved_evidence=1`.
- Tool argument scan: `forbidden_tool_arg_hits=0` for unsupported exact target names and invalid evidence strings in `query` / `rationale`.

### Notes
Retries are working, but the teacher endpoint is still timing out frequently. The strict neutral-candidate strategy remains safe but currently produces no trainable rows on this five-sample Cherry Normal leaf pilot.

### Recommended Next Steps
1. Add sample-level resume/retry or shorter teacher calls so one bad sample does not consume many minutes.
2. Improve retrieval recall for neutral healthy/normal leaf queries.
3. Keep exact `name` lookup restricted to names copied from retrieved evidence.

## 2026-06-15 08:13:35 CST - Pre-name-verification wording fix handoff

### Context
The candidate-name-verification strategy remains active, but the pre-tool assistant thought before `name` lookup must not claim that the candidate name has already appeared in evidence unless it actually has. The previous student file contained the overclaim `This exact candidate name has appeared in evidence...` before `Cherry Normal leaf` appeared in a tool response.

### Current State
- `src/agrinet/rag/distill/run_pilot.py` now generates neutral name pre-tool thinking: `I should verify this exact database candidate name with the name lookup: ...`.
- `src/agrinet/rag/distill/convert_to_student_sft.py` regenerates pre-tool think messages during conversion, so old raw/accepted traces can be reconverted safely.
- Current refreshed student file: `outputs/rag_distill/agrinet_rag_toolcall_v5_candidate_name_verify_rerun_20260615_075540/train/agent_sft.student.jsonl`.
- Explicit validation on the refreshed student file passed with `accepted_count=3`, `error_count=0`.
- Scan of the refreshed student file found no `appeared in evidence` / `This exact candidate name has appeared` strings.

### Next
Keep the distinction clear in future prompts and reviews: before the `name` response, `Cherry Normal leaf` is a candidate being verified, not evidence already returned by the tool.

## 2026-06-15 08:05:04 CST - RAG distillation candidate-name-verification handoff

### Context
The current useful RAG distillation strategy allows the teacher to first inspect visual and retrieved similar evidence, then verify an exact database candidate name with the `name` tool. This improves acceptance without using larger `top_k` or constructed target evidence.

### Current State
- Current prompt version: `agrinet_rag_toolcall_v5_candidate_name_verify_retrieved_evidence_only_private_gt`.
- Current artifact: `outputs/rag_distill/agrinet_rag_toolcall_v5_candidate_name_verify_rerun_20260615_075540`.
- Current student SFT: `outputs/rag_distill/agrinet_rag_toolcall_v5_candidate_name_verify_rerun_20260615_075540/train/agent_sft.student.jsonl`.
- Slurm job `19468`: `COMPLETED`, `ExitCode=0:0`, elapsed `00:07:59`.
- Scheduler logs: `slurm/rag-toolcall-v5-pilot5_19468.out`, `slurm/rag-toolcall-v5-pilot5_19468.err`.
- Validation: `accepted_count=3`, `rejected_count=2`, `retrieval_call_count=15`, `retrieval_success_rate=1.0`, `final_label_accuracy=1.0`, `error_count=0`.

### Implementation Notes
- First tool call remains visual and natural English.
- After real visual retrieval, the runner still adds balanced comparison over retrieved similar class names.
- After at least one real retrieval, the runner can issue `name` lookup for an exact database candidate label from the sample candidate set. First-turn `name` queries are still blocked and converted to safe semantic fallback.
- The final answer is accepted only if supported by real tool evidence. No `supplemental_evidence` or `agrinet_candidate_evidence` is allowed.
- `src/agrinet/rag/distill/name_eval_smoke.py` covers first-turn target-name blocking, post-retrieval exact-name verification allowance, and rejection of non-retrieved candidate evidence.

### Results
- Accepted rows: `disease_N04029_N04029_P00001`, `disease_N04029_N04029_P00002`, `disease_N04029_N04029_P00003`.
- Accepted queries are staged and English-only:
  - visual: `oval serrated leaf` or `green serrated leaf`
  - balanced: retrieved-candidate comparisons such as `Compare Apricot shot hole, Apricot Normal, Cherry brown spot by visible leaf traits and symptoms`
  - name: `Cherry Normal leaf`
- All accepted tool calls are immediately preceded by assistant `<think>...</think>`.
- Scan found no constructed evidence strings: `supplemental_evidence`, `agrinet_candidate_evidence`, or `candidate_evidence_added_after_retrieval_miss`.
- Rejected rows `disease_N04029_N04029_P00004` and `disease_N04029_N04029_P00005` failed due to teacher API read timeouts.

### Recommended Next Steps
1. Qualitatively inspect the three accepted trajectories before scaling.
2. Add teacher API retry-on-timeout so rows like 4 and 5 can be recovered.
3. Keep monitoring how often exact candidate-name verification is used when scaling; it is valid retrieval evidence, but still teacher-assisted candidate selection.

## 2026-06-15 07:25:45 CST - RAG distillation retrieved-evidence-only handoff

### Context
The previous supplemented-evidence approach is deprecated. The `rank=4, score=null` row was constructed from private target candidate evidence after retrieval miss, not returned by Milvus, so it is not valid evidence for distillation.

### Current State
- Current runner prompt version: `agrinet_rag_toolcall_v5_preturn_think_retrieved_evidence_only_private_gt`.
- Evidence policy: accepted rows must be supported by real tool retrieval results only. If retrieval does not return the target class or alias, the row must search again or be rejected.
- Deprecated artifacts: `outputs/rag_distill/agrinet_rag_toolcall_v5_supplemented_evidence_rerun_20260615_050014` and `outputs/rag_distill/agrinet_rag_toolcall_v5_preturn_think_rerun_20260615_062453`.
- Deprecated jobs: `19461` and `19462`; do not use their accepted rows for SFT because they include constructed private-target evidence.

### Implementation Notes
- `src/agrinet/rag/distill/run_pilot.py` no longer appends target candidate evidence after retrieval misses.
- `src/agrinet/rag/distill/validate_artifact.py` rejects `supplemental_evidence` and `source_dataset=agrinet_candidate_evidence` in tool responses.
- `src/agrinet/rag/distill/name_eval_smoke.py` includes a regression check for these invalid evidence fields.
- Safe behavior retained from the prior iteration: English-only natural query fields, pre-tool assistant thinking messages, dense-only `rrf`, sparse lookup isolated to exact `name`, and follow-up searches based on retrieved similar class names.

### Recommended Next Steps
1. Run smoke validation and verify old supplemented artifacts now fail validation.
2. Rerun the v5 Slurm pilot with retrieved-evidence-only generation.
3. Improve retrieval for healthy/normal leaf classes rather than injecting target-derived evidence.

## 2026-06-15 05:36:34 CST - RAG distillation supplemented-evidence handoff

### Context
The current RAG distillation work is focused on producing student-safe AgriNet tool-call trajectories without leaking private target names in search queries. The latest successful pilot uses English natural queries, retrieved similar-class follow-up search, and marked supplemented candidate evidence when dense retrieval misses the target class.

### Current State
- Current successful artifact: `outputs/rag_distill/agrinet_rag_toolcall_v5_supplemented_evidence_rerun_20260615_050014`.
- Current student SFT file: `outputs/rag_distill/agrinet_rag_toolcall_v5_supplemented_evidence_rerun_20260615_050014/train/agent_sft.student.jsonl`.
- Current Slurm job: `19461`, `COMPLETED`, `ExitCode=0:0`, elapsed `00:09:09`.
- Scheduler logs: `slurm/rag-toolcall-v5-pilot5_19461.out`, `slurm/rag-toolcall-v5-pilot5_19461.err`.
- Prompt version: `agrinet_rag_toolcall_v5_auto_similar_search_supplemented_evidence_private_gt`.
- Acceptance policy: `rag_toolcall_v5_think_answer_evidence_gated_private_gt_student_safe`.
- Final validation on student rows passed with `accepted_count=2`, `rejected_count=3`, `retrieval_success_rate=1.0`, `final_label_accuracy=1.0`, and `error_count=0`.

### Implementation Notes
- `src/agrinet/rag/distill/run_pilot.py` now:
  - Uses an English student prompt: `Identify the agricultural object or disease in the image and output its canonical name.`
  - Blocks target-like early queries, including reordered near leaks such as `healthy cherry leaf` / `cherry healthy leaf`.
  - Allows exact similar class names after they appear in retrieved tool evidence.
  - Automatically adds a balanced follow-up after first successful visual retrieval by comparing top retrieved candidate class names.
  - Adds marked supplemented candidate evidence after a retrieval miss on `balanced`, `semantic`, or `rrf`, but not as a target-name tool query.
- `src/agrinet/rag/distill/schema.py` now documents that `name` retrieval is exact class-name/alias lookup only and should be used only for exact database names already present in evidence.
- `src/agrinet/rag/milvus_tools/search_api.py` presets now isolate sparse lookup to `name`: `balanced`, `visual`, `semantic`, and `rrf` have `sparse_weight=0.0`; `name` has `sparse_weight=1.0`.
- `src/agrinet/rag/milvus_tools/search_agrinet_wiki.py` also forces `sparse_weight=0.0` for `rrf` in the lower-level hybrid search path.
- `src/agrinet/rag/distill/name_eval_smoke.py` covers early target-like query rejection, evidence-anchored similar-name query allowance, and exact retrieved-name behavior for `name` lookup.

### Latest Accepted Rows
- `disease_N04029_N04029_P00004`
- `disease_N04029_N04029_P00005`

Accepted tool queries:
- `healthy serrated leaf`
- `green serrated leaf`
- `Compare Apricot shot hole, Apricot Normal, Walnut Anthracnose leaf disease by visible leaf traits and symptoms`
- `Compare Apricot shot hole, Apricot Normal, Pear fire blight by visible leaf traits and symptoms`

Both accepted rows contain `supplemental_evidence=candidate_evidence_added_after_retrieval_miss` in a balanced tool response.

### Known Issues
- The current pilot is small and single-class; acceptance is only 2/5.
- Supplemented evidence is not a pure retrieval result. It is marked, but downstream scaling should track its frequency and decide whether to train with it, filter it, or use it only for preference/repair data.
- Dense retrieval misses `Cherry Normal leaf` frequently for these samples, returning close disease/healthy alternatives instead.
- Student-visible tool responses still include Chinese evidence fields. Query text is English-only, but the overall message content is bilingual.
- Several rejected trajectories fail with runtime/final-answer issues rather than retrieval failure alone.

### Recommended Next Steps
1. Qualitatively inspect the two accepted student rows before using them in SFT.
2. Add report fields for supplemented-evidence count/rate so scaling can monitor how often candidate evidence is injected.
3. Consider improving retrieval for healthy/normal classes before scaling, especially neutral queries such as `healthy leaf without spots or lesions`.
4. If pure-English training messages are required, remove or mask `chinese_name` and `chinese_aliases` in compact tool responses.
5. For future Slurm submissions requiring Yunwu, keep using submit-time credential export: `eval "$(~/.apikeys/bin/apikey env yunwu)"` followed by `sbatch --export=ALL ...`.
