# Plans — 2026-W37 (0907–0913)

## 2026-09-10 12:49:28 CST - Freeze RAG-adjacent E3 class folds with three-fold preference

Tags: open-agri-v3, classifier, rag, class-holdout, e3

### Goal

Cover all 107 Known classes with classifier-unseen evaluation while retaining a
RAG-visible neighbour for every held-out class. Prefer three folds; use five
only if no balanced three-fold assignment satisfies every bridge.

### Verified design

- Adjacency reuses the frozen SigLIP2 joint text/image Top-12 RAG ranking at
  `outputs/milvus/wiki_similar_classes_siglip2_report.top12.json` (SHA-256
  `a92a724a82d170cad97ba3b6b3c4a337650a20ce8f1382724488ccd4f8545676`).
- The E3 Known subgraph retains the first three distinct, same-domain, canonical
  Known neighbours in original rank order. It is directed and not manually or
  symmetrically expanded. All 107 classes have at least one such neighbour.
- Three folds are feasible and selected: held-out sizes 36/36/35, with domain
  counts 24+12, 24+12, and 24+11 (disease+pest). Every class is held out once.
- Per-fold classifier label spaces contain 71/71/72 classes and exclude all
  held-out class images and labels. RAG retains the complete 211-class registry.
- The builder found 805 valid three-fold candidates in 20,000 deterministic
  attempts and selected a valid image-balanced candidate. Five-fold fallback was
  therefore not evaluated or selected.

### Evidence

- Contract: `configs/sampling/micu-classifier-hcv-e3-classfold/contract-v1.yaml`
- Builder: `scripts/vision/build_adjacent_class_folds.py`
- Summary: `outputs/artifacts/vision/openagri-v3-known-vitl-e3-adjacent-classfold-v1/folds.json`
- Per-class witnesses: `outputs/artifacts/vision/openagri-v3-known-vitl-e3-adjacent-classfold-v1/class_assignments.jsonl`
- Validation: `tests/test_adjacent_class_folds.py` passed 3 tests; artifact
  audit verified 107 unique assignments, non-empty witnesses, disjoint
  classifier label spaces, and complete RAG visibility.

### Next

Finish the already-running E2 collection and audit first. Do not launch E3
classifier training or teacher collection without a separate experiment config
and explicit execution decision; all current E3 artifacts remain
`training_eligible=false`.

## 2026-09-10 19:27:41 CST - Sol E2 retry-v2 risk-exception preflight plan

Tags: micu, hcv, classifier, e2, retry-v2, sol, no-sft

### Trigger and boundary

The two-round no-data `gpt-5.6-sol` SLB canary delivered 18/20 valid responses
but failed its strict 10/10-per-round readiness gate. The user explicitly
authorized continuing with Sol. This is a one-off, bounded risk exception, not
a change to readiness policy and not authorization to replay existing requests
or start SFT.

### Next executable unit

Create `retry-v2-sol-preflight` as a new registered experiment and output root.
Before any provider request, extend the retry executor without changing retry-v1
behavior so it accepts an explicit selection manifest, teacher model and bounded
concurrency. Freeze a manifest containing at most 16 strict descendants of the
E2 retry sidecar: target coverage is 8 parent, 5 parent-audit, 2 G1 and 1
G2-audit. Exclude quarantined records and boundary exclusions; retain old request
IDs only as provenance.

### Execution gates

1. Unit-test model propagation and selection-subset validation; preserve retry-v1
   behavior: exactly 130 rows at concurrency four.
2. Run registered `show` and `submit --dry-run`; verify Sol, SLB, at most 16
   scopes, concurrency one, a new output root, no old provider IDs,
   `no_automatic_replay=true`, and `training_eligible=false`.
3. Only after dry-run evidence passes, submit bounded collection detached. Do not
   retry a timeout, disconnect, invalid delivery, or unknown delivery.
4. Run a retry-v2-specific private audit and final delivery/quality audit. Report
   scope accounting, ID disjointness, canary comparison, and whether a separate
   expansion decision is warranted. Keep SFT disabled in every outcome.

### Non-concurrent work

E3's RAG-adjacent class folds remain frozen and must not train or collect in
parallel with this preflight. The retry-v2 result determines provider-path
usability only; it does not change the E3 classifier/RAG split.

### Evidence

- Sol canary: `outputs/artifacts/micu-slb-canary/v2-sol/report.json`
- E2 retry sidecar: `outputs/artifacts/micu-classifier-hcv-e2/exploration-v1/audit/retry_pending.jsonl`
- Retry-v1 audit: `outputs/artifacts/micu-classifier-hcv-e2/retry-v1/audit/final.json`
## 2026-09-13 05:22:24 CST - Implement E3.9 prospective HCV cascade audit

Tags: e39, hcv, hermes, rag-distillation

### Goal
Replace E3.5's prompt-only reasoning request with an executable E3.9 public HCV/Hermes contract, validated first on the frozen balanced 32-image audit source.

### Steps
1. Materialize an immutable E3.9 source from the E3.5 v5 audit source with a private six-route coverage sidecar.
2. Run only the E3.9 R0/R1/R2 audit lineage; require accepted Direct, Classifier, and RAG witnesses in both arms.
3. On gate pass, create only the exact 1,038 non-audit full manifest under the 8M uncached-input cap.

### Acceptance
All terminals use Hermes HCV fields; Classifier is predict-first; RAG is predict-first plus one to three actual searches; no SFT/training authorization is created.

### Risks
Provider and local-RAG delivery remains volatile. Historical E3.5 trajectories are immutable and cannot satisfy E3.9's new format contract retroactively.

### Next
Review the generated E3.9 audit source/manifest and launch the registered 32-image audit only when explicitly requested.

## 2026-09-13 05:30:00 CST - E3.9 execution contract

Tags: e39, hcv, hermes, recovery, no-sft

### Goal
Produce audited E3.9 HCV teacher trajectories for the frozen dual-arm source while preserving all E3.5 history and keeping training disabled.

### Steps
1. Freeze E3.9 coverage, source, and R0 manifest from the exact v5 32-image source.
2. Run R0 and generate immutable round summary; use R1/R2 only for provider-delivery ambiguity.
3. Reconcile all 32 terminals. Require each arm to have an accepted Direct, Classifier, and RAG coverage winner with actual public evidence.
4. If and only if the audit report passes, freeze the E3.9 non-audit source and 1,038-row full R0 manifest; collect under the independent 8M token cap.
5. Report routes, arms, RAG outcomes, delivery/quality/tool shortfalls, per-class closure, and preserve all no-training flags.

### Acceptance
Direct has 2--3 named visual hypotheses; Classifier is predict-first; RAG is predict-first and uses one to three real searches; all final and pre-tool text is legal Hermes and public-only HCV reasoning.

### Risks
Provider instability, tool-service interruption, missing route coverage, and token cap exhaustion are reported as their own terminal states; none may be silently replayed or converted into quality evidence.

### Next
Use `docs/logs/handoffs/2026-W37-e39-hcv-cascade-memory.md` as the resume entrypoint, then perform the immutable audit preparation and registered dry-run.

## 2026-09-13 11:00:00 CST - E3.10 successor audit improvement

Tags: e310, hcv, hermes, native-tools, no-sft

### Goal
Address E3.9 v1's high delivered contract-reject rate without reinterpreting its terminal records.

### Changes
1. Split pre-tool Hermes planning and native tool call into separate turns; apply forced tool choice only after a valid planning turn.
2. Accept clearly separated paragraph observations and rejected-alternative clauses, while retaining semantic HCV field requirements.
3. Freeze a new 32-image audit source disjoint from E3.9 and require classifier-predict-once/RAG-search-one-to-three.
4. After the actual classifier card, require a dedicated public RAG-query planning turn before permitting the first RAG call.

### Validation
E3.10 source has 32 rows and zero E3.9 sample overlap. Registered live dry-run passed with zero teacher requests, local RAG health, fixed registry SHA, and no training flags.

### Next
E3.10 R0 is prepared but not launched; do not use E3.9 terminal rows as its source or recovery scope.

## 2026-09-13 11:50:00 CST - E3.10 closure and E3.11 HCV compatibility successor

Tags: e310, e311, hcv, hermes, cascade, recovery, no-sft

### Goal

Close E3.10's immutable 32-image lineage correctly, then run a separate prospective 32-image E3.11 audit that fixes observed public HCV-format incompatibilities without weakening Hermes, tool, truth-isolation, or recovery controls.

### Verified trigger

E3.10 R0 closed 32 rows: 30 delivered route-contract terminals and two `unknown_delivery` rows. The delivered terminals are not eligible for replay. Their public-safe causes are 16 `hcv_rejected_alternatives`, 11 `hcv_uncertainty`, and 3 `hcv_fields`. The first two classes show content-rich HCV prose that fails a narrow surface parser; the last class places HCV material in `<answer>` and remains a true Hermes failure. Since pure public-format failure occurs before the cascade continuation decision, it also prevents otherwise private-audit-rejected Direct parents from reaching Classifier/RAG.

### Steps

1. Freeze E3.10 R0 summary; run R1 only for its exact two `unknown_delivery` work items. Freeze R1 summary and generate R2 only for any remaining delivery ambiguity. Freeze the final E3.10 report regardless of gate result.
2. Preserve all E3.10 delivered terminals and request IDs as immutable evidence. Do not reinterpret them as unknown delivery, replace images, or use them as E3.11 input.
3. Create E3.11 from a new deterministic, identity-disjoint 32-image audit source. Exclude every E3.9 and E3.10 audit identity at image SHA, source-group, near-duplicate-group, and sample-ID levels.
4. Keep strict constraints: final exactly `<think>…</think><answer>…</answer>`; all six HCV headings inside `<think>`; Option answer `class name — letter`; private truth/coverage isolation; actual native tool-call/tool-response pairing; predict-once; Classifier expand at most once; RAG search one to three; R0/R1/R2 delivery-only recovery; no training.
5. Repair expression compatibility: accept candidate/reason clauses expressed as `candidate: conflict`, `candidate is rejected because conflict`, or `candidate is less likely because conflict`; require an explicit uncertainty judgment plus evidence limitation, while prompt-prefering `low`, `medium`, or `high` rather than treating their absence alone as failure.
6. Repair cascade sequencing: for a Direct parent that has already been private-audited as `reject`, treat an otherwise public HCV formatting failure as a non-winning terminal eligible to advance to Classifier in the same attempt; retain it as a public contract observation. A malformed Hermes outer structure, invalid tool interaction, private-audit delivery ambiguity, or any RAG/tool failure still stops immediately and never upgrades.
7. Run E3.11 R0, then R1/R2 only for exact delivery gaps. Require public route/format validity, at least one retained correct RAG evidence trajectory, and private coverage winners for Direct/Classifier/RAG in each arm before considering any separate full-campaign authorization.

### Acceptance

E3.10 is conclusively reported without illegal replay. E3.11 has a tested, immutable contract and source/manifest before provider launch; its validators distinguish substantive Hermes/tool violations from equivalent HCV prose. No full collection, conversion, SFT, or training is authorized by either audit.

### Next

Launch the already reviewed two-row E3.10 R1, then inspect its immutable outcome before creating R2.

## 2026-09-13 13:00:00 CST - E3.12 closeout decision

Tags: e312, hcv, hermes, rag, recovery, no-sft

### Verified closeout

E3.12's R0/R1/R2 chain is complete and its final report is immutable at
`outputs/artifacts/e312-hcv-cascade/reports/e312-hcv-audit-v1-final.json`. The
wire-normalization change met its narrow target: a provider-generated pure
Hermes planning string that arrived alongside one native call was preserved
verbatim as the immediately preceding assistant plan; ordinary mixed text was
still rejected. This yielded retained RAG evidence with no E3.11 pre-tool-format
failure.

The audit nevertheless failed its deliberate coverage gate: the six
private-designated arm/route witnesses did not produce Direct, Classifier, and
RAG accepted winners in each arm. The only unresolved provider lineage remained
unknown through attempt 2 and is terminal `delivery_shortfall`.

### Consequence

Do not launch the 942-candidate full campaign, SFT conversion, or training from
E3.12. Any future successor must use a fresh identity-disjoint audit source and
an explicitly reviewed strategy for raising HCV content compliance and coverage
without replaying E3.12 delivered terminals.

## 2026-09-13 14:30:00 CST - E3.13 closeout decision

Tags: e313, hcv, hermes, rag, recovery, no-sft

### Verified closeout

E3.13's independently frozen R0/R1/R2 chain is complete at
`outputs/artifacts/e313-hcv-cascade/reports/e313-hcv-audit-v1-final.json`. It
fixed the intended HCV content failure modes without weakening contracts: the
explicit Option and named-rejection prompt eliminated E3.12's
`hcv_option_coverage` and `hcv_rejected_alternatives` rejects and increased
accepted rows from nine to 20.

The protocol gate is nevertheless false. The seven RAG terminals did not yield
an accepted retained-evidence winner, and private route coverage closed only
Direct in each arm. One provider lineage was unknown through its R2 attempt and
is a final delivery shortfall.

### Consequence

Do not launch the 910-candidate full campaign, SFT conversion, or training. Any
future successor must use a new identity-disjoint source and specifically repair
the remaining native tool-order/RAG acceptance behavior without reinterpreting
or replaying E3.13 delivered terminals.

## 2026-09-13 15:35:00 CST - E3.14 conditional-cascade closeout

Tags: e314, hcv, classifier, rag, recovery, no-sft

### Verified closeout

E3.14's independent R0/R1/R2 audit is complete at
`outputs/artifacts/e314-hcv-cascade/reports/e314-hcv-audit-v1-final.json`. It
implements the requested classifier-first semantic: a private-approved
Classifier final closes as Classifier; only a private retrieval-discriminator
decision, or a private RAG witness, can advance a rejected Classifier final.
Every retained RAG trace contains a genuine actual RAG call/response, and no
classifier-only trajectory is counted as RAG.

The gate remains false because no RAG terminal was accepted, so there is no
retained accepted RAG evidence and no complete Direct/Classifier/RAG coverage
in both arms. The R2 recovery cap correctly leaves one real delivery shortfall.

### Consequence

Do not launch the 878-candidate full campaign, SFT conversion, or training. A
future successor must use a fresh identity-disjoint source and isolate the
remaining cause of RAG private-audit rejection while preserving the conditional
Classifier-close and actual-retrieval requirements.

## 2026-09-13 18:04:20 CST - E3.16 discriminative-RAG successor plan

Tags: e316, rag, hcv, hermes, counterfactual-retrieval, no-sft

### Goal

Correct E3.14's RAG failure modes in a prospective identity-disjoint audit
without replaying delivered terminal records: incomplete RAG arguments,
single-candidate confirmation, unsupported nearest-neighbor conclusions, and
unsupported abstention.

### Steps

1. Freeze a new 32-image source excluding E3.9--E3.15 at all four identity layers and bind new route-coverage witnesses privately.
2. Run the E3.16 RAG prompt: classifier candidate generation followed by a first query contrasting the leading candidate and nearest visually confusable alternative; all calls require `query`, `retrieval_type`, and `rationale`.
3. Require final HCV linkage among a visible image trait, actual RAG response, and explicit rejection of that nearest alternative.
4. Use `INSUFFICIENT_EVIDENCE` only after actual RAG when that decisive trait remains unavailable; retain R0/R1/R2 delivery-only recovery and all training gates.

### Acceptance

- Prompt and controller reject a missing `retrieval_type`, missing actual RAG response, unpaired nearest alternative, absent evidence linkage, and unsupported abstention.
- Private RAG coverage cannot accept a pre-RAG Classifier final.
- No E3.14/E3.15 artifact is mutated; E3.16 remains designed-not-launched until a new source is frozen.

### Next

Prepare the new candidate-source identity audit and E3.16 32-image source only when collection is separately requested.

## 2026-09-13 18:49:01 CST - E3.16 canary follow-up plan

Tags: e316, canary, rag, recovery, no-sft

### Verified state

The four-image canary established that the revised RAG teacher follows the required schema and candidate-vs-nearest-neighbor retrieval form. It did not establish a concrete-class RAG winner. Its immutable R0/R1 records must not be overwritten.

### Next

1. Do not replay the delivered R0 Option terminals or increase the closed canary budget.
2. Before another collection, retain E3.15 Option format one-shot in all E3.16 Classifier prompts and keep the corrected abstention validator.
3. If separately authorized, create a new identity-disjoint prospective source with a new budget and run the full 32-image E3.16 audit; evaluate accepted RAG winners separately from compliant RAG refusals.

## 2026-09-13 23:09:40 CST - E3.20 four-stage semantic cascade plan

Tags: e320, cascade, semantic-audit, quality-repair, rag, hermes, no-sft

### Goal

Separate wrong answers from poor-quality trajectories: semantic errors advance
one stage (`Direct → Classifier → RAG → Reject`), while quality/protocol errors
receive one same-stage prompt-repair resample.

### Acceptance

- New source/manifest bind 32 all-Unknown rows, 11/11/10 folds, 8 per question/domain cell, 512px, 8M cap, and false training flags.
- Tests prove semantic progression, bounded same-stage repair, RAG standard-name grounding, Reject-stage prior-RAG binding, and two-axis private audit parsing.
- R0/R1/R2 are reserved for unknown delivery; the dedicated four-stage
  collector must pass a no-provider dry-run before any provider use.

### Next

The specialized collector and dry-run gate now pass. E3.19 remains immutable
and `superseded_not_launched`; do not launch collection, conversion, SFT, or
training without separate authorization.

## 2026-09-14 00:54:00 CST - E3.21 Direct-First HCV Cascade execution plan

Tags: e321, direct-first, hcv, four-stage, budget, no-sft

### Goal

Collect only the Direct portion of the frozen dual-arm 1,070-image corpus while preserving a complete future `Direct → Classifier → RAG → Reject` semantic cascade plan. Use `gpt-5.6-sol`, 512px transport, shared 16M uncached-input accounting, and isolated private audit.

### Execution

1. Reuse 25 verified E3.20 Direct winners, defer six semantic-wrong rows to unexecuted Classifier, and run the one predecessor-bound quality Q1 repair plus 1,038 novel Direct R0 items.
2. Run Q1 then eight immutable source-SHA-bound R0 shards sequentially. Before every provider generation/audit reserve 1,000/2,500 tokens; settle delivered usage and retain unknown exposure.
3. Treat semantic error as a frozen future one-stage escalation; quality failure gets one same-stage repair; only unknown delivery can create R1/R2. Never launch Classifier/RAG/Reject in this goal phase.
4. Audit each completed shard before the next dispatch. Report arm, class/form coverage, Hermes/HCV/Option/tool compliance, semantic/quality/delivery states, unique winners, token budget, and frozen future queues.

### Acceptance

- Direct provider scope is exactly 1,039 and all full-pool 1,070 image identities have exactly one accounting state.
- Shared token and intent ledgers are durable/atomic; unknown delivery has predecessor-bound R0/R1/R2 recovery and no R3.
- No Direct tools, no truth/fold/private leakage, no conversion/SFT/training authorization, and no execution of future cascade routes.
