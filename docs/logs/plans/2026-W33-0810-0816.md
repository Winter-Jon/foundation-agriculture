# Plans 2026-W33

## 2026-08-15 08:32:17 CST - Run gated Pilot-ablation-SFT iteration loop

Tags: rag-sft, blind-safe, option, gpt-5.6-luna

### Goal

Continue the handoff's protocol-first RAG SFT objective through bounded Pilot, ablation, freeze, milestone SFT, and forced-retrieval-off diagnostic loops. Preserve all trajectory, evidence, language, privacy, deduplication, and answer-contract gates.

### Steps

1. Establish Option protocol reliability with one fresh four-row Pilot covering A/B/C/D, English/Chinese, disease/pest, and four distinct classes.
2. Retain phase separation, mixed-content hard rejection, and bounded budget finalization based on the Round106/109/110 historical counterfactual ablation at `outputs/experiments/rag_sft_iteration/reviews/round111_protocol_ablation.json`.
3. Strictly audit every new raw provider response, retrieval call, target evidence rank, language, private-label boundary, query-image uniqueness, protocol validator, and semantic validator. Reject rather than rewrite failures.
4. Rebuild a strict candidate view from current-contract rows only. Exclude stale Round097 Option rows and diagnostic-only normalized trajectories.
5. Run the freeze gate only when the reviewed corpus reaches 48 balanced RAG plus 32 balanced Direct rows with unique provenance and zero hard-gate errors. Keep `training_authorized=false` while any quota or quality condition is unmet.
6. For each materially revised, reviewed corpus, freeze once and identify it by content hash. Never train the same freeze hash twice.
7. When the complete training gate is green, run one local milestone SFT from retained non-RAG checkpoint-165. Do not substitute a smaller or partial corpus for the required complete freeze.
8. Evaluate the milestone with forced retrieval disabled: contract smoke first, then a balanced diagnostic on the same manifest as checkpoint-165. Two consecutive failed milestones return the loop to Pilot/data construction; three milestones trigger a strategy review.
9. Keep the formal 618-row evaluation behind a separate review and explicit authorization.

### Acceptance

- Pilot gate: all planned rows have pure raw tool calls, successful real Milvus retrieval, target-supported final answers, exact Open/Option contracts, semantic validity, no private-label leakage, and no image overlap.
- Freeze gate: 48 balanced RAG + 32 balanced Direct, 80 unique rows/images with provenance, A/B/C/D and route/language/domain coverage, answer and retrieval accuracy 1.0, and zero protocol, semantic, failed-tool, leakage, overlap, mismatch, or quota errors.
- Milestone gate: unique reviewed freeze hash, initialization from checkpoint-165, successful local completion, autonomous tool use with forced retrieval off, and no hard-contract regression.
- Goal completion: a reproducible retained method has evidence of improvement over checkpoint-165 on bounded matched diagnostics and is ready for a separately authorized formal 618 comparison, or repeated evidence justifies a documented strategy redesign.

### Risks

- A single Round110 pass does not estimate population reliability; Round111 must not be interpreted as broad class coverage if only four rows pass.
- Teacher protocol variance, retrieval-class hardness, and language effects must be separated rather than repaired by post-hoc rewriting.
- The working tree is heavily dirty; only target files may be changed.
- SFT is authorized only after the complete gate, while formal 618 evaluation remains separately unauthorized.

### Next

Complete and strictly audit the already-started Round111 four-row coverage Pilot. If any row fails, identify the narrow protocol, retrieval, or language layer and return to one bounded repair Pilot; if all pass, rebuild the strict candidate coverage view before considering any SFT.

## 2026-08-15 09:35:00 CST - Replace fixed bulk sampling with adaptive rejection-sampling loop

### Goal

Reach a verifiable full-corpus milestone with less invalid teacher distillation by separating Pilot stability, bulk candidate-pool collection, rejection sampling, freeze, and SFT validation.

### Design

1. Require two consecutive bounded Pilots with zero protocol, semantic, language, boundary, and image-isolation errors, plus coverage of the active answer contracts, before bulk release.
2. Prepare a larger fresh, preflight-eligible reserve pool; the current teacher-free plan has 75 targets and 225 maximum candidate attempts. These are reserves, not guaranteed training rows.
3. After approval, release only a 20-attempt calibration batch (two per deficient quota cell). Audit raw trajectories and compute acceptance by exact cell, route, question type, rejection reason, and hard-gate status.
4. Release at most one conditional shard at a time, prioritizing cells with remaining quota and the lowest Wilson lower-confidence acceptance estimate. Stop at the 48-row freeze, approved budget exhaustion, or repeated hard-gate regression.
5. Run the milestone SFT once on the immutable 48 RAG + 32 Direct freeze from checkpoint-165, then perform forced-retrieval-off contract smoke and matched subgroup diagnostics. Use the observed SFT effect to choose the next Pilot/data revision.

### Verified planning result

`outputs/experiments/rag_sft_iteration/approval/bulk_distill_plan_v1/report.json` records 75 fresh targets, 225 maximum attempts, 20 initial calibration attempts, and `pilot_entry_gate.passed=false`. No teacher batch was launched.

### Next

Do not release calibration attempts until the Pilot stability gate and explicit material-sampling approval are both satisfied.

## 2026-08-15 09:44:11 CST - Simplify the adaptive milestone loop

### Plan

Reuse current-contract historical data before new distillation. The 32 Direct rows are deterministic rerenders of historical source data and now carry provenance; historical RAG is reusable only after current raw-trajectory validation. For missing RAG cells, calibrate with one attempt on three independent targets per cell, then continue only where observed yield is reasonable; concentrated failures return to a targeted Pilot. Freeze 48 RAG + 32 Direct for one checkpoint-165 milestone SFT, and let matched forced-off diagnostics choose repair or a later 300–500-row expansion.

### Status

The rebuilt Direct gate passes 32/32 with zero overlap. The bulk reserve contains 75 targets and 225 maximum attempts; the initial calibration is 30 attempts. Sampling, SFT, and formal evaluation remain unauthorized.

### Next

Review stop-correction Pilot collectability, then request material-sampling approval for the 30-attempt calibration release.

## 2026-08-15 18:35:00 CST - Stage-A-only balanced milestone loop

Supersedes the operating instructions in the earlier entries above; those entries remain historical planning evidence.

### Current goal

Build one immutable **64-row Stage-A corpus**: 32 strict `standard` RAG rows plus 32 current-contract Direct replay rows. The RAG corpus is stratified by Open/Option × English/Chinese × disease/pest: eight cells, exactly four strict accepted rows per cell. Current state is 17/32 RAG and 32/32 Direct, leaving 15 RAG deficits.

### Loop

1. Check unknown deliveries, processes, exit markers, and artifacts before every teacher request; never replay ambiguous delivery.
2. Select a fresh Blind target from a deficient cell, run strict real-image preflight, then run a bounded standard Pilot. A Pilot tests stability and does not need to fill a quota.
3. After a reviewed Pilot and explicit sampling authorization, sample by remaining cell deficit. Randomize only inside the fresh eligible pool for a cell; stop that cell immediately at four strict accepts. Preserve raw trajectories and rejection reasons.
4. Repeated hard-gate failure pauses that cell for targeted repair; ordinary semantic/retrieval misses remain rejection evidence. Do not relax validators or spend merely to reach a call cap.
5. Freeze only when all 32 RAG and 32 Direct rows pass current protocol, semantic, evidence, language, boundary, deduplication, and evaluation-isolation gates. One freeze hash receives at most one separately approved checkpoint-165 SFT.
6. Evaluate the SFT with forced retrieval off: protocol smoke, matched balanced diagnostic on the checkpoint-165 manifest, baseline comparison, Direct retention, and milestone review. Record an explicit next-step decision. Formal 618 remains separately approved.

### Authorization and next action

No bulk sampling, freeze, SFT, or formal evaluation is authorized. The next safe action is one new-ID strict preflight and one bounded standard Pilot selected from `standard_targets.jsonl`; only a reviewed nonzero strict acceptance can support a request to release adaptive sampling.

## 2026-08-16 01:03:24 CST - Chinese Open re-entry gate after Option completion

### Goal

Complete the four remaining Stage-A rows without weakening the Chinese Open contract: two `standard/open/zh/disease` and two `standard/open/zh/pest` strict Blind accepts. All Option cells are already exactly full and excluded from further selection.

### Evidence and decision

- The current reserve has four unique, isolated, locally preflight-eligible Chinese Open targets: two disease and two pest. They have no overlap with current RAG, Direct, evaluation images, or known-unknown-delivery hashes.
- Historical Open negatives were concentrated in Luna's post-budget fourth-tool behavior (Rounds127/128), before the closed final-answer state was separated. Round131 later accepted a normal Chinese Open trajectory, but did not exercise budget closure.
- Terra/Micu-SLB now has five consecutive known-delivery, zero-error Option Pilots after the public-only final-contract repair. This supports a bounded Open re-entry Pilot, but is not evidence to release Open collection unconditionally.

### Proposed gated release (requires explicit user authorization)

1. Change no code or validator unless an observed new hard-gate failure requires a targeted repair. Release only the first disease target `rag_open-zh-disease_N04096_N04096_P00003` for one new-ID real-image preflight and one one-sample Terra Pilot.
2. Preflight must report endpoint and visual reachability plus exactly one legal manual tool call. Any unknown delivery makes that image non-replayable and stops Open release.
3. Pilot must pass independent strict validation with zero errors, retrieval success 1.0, final-label accuracy 1.0, Chinese language/answer contract, Blind boundaries, and no duplicate/evaluation image. A `tool_call_after_budget_finalization` or other repeated protocol hard gate stops Open immediately.
4. Only after that reviewed pass, release the remaining three current first attempts one at a time, always selecting the still-deficient cell and stopping each cell at four accepts. Ordinary semantic/retrieval misses remain rejected evidence; do not replace or rewrite them.
5. After every accepted row, explicitly update the strict source list, rebuild the strict gate and both plans, and recheck isolation before the next teacher request. Do not freeze or run SFT until all four Open rows are accepted and the full 32+32 gate is verified.

### Approval boundary

At the time of this earlier plan, the runner intentionally accepted only `stage_a_option_calibration`; deferred Open rows used `deferred_open_strategy_review`. The later Open authorization and matching `stage_a_open_reentry` guard are recorded below.

## 2026-08-16 01:09:21 CST - Autonomous validation-to-scale loop

### Decision

The 32 strict RAG + 32 Direct corpus is a protocol-and-signal validation SFT, not the final training scale. Sampling and the two internal SFT stages now proceed without per-run user approval; the formal forced-retrieval-off 618-row evaluation remains separately approved.

### Loop

1. Complete the 64-row strict validation freeze and run one SFT from checkpoint-165 per unique freeze hash.
2. On a fixed 192-row matched diagnostic, require zero hard-gate errors, stable autonomous tool use, at least +3pp on the RAG-targeted slice, a paired-bootstrap 95% lower confidence bound above zero, no unacceptable language/domain/answer-type regression, and acceptable Direct retention.
3. Only on that robust positive signal, build a new isolated and locally preflighted candidate pool, then run balanced Blind rejection sampling toward 512 strict RAG rows (64 per standard cell) plus 256 Direct rows. Each cell must span at least 16 canonical classes, with at most four accepted images per class.
4. Keep only individually strict accepted rows. Raw teacher acceptance does not need to be 100%; after 12 attempts a cell must have at least 3 strict accepts and 25% acceptance to receive its scaled budget. Repeated protocol/boundary hard gates pause that cell regardless of acceptance rate.
5. Freeze the 512-RAG + 256-Direct class-balanced corpus and run one final internal SFT from checkpoint-165. Its forced-off smoke, matched diagnostic, subgroup deltas, and Direct retention are the primary internal result. Formal 618 remains a separate decision.

## 2026-08-16 01:50:48 CST - Reset Stage-A goal with bounded validation-first execution

Supersedes the active-loop wording above where it could be read as continuing collection or training after a weak result. Stage B and stop-correction remain out of scope.

### Goal

First finish exactly one reproducible Stage-A validation milestone: an immutable 64-row freeze, one checkpoint-165 validation SFT, and its fixed diagnostic review. Scale only after that review demonstrates a credible positive signal.

### Steps

1. Build a deterministic 32+32 freeze: eight `standard` RAG cells each contribute exactly four strict-valid rows, plus 32 current-contract Direct rows. Record source provenance and deterministic exclusion of RAG surplus; reject duplicate or evaluation-overlapping rows.
2. Locate the retained checkpoint-165 and verify the SFT, smoke, and 192-row matched-diagnostic configurations before launching. A freeze hash is single-use: it receives at most one validation SFT.
3. Run that SFT, then report forced-retrieval-off smoke, matched checkpoint comparison with paired bootstrap, language/domain/answer-type slices, and Direct retention.
4. If any hard gate fails or the result is weak, record one bounded repair hypothesis and stop; do not resample, retrain, or replay the same freeze automatically.
5. Only if all acceptance gates pass (zero protocol/boundary errors, stable tool use, RAG-targeted gain >= +3pp, paired-bootstrap 95% lower bound > 0, acceptable subgroup deltas and Direct retention), create a fresh isolated pool and scale to 512 strict RAG (64/cell, >=16 classes/cell, <=4/class/cell) plus 256 unique revalidated Direct rows. Each cell gets at most 12 calibration attempts and needs >=3 accepts and >=25% raw acceptance before expansion.

### Current verified state

- The strict view contains 34 raw current-contract RAG candidates, of which exactly 32 are quota-usable for the standard milestone, plus 32 Direct rows. All eight standard cells are full. Evidence: `outputs/experiments/rag_sft_iteration/reviews/strict_candidate_view_v1_gate.json`.
- Rounds155--157 are accepted Chinese Open Terra sources and must be registered in `src/agrinet/rag/distill/build_strict_candidate_view.py` before the freeze is built.
- The remaining immediate work is the dedicated deterministic 64-row freeze builder and checkpoint-165/configuration verification. No SFT has yet launched.

### Acceptance

The current goal completes with a recorded Stage-A validation result and a decisive next action: release scaled collection only after the stated positive-signal gate, otherwise a documented bounded repair. Formal 618 evaluation never starts automatically.

## 2026-08-16 08:45:00 CST - Standing autonomous execution authorization

The user grants standing authorization for this RAG-SFT loop. Codex must autonomously decide whether to run bounded preflight/Pilot, strict validation, an immutable freeze, one internal SFT per unique freeze, smoke and matched diagnostics, and gated scaled Blind rejection sampling. Do not request per-step authorization or wait merely because an internal repair creates a newly reviewed freeze. Continue to enforce strict validation, unknown-delivery non-replay, isolation, per-freeze single-use, and all stated stop conditions.

Only the formal forced-retrieval-off 618-row evaluation, a base-model change, or an evaluation-protocol change requires a new explicit user decision.
