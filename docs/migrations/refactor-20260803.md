# Refactor migration map (2026-08-03)

The paths on the left are pre-migration history. Current workflows consume the registered artifact on the right. No compatibility symlink is used.

| Historical path | Current artifact | Status | Verification |
|---|---|---|---|
| `outputs/vlm_sft/qwen3_vl_4b_rag_distill_student_e5_len8192_liger_lr1e5_singleimg/v0-20260703-004513/checkpoint-224` | `outputs/artifacts/models/qwen3vl4b-rag-sft-full-v1` | copied; source retained | 13 files, 8,887,255,685 bytes, all SHA-256 checksums equal; `AutoConfig` and `Qwen3VLProcessor` load |
| `outputs/milvus/agrinet_wiki_lite.db` | same current local database, artifact registration pending index rebuild | available | collections: 320 classes, 578 images; real A800 query passed |
| legacy hard-negative inputs referenced by old tools | none | unavailable | files absent; deterministic bounded migration baseline registered instead |

The existing `rag-sft-baseline-20260703` tag and commit `b0b031b2c` are unchanged. Historical Slurm logs retain their original path semantics.
