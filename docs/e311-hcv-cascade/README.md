# E3.11 HCV Cascade — Expression-Compatible Successor

E3.11 is a new prospective audit after terminal E3.9 and E3.10 lineages. It never replays or relabels their delivered terminals.

It retains strict Hermes outer structure, native-tool sequencing, private truth/coverage isolation, RAG evidence retention, and R0/R1/R2 delivery recovery. It accepts three semantically equivalent public forms for rejected candidate reasoning and requires a meaningful uncertainty judgement with limitation rather than a literal three-word vocabulary.

For Direct only, an already private-audited `reject` may advance to Classifier when the public trajectory has only an internal HCV-observation shortfall. Invalid outer Hermes, tool protocol failures, private-audit ambiguity, local RAG failures, and all RAG failures remain terminal.

Training remains ineligible and unauthorized. Full collection, SFT export, conversion, and training require an independently passing E3.11 final audit and separate authorization.

## Terminal E3.11 v1 result

The immutable R0/R1/R2 lineage is complete. Its final report is `outputs/artifacts/e311-hcv-cascade/reports/e311-hcv-audit-v1-final.json`: 11 accepted, 18 delivered route-contract rejects, and three R2 delivery shortfalls. Routes reached Direct 22, Classifier 7, and RAG 3.

The expression-compatible HCV change worked: 24 public trajectories were retained and the prior all-Direct collapse did not recur. However, all three RAG terminals were delivered `hermes_pretool_format` rejects: the provider omitted a required standalone pure planning turn before a native tool call. Thus no accepted RAG trajectory retains public RAG evidence; `rag_evidence_closed=false`, `protocol_gate_passed=false`, and full collection remains forbidden. The 18 delivered terminals and three R2 delivery shortfalls are immutable.
