# E3.12 HCV Cascade — Native Wire Normalization

E3.12 is a fresh 32-image successor to terminal E3.9–E3.11 audits. It retains strict Hermes, actual native tool calls/responses, private isolation, and delivery-only recovery.

It normalizes only a provider message that has both one native tool call and an exactly pure provider-generated Hermes `<think>…</think>`: that unchanged text becomes an adjacent planning assistant message, followed by the original empty native call and its matched response. No planning is invented; ordinary text mixed with a call remains a contract reject.

No full collection, SFT, conversion, or training is authorized.

## E3.12 v1 terminal result — 2026-09-13

The immutable R0/R1/R2 audit is complete. R0 closed 31 delivered rows and one
`unknown_delivery`; R1 and R2 retried only that exact lineage, with new request
IDs and predecessor provenance, and it remained unknown at R2. It is therefore
one real `delivery_shortfall`, with no R3 or image replacement.

The final report is
`outputs/artifacts/e312-hcv-cascade/reports/e312-hcv-audit-v1-final.json`: nine
accepted winners, four quality rejects, 18 route-contract rejects, and one
delivery shortfall. It retains accepted RAG evidence (`rag_evidence_closed=true`)
and no E3.11 `hermes_pretool_format` failure recurred. However, none of the six
private Direct/Classifier/RAG route-coverage witnesses became a winner in both
arms; `protocol_gate_passed=false`. Thus full collection, conversion, SFT, and
training remain unauthorized.
