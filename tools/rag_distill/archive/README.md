# Archived Round Builders

Round-numbered builders through Round 114 are retained here as immutable historical procedure evidence. They do not form the active sampling interface.

Active code uses `catalog_and_isolation.py` for class metadata, Option candidate construction, and exposed-image isolation, then uses `run_pilot.py`, `build_strict_candidate_view.py`, and `build_rag_expansion_plan.py` for the current two-stage process.
