You are a professional agricultural image recognition expert. Analyze the given plant/pest/disease image and provide a detailed reasoning chain for identification.

CRITICAL: You MUST begin your entire response with a free-text reasoning chain inside `<think>`...`</think>` tags. Do NOT output any JSON before the closing `</think>` tag.

Image id: {{ img_name }}
Task domain: {{ task_domain | default("unknown") }}
Ground-truth label for annotation: {{ gt_label }}
Ground-truth Chinese label: {{ gt_label_zh | default("") }}
Candidate labels: {{ candidate_labels | tojson }}
Candidate label details: {{ candidate_label_details | default([]) | tojson(indent=2) }}

{% if knowledge_available %}
Structured agricultural knowledge for the ground-truth label:
{{ class_knowledge | tojson(indent=2) }}
{% else %}
No structured agricultural knowledge is available for this label. Use only visible image evidence and set wiki_evidence to ["insufficient wiki knowledge"].
{% endif %}

Task:
Analyze the query image and provide a detailed reasoning chain for identifying the plant disease or pest. First, perform a step-by-step reasoning analysis inside `<think>`...`</think>` tags. Then, output the structured JSON conclusion.

Your reasoning should cover:
1. 视觉观察：描述你在图片中观察到的可见特征、症状或形态特征。
2. 候选分析：对于每个候选标签，解释为什么基于视觉证据它匹配或不匹配。
3. 知识验证：引用提供的结构化知识中的相关农业或分类学事实来支持你的结论。
4. 不确定性评估：简要说明你是自信（低）、有些不确定（中）还是高度不确定（高）以及原因。

重要提示：请像分析单张图片并运用内部知识一样撰写推理。不要引用任何外部图片、对比图片或编号图片。不要提及"正例参考"、"负例参考"、"参考图像"或类似术语。专注于描述你在查询图片中看到的内容并应用你的农业知识。

请用中文撰写推理过程和JSON值。在适当的地方使用原始科学名称（病原体、物种名称），但描述和推理应使用自然、准确的中文。

Rules:
- Start your response with a `<think>` block containing the free-text reasoning chain described above.
- After the `</think>` tag, output only valid JSON. Do not wrap the JSON in Markdown fences.
- Do not invent visual details that are not visible in the image.
- Do not invent agricultural knowledge. If knowledge is missing, write exactly "insufficient wiki knowledge".
- The final_label in the JSON must be one of candidate_labels.
- Use concise evidence phrases in the JSON fields that are suitable for later SFT or preference-data construction.
- If the image is ambiguous, keep final_label as the annotation label but set uncertainty to "medium" or "high" and explain why.

Return format:
<think>
1. 视觉观察：...
2. 候选分析：...
3. 知识验证：...
4. 不确定性评估：...
</think>

Then return this exact JSON schema:
{
  "task_domain": "{{ task_domain | default("unknown") }}",
  "final_label": "{{ gt_label }}",
  "final_label_zh": "{{ gt_label_zh | default("") }}",
  "candidate_labels": {{ candidate_labels | tojson }},
  "query_visual_evidence": [
    "visible trait in the query image supporting the final label"
  ],
  "positive_reference_alignment": [
    "how the query aligns with known characteristics of the predicted class"
  ],
  "negative_reference_contrast": [
    {
      "candidate": "candidate label being contrasted",
      "why_less_likely": "visual distinction from the predicted class"
    }
  ],
  "wiki_evidence": [
    "agricultural or taxonomic fact from the provided wiki knowledge, or insufficient wiki knowledge"
  ],
  "agricultural_interpretation": "short interpretation supported by visible evidence and provided wiki knowledge",
  "uncertainty": "low | medium | high",
  "answer_check": {
    "label_in_candidates": true,
    "uses_ground_truth_label_for_annotation": true,
    "evidence_mentions_visual_traits": true,
    "positive_references_used": {{ (positive_reference_images | default([]) | length > 0) | tojson }},
    "negative_references_used": {{ (negative_reference_images | default([]) | length > 0) | tojson }},
    "wiki_available": {{ knowledge_available | tojson }}
  }
}
