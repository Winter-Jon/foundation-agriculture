# Plan

## Goal
G-20260917-001

## Current Hypothesis
The full tool chain is a testable data intervention; prior routing results do not establish benefit. This execution Goal measures pilot quality and cost, not downstream SFT gains.

## Current Strategy
1. Recover the running feature pass and inspect existing implementation for correctness gaps.
2. Resolve evaluation metadata where possible; report remaining isolation limits. Verify checkpoint-label-training bindings against current OOF attempt3 manifests.
3. Finish cluster inventory and priority allocation; prepare independent options and fresh actual classifier cards.
4. Register a bounded 32-image campaign with public-only generation, isolated audit, durable ledger recovery and complete accounting.
5. Run tests, pilot, export only qualified trajectories, and validate the actual SFT template. Deliver report and remaining deficits.

## Decision Logic
Do not replay unresolved teacher requests. Preserve failed attempts. Never use private semantic corrections in repairs. If selected inventory has a deficit retain it. Missing cost rates mean unknown cost, not zero. No full collection or training.

## Current Milestones
Contract, readiness workflow and preparation skeleton exist. Feature extraction is active. Live adapter, orchestration, repairs, review and template verification need completion.

## Discoveries Affecting the Plan
Current OOF models bind to manifests-attempt3, not the older manifests directory. Candidate pool has 142726 eligible rows across 107 classes; preparation retained 142712 after initial exclusions. Evaluation public manifests omit source/near-duplicate identifiers. Existing near-duplicate audit is partial.

## Pilot observations affecting interpretation
The running pilot includes case-only canonical-name mismatches and actual wrong classifications. Preserve original review outcomes and strict export gates; distinguish naming protocol failures in the final report rather than silently treating all as recognition errors. User waived monetary cost reporting. No semantic correction or rerun of failed images.
