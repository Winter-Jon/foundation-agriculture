# Archived Vision Experiment Drafts

These experiment definitions are retained as historical, unapproved drafts.
They are deliberately outside `configs/experiments/` and are not registered
for new runs. Their resource settings and the accompanying test expectations
were inconsistent when archived on 2026-09-20.

Before reviving either draft, reconcile the GPU and MAE batch-size settings,
remove duplicate YAML keys, create a new immutable experiment definition under
`configs/experiments/vision/`, and add passing coverage to the active test
suite.
